# 课小秘 商家视角 API 工具筛选目录

> 内部 AI 工具目录。只用于选择和约束 API 调用，不得把路由、内部类型、鉴权字段、Provider 名称或内部主键直接回复给客户。

## 目录元数据

- 源项目：`LingKe.CRMApi`（`Public/LingKe/Link.CRMApi`）
- 目标框架：`.NET 8 / ASP.NET Core Controller API`
- 视角：`商家视角`
- 源码提交：`07b36b96d`
- 生成日期：`2026-09-03`
- Controller 数：`82`
- 实际 Http Action 数：`960`
- 工具等级：`A=1`、`B=379`、`C=18`、`D=562`
- Controller 源码指纹：`sha256:4142938d40c5223977ccebe653ee7c692d6914a37d9a0a4b5e003ada819945e2`
- 机器目录：`crmapi-api-tool-manifest.json`（所有工具默认关闭）

## 使用边界

- 本目录从当前 Controller、DTO、继承字段、代码内 `if` 校验和一层 Provider/Service 调用静态生成；静态结果不是生产可用性证明。
- A/B/C 级才会提供详细契约。D 级仍保留完整 Action 索引和排除原因，但不得注册为客服查询工具。
- `Uid`、`StoreId`、`AppId` 和内部业务 ID 不能由模型猜测；必须由已验证会话注入，或来自同一会话上游 API 的服务端引用。
- `[Required]` 只代表模型特性声明。值类型默认值和 Controller 条件会改变真实必填性，因此每个字段同时列出代码校验证据。
- 空集合只表示本次条件没有匹配结果，不能自行推断对象不存在、已经删除或某个业务原因成立。
- 当前查询只能证明调用时事实。历史问题必须使用明确的日志、记录或历史接口，不能用当前状态冒充过去状态。
- 直接响应可能含个人信息、内部 ID 或凭据字段；调用执行器必须按目录标记做最小化投影，不能把完整响应无条件交给模型。
- C 端与商家端即使 Action 同名，也代表不同视角；需要解释差异时应分别取证，不能互相替代。
- 文档中的使用时机只是能力理解示例，不是运行时问题路由；机器目录不包含问题关键词、固定场景到接口映射或固定回答。

## Agent 选取与调用流程

1. 从已验证的商家会话注入当前操作人 `Uid`、`StoreId`、`AppId` 和权限上下文，模型不得使用请求正文覆盖身份。
2. 按问题域检索本目录，只选择 A/B 级候选；C 级需要人工复核，D 级不得调用。
3. 商家反馈顾客端展示问题时，转入 C 端流程：取得顾客手机号、限定门店、解析目标会员引用，再以顾客视角查询。
4. 对 C/B 两端结果按字段和数据时点比较，只陈述响应能证明的差异，不把当前状态解释成历史原因。

### 给工具筛选器的检索建议

- 不要把整份目录一次性放入模型上下文。先按问题域、工具编号、用途和使用时机召回少量候选，再读取对应的详细契约。
- 第一轮优先 A/B 级；只有候选不足且有人工审批时才考虑 C 级。D 级只用于审计覆盖，不参与召回。
- 同一问题需要多步查询时，上一步返回的内部 ID 只能作为执行器保存的服务端引用传给下一步，不能暴露给模型自由改写。
- 典型顺序是“定位主体 → 查询列表/概览 → 用上游引用查询详情 → 必要时做 C/B 对照”，避免无边界批量拉取。

## 公共响应包装

| 包装 | 字段 | 含义 |
| --- | --- | --- |
| `DataResult<T>` | `State`、`StatusCode`、`Message`、`MsgType`、`Data` | 单对象结果；只有 `State=true` 才能把 `Data` 当成功事实 |
| `DataList<T>` | 上述状态字段及 `PageIndex`、`PageSize`、`PageCount`、`TotalCount`、`Data` | 分页列表；空列表不能解释为空的业务原因 |
| `PageData<T>` | `PageIndex`、`PageSize`、`PageCount`、`TotalCount`、`IsNext`、`Data` | 常作为 `DataResult<T>.Data` 的分页载荷 |

## 工具等级

| 等级 | 含义 | 运行要求 |
| --- | --- | --- |
| A | 静态路径未发现参数或明显副作用 | 仍需 AI 专用部署的只读验收 |
| B | 候选只读接口，但需要身份注入、上游对象引用或响应脱敏 | 满足约束后才可注册 |
| C | 查询路径存在疑似副作用、凭据参数或语义不充分 | 人工复核通过前不开放 |
| D | 写接口、登录、回调、测试或无法证明只读 | 永久排除或另行审批 |

## 全部 Action 索引

| 工具编号 | 业务域 | 方法与路由 | 摘要/用途 | 鉴权 | 等级 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| `crmapi.activity.business_activity_data_statistical` | Activity 模块 | `POST /api/Activity/BusinessActivityDataStatistical` | 活动报告统计 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.activity.business_activity_forwarder_data_list` | Activity 模块 | `POST /api/Activity/BusinessActivityForwarderDataList` | 活动报告 转发者数据列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.activity.business_activity_new_user_data_list` | Activity 模块 | `POST /api/Activity/BusinessActivityNewUserDataList` | 活动报告 新客数据列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.activity.business_add_activity` | Activity 模块 | `POST /api/Activity/BusinessAddActivity` | 创建活动 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.activity.business_del_activity` | Activity 模块 | `POST /api/Activity/BusinessDelActivity` | 删除活动 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.activity.business_get_activity` | Activity 模块 | `POST /api/Activity/BusinessGetActivity` | 获取活动详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.activity.business_get_activity_list` | Activity 模块 | `POST /api/Activity/BusinessGetActivityList` | 获取活动管理数据列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.activity.business_get_share_qr_code` | Activity 模块 | `POST /api/Activity/BusinessGetShareQrCode` | 生成活动分享二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.activity.business_update_activity` | Activity 模块 | `POST /api/Activity/BusinessUpdateActivity` | 修改活动详情 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.activity.business_update_activity_is_top` | Activity 模块 | `POST /api/Activity/BusinessUpdateActivityIsTop` | 群发给会员 首页显示开关 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.activity.business_update_activity_is_wechat` | Activity 模块 | `POST /api/Activity/BusinessUpdateActivityIsWechat` | 群发给会员 通过微信发送活动通知 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.activity.get_activity_template_list` | Activity 模块 | `POST /api/Activity/GetActivityTemplateList` | 获取活动背景图列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.admin_web_auth.complete_mobile_binding` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/CompleteMobileBinding` | 手机微信回调页提交 OAuth code，完成发起绑定的已登录员工身份关联。 目标 UID 只从服务端短时会话恢复，匿名回调请求不能指定或修改目标账号。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.complete_wechat_login` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/CompleteWechatLogin` | 手机微信回调页提交 OAuth code，完成员工身份确认。 最终商户登录会话只保存在 Redis，手机端不会取得桌面登录凭据。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.complete_wechat_mobile_authentication` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/CompleteWechatMobileAuthentication` | 完成扫码登录的强制手机号认证，并在账号与门店准备完成后返回最终登录会话。 UID、微信身份和门店均从服务端扫码会话及数据库恢复，匿名请求不能自行指定。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.create_wechat_login` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/CreateWechatLogin` | 创建微信扫码登录会话。 二维码仅包含固定域名的公众号 OAuth 地址，不携带员工或门店身份。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.exchange_admin_entry` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/ExchangeAdminEntry` | 核销 LingKe.Admin 签发的一次性交接凭证，并创建指定店铺的课小秘 PC 会话。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.get_login_captcha` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/GetLoginCaptcha` | 获取绑定浏览器设备的一次性图形验证码。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.get_wechat_login_status` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/GetWechatLoginStatus` | 查询微信扫码登录状态；最终登录会话只能由创建二维码的桌面设备领取一次。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.send_login_sms` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/SendLoginSms` | 校验图形验证码后发送登录短信。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.admin_web_auth.send_wechat_mobile_sms` | AdminWebAuth 模块 | `POST /api/AdminWebAuth/SendWechatMobileSms` | 为已完成微信扫码但尚未绑定手机号的会话发送绑定类短信验证码。 登录标识和设备必须先通过服务端扫码会话校验，不能作为通用匿名短信接口使用。 | 源码标记免登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.agency_matter.examine_agency_matter` | AgencyMatter 模块 | `POST /api/AgencyMatter/ExamineAgencyMatter` | 审核代办事项审核操作接口 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.agency_matter.get_agency_matter_count` | AgencyMatter 模块 | `POST /api/AgencyMatter/GetAgencyMatterCount` | 根据门店ID，获取门店下面待办事项数量 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.agency_matter.get_agency_matter_list` | AgencyMatter 模块 | `POST /api/AgencyMatter/GetAgencyMatterList` | 获取待审核列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.agent.agent_submit_review` | Agent 模块 | `POST /api/Agent/AgentSubmitReview` | 用户提交代理信息审核 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.agent.get_agent_info` | Agent 模块 | `POST /api/Agent/GetAgentInfo` | 获取用户代理信息 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：PromotionCodeProvider.PromotionCodeAdd |
| `crmapi.agent.get_promotion_list` | Agent 模块 | `POST /api/Agent/GetPromotionList` | 获取推广收益列表 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：SoftOrderProvider.GetFirstPayOrder |
| `crmapi.agent.gete_income_list` | Agent 模块 | `POST /api/Agent/GeteIncomeList` | 获取提现列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.ali_pay.admin_login` | AliPay 模块 | `POST /api/AliPay/AdminLogin` | 管理员支付宝支付宝 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.ali_pay.login_user` | AliPay 模块 | `POST /api/AliPay/LoginUser` | 支付宝登录 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.ali_pay.user_authorization` | AliPay 模块 | `POST /api/AliPay/UserAuthorization` | 支付宝授权修改用户数据 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.ali_pay.user_mobile_decryption` | AliPay 模块 | `POST /api/AliPay/UserMobileDecryption` | 支付宝解密手机号 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.agent_sign` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AgentSign` | 代商户签约当面付 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.ali_order_sync` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AliOrderSync` | 【源码推断】AliOrderSync 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.alipay_notify` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AlipayNotify` | 支付宝通知回调 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.alipay_third_platform.ant_merchant_expand_item_open_create` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AntMerchantExpandItemOpenCreate` | 支付宝创建商品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.app_aes_get` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AppAesGet` | 查询 AES 密钥 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.app_aes_set` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AppAesSet` | 设置/更新 AES 密钥 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.audit_apply` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AuditApply` | 小程序提交审核 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.audited_cancel` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AuditedCancel` | 小程序退回开发版本 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.axc_merchantremaining_query` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/AxcMerchantremainingQuery` | 商家余额查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.axc_profile_get` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/AxcProfileGet` | 安心充卡样查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.axc_profile_set` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/AxcProfileSet` | 安心充卡样创建 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.axc_promotion_plan_create` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/AxcPromotionPlanCreate` | 安心充方案创建 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.axc_promotion_plan_delete` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/AxcPromotionPlanDelete` | 安心充方案创建 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.axc_promotion_plan_query` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/AxcPromotionPlanQuery` | 安心充方案查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.axc_test` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/AxcTest` | 安心充测试接口 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.base_info_modify` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/BaseInfoModify` | 小程序修改基础信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.category_query` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/CategoryQuery` | 小程序类目查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.expand_order_query` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/ExpandOrderQuery` | 商户申请单查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.experience_create` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/ExperienceCreate` | 小程序生成体验版 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.experience_query` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/ExperienceQuery` | 小程序体验版状态查询接口 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.gate_way.get` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/GateWay` | 阿里回调网关 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.gate_way.post` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/GateWay` | 阿里回调网关 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.generate_auth_url` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/GenerateAuthUrl` | 生成授权链接 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.individual_business_certify` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/IndividualBusinessCertify` | 个体升级个体工商户 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.members_create` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/MembersCreate` | 应用添加成员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.open_agent_order_query` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/OpenAgentOrderQuery` | 查询申请单状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.qr_code_bind` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/QrCodeBind` | 绑定普通二维码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.save_auth_info` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/SaveAuthInfo` | 通过授权码保存授权信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.test` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/Test` | 【源码推断】Test 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.topic_subscribe` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/TopicSubscribe` | 【源码推断】TopicSubscribe 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.topic_subscribe_query` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/TopicSubscribeQuery` | 【源码推断】TopicSubscribeQuery 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.upload_template` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/UploadTemplate` | 小程序基于模板上传版本 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.user_receive_coupon` | AlipayThirdPlatform 模块 | `GET /api/AlipayThirdPlatform/UserReceiveCoupon` | 【源码推断】UserReceiveCoupon 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alipay_third_platform.version_detail_query` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/VersionDetailQuery` | 小程序版本查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alipay_third_platform.version_online` | AlipayThirdPlatform 模块 | `POST /api/AlipayThirdPlatform/VersionOnline` | 小程序上线 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alliance.get_alliance_member` | Alliance 模块 | `POST /api/Alliance/GetAllianceMember` | 获取联盟内成员 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alliance.get_bind_alliance_qr_code_str` | Alliance 模块 | `POST /api/Alliance/GetBindAllianceQrCodeStr` | 获取联盟绑定二维码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alliance.join_alliance` | Alliance 模块 | `POST /api/Alliance/JoinAlliance` | 加入联盟（扫码） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.alliance.join_alliance_check` | Alliance 模块 | `POST /api/Alliance/JoinAllianceCheck` | 判断用户是否可以加入联盟（扫码） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.alliance.quit_alliance_member` | Alliance 模块 | `POST /api/Alliance/QuitAllianceMember` | 退出联盟内 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.article.article_get_info` | Article 模块 | `POST /api/Article/ArticleGetInfo` | 获取文章详情 新 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.article.article_get_list` | Article 模块 | `POST /api/Article/ArticleGetList` | 获取文章列表 新 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.article.get_article_info` | Article 模块 | `POST /api/Article/GetArticleInfo` | 获取文章详细 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.article.get_article_list` | Article 模块 | `POST /api/Article/GetArticleList` | 获取文章列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.article.get_article_type_list` | Article 模块 | `POST /api/Article/GetArticleTypeList` | 获取文章类型 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.ask_for_items.business_delete_ask_for_item` | AskForItems 模块 | `POST /api/AskForItems/BusinessDeleteAskForItem` | 修改物料收货信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.ask_for_items.business_get_agreement_info` | AskForItems 模块 | `POST /api/AskForItems/BusinessGetAgreementInfo` | 获取电子合同信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.ask_for_items.business_get_ask_for_items_info` | AskForItems 模块 | `POST /api/AskForItems/BusinessGetAskForItemsInfo` | 获取物料发货信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.ask_for_items.business_get_store_user_mobile` | AskForItems 模块 | `POST /api/AskForItems/BusinessGetStoreUserMobile` | 获取店铺用户默认手机号 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.ask_for_items.business_receive_agreement` | AskForItems 模块 | `POST /api/AskForItems/BusinessReceiveAgreement` | 申请领取电子合同 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.ask_for_items.business_update_ask_for_items_info` | AskForItems 模块 | `POST /api/AskForItems/BusinessUpdateAskForItemsInfo` | 修改物料收货信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.banner.banner_get_list` | Banner 模块 | `POST /api/Banner/BannerGetList` | banner列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.banner.banner_get_list_v2` | Banner 模块 | `POST /api/Banner/BannerGetListV2` | banner列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.generate_content` | Base 模块 | `POST /api/Base/GenerateContent` | AI生成内容 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.base.get_areas` | Base 模块 | `POST /api/Base/GetAreas` | 获取省市数据 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.get_bank` | Base 模块 | `POST /api/Base/GetBank` | 获取银行数据 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.get_bank_branch` | Base 模块 | `POST /api/Base/GetBankBranch` | 获取银行分行数据 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.get_bank_name` | Base 模块 | `POST /api/Base/GetBankName` | 通过银行卡图片获取 总行名称 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.get_default_store_agreement` | Base 模块 | `POST /api/Base/GetDefaultStoreAgreement` | 获取默认合同协议 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.get_store_class` | Base 模块 | `POST /api/Base/GetStoreClass` | 获取店铺分类 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.get_store_source` | Base 模块 | `POST /api/Base/GetStoreSource` | 获取信息来源 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.base.get_week_data` | Base 模块 | `POST /api/Base/GetWeekData` | 获取星期数据 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.commission.cashout_agree` | Commission 模块 | `POST /api/Commission/CashoutAgree` | 通过提现申请 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.commission.cashout_reject` | Commission 模块 | `POST /api/Commission/CashoutReject` | 拒绝提现申请 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.commission.get_admin_cash_out_logs` | Commission 模块 | `POST /api/Commission/GetAdminCashOutLogs` | 审核人员获取佣金提现申请记录 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.commission.get_audit_cash_out_count` | Commission 模块 | `POST /api/Commission/GetAuditCashOutCount` | 获取待审批佣金提现申请记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.commission.get_commission_rank` | Commission 模块 | `POST /api/Commission/GetCommissionRank` | 佣金总榜 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.commission.get_consumption_rank` | Commission 模块 | `POST /api/Commission/GetConsumptionRank` | 消费总榜 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.commission.get_user_commission_info` | Commission 模块 | `POST /api/Commission/GetUserCommissionInfo` | 获取用户佣金信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.commission.get_user_commission_log_list` | Commission 模块 | `POST /api/Commission/GetUserCommissionLogList` | 获取用户佣金消费明细 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.common.add_statistics_log` | Common 模块 | `POST /api/Common/AddStatisticsLog` | 新增网站统计日志 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.common.check_new_version` | Common 模块 | `POST /api/Common/CheckNewVersion` | 检查版本更新 | 源码标记免登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.common.check_state` | Common 模块 | `POST /api/Common/CheckState` | 校验用户状态(Y) | 源码标记免登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.common.get_oss_conf` | Common 模块 | `POST /api/Common/GetOssConf` | 获取OSS配置信息 | 源码标记免登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.common.get_promotion_url` | Common 模块 | `POST /api/Common/GetPromotionUrl` | 获取推广链接 | 源码标记免登录 | A | 无请求参数且静态调用链未发现明显写入，仍需部署前只读验收。 |
| `crmapi.common.send_sms` | Common 模块 | `POST /api/Common/SendSms` | 发送手机验证码 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.common.send_ws_message` | Common 模块 | `POST /api/Common/SendWsMessage` | 发起Ws消息通知 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.common.upload_file` | Common 模块 | `POST /api/Common/UploadFile` | 上传文件 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.common.upload_file_for_xcx` | Common 模块 | `POST /api/Common/UploadFileForXcx` | 上传文件for小程序 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.common.upload_files` | Common 模块 | `POST /api/Common/UploadFiles` | OSS上传文件后回调借口 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.common.upload_img` | Common 模块 | `POST /api/Common/UploadImg` | 上传图片 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.content.add_banner_log` | Content 模块 | `POST /api/Content/AddBannerLog` | 记录Banner日志 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.content.filter_word` | Content 模块 | `POST /api/Content/FilterWord` | 过滤敏感词 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.content.get_index_banner` | Content 模块 | `POST /api/Content/GetIndexBanner` | 获取首页Banner | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.cooperation.start_consult` | Cooperation 模块 | `POST /api/Cooperation/StartConsult` | 发起咨询/预约顾问 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.costly_activity.get_costly_activity` | CostlyActivity 模块 | `POST /api/CostlyActivity/GetCostlyActivity` | 进入营销活动 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.costly_activity.get_costly_activity_list` | CostlyActivity 模块 | `POST /api/CostlyActivity/GetCostlyActivityList` | 获取营销活动列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.costly_activity.get_now_costly_activity_list` | CostlyActivity 模块 | `POST /api/CostlyActivity/GetNowCostlyActivityList` | 获取当前活动列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.costly_activity.get_nut_gold_information` | CostlyActivity 模块 | `POST /api/CostlyActivity/GetNutGoldInformation` | 获取坚果币余额信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.costly_activity.get_recharge_nut_gold_list` | CostlyActivity 模块 | `POST /api/CostlyActivity/GetRechargeNutGoldList` | 获取充值列表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.costly_activity.get_vip_card_poster_list` | CostlyActivity 模块 | `POST /api/CostlyActivity/GetVipCardPosterList` | 获取会员卡海报列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.costly_activity.recharge_nut_gold` | CostlyActivity 模块 | `POST /api/CostlyActivity/RechargeNutGold` | 充值 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.costly_activity.sign_up_costly_activity` | CostlyActivity 模块 | `POST /api/CostlyActivity/SignUpCostlyActivity` | 报名营销活动 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.custom_controls.business_add_custom_controls` | CustomControls 模块 | `POST /api/CustomControls/BusinessAddCustomControls` | 商家新增自定义会员控件(会员资料设置页) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.custom_controls.business_del_custom_controls` | CustomControls 模块 | `POST /api/CustomControls/BusinessDelCustomControls` | 商家删除自定义会员控件信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.custom_controls.business_get_custom_controls` | CustomControls 模块 | `POST /api/CustomControls/BusinessGetCustomControls` | 商家获取自定义会员控件信息(会员资料设置页) | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：CustomControlsProvider.AddCustomControls, StoreSetingProvider.StoreSetingByUpdateSet |
| `crmapi.custom_controls.business_get_store_controls` | CustomControls 模块 | `POST /api/CustomControls/BusinessGetStoreControls` | 获取店铺用户自定义控件（会员编辑资料页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.custom_controls.business_set_custom_controls` | CustomControls 模块 | `POST /api/CustomControls/BusinessSetCustomControls` | 商家设置自定义会员控件信息(会员资料设置页) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.custom_controls.business_update_custom_controls` | CustomControls 模块 | `POST /api/CustomControls/BusinessUpdateCustomControls` | 商家修改自定义会员控件(会员资料设置页) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.custom_controls.set_store_controls_sort` | CustomControls 模块 | `POST /api/CustomControls/SetStoreControlsSort` | 设置控件排序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.device.add_bind_device` | Device 模块 | `POST /api/Device/AddBindDevice` | 添加硬件设备 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.device.edit_bind_device` | Device 模块 | `POST /api/Device/EditBindDevice` | 修改设备 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.device.edit_bind_device_state` | Device 模块 | `POST /api/Device/EditBindDeviceState` | 修改设备状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.device.get_bind_device_by_id` | Device 模块 | `POST /api/Device/GetBindDeviceById` | 获取设备详情 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.device.get_bind_device_list` | Device 模块 | `POST /api/Device/GetBindDeviceList` | 获取绑定设备列表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.device.get_bind_device_type_list` | Device 模块 | `POST /api/Device/GetBindDeviceTypeList` | 获取绑定设备类型列表（打印机、云喇叭） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.distribution.add_store_promoter` | Distribution 模块 | `POST /api/Distribution/AddStorePromoter` | 添加店铺推广员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.distribution.delete_store_promoter` | Distribution 模块 | `POST /api/Distribution/DeleteStorePromoter` | 删除店铺推广员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.distribution.edit_store_distribution_setting` | Distribution 模块 | `POST /api/Distribution/EditStoreDistributionSetting` | 编辑分销设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.distribution.get_store_distribution_setting` | Distribution 模块 | `POST /api/Distribution/GetStoreDistributionSetting` | 获取分销设置 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.distribution.get_store_promoter_list` | Distribution 模块 | `POST /api/Distribution/GetStorePromoterList` | 获取店铺推广员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.distribution.get_store_vip_list` | Distribution 模块 | `POST /api/Distribution/GetStoreVipList` | 获取店铺会员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.log.push_share_log` | Log 模块 | `POST /api/Log/PushShareLog` | 写入分享日志 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lottery.business_add_lottery` | Lottery 模块 | `POST /api/Lottery/BusinessAddLottery` | 创建抽奖 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lottery.business_delete_lottery` | Lottery 模块 | `POST /api/Lottery/BusinessDeleteLottery` | 删除抽奖活动 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lottery.business_get_lottery_by_id` | Lottery 模块 | `POST /api/Lottery/BusinessGetLotteryById` | 获取活动详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lottery.business_get_lottery_list` | Lottery 模块 | `POST /api/Lottery/BusinessGetLotteryList` | 获取抽奖活动列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lottery.business_get_lottery_win_user_list` | Lottery 模块 | `POST /api/Lottery/BusinessGetLotteryWinUserList` | 中奖用户列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lottery.business_lottery_data_statistical` | Lottery 模块 | `POST /api/Lottery/BusinessLotteryDataStatistical` | 抽奖活动数据统计 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lottery.business_update_lottery` | Lottery 模块 | `POST /api/Lottery/BusinessUpdateLottery` | 修改抽奖活动 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_activity_coupon_group_send_get_list` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessActivityCouponGroupSendGetList` | 获取优惠券活动短信群发记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_cancel_task` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessCancelTask` | 取消短信任务发送 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_edit_coupon_center_count` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessEditCouponCenterCount` | 修改券包数量 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_edit_coupon_count` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessEditCouponCount` | 修改优惠券数量 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_fast_coupon_send` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessFastCouponSend` | 快速发券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_group_send_sms_get_list` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessGroupSendSmsGetList` | 获取营销短信群发记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_group_send_sms_set` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessGroupSendSmsSet` | 提交营销活动 （ 短信群发设置） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_activity_add` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingActivityAdd` | 创建优惠券活动-满赠 券包 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_activity_delete` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingActivityDelete` | 删除营销活动 （优惠券活动，满赠，券包） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_activity_edit` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingActivityEdit` | 修改优惠券活动-满赠 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_activity_get_info` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingActivityGetInfo` | 通过marketingId 获取营销活动内容详细 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.marketing_center.business_marketing_activity_statistics` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingActivityStatistics` | 营销活动优惠券领取记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_birthday_get_info` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingBirthdayGetInfo` | 获取生日营销详细 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.marketing_center.business_marketing_birthday_send_list` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingBirthdaySendList` | 生日营销的领取记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_get_list` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingGetList` | 营销列表查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.marketing_center.business_marketing_qr_code_share` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingQrCodeShare` | 生成二维码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_sms_send_get_info` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingSmsSendGetInfo` | 获取群发短信的短信发送详细 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_marketing_sms_send_log_get_list` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessMarketingSmsSendLogGetList` | 获取群发短信的优惠券领取记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.marketing_center.business_update_alliance_activity_state` | MarketingCenter 模块 | `POST /api/MarketingCenter/BusinessUpdateAllianceActivityState` | 修改联盟券活动状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.materiel.add_materiel_trade` | Materiel 模块 | `POST /api/Materiel/AddMaterielTrade` | 新增物料订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.materiel.get_materiel_by_id` | Materiel 模块 | `POST /api/Materiel/GetMaterielById` | 获取单个物料 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.materiel.get_materiel_list` | Materiel 模块 | `POST /api/Materiel/GetMaterielList` | 获取物料列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.materiel.get_materiel_trade_list` | Materiel 模块 | `POST /api/Materiel/GetMaterielTradeList` | 获取物料订单列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.member_growth.get_follow_up_history` | MemberGrowth 模块 | `POST /api/MemberGrowth/GetFollowUpHistory` | 按时间倒序读取会员的完整跟进历史。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.member_growth.get_workbench` | MemberGrowth 模块 | `POST /api/MemberGrowth/GetWorkbench` | 读取指定日期范围内的增长汇总和体验会员分页列表。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.member_growth.save_follow_up` | MemberGrowth 模块 | `POST /api/MemberGrowth/SaveFollowUp` | 校验会员和跟进内容后追加一条跟进历史。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.merchant.business_certification_get_info` | Merchant 模块 | `POST /api/Merchant/BusinessCertificationGetInfo` | 获取店铺认证申请详细信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.merchant.business_certification_get_mer_state` | Merchant 模块 | `POST /api/Merchant/BusinessCertificationGetMerState` | 获取签约状态 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.merchant.business_certification_up_grade` | Merchant 模块 | `POST /api/Merchant/BusinessCertificationUpGrade` | 认证 个人升级公司 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.merchant.business_get_merchant_info` | Merchant 模块 | `POST /api/Merchant/BusinessGetMerchantInfo` | 获取开户信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.merchant.business_get_merchants_config_state` | Merchant 模块 | `POST /api/Merchant/BusinessGetMerchantsConfigState` | 获取店铺收款管理状态 是否认证 体验快捷收款状态 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.merchant.business_is_need_certification` | Merchant 模块 | `POST /api/Merchant/BusinessIsNeedCertification` | 是否需要提示认证 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.merchant.business_pay_money_to_wechat` | Merchant 模块 | `POST /api/Merchant/BusinessPayMoneyToWechat` | 微信打款认证 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.merchant.business_set_certification` | Merchant 模块 | `POST /api/Merchant/BusinessSetCertification` | 店铺认证申请 创建 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.merchant.business_set_merchant_info` | Merchant 模块 | `POST /api/Merchant/BusinessSetMerchantInfo` | 设置开户信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.merchant.confirm_certification` | Merchant 模块 | `POST /api/Merchant/ConfirmCertification` | 确认开通收款 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.mini_program.get_mini_program_meun_contents` | MiniProgram 模块 | `POST /api/MiniProgram/GetMiniProgramMeunContents` | 获取小程序菜单对应子项 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.mini_program.get_mini_program_meuns` | MiniProgram 模块 | `POST /api/MiniProgram/GetMiniProgramMeuns` | 获取小程序菜单列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.notice_center.get_store_notice_set` | NoticeCenter 模块 | `POST /api/NoticeCenter/GetStoreNoticeSet` | 获取通知中心设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.notice_center.set_store_notice_remind` | NoticeCenter 模块 | `POST /api/NoticeCenter/SetStoreNoticeRemind` | 设置通知中心设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.notice_center.set_store_notice_set` | NoticeCenter 模块 | `POST /api/NoticeCenter/SetStoreNoticeSet` | 设置通知中心设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.official_we_chat.get_auth_url` | OfficialWeChat 模块 | `POST /api/OfficialWeChat/GetAuthUrl` | 获取授权地址 | 源码标记免登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.pay.ali_pay_result_notify` | Pay 模块 | `POST /api/Pay/AliPayResultNotify` | 获取异步支付通知 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.pay.get_business_pay_param` | Pay 模块 | `POST /api/Pay/GetBusinessPayParam` | 获取Js签名 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.pay.we_chat_pay_result_notify` | Pay 模块 | `POST /api/Pay/WeChatPayResultNotify` | 获取异步支付通知 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.performance.apply_cash_out` | Performance 模块 | `POST /api/Performance/ApplyCashOut` | 员工申请提现 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.cashout_agree` | Performance 模块 | `POST /api/Performance/CashoutAgree` | 通过提现申请 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.cashout_reject` | Performance 模块 | `POST /api/Performance/CashoutReject` | 拒绝提现申请 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.consumption_performance_items_set` | Performance 模块 | `POST /api/Performance/ConsumptionPerformanceItemsSet` | 绩效订单关联服务项目修改 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.excel_staff_performance` | Performance 模块 | `POST /api/Performance/ExcelStaffPerformance` | Excel导出店铺某一个员工绩效 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.excel_store_performance` | Performance 模块 | `POST /api/Performance/ExcelStorePerformance` | Excel导出店铺所有员工绩效 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.get_admin_cash_out_logs` | Performance 模块 | `POST /api/Performance/GetAdminCashOutLogs` | 审核人员获取提现申请记录 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_audit_cash_out_count` | Performance 模块 | `POST /api/Performance/GetAuditCashOutCount` | 获取待审批提现申请 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.get_cash_out_base_info` | Performance 模块 | `POST /api/Performance/GetCashOutBaseInfo` | 获取员工提现基础信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_cash_out_records` | Performance 模块 | `POST /api/Performance/GetCashOutRecords` | 获取提现记录列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_consumption_performance_info` | Performance 模块 | `POST /api/Performance/GetConsumptionPerformanceInfo` | 获取订单的详细绩效 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_last_cashout_record` | Performance 模块 | `POST /api/Performance/GetLastCashoutRecord` | 获取最后一次提现记录 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_lessons_card_money` | Performance 模块 | `POST /api/Performance/GetLessonsCardMoney` | 获取上课销卡金额统计 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_lessons_info_by_card` | Performance 模块 | `POST /api/Performance/GetLessonsInfoByCard` | 获取课程绩效用卡详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_staff_all_performance_list` | Performance 模块 | `POST /api/Performance/GetStaffAllPerformanceList` | 获取店铺某一个员工绩效列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_staff_performance_list` | Performance 模块 | `POST /api/Performance/GetStaffPerformanceList` | 获取店铺某一个员工绩效列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_store_performance_items` | Performance 模块 | `POST /api/Performance/GetStorePerformanceItems` | 获取店铺统计绩效方式 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.get_store_performance_list` | Performance 模块 | `POST /api/Performance/GetStorePerformanceList` | 获取店铺所有员工绩效列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.performance.print_staff_performance` | Performance 模块 | `POST /api/Performance/PrintStaffPerformance` | 打印店铺某一个员工绩效小票 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.print_store_performance` | Performance 模块 | `POST /api/Performance/PrintStorePerformance` | 打印店铺所有员工绩效小票 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.set_performance_remark` | Performance 模块 | `POST /api/Performance/SetPerformanceRemark` | 添加绩效备注 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.set_performance_state` | Performance 模块 | `POST /api/Performance/SetPerformanceState` | 设置订单的绩效状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.performance.set_store_performance_item` | Performance 模块 | `POST /api/Performance/SetStorePerformanceItem` | 设置绩效店铺项目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.poster.get_copywriting` | Poster 模块 | `POST /api/Poster/GetCopywriting` | 获取文案 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.poster.get_poster_by_id` | Poster 模块 | `POST /api/Poster/GetPosterById` | 获取单个海报 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.poster.get_poster_list` | Poster 模块 | `POST /api/Poster/GetPosterList` | 获取海报列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.poster.get_poster_qr_code` | Poster 模块 | `POST /api/Poster/GetPosterQrCode` | 获取海报二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.promote.business_get_promote_user_list` | Promote 模块 | `POST /api/Promote/BusinessGetPromoteUserList` | 获取推广人员信息列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.promote.get_promote_report` | Promote 模块 | `POST /api/Promote/GetPromoteReport` | 获取推广信息报表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.promote.promote_total_rank` | Promote 模块 | `POST /api/Promote/PromoteTotalRank` | 推广总榜 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.recommend.business_get_recommend_qr_code` | Recommend 模块 | `POST /api/Recommend/BusinessGetRecommendQrCode` | 当面邀请二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.recommend.get_recommend_statistical` | Recommend 模块 | `POST /api/Recommend/GetRecommendStatistical` | 获取邀请数据统计信息（暂时不用） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.recommend.get_recommend_store_list` | Recommend 模块 | `POST /api/Recommend/GetRecommendStoreList` | 获取邀请店铺列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.search.get_serarch_hot` | Search 模块 | `POST /api/Search/GetSerarchHot` | 获取搜索热词 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.service_item.add_service_category` | ServiceItem 模块 | `POST /api/ServiceItem/AddServiceCategory` | 创建服务分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.business_add_store_service_item` | ServiceItem 模块 | `POST /api/ServiceItem/BusinessAddStoreServiceItem` | 添加/ 价目表服务 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.business_update_store_service_item` | ServiceItem 模块 | `POST /api/ServiceItem/BusinessUpdateStoreServiceItem` | /修改 价目表服务 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.del_service_category` | ServiceItem 模块 | `POST /api/ServiceItem/DelServiceCategory` | 删除服务分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.del_service_item` | ServiceItem 模块 | `POST /api/ServiceItem/DelServiceItem` | 删除预约项目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.del_store_service_item` | ServiceItem 模块 | `POST /api/ServiceItem/DelStoreServiceItem` | 删除价目表项目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.edit_service_category` | ServiceItem 模块 | `POST /api/ServiceItem/EditServiceCategory` | 编辑服务分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.edit_service_category_info` | ServiceItem 模块 | `POST /api/ServiceItem/EditServiceCategoryInfo` | 编辑服务的分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.get_is_service` | ServiceItem 模块 | `POST /api/ServiceItem/GetIsService` | 获取是否有服务 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.service_item.get_pay_service_item_list` | ServiceItem 模块 | `POST /api/ServiceItem/GetPayServiceItemList` | 获取项目列表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.get_service_category_list` | ServiceItem 模块 | `POST /api/ServiceItem/GetServiceCategoryList` | 获取服务分类 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.service_item.get_service_item_info` | ServiceItem 模块 | `POST /api/ServiceItem/GetServiceItemInfo` | 获取项目详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.service_item.get_service_item_list` | ServiceItem 模块 | `POST /api/ServiceItem/GetServiceItemList` | 获取项目列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.service_item.get_service_item_staff` | ServiceItem 模块 | `POST /api/ServiceItem/GetServiceItemStaff` | 获取项目店员预约开启状态 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.service_item.get_service_list` | ServiceItem 模块 | `POST /api/ServiceItem/GetServiceList` | 获取服务列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.service_item.service_on_off` | ServiceItem 模块 | `POST /api/ServiceItem/ServiceOnOff` | 上/下架服务 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.set_service_item_info` | ServiceItem 模块 | `POST /api/ServiceItem/SetServiceItemInfo` | 设置预约项目详细 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.sort_service` | ServiceItem 模块 | `POST /api/ServiceItem/SortService` | 排序服务 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.service_item.sort_service_category` | ServiceItem 模块 | `POST /api/ServiceItem/SortServiceCategory` | 排序服务分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.sms_center.business_get_sign_name_length` | SmsCenter 模块 | `POST /api/SmsCenter/BusinessGetSignNameLength` | 获取短信签名长度 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.sms_center.business_get_sms_config` | SmsCenter 模块 | `POST /api/SmsCenter/BusinessGetSmsConfig` | 获取短信预览内容配置信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.sms_center.buy_sms_added_service` | SmsCenter 模块 | `POST /api/SmsCenter/BuySmsAddedService` | 购买短信增值 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.sms_center.get_sms_added_service_order` | SmsCenter 模块 | `POST /api/SmsCenter/GetSmsAddedServiceOrder` | 短信购买记录 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.sms_center.get_sms_added_services` | SmsCenter 模块 | `POST /api/SmsCenter/GetSmsAddedServices` | 获取短信充值列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.sms_center.get_store_sms_balance` | SmsCenter 模块 | `POST /api/SmsCenter/GetStoreSmsBalance` | 获取短余额 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.sms_center.get_store_sms_notice_set` | SmsCenter 模块 | `POST /api/SmsCenter/GetStoreSmsNoticeSet` | 获取店铺短信通知设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.sms_center.get_store_sms_send_log` | SmsCenter 模块 | `POST /api/SmsCenter/GetStoreSmsSendLog` | 获取短信发送日志 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.sms_center.get_store_sms_send_log_count` | SmsCenter 模块 | `POST /api/SmsCenter/GetStoreSmsSendLogCount` | 获取短信发送总条数 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.sms_center.get_store_sms_template_list` | SmsCenter 模块 | `POST /api/SmsCenter/GetStoreSmsTemplateList` | 获取短信模板列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.sms_center.update_store_sms_notice_set` | SmsCenter 模块 | `POST /api/SmsCenter/UpdateStoreSmsNoticeSet` | 修改店铺短信通知设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.sms_notice_set.get_store_sms_notice_set_model` | SmsNoticeSet 模块 | `POST /api/SmsNoticeSet/GetStoreSmsNoticeSetModel` | 根据店铺ID获取店铺短信中心设置实体 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.sms_notice_set.store_sms_notice_seting` | SmsNoticeSet 模块 | `POST /api/SmsNoticeSet/StoreSmsNoticeSeting` | 设定短信中心提醒 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.add_template` | Template 模块 | `POST /api/Template/AddTemplate` | 新增页面模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_add_store_template_control` | Template 模块 | `POST /api/Template/BusinessAddStoreTemplateControl` | 新增页面模板控件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_add_template` | Template 模块 | `POST /api/Template/BusinessAddTemplate` | 新增页面模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_delete_template_data` | Template 模块 | `POST /api/Template/BusinessDeleteTemplateData` | 删除页面模板数据 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_ge_program_control_list` | Template 模块 | `POST /api/Template/BusinessGeProgramControlList` | 获取所有模板系统控件 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template.business_ge_program_control_style` | Template 模块 | `POST /api/Template/BusinessGeProgramControlStyle` | 获取系统控件样式 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_get_program_template_list` | Template 模块 | `POST /api/Template/BusinessGetProgramTemplateList` | 获取店铺模板列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template.business_get_store_template_list` | Template 模块 | `POST /api/Template/BusinessGetStoreTemplateList` | 获取店铺模板列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template.business_get_template` | Template 模块 | `POST /api/Template/BusinessGetTemplate` | 获取页面模板 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template.business_get_template_data` | Template 模块 | `POST /api/Template/BusinessGetTemplateData` | 获取页面模板数据 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template.business_set_program_template` | Template 模块 | `POST /api/Template/BusinessSetProgramTemplate` | 店铺启用模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_sort_store_template_control` | Template 模块 | `POST /api/Template/BusinessSortStoreTemplateControl` | 排序页面模板控件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_update_store_template` | Template 模块 | `POST /api/Template/BusinessUpdateStoreTemplate` | 修改页面模板主題 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_update_store_template_control` | Template 模块 | `POST /api/Template/BusinessUpdateStoreTemplateControl` | 修改页面模板控件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_update_store_template_control_state` | Template 模块 | `POST /api/Template/BusinessUpdateStoreTemplateControlState` | 修改页面模板控件状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_update_template_data` | Template 模块 | `POST /api/Template/BusinessUpdateTemplateData` | 修改页面模板数据 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.business_update_template_page` | Template 模块 | `POST /api/Template/BusinessUpdateTemplatePage` | 修改模板主题 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.template.get_mini_program_meun_contents` | Template 模块 | `POST /api/Template/GetMiniProgramMeunContents` | 获取小程序菜单对应子项 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template.get_mini_program_meuns` | Template 模块 | `POST /api/Template/GetMiniProgramMeuns` | 获取小程序菜单列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template.get_template_banner` | Template 模块 | `POST /api/Template/GetTemplateBanner` | 获取模板banner图库 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.template_test.program_control_style_add` | TemplateTest 模块 | `POST /api/TemplateTest/ProgramControlStyleAdd` | 修改页面模板控件 | 需要登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.unit_test.aliyun_sms_template_send_test` | UnitTest 模块 | `POST /api/UnitTest/AliyunSmsTemplateSendTest` | 阿里云短信发送测试。默认只预览参数，不真实发送。 | 需要登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.unit_test.aliyun_sms_template_submit_test` | UnitTest 模块 | `POST /api/UnitTest/AliyunSmsTemplateSubmitTest` | 阿里云短信模板迁移测试。默认只预览转换结果，不提交审核、不更新数据库。 | 需要登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.unit_test.send_wx_img_message_test` | UnitTest 模块 | `POST /api/UnitTest/SendWxImgMessageTest` | 【源码推断】SendWxImgMessageTest 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.unit_test.test_province` | UnitTest 模块 | `POST /api/UnitTest/testProvince` | 【源码推断】testProvince 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | 测试、模板测试或后台认证 Controller，不进入客服工具。 |
| `crmapi.we_chat_third_platform.add_category` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/AddCategory` | 添加类目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.add_material` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/AddMaterial` | 添加永久素材 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.add_template` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/AddTemplate` | 将草稿添加到代码模板库 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.api_get_authorizer_info` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/ApiGetAuthorizerInfo` | 获取授权方账号信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.auto_config_template` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/AutoConfigTemplate` | 配置三方消息模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.bind_store` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/BindStore` | 绑定店铺 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.bind_tester` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/BindTester` | 绑定微信用户为体验者 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.create_register_we_app` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/CreateRegisterWeApp` | 企业快速创建小程序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.customer_call_back` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/{appid}/CustomerCallBack` | 【源码推断】CustomerCallBack 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.we_chat_third_platform.del_private_template` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/DelPrivateTemplate` | 删除模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.get_all_categories` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetAllCategories` | 获取可以设置的所有类目 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_all_private_template` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetAllPrivateTemplate` | 获取已添加至帐号下所有模板列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_audit_status` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetAuditStatus` | 查询指定发布审核单的审核状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.get_authorizer_info` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetAuthorizerInfo` | 获取APP授权信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_category` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetCategory` | 获取已设置的所有类目 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_industry` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetIndustry` | 获取三方行业 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_privacy_interface` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetPrivacyInterface` | 获取API审核结果 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_privacy_setting` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetPrivacySetting` | 获取小程序用户隐私保护指引 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_query_nickname` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetQueryNickname` | 查询改名审核状态 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_template_draft_list` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetTemplateDraftList` | 获取代码草稿列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.get_template_list` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/GetTemplateList` | 获取代码模板列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.media_upload` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/MediaUpload` | 添加临时素材 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.qr_code_jump_add` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/QrCodeJumpAdd` | 增加或修改二维码规则 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.qr_code_jump_download` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/QrCodeJumpDownload` | 获取校验文件名称及内容 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.qr_code_jump_get` | WeChatThirdPlatform 模块 | `GET /api/WeChatThirdPlatform/QrCodeJumpGet` | 获取已设置的二维码规则 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.qr_code_jump_mime_get` | WeChatThirdPlatform 模块 | `GET /api/WeChatThirdPlatform/QrCodeJumpMimeGet` | 获取已设置的二维码规则 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.we_chat_third_platform.qr_code_jump_publish` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/QrCodeJumpPublish` | 发布已设置的二维码规则 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.serch_register_we_app` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/SerchRegisterWeApp` | 查询企业快速创建小程序审核结果 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.set_domain` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/SetDomain` | 设置域名 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.set_industry` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/SetIndustry` | 设置三方行业 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.set_nick_name` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/SetNickName` | 设置小程序名称 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.set_privacy_setting` | WeChatThirdPlatform 模块 | `GET /api/WeChatThirdPlatform/SetPrivacySetting` | 配置小程序用户隐私保护指引 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.sync_xcx` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/SyncXcx` | 获取API审核结果 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.update_show_wxa_item` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/UpdateShowWxaItem` | 设置展示的公众号信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.we_chat_third_platform.we_chat_apply_privacy_interface` | WeChatThirdPlatform 模块 | `POST /api/WeChatThirdPlatform/WeChatApplyPrivacyInterface` | 申请API审核 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.admin_login` | WeiXin 模块 | `POST /api/WeiXin/AdminLogin` | 管理员微信登录 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.advertising_call_back` | WeiXin 模块 | `GET /api/WeiXin/AdvertisingCallBack` | 【源码推断】AdvertisingCallBack 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.bind` | WeiXin 模块 | `POST /api/WeiXin/Bind` | 将公众号/小程序绑定到开放平台帐号下 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.business_we_chat_call_back.get` | WeiXin 模块 | `GET /api/WeiXin/BusinessWeChatCallBack` | 公众号接入测试 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.business_we_chat_call_back.post` | WeiXin 模块 | `POST /api/WeiXin/BusinessWeChatCallBack` | 公众号异步消息处理 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.check_user_is_attention_official_account` | WeiXin 模块 | `POST /api/WeiXin/CheckUserIsAttentionOfficialAccount` | 判断当前用户是否关注公众号 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：WechatAttentionProvider.GetOpenIdListByUid |
| `crmapi.wei_xin.check_wx_verify_nick_name` | WeiXin 模块 | `POST /api/WeiXin/CheckWxVerifyNickName` | 微信认证名称检测 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.wei_xin.code_commit` | WeiXin 模块 | `POST /api/WeiXin/CodeCommit` | 上传代码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.create` | WeiXin 模块 | `POST /api/WeiXin/Create` | 创建开放平台帐号并绑定公众号/小程序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.customer_we_chat_call_back` | WeiXin 模块 | `POST /api/WeiXin/CustomerWeChatCallBack` | 三方平台回调接口 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.fast_register` | WeiXin 模块 | `POST /api/WeiXin/FastRegister` | 复用公众号主体信息快速创建小程序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.get` | WeiXin 模块 | `POST /api/WeiXin/Get` | 获取公众号/小程序所绑定的开放平台帐号 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.wei_xin.get_audit_status` | WeiXin 模块 | `POST /api/WeiXin/GetAuditStatus` | 查询指定发布审核单的审核状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.get_code_template_list` | WeiXin 模块 | `POST /api/WeiXin/GetCodeTemplateList` | 获取代码模板列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.wei_xin.get_pre_auth_code` | WeiXin 模块 | `POST /api/WeiXin/GetPreAuthCode` | 获取预授权码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.wei_xin.get_user_info_by_work_we_chat` | WeiXin 模块 | `POST /api/WeiXin/GetUserInfoByWorkWeChat` | 企业微信获取用户信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.wei_xin.login_user` | WeiXin 模块 | `POST /api/WeiXin/LoginUser` | 微信登录 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.modify_head_image` | WeiXin 模块 | `POST /api/WeiXin/ModifyHeadImage` | 修改头像 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.modify_signature` | WeiXin 模块 | `POST /api/WeiXin/ModifySignature` | 修改功能介绍 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.news_upload_img` | WeiXin 模块 | `POST /api/WeiXin/NewsUploadImg` | 发送图文消息 上传图片到微信服务器 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.qr_code_jump_add` | WeiXin 模块 | `POST /api/WeiXin/QrCodeJumpAdd` | 增加或修改二维码规则 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.qr_code_jump_download` | WeiXin 模块 | `POST /api/WeiXin/QrCodeJumpDownload` | 获取校验文件名称及内容 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.release` | WeiXin 模块 | `POST /api/WeiXin/Release` | 发布 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.save_authroize_info` | WeiXin 模块 | `POST /api/WeiXin/SaveAuthroizeInfo` | 保存授权信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.send_custom_message` | WeiXin 模块 | `POST /api/WeiXin/SendCustomMessage` | 发送客服消息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.send_img_message` | WeiXin 模块 | `POST /api/WeiXin/SendImgMessage` | 发送图文消息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.start_push_ticket` | WeiXin 模块 | `POST /api/WeiXin/StartPushTicket` | 启动Ticket推送 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.submit_audit` | WeiXin 模块 | `POST /api/WeiXin/SubmitAudit` | 提交审核 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.undo_code_audit` | WeiXin 模块 | `POST /api/WeiXin/UndoCodeAudit` | 审核撤回 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.upload_news` | WeiXin 模块 | `POST /api/WeiXin/UploadNews` | 发送图文消息 上传文章 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.user_authorization` | WeiXin 模块 | `POST /api/WeiXin/UserAuthorization` | 微信授权修改用户数据 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.user_mobile_decryption` | WeiXin 模块 | `POST /api/WeiXin/UserMobileDecryption` | 微信解密手机号 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.wei_xin.we_chat_call_back.get` | WeiXin 模块 | `GET /api/WeiXin/WeChatCallBack` | 公众号接入测试 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.we_chat_call_back.post` | WeiXin 模块 | `POST /api/WeiXin/WeChatCallBack` | 公众号异步消息处理 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.we_chat_kf_call_back` | WeiXin 模块 | `GET /api/WeiXin/WeChatKFCallBack` | 公众号接入测试 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.work_we_chat_call_back` | WeiXin 模块 | `GET /api/WeiXin/WorkWeChatCallBack` | 企业微信公众号接入测试 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.wei_xin.work_we_chat_get_js_signature` | WeiXin 模块 | `POST /api/WeiXin/WorkWeChatGetJsSignature` | 企业微信获取JS签名 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.wei_xin.wx_pay_notify` | WeiXin 模块 | `POST /api/WeiXin/WxPayNotify` | 领券回调通知 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.work_we_chat.add_msg_template` | 企业微信 | `GET /api/WorkWeChat/AddMsgTemplate` | 群发消息测试 | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.work_we_chat.external_user_id_to_pending_id` | 企业微信 | `GET /api/WorkWeChat/ExternalUserIdToPendingId` | UnionId转ExternalUserId | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.work_we_chat.get_auth_url` | 企业微信 | `POST /api/WorkWeChat/GetAuthUrl` | 获取授权地址 | 源码标记免登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_we_chat.get_external_contact_list` | 企业微信 | `GET /api/WorkWeChat/GetExternalContactList` | 获取配置了客户联系功能的成员列表 | 源码标记免登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_we_chat.get_follow_user_list` | 企业微信 | `GET /api/WorkWeChat/GetFollowUserList` | 获取配置了客户联系功能的成员列表 | 源码标记免登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：WorkWechatAttentionProvider.AddWorkWechatAttentions |
| `crmapi.work_we_chat.get_js_signature` | 企业微信 | `POST /api/WorkWeChat/GetJsSignature` | 获取Js签名 | 源码标记免登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_we_chat.login_user` | 企业微信 | `POST /api/WorkWeChat/LoginUser` | 微信登录 | 源码标记免登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.work_we_chat.union_id_to_external_user_id` | 企业微信 | `GET /api/WorkWeChat/UnionIdToExternalUserId` | UnionId转ExternalUserId | 源码标记免登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.work_we_chat_code.add_code` | 企业微信 | `POST /api/WorkWeChatCode/AddCode` | 新增活码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.work_we_chat_code.delete_code` | 企业微信 | `POST /api/WorkWeChatCode/DeleteCode` | 删除活码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.work_we_chat_code.edit_code` | 企业微信 | `POST /api/WorkWeChatCode/EditCode` | 修改活码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.work_we_chat_code.get_code_info` | 企业微信 | `POST /api/WorkWeChatCode/GetCodeInfo` | 获取单个活码详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_we_chat_code.get_codes` | 企业微信 | `POST /api/WorkWeChatCode/GetCodes` | 获取所有活码列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.business_check_user_coupon_used` | 优惠券 | `POST /api/Coupon/BusinessCheckUserCouponUsed` | 判断优惠券是否可用 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.business_export_coupon_report` | 优惠券 | `POST /api/Coupon/BusinessExportCouponReport` | 导出店铺优惠券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon.business_get_coupon_count` | 优惠券 | `POST /api/Coupon/BusinessGetCouponCount` | 获取每个状态对应的优惠券总数量 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.business_get_coupon_list` | 优惠券 | `POST /api/Coupon/BusinessGetCouponList` | 查询店铺优惠券 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.business_get_coupon_list_by_store_id` | 优惠券 | `POST /api/Coupon/BusinessGetCouponListByStoreId` | 获取当前店铺的所有优惠券列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.business_get_coupon_money` | 优惠券 | `POST /api/Coupon/BusinessGetCouponMoney` | 查询店铺优惠券代金券总额 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.business_get_export_coupon_data_log` | 优惠券 | `POST /api/Coupon/BusinessGetExportCouponDataLog` | 优惠券数据导出记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon.business_user_coupon_add` | 优惠券 | `POST /api/Coupon/BusinessUserCouponAdd` | 为会员 赠送优惠券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon.business_user_coupon_used` | 优惠券 | `POST /api/Coupon/BusinessUserCouponUsed` | 使用优惠券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon.get_user_coupon` | 优惠券 | `POST /api/Coupon/GetUserCoupon` | 获取单个优惠券 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.get_user_coupon_count` | 优惠券 | `POST /api/Coupon/GetUserCouponCount` | 获取用户可用优惠券数量 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon.get_user_coupon_list` | 优惠券 | `POST /api/Coupon/GetUserCouponList` | 获取优惠券列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon_center.business_add_coupon_centers` | 优惠券 | `POST /api/CouponCenter/BusinessAddCouponCenters` | 优惠中心优惠券 创建 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon_center.business_coupon_center_coupon_report` | 优惠券 | `POST /api/CouponCenter/BusinessCouponCenterCouponReport` | 优惠中心 优惠券详细页面报表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon_center.business_coupon_center_info` | 优惠券 | `POST /api/CouponCenter/BusinessCouponCenterInfo` | 优惠中心券 详细信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon_center.business_coupon_center_statistics` | 优惠券 | `POST /api/CouponCenter/BusinessCouponCenterStatistics` | 优惠中心类别列表统计 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon_center.business_coupon_centers_update` | 优惠券 | `POST /api/CouponCenter/BusinessCouponCentersUpdate` | 修改优惠中心优惠券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon_center.business_coupon_centers_update_one` | 优惠券 | `POST /api/CouponCenter/BusinessCouponCentersUpdateOne` | 修改优惠中心优惠券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon_center.business_coupon_qr_code_share` | 优惠券 | `POST /api/CouponCenter/BusinessCouponQrCodeShare` | 生成定向券二维码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon_center.business_share_coupon_get_random_param` | 优惠券 | `POST /api/CouponCenter/BusinessShareCouponGetRandomParam` | 分享优惠券生成随机密钥 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：CouponPasswordProvider.CouponPasswordAdd |
| `crmapi.coupon_link.del_coupon_link` | 优惠券 | `POST /api/CouponLink/DelCouponLink` | 删除支付宝内券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.coupon_link.get_coupon_link_detail` | 优惠券 | `POST /api/CouponLink/GetCouponLinkDetail` | 获取支付宝内券详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon_link.get_coupon_link_list` | 优惠券 | `POST /api/CouponLink/GetCouponLinkList` | 获取支付宝内券列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.coupon_link.save_coupon_link` | 优惠券 | `POST /api/CouponLink/SaveCouponLink` | 添加/修改支付宝内券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_coupon_center.business_store_coupon_center_set` | 优惠券 | `POST /api/StoreCouponCenter/BusinessStoreCouponCenterSet` | 修改店铺优惠中心券类型开关 满送，生日，老带新 功能开关 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_user_black_list.add_store_user_black_list` | 会员与身份 | `POST /api/StoreUserBlackList/AddStoreUserBlackList` | 会员加入黑名单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_user_black_list.edit_store_user_black_list` | 会员与身份 | `POST /api/StoreUserBlackList/EditStoreUserBlackList` | 修改黑名单信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_user_black_list.get_store_user_black_list` | 会员与身份 | `POST /api/StoreUserBlackList/GetStoreUserBlackList` | 黑名单列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_user_black_list.get_store_user_black_list_logs` | 会员与身份 | `POST /api/StoreUserBlackList/GetStoreUserBlackListLogs` | 黑名单操作日志列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_user_black_list.remove_store_user_black_list` | 会员与身份 | `POST /api/StoreUserBlackList/RemoveStoreUserBlackList` | 会员移出黑名单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.app_request_voice` | 会员与身份 | `POST /api/User/AppRequestVoice` | App响应语音播报设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.batch_edit_user_tags` | 会员与身份 | `POST /api/User/BatchEditUserTags` | 批量会员打标 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.business_get_store_user` | 会员与身份 | `POST /api/User/BusinessGetStoreUser` | 加载用户信息（会员编辑资料页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.business_get_u_statistical` | 会员与身份 | `POST /api/User/BusinessGetUStatistical` | 会员信息统计 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.business_get_user_info` | 会员与身份 | `POST /api/User/BusinessGetUserInfo` | 获取用户全部详细信息和名下所有会员卡（会员详情页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.business_set_store_user` | 会员与身份 | `POST /api/User/BusinessSetStoreUser` | 修改用户信息（会员编辑资料页） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.check_user_by_mobile` | 会员与身份 | `POST /api/User/CheckUserByMobile` | 检查体验手机号 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.check_user_mobile` | 会员与身份 | `POST /api/User/CheckUserMobile` | 检测手机号能否使用 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.create_user` | 会员与身份 | `POST /api/User/CreateUser` | 管理员新增用户（新增会员） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.delete_user` | 会员与身份 | `POST /api/User/DeleteUser` | 注销会员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.edit_user_info` | 会员与身份 | `POST /api/User/EditUserInfo` | 修改用户信息（会员编辑资料页） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.edit_user_search_condition` | 会员与身份 | `POST /api/User/EditUserSearchCondition` | 修改会员搜索条件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.edit_user_search_condition_state` | 会员与身份 | `POST /api/User/EditUserSearchConditionState` | 修改会员搜索条件状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.get_birthday_users_list` | 会员与身份 | `POST /api/User/GetBirthdayUsersList` | 获取临近生日列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_experience_users_list` | 会员与身份 | `POST /api/User/GetExperienceUsersList` | 获取体验人员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_low_balance_users_list` | 会员与身份 | `POST /api/User/GetLowBalanceUsersList` | 获取低余额会员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_user_info` | 会员与身份 | `POST /api/User/GetUserInfo` | 获取用户详细信息（会员详情页） | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：WorkWeChatProvider.ConvertAddWay |
| `crmapi.user.get_user_search_condition_info` | 会员与身份 | `POST /api/User/GetUserSearchConditionInfo` | 获取单一会员搜索条件 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_user_search_condition_list` | 会员与身份 | `POST /api/User/GetUserSearchConditionList` | 获取会员搜索条件列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_user_sign_agreement_info` | 会员与身份 | `POST /api/User/GetUserSignAgreementInfo` | 签署合同 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_users_list` | 会员与身份 | `POST /api/User/GetUsersList` | 获取会员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_users_list_v2` | 会员与身份 | `POST /api/User/GetUsersListV2` | 获取会员列表V2 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.get_users_sum` | 会员与身份 | `POST /api/User/GetUsersSum` | 获取会员数量统计 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user.import_work_we_chat_user` | 会员与身份 | `POST /api/User/ImportWorkWeChatUser` | 导入企业微信联系人 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.login` | 会员与身份 | `POST /api/User/Login` | 用户登陆 | 需要登录 | D | 登录、回调或通知入口，不是只读业务查询。 |
| `crmapi.user.save_user_search_condition` | 会员与身份 | `POST /api/User/SaveUserSearchCondition` | 保存会员搜索条件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.set_user_mobile` | 会员与身份 | `POST /api/User/SetUserMobile` | 修改手机号码（会员编辑资料页） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.update_user_image` | 会员与身份 | `POST /api/User/UpdateUserImage` | 更新头像 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.user_quit` | 会员与身份 | `POST /api/User/UserQuit` | 用户退出 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user.user_sign_agreement` | 会员与身份 | `POST /api/User/UserSignAgreement` | 签署合同 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user_group.add_group` | 会员与身份 | `POST /api/UserGroup/AddGroup` | 添加会员分组 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user_group.delete_group` | 会员与身份 | `POST /api/UserGroup/DeleteGroup` | 删除会员分组 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user_group.get_group` | 会员与身份 | `POST /api/UserGroup/GetGroup` | 获取会员分组 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user_group.get_group_list` | 会员与身份 | `POST /api/UserGroup/GetGroupList` | 获取商家会员分组列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.user_group.group_add_user` | 会员与身份 | `POST /api/UserGroup/GroupAddUser` | 向分组添加会员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.user_group.update_group` | 会员与身份 | `POST /api/UserGroup/UpdateGroup` | 修改会员分组 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.add_source_way` | 会员卡与课卡 | `POST /api/Card/AddSourceWay` | 新增会员来源渠道 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_add_user_card` | 会员卡与课卡 | `POST /api/Card/BusinessAddUserCard` | 管理员新增会员卡（新增会员卡） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_add_user_card_operation_log` | 会员卡与课卡 | `POST /api/Card/BusinessAddUserCardOperationLog` | 停卡 添加停卡记录（时效卡） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_cancel_user_card` | 会员卡与课卡 | `POST /api/Card/BusinessCancelUserCard` | 注销会员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_cancel_user_card_child` | 会员卡与课卡 | `POST /api/Card/BusinessCancelUserCardChild` | 注销会员的单张会员卡 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_car_number_by_num` | 会员卡与课卡 | `POST /api/Card/BusinessCarNumberByNum` | 通过车牌号模糊 搜索汽车车牌 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_get_cancel_user_log` | 会员卡与课卡 | `POST /api/Card/BusinessGetCancelUserLog` | 获取用户会员卡回收日志列表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_get_card_info_by_number` | 会员卡与课卡 | `POST /api/Card/BusinessGetCardInfoByNumber` | 通过会员卡号 获取CardId 和UID | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_message_template` | 会员卡与课卡 | `POST /api/Card/BusinessGetMessageTemplate` | 获取短信模板 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_user_card_balance` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCardBalance` | 获取会员卡余额信息（调整余额页面） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_user_card_by_key` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCardByKey` | 根据兑换秘钥获取会员卡 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_user_card_child_qr_code` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCardChildQrCode` | 获取单个会员子卡的二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_user_card_operation_log` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCardOperationLog` | 停卡记录（时效卡） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_user_cards` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCards` | 获取用户会员卡列表(会员首页,筛选页) | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_user_cards_by_marketing` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCardsByMarketing` | 筛选会员 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_user_cards_by_marketing_set` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCardsByMarketingSet` | 获取用户会员卡列表 无分页(营销) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_get_user_cards_by_source_way` | 会员卡与课卡 | `POST /api/Card/BusinessGetUserCardsBySourceWay` | 获取会员渠道 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_get_users` | 会员卡与课卡 | `POST /api/Card/BusinessGetUsers` | 获取会员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.business_open_user_card_operation_log` | 会员卡与课卡 | `POST /api/Card/BusinessOpenUserCardOperationLog` | 手动解停卡（时效卡） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_operation_commission` | 会员卡与课卡 | `POST /api/Card/BusinessOperationCommission` | 商家手动核减、增加佣金 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_operation_integral` | 会员卡与课卡 | `POST /api/Card/BusinessOperationIntegral` | 调整会员积分 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_recover_user_card` | 会员卡与课卡 | `POST /api/Card/BusinessRecoverUserCard` | 恢复会员/卡 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_send_associate_card_message` | 会员卡与课卡 | `POST /api/Card/BusinessSendAssociateCardMessage` | 发送关联会员卡短信 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.business_set_user_card_reamrk_name` | 会员卡与课卡 | `POST /api/Card/BusinessSetUserCardReamrkName` | 设置会员备注名 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.check_delete_source_way` | 会员卡与课卡 | `POST /api/Card/CheckDeleteSourceWay` | 检查删除会员来源渠道 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.delete_source_way` | 会员卡与课卡 | `POST /api/Card/DeleteSourceWay` | 删除会员来源渠道 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.get_card_group_users_list` | 会员卡与课卡 | `POST /api/Card/GetCardGroupUsersList` | 会员卡分组会员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.get_source_way` | 会员卡与课卡 | `POST /api/Card/GetSourceWay` | 会员来源渠道列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.get_source_way_user_statistical` | 会员卡与课卡 | `POST /api/Card/GetSourceWayUserStatistical` | 统计来源渠道 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.get_staff_user_card_count` | 会员卡与课卡 | `POST /api/Card/GetStaffUserCardCount` | 获取会籍顾问下会员数量 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.card.set_user_card_source_way` | 会员卡与课卡 | `POST /api/Card/SetUserCardSourceWay` | 设置会员来源渠道 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.card.set_user_card_staff` | 会员卡与课卡 | `POST /api/Card/SetUserCardStaff` | 设置会员销售顾问 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_card.business_add_store_prepaid_card` | 会员卡与课卡 | `POST /api/StoreCard/BusinessAddStorePrepaidCard` | 商家新增充值卡 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_card.business_del_store_prepaid_card` | 会员卡与课卡 | `POST /api/StoreCard/BusinessDelStorePrepaidCard` | 商家删除充值卡 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_card.business_get_prepaid_card_qr_code` | 会员卡与课卡 | `POST /api/StoreCard/BusinessGetPrepaidCardQrCode` | 获取购买储值卡的二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_card.business_get_store_card` | 会员卡与课卡 | `POST /api/StoreCard/BusinessGetStoreCard` | 获取商户默认会员卡 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_card.business_get_store_prepaid_card` | 会员卡与课卡 | `POST /api/StoreCard/BusinessGetStorePrepaidCard` | 获取商家单个充值卡 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_card.business_get_store_prepaid_card_sales_info` | 会员卡与课卡 | `POST /api/StoreCard/BusinessGetStorePrepaidCardSalesInfo` | 获取商家单个充值卡销售汇总信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_card.business_get_store_prepaid_card_sales_summary` | 会员卡与课卡 | `POST /api/StoreCard/BusinessGetStorePrepaidCardSalesSummary` | 获取商家单个充值卡销售汇总信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_card.business_get_store_prepaid_cards` | 会员卡与课卡 | `POST /api/StoreCard/BusinessGetStorePrepaidCards` | 获取商家所有充值卡 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StorePrepaidCardProvider.StorePrepaidCardStateUpdate |
| `crmapi.store_card.business_get_store_service_items` | 会员卡与课卡 | `POST /api/StoreCard/BusinessGetStoreServiceItems` | 获取服务项目列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_card.business_set_store_prepaid_card` | 会员卡与课卡 | `POST /api/StoreCard/BusinessSetStorePrepaidCard` | 商家修改充值卡 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_card.business_set_store_prepaid_card_state` | 会员卡与课卡 | `POST /api/StoreCard/BusinessSetStorePrepaidCardState` | 商家修改充值卡状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_card.business_share_card` | 会员卡与课卡 | `POST /api/StoreCard/BusinessShareCard` | 分享优惠券生成随机密钥 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_card.business_store_prepaid_card_seting` | 会员卡与课卡 | `POST /api/StoreCard/BusinessStorePrepaidCardSeting` | 获取商家充值卡基础设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_card.get_store_card_by_group_info` | 会员卡与课卡 | `POST /api/StoreCard/GetStoreCardByGroupInfo` | 获取会员卡分组详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_card.get_store_card_by_group_list` | 会员卡与课卡 | `POST /api/StoreCard/GetStoreCardByGroupList` | 获取会员卡分组列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.employee.get_store_employees` | 员工 | `POST /api/Employee/GetStoreEmployees` | 获取所有员工信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.business_add_admin` | 员工与教练 | `POST /api/Staff/BusinessAddAdmin` | 添加临时员工 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_bind_admin` | 员工与教练 | `POST /api/Staff/BusinessBindAdmin` | 用户绑定管理员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_bind_admin_check` | 员工与教练 | `POST /api/Staff/BusinessBindAdminCheck` | 判断用户是否可以绑定管理员（扫码） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_check_transfer_admin` | 员工与教练 | `POST /api/Staff/BusinessCheckTransferAdmin` | 检测转移管理权限 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_get_admin_info` | 员工与教练 | `POST /api/Staff/BusinessGetAdminInfo` | 获取管理员详细信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.business_get_admin_list` | 员工与教练 | `POST /api/Staff/BusinessGetAdminList` | 获取管理员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.business_get_admin_permissions` | 员工与教练 | `POST /api/Staff/BusinessGetAdminPermissions` | 获取管理员权限 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.business_get_bind_admin_qr_code_str` | 员工与教练 | `POST /api/Staff/BusinessGetBindAdminQrCodeStr` | 获取绑定管理员二维码参数(参数有效时间10分钟) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_get_privilege_group` | 员工与教练 | `POST /api/Staff/BusinessGetPrivilegeGroup` | 获取权限组 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.business_remove_admin` | 员工与教练 | `POST /api/Staff/BusinessRemoveAdmin` | 用户取消管理员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_set_admin_info` | 员工与教练 | `POST /api/Staff/BusinessSetAdminInfo` | 修改管理员详细信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_set_admin_name` | 员工与教练 | `POST /api/Staff/BusinessSetAdminName` | 修改管理员名字 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_set_admin_permissions` | 员工与教练 | `POST /api/Staff/BusinessSetAdminPermissions` | 修改管理员权限 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_set_admin_permissions_for_privilege_group` | 员工与教练 | `POST /api/Staff/BusinessSetAdminPermissionsForPrivilegeGroup` | 修改管理员权限根据权限分组 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_set_admin_state` | 员工与教练 | `POST /api/Staff/BusinessSetAdminState` | 修改管理员状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_set_is_voice` | 员工与教练 | `POST /api/Staff/BusinessSetIsVoice` | 修改管理员名字 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.business_transfer_admin` | 员工与教练 | `POST /api/Staff/BusinessTransferAdmin` | 转移管理权限 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.del_staff_info` | 员工与教练 | `POST /api/Staff/DelStaffInfo` | 删除员工（旧） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.delete_staff_operate_info` | 员工与教练 | `POST /api/Staff/DeleteStaffOperateInfo` | 删除员工工作时间 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.delete_staff_vacation` | 员工与教练 | `POST /api/Staff/DeleteStaffVacation` | 删除请假 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.get_staff_info` | 员工与教练 | `POST /api/Staff/GetStaffInfo` | 技师详情（旧） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.get_staff_list` | 员工与教练 | `POST /api/Staff/GetStaffList` | 获取技师列表（旧） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.get_staff_operate_info` | 员工与教练 | `POST /api/Staff/GetStaffOperateInfo` | 获取员工工作时间 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.get_staff_operate_list` | 员工与教练 | `POST /api/Staff/GetStaffOperateList` | 获取员工工作时间 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.get_staff_vacation` | 员工与教练 | `POST /api/Staff/GetStaffVacation` | 获取管理员请假列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.get_staff_vacation_info` | 员工与教练 | `POST /api/Staff/GetStaffVacationInfo` | 获取请假详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.get_staff_vacation_set` | 员工与教练 | `POST /api/Staff/GetStaffVacationSet` | 获取管理员请假设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.get_store_staff_vacation_calendar` | 员工与教练 | `POST /api/Staff/GetStoreStaffVacationCalendar` | 获取与指定日期范围相交的门店员工请假记录。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.set_staff_info` | 员工与教练 | `POST /api/Staff/SetStaffInfo` | 修改技师详情（旧） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.set_staff_notice` | 员工与教练 | `POST /api/Staff/SetStaffNotice` | 设置接收通知 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.set_staff_operate_info` | 员工与教练 | `POST /api/Staff/SetStaffOperateInfo` | 设置员工工作时间 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.set_staff_order_by` | 员工与教练 | `POST /api/Staff/SetStaffOrderBy` | 设置排序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.set_staff_vacation` | 员工与教练 | `POST /api/Staff/SetStaffVacation` | 新增请假 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.set_staff_vacation_check` | 员工与教练 | `POST /api/Staff/SetStaffVacationCheck` | 新增请假检查 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.staff_quit_job_check` | 员工与教练 | `POST /api/Staff/StaffQuitJobCheck` | 管理员离职检查 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.staff.update_staff_vacation_set` | 员工与教练 | `POST /api/Staff/UpdateStaffVacationSet` | 获取管理员请假设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.staff.user_remove_store_admin` | 员工与教练 | `POST /api/Staff/UserRemoveStoreAdmin` | 用户解绑店铺 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.add_product` | 商品 | `POST /api/Product/AddProduct` | 添加商品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.add_product_order` | 商品 | `POST /api/Product/AddProductOrder` | 创建订购商城订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.add_sku` | 商品 | `POST /api/Product/AddSku` | 添加规格 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.check_product_sku_pno` | 商品 | `POST /api/Product/CheckProductSkuPno` | 检查规格货号是否可用 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product.del_product` | 商品 | `POST /api/Product/DelProduct` | 删除 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.del_sku` | 商品 | `POST /api/Product/DelSku` | 删除规格 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.edit_product` | 商品 | `POST /api/Product/EditProduct` | 修改商品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.edit_product_category_info` | 商品 | `POST /api/Product/EditProductCategoryInfo` | 编辑商品分类信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.get_add_product_order_price` | 商品 | `POST /api/Product/GetAddProductOrderPrice` | 获取订购商城价格 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.get_product_by_id` | 商品 | `POST /api/Product/GetProductById` | 商品详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product.get_sku_list` | 商品 | `POST /api/Product/GetSkuList` | 获取规格列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product.product_add_quantity` | 商品 | `POST /api/Product/ProductAddQuantity` | 增加商品库存 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product.products_search` | 商品 | `POST /api/Product/ProductsSearch` | 搜索商品 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product.a` | 商品 | `POST /api/Product/a` | 【源码推断】a 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.business_products_class_add` | 商品 | `POST /api/ProductCenter/BusinessProductsClassAdd` | 创建商品分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.business_products_class_del` | 商品 | `POST /api/ProductCenter/BusinessProductsClassDel` | 商品分类删除 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.business_products_class_sort` | 商品 | `POST /api/ProductCenter/BusinessProductsClassSort` | 商品分类排序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.business_products_class_update` | 商品 | `POST /api/ProductCenter/BusinessProductsClassUpdate` | 商品分类修改 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.business_products_on_sale` | 商品 | `POST /api/ProductCenter/BusinessProductsOnSale` | 商品上下架接口 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.business_products_sort_by_class_id` | 商品 | `POST /api/ProductCenter/BusinessProductsSortByClassId` | 商品类别下商品排序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.get_business_products_class_by_store_id` | 商品 | `POST /api/ProductCenter/GetBusinessProductsClassByStoreId` | 根据门店ID获取B端商品分类集合 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product_center.get_business_products_list_by_store_id` | 商品 | `POST /api/ProductCenter/GetBusinessProductsListByStoreId` | 根据门店ID获取门店下面对应的商品列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product_center.get_business_products_message_by_product_id` | 商品 | `POST /api/ProductCenter/GetBusinessProductsMessageByProductId` | 根据商品ID获取商品信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product_center.get_product_class_ico_list` | 商品 | `POST /api/ProductCenter/GetProductClassIcoList` | 获取商品分类图标 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product_center.get_store_shop_lnventory_warning_count` | 商品 | `POST /api/ProductCenter/GetStoreShopLnventoryWarningCount` | 获取商城库存商品数量 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreShopSetingProvider.GetStoreShopSetByStoreId |
| `crmapi.product_center.get_store_shop_lnventory_warning_product` | 商品 | `POST /api/ProductCenter/GetStoreShopLnventoryWarningProduct` | 获取商城库存预警的商品 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.product_center.get_store_shop_lnventory_warning_seting` | 商品 | `POST /api/ProductCenter/GetStoreShopLnventoryWarningSeting` | 获取商城库存预警设置 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreShopSetingProvider.GetStoreShopSetByStoreId |
| `crmapi.product_center.get_store_shop_set_by_store_id` | 商品 | `POST /api/ProductCenter/GetStoreShopSetByStoreId` | 获取门店商城高级设置接口 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.set_store_shop_lnventory_warning_seting` | 商品 | `POST /api/ProductCenter/SetStoreShopLnventoryWarningSeting` | 修改商城库存预警设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.product_center.store_shop_seting_edit` | 商品 | `POST /api/ProductCenter/StoreShopSetingEdit` | 门店商城高级设置保存接口 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.work_bench.get_dashboard_overview` | 工作台 | `POST /api/WorkBench/GetDashboardOverview` | 获取 PC 运营首页聚合数据。 数据范围严格来自当前登录员工在当前门店的权限，不接受前端声明 StaffId。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_bench.get_reservation_attention_counts` | 工作台 | `POST /api/WorkBench/GetReservationAttentionCounts` | 获取当前门店当天的候补与待签到记录数。 身份、预约处理权限和课程数据范围均来自当前已验证会话，不接受前端传入员工范围。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_bench.get_store_workbench` | 工作台 | `POST /api/WorkBench/GetStoreWorkbench` | 获取门店工作台列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_bench.get_work_bench_all` | 工作台 | `POST /api/WorkBench/GetWorkBenchAll` | 获取所有工作台入口 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.work_bench.set_store_workbench` | 工作台 | `POST /api/WorkBench/SetStoreWorkbench` | 设置工作台 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.add_work_we_chat_msg` | 消息 | `POST /api/Message/AddWorkWeChatMsg` | 新增商家单个消息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.add_work_we_chat_msg_template` | 消息 | `POST /api/Message/AddWorkWeChatMsgTemplate` | 新增商家单个消息模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.delete_work_we_chat_msg` | 消息 | `POST /api/Message/DeleteWorkWeChatMsg` | 删除商家单个消息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.delete_work_we_chat_msg_template` | 消息 | `POST /api/Message/DeleteWorkWeChatMsgTemplate` | 删除商家单个消息模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.edit_work_we_chat_msg` | 消息 | `POST /api/Message/EditWorkWeChatMsg` | 修改商家单个消息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.edit_work_we_chat_msg_template` | 消息 | `POST /api/Message/EditWorkWeChatMsgTemplate` | 修改商家单个消息模板 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.get_send_msg_people_sum` | 消息 | `POST /api/Message/GetSendMsgPeopleSum` | 获取消息发送关联会员数量 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.get_work_we_chat_msg_info` | 消息 | `POST /api/Message/GetWorkWeChatMsgInfo` | 获取商家单个消息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.message.get_work_we_chat_msg_template_info` | 消息 | `POST /api/Message/GetWorkWeChatMsgTemplateInfo` | 获取商家单个消息模板 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.message.get_work_we_chat_msg_templates` | 消息 | `POST /api/Message/GetWorkWeChatMsgTemplates` | 获取商家所有消息模板 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.message.get_work_we_chat_msgs` | 消息 | `POST /api/Message/GetWorkWeChatMsgs` | 获取商家所有群发消息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.message.get_work_we_chat_msgs_send_log` | 消息 | `POST /api/Message/GetWorkWeChatMsgsSendLog` | 获取商家群发消息记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.message.update_work_we_chat_msg_state` | 消息 | `POST /api/Message/UpdateWorkWeChatMsgState` | 商家修改被动群发消息状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.add_custom_pay_way` | 消费与核销 | `POST /api/Consumption/AddCustomPayWay` | 添加自定义收款渠道 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_add_consumption` | 消费与核销 | `POST /api/Consumption/BusinessAddConsumption` | 商家增加消费记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_add_offline_consumption` | 消费与核销 | `POST /api/Consumption/BusinessAddOfflineConsumption` | 商家增加线下消费记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_cancel_record` | 消费与核销 | `POST /api/Consumption/BusinessCancelRecord` | 撤销收款记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_get_consumption_info` | 消费与核销 | `POST /api/Consumption/BusinessGetConsumptionInfo` | 获取单个消费明细信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.consumption.business_get_consumption_list` | 消费与核销 | `POST /api/Consumption/BusinessGetConsumptionList` | 获取消费明细列表(首页) | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.consumption.business_get_consumption_rank` | 消费与核销 | `POST /api/Consumption/BusinessGetConsumptionRank` | 获取消费排名 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.consumption.business_get_consumption_refunds_info` | 消费与核销 | `POST /api/Consumption/BusinessGetConsumptionRefundsInfo` | 获取单个消费明细退款信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_get_user_consumption_list` | 消费与核销 | `POST /api/Consumption/BusinessGetUserConsumptionList` | 获取会员消费明细列表(会员详情页) | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.consumption.business_operation_user_consumption` | 消费与核销 | `POST /api/Consumption/BusinessOperationUserConsumption` | 商家调整会员余额 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_refunds` | 消费与核销 | `POST /api/Consumption/BusinessRefunds` | 退款 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_refunds_card_info` | 消费与核销 | `POST /api/Consumption/BusinessRefundsCardInfo` | 退卡信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_refunds_check` | 消费与核销 | `POST /api/Consumption/BusinessRefundsCheck` | 退款检查 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_set_remark` | 消费与核销 | `POST /api/Consumption/BusinessSetRemark` | 设置商家备注 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.business_update_consumption_log_bookkeeping` | 消费与核销 | `POST /api/Consumption/BusinessUpdateConsumptionLogBookkeeping` | 管理员更新订单记账状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.delete_custom_pay_way` | 消费与核销 | `POST /api/Consumption/DeleteCustomPayWay` | 删除自定义收款渠道 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.get_pay_way_list` | 消费与核销 | `POST /api/Consumption/GetPayWayList` | 获取收款渠道列表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.consumption.print_consumption` | 消费与核销 | `POST /api/Consumption/PrintConsumption` | 商家重新打印小票 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_integral.business_get_integral_set` | 积分 | `POST /api/StoreIntegral/BusinessGetIntegralSet` | 获取积分设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_integral.business_set_is_open_integral` | 积分 | `POST /api/StoreIntegral/BusinessSetIsOpenIntegral` | 商户设置积分开关 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_integral.business_update_integral_set` | 积分 | `POST /api/StoreIntegral/BusinessUpdateIntegralSet` | 修改积分设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.mobile_management.create_binding` | 移动端绑定 | `POST /api/MobileManagement/CreateBinding` | 为当前登录员工创建一次性微信绑定二维码。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.mobile_management.get_binding_status` | 移动端绑定 | `POST /api/MobileManagement/GetBindingStatus` | 查询当前登录员工发起的微信绑定进度。 UID 与设备摘要必须同时匹配，其他已登录账号不能读取或领取该会话结果。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.mobile_management.get_status` | 移动端绑定 | `POST /api/MobileManagement/GetStatus` | 获取当前登录员工的移动端开通状态。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_add_complaint` | 经营报表 | `POST /api/OperatingReports/BusinessAddComplaint` | 报表 建议 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.operating_reports.business_get_card_analyse` | 经营报表 | `POST /api/OperatingReports/BusinessGetCardAnalyse` | 会员卡分析 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_card_reservation` | 经营报表 | `POST /api/OperatingReports/BusinessGetCardReservation` | 上课用卡分析 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_card_reservation_course` | 经营报表 | `POST /api/OperatingReports/BusinessGetCardReservationCourse` | 上课用卡分析 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_expiring` | 经营报表 | `POST /api/OperatingReports/BusinessGetExpiring` | 临期会员统计信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_lessons_data_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetLessonsDataStatistical` | 上课信息统计 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreLessonsProvider.BusinessGetCancelRate, StoreLessonsProvider.BusinessGetOpenGroupLessonsProportion |
| `crmapi.operating_reports.business_get_lessons_people` | 经营报表 | `POST /api/OperatingReports/BusinessGetLessonsPeople` | 每日上课人数统计 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_lessons_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetLessonsStatistical` | 获取商户课程预约统计信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_lessons_time_heatmap` | 经营报表 | `POST /api/OperatingReports/BusinessGetLessonsTimeHeatmap` | 获取团课、班课按星期和三小时时段聚合的预约人数利用率。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_liability_card_info` | 经营报表 | `POST /api/OperatingReports/BusinessGetLiabilityCardInfo` | 负债卡信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_liability_info` | 经营报表 | `POST /api/OperatingReports/BusinessGetLiabilityInfo` | 负债信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_liability_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetLiabilityStatistical` | 负债统计 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_liability_user_card_info` | 经营报表 | `POST /api/OperatingReports/BusinessGetLiabilityUserCardInfo` | 负债卡会员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_income_report_table` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreIncomeReportTable` | 获取商户店铺提现统计报表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_income_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreIncomeStatistical` | 收款管理统计 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_pay_money_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetStorePayMoneyStatistical` | 获取商户店铺收入统计信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.operating_reports.business_get_store_people_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetStorePeopleStatistical` | 获取商户店铺顾客统计信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_report_table` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreReportTable` | 获取商户店铺统计报表（报表页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_report_table_v2` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreReportTableV2` | 获取商户店铺统计报表（报表页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_reservation_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreReservationStatistical` | 获取商户店铺统计报表（报表页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_sell_card_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreSellCardStatistical` | 获取商户店铺售卡统计信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreStatistical` | 获取商户店铺统计信息（首页 报表页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_statistical_v2` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreStatisticalV2` | 获取商户店铺统计信息（首页 报表页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.business_get_store_used_card_statistical` | 经营报表 | `POST /api/OperatingReports/BusinessGetStoreUsedCardStatistical` | 获取商户店铺销卡统计信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.export_store_report_table` | 经营报表 | `POST /api/OperatingReports/ExportStoreReportTable` | 导出商户店铺统计报表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.operating_reports.get_export_store_report_table_log` | 经营报表 | `POST /api/OperatingReports/GetExportStoreReportTableLog` | 流水导出记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.operating_reports.get_lessons_calendar_scheduling` | 经营报表 | `POST /api/OperatingReports/GetLessonsCalendarScheduling` | 上课统计信息（日历） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.get_pay_money_calendar_scheduling` | 经营报表 | `POST /api/OperatingReports/GetPayMoneyCalendarScheduling` | 获取财务统计信息（日历） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.operating_reports.get_staff_card_reservation` | 经营报表 | `POST /api/OperatingReports/GetStaffCardReservation` | 上课用卡统计 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.operating_reports.trial_conversion_course` | 经营报表 | `POST /api/OperatingReports/TrialConversionCourse` | 试听转化课目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.operating_reports.trial_conversion_statistical` | 经营报表 | `POST /api/OperatingReports/TrialConversionStatistical` | 试听转化统计 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.shop_order.business_get_shop_order_list` | 订单 | `POST /api/ShopOrder/BusinessGetShopOrderList` | 商家端查询订单列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.shop_order.close_shop_order` | 订单 | `POST /api/ShopOrder/CloseShopOrder` | 关闭商城订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.shop_order.deliver_goods` | 订单 | `POST /api/ShopOrder/DeliverGoods` | 发货 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.shop_order.finish_shop_order` | 订单 | `POST /api/ShopOrder/FinishShopOrder` | 完成订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.shop_order.get_shop_order_detail` | 订单 | `POST /api/ShopOrder/GetShopOrderDetail` | 获取订单详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.add_classes_lessons_reservation` | 课次与排课 | `POST /api/Lessons/AddClassesLessonsReservation` | 新增班课补课 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.add_group_lessons` | 课次与排课 | `POST /api/Lessons/AddGroupLessons` | 新增团课排课 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.add_group_lessons_v2` | 课次与排课 | `POST /api/Lessons/AddGroupLessonsV2` | 快速批量新增团课排课。CheckOnly 用于预检；正式提交会再次校验，并可跳过冲突项。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.add_lessons_reservation` | 课次与排课 | `POST /api/Lessons/AddLessonsReservation` | 新增课程预约 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.add_lessons_reservation_check` | 课次与排课 | `POST /api/Lessons/AddLessonsReservationCheck` | 新增课程预约检查 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.add_user_lessons_note` | 课次与排课 | `POST /api/Lessons/AddUserLessonsNote` | 新增上课笔记 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.business_get_lessons_rank` | 课次与排课 | `POST /api/Lessons/BusinessGetLessonsRank` | 获取上课排名 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.check_user_reservation_seting` | 课次与排课 | `POST /api/Lessons/CheckUserReservationSeting` | 获取商家预约设置 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreReservationProvider.GetUserCancelSum |
| `crmapi.lessons.copy_lessons_scheduling` | 课次与排课 | `POST /api/Lessons/CopyLessonsScheduling` | 复制课表排课 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.delete_group_lessons` | 课次与排课 | `POST /api/Lessons/DeleteGroupLessons` | 删除团课程 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.delete_lessons_scheduling` | 课次与排课 | `POST /api/Lessons/DeleteLessonsScheduling` | 清除课表排课 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.delete_user_lessons_note` | 课次与排课 | `POST /api/Lessons/DeleteUserLessonsNote` | 删除上课笔记 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.download_lessons_scheduling` | 课次与排课 | `POST /api/Lessons/DownloadLessonsScheduling` | 下载课表排课 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.download_lessons_scheduling_log` | 课次与排课 | `POST /api/Lessons/DownloadLessonsSchedulingLog` | 下载课表排课日志 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.generate_lessons_timetable_v2` | 课次与排课 | `POST /api/Lessons/GenerateLessonsTimetableV2` | 保存当前设置并生成 PNG 文件。CSV 继续由同一快照在浏览器端导出。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.get_always_lessons_user` | 课次与排课 | `POST /api/Lessons/GetAlwaysLessonsUser` | 获取常用会员 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_course_scheduling` | 课次与排课 | `POST /api/Lessons/GetCourseScheduling` | 按日期范围读取门店排课日历，可分别返回排课时间、排课数量或预约人数。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons` | 课次与排课 | `POST /api/Lessons/GetLessons` | 获取单个课程信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons_download_set` | 课次与排课 | `POST /api/Lessons/GetLessonsDownloadSet` | 获取下载课表设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.get_lessons_list` | 课次与排课 | `POST /api/Lessons/GetLessonsList` | 获取课程列表(排课) | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons_list_v2` | 课次与排课 | `POST /api/Lessons/GetLessonsListV2` | 获取课程列表(排课 分页版) | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons_logs` | 课次与排课 | `POST /api/Lessons/GetLessonsLogs` | 获取约课log | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons_reservation_info` | 课次与排课 | `POST /api/Lessons/GetLessonsReservationInfo` | 获取课程预约信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons_reservation_list_v2` | 课次与排课 | `POST /api/Lessons/GetLessonsReservationListV2` | 获取已约列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons_timetable_logs` | 课次与排课 | `POST /api/Lessons/GetLessonsTimetableLogs` | 返回当前门店最近的课表生成记录；未部署日志表时安全返回空列表。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_lessons_timetable_templates` | 课次与排课 | `POST /api/Lessons/GetLessonsTimetableTemplates` | 获取可用课表模板。模板元数据与业务数据分离，便于后续新增商家或 AI 模板来源。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_people_reservation_info` | 课次与排课 | `POST /api/Lessons/GetPeopleReservationInfo` | 获取个人预约信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_private_lessons_reservation_time_seting` | 课次与排课 | `POST /api/Lessons/GetPrivateLessonsReservationTimeSeting` | 获取私教预约时间段 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet |
| `crmapi.lessons.get_staff_private_lesson_list` | 课次与排课 | `POST /api/Lessons/GetStaffPrivateLessonList` | 获取教练私教列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_user_lessons_note` | 课次与排课 | `POST /api/Lessons/GetUserLessonsNote` | 获取会员上课笔记详细信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_user_lessons_note_list` | 课次与排课 | `POST /api/Lessons/GetUserLessonsNoteList` | 获取会员上课笔记列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_user_lessons_reservation_list` | 课次与排课 | `POST /api/Lessons/GetUserLessonsReservationList` | 获取会员已约列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.get_user_lessons_scheduling` | 课次与排课 | `POST /api/Lessons/GetUserLessonsScheduling` | 获取上课日历 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.lessons_reservation_sign` | 课次与排课 | `POST /api/Lessons/LessonsReservationSign` | 预约签到 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.preview_lessons_timetable_v2` | 课次与排课 | `POST /api/Lessons/PreviewLessonsTimetableV2` | 获取统一课表快照预览；预览不会保存设置或生成文件。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.lessons.update_group_lessons` | 课次与排课 | `POST /api/Lessons/UpdateGroupLessons` | 修改团课信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_group_lessons_auto_cancel` | 课次与排课 | `POST /api/Lessons/UpdateGroupLessonsAutoCancel` | 修改团课程自动取消状态 (只针对未开课生效) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_group_lessons_staff` | 课次与排课 | `POST /api/Lessons/UpdateGroupLessonsStaff` | 修改团课教练 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_group_lessons_state` | 课次与排课 | `POST /api/Lessons/UpdateGroupLessonsState` | 修改课程状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_lessons_download_set` | 课次与排课 | `POST /api/Lessons/UpdateLessonsDownloadSet` | 修改下载排课设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_lessons_reservation_state` | 课次与排课 | `POST /api/Lessons/UpdateLessonsReservationState` | 修改课程状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_lessons_reservation_state_check` | 课次与排课 | `POST /api/Lessons/UpdateLessonsReservationStateCheck` | 修改课程预约状态校验 （目前主要用于校验旷课次数） | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_lessons_reservation_time` | 课次与排课 | `POST /api/Lessons/UpdateLessonsReservationTime` | 修改私教课程预约时间 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_private_lessons` | 课次与排课 | `POST /api/Lessons/UpdatePrivateLessons` | 修改私教信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.lessons.update_user_lessons_note` | 课次与排课 | `POST /api/Lessons/UpdateUserLessonsNote` | 修改上课笔记 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.add_course` | 课程 | `POST /api/Course/AddCourse` | 新增课目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.add_course_link_name` | 课程 | `POST /api/Course/AddCourseLinkName` | 新增课目关联名称 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.add_staff_course` | 课程 | `POST /api/Course/AddStaffCourse` | 新增教练私教课目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.delete_course` | 课程 | `POST /api/Course/DeleteCourse` | 删除课目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.delete_course_link_name` | 课程 | `POST /api/Course/DeleteCourseLinkName` | 删除课目关联名称 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.delete_staff_course` | 课程 | `POST /api/Course/DeleteStaffCourse` | 删除教练私教课目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.get_card_course_bind_info` | 课程 | `POST /api/Course/GetCardCourseBindInfo` | 获取卡课绑定信息(团课) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.get_card_private_course_bind_info` | 课程 | `POST /api/Course/GetCardPrivateCourseBindInfo` | 获取卡课绑定信息(私教) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.get_course` | 课程 | `POST /api/Course/GetCourse` | 获取单个课目 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.course.get_course_card_bind_info` | 课程 | `POST /api/Course/GetCourseCardBindInfo` | 获取课卡绑定信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.get_course_card_price_info` | 课程 | `POST /api/Course/GetCourseCardPriceInfo` | 获取单次付费信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.course.get_course_images` | 课程 | `POST /api/Course/GetCourseImages` | 获取课目图片 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.course.get_course_link_name` | 课程 | `POST /api/Course/GetCourseLinkName` | 获取课目关联名称 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.course.get_course_list` | 课程 | `POST /api/Course/GetCourseList` | 获取课目列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.course.get_private_lesson_staff_list` | 课程 | `POST /api/Course/GetPrivateLessonStaffList` | 获取私教列表 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet |
| `crmapi.course.get_staff_private_lesson_set` | 课程 | `POST /api/Course/GetStaffPrivateLessonSet` | 获取私教设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_card_course_bind_info` | 课程 | `POST /api/Course/UpdateCardCourseBindInfo` | 修改卡课绑定信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_course` | 课程 | `POST /api/Course/UpdateCourse` | 修改课目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_course_card_bind_info` | 课程 | `POST /api/Course/UpdateCourseCardBindInfo` | 修改课卡绑定信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_course_price_info` | 课程 | `POST /api/Course/UpdateCoursePriceInfo` | 修改单次付费 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_course_state` | 课程 | `POST /api/Course/UpdateCourseState` | 修改课目状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_staff_course` | 课程 | `POST /api/Course/UpdateStaffCourse` | 修改教练私教课目 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_staff_private_lesson_set` | 课程 | `POST /api/Course/UpdateStaffPrivateLessonSet` | 修改私教设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.course.update_staff_private_lesson_state` | 课程 | `POST /api/Course/UpdateStaffPrivateLessonState` | 修改私教设置状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_course.add_store_course` | 课程 | `POST /api/StoreCourse/AddStoreCourse` | 添加课程 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_course.confirm_edit_store_course` | 课程 | `POST /api/StoreCourse/ConfirmEditStoreCourse` | 判断课程是否有人预约 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_course.edit_store_course` | 课程 | `POST /api/StoreCourse/EditStoreCourse` | 修改课程 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_course.edit_store_course_state` | 课程 | `POST /api/StoreCourse/EditStoreCourseState` | 修改课程状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_course.get_store_course_by_id` | 课程 | `POST /api/StoreCourse/GetStoreCourseById` | 获取单条课程信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_course.get_store_course_list` | 课程 | `POST /api/StoreCourse/GetStoreCourseList` | 获取课程列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_course.get_store_course_type` | 课程 | `POST /api/StoreCourse/GetStoreCourseType` | 获取当前店铺团课、私教课信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.accept_qr` | 软件订购与版本 | `POST /api/Soft/AcceptQr` | 商户扩展接受 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.soft.business_get_soft_activity` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftActivity` | 获取软件活动 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_soft_discount_coupon` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftDiscountCoupon` | 获取优惠券信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_soft_order` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftOrder` | 获取软件订购信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_soft_price` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftPrice` | 获取软件版本价格类目列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_soft_server_end_date` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftServerEndDate` | 获取店铺软建到期日期 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_soft_server_free_date` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftServerFreeDate` | 获取店铺软件服务免费使用时间 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_soft_version_info` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftVersionInfo` | 获取软件版本价格类目列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_soft_versions` | 软件订购与版本 | `POST /api/Soft/BusinessGetSoftVersions` | 获取版本列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_get_upgrade_soft_price` | 软件订购与版本 | `POST /api/Soft/BusinessGetUpgradeSoftPrice` | 获取软件版本价格类目列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.business_pay_soft_price` | 软件订购与版本 | `POST /api/Soft/BusinessPaySoftPrice` | 支付软件订购费用 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.soft.business_used_soft_discount_coupon` | 软件订购与版本 | `POST /api/Soft/BusinessUsedSoftDiscountCoupon` | 店铺使用软件订购优惠券 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.soft.buy_module` | 软件订购与版本 | `POST /api/Soft/BuyModule` | 订购 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.soft.get_module` | 软件订购与版本 | `POST /api/Soft/GetModule` | 获取模块信息 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：SoftOrderProvider.GetRenewModule, SoftOrderProvider.GetAddModule |
| `crmapi.soft.get_order_code_info` | 软件订购与版本 | `POST /api/Soft/GetOrderCodeInfo` | 获取二维码后的内容 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.get_qr_img_info` | 软件订购与版本 | `POST /api/Soft/GetQrImgInfo` | 扫码商户扩展二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.get_soft_server_info` | 软件订购与版本 | `POST /api/Soft/GetSoftServerInfo` | 获取店铺软件服务信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.get_store_module` | 软件订购与版本 | `POST /api/Soft/GetStoreModule` | 获取店铺的模块 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.soft.try_out_again` | 软件订购与版本 | `POST /api/Soft/TryOutAgain` | 试用期到期再次试用 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.admin_store.enter_store` | 门店 | `POST /api/AdminStore/EnterStore` | 为系统管理员返回目标店铺上下文，用于当前 PC 后台的一键进入。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.admin_store.get_access` | 门店 | `POST /api/AdminStore/GetAccess` | 返回当前登录用户是否具备系统店铺管理权限。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.admin_store.get_stores` | 门店 | `POST /api/AdminStore/GetStores` | 获取系统管理员可进入的店铺分页列表。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.add_return_visit` | 门店 | `POST /api/Store/AddReturnVisit` | 设置添加客服微信 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.add_store` | 门店 | `POST /api/Store/AddStore` | 创建分店 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.bind_store_pay_code` | 门店 | `POST /api/Store/BindStorePayCode` | 设置店铺收账号 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_add_custom_controls` | 门店 | `POST /api/Store/BusinessAddCustomControls` | 商家新增自定义会员控件(会员资料设置页) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_application_export_user_data` | 门店 | `POST /api/Store/BusinessApplicationExportUserData` | 申请导出数据 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_del_custom_controls` | 门店 | `POST /api/Store/BusinessDelCustomControls` | 商家删除自定义会员控件信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_demo_get_store_list` | 门店 | `POST /api/Store/BusinessDemoGetStoreList` | 如果是Demo环境，调用DEMO店铺列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_card_agreement` | 门店 | `POST /api/Store/BusinessGetCardAgreement` | 获取会员协议 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_card_interests` | 门店 | `POST /api/Store/BusinessGetCardInterests` | 获取会员卡权益 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_cloud_index_banner_detail` | 门店 | `POST /api/Store/BusinessGetCloudIndexBannerDetail` | 获取云店首页banner详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_cloud_store_index_modules` | 门店 | `POST /api/Store/BusinessGetCloudStoreIndexModules` | 获取云店首页模块信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_current_store_list` | 门店 | `POST /api/Store/BusinessGetCurrentStoreList` | 获取商户信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_custom_controls` | 门店 | `POST /api/Store/BusinessGetCustomControls` | 商家获取自定义会员控件信息(会员资料设置页) | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：CustomControlsProvider.AddCustomControls, StoreSetingProvider.StoreSetingByUpdateSet |
| `crmapi.store.business_get_demo_store_qr_img` | 门店 | `POST /api/Store/BusinessGetDemoStoreQrImg` | 获取演示店铺的小程序二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_export_user_data_log` | 门店 | `POST /api/Store/BusinessGetExportUserDataLog` | 会员数据导出记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_get_print_by_store_id` | 门店 | `POST /api/Store/BusinessGetPrintByStoreId` | 商家添加打印机 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_store_email` | 门店 | `POST /api/Store/BusinessGetStoreEmail` | 获取店铺默认邮箱 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_store_info` | 门店 | `POST /api/Store/BusinessGetStoreInfo` | 获取商户信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_store_list` | 门店 | `POST /api/Store/BusinessGetStoreList` | 获取商户信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_store_management_data` | 门店 | `POST /api/Store/BusinessGetStoreManagementData` | 获取商户信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_store_pay_account` | 门店 | `POST /api/Store/BusinessGetStorePayAccount` | 获取店铺收款账号 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_get_store_pay_qr_code` | 门店 | `POST /api/Store/BusinessGetStorePayQrCode` | 获取支付二维码 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_get_store_qr_code_list` | 门店 | `POST /api/Store/BusinessGetStoreQrCodeList` | 获取店铺二维码列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_store_seting` | 门店 | `POST /api/Store/BusinessGetStoreSeting` | 获取商户设置信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_get_store_user_statistical` | 门店 | `POST /api/Store/BusinessGetStoreUserStatistical` | 获取商户店铺会员统计信息(会员首页) | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_set_card_agreement` | 门店 | `POST /api/Store/BusinessSetCardAgreement` | 设置会员卡协议 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_card_balance_mergers` | 门店 | `POST /api/Store/BusinessSetCardBalanceMergers` | 设置会员卡余额合并 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_card_interests` | 门店 | `POST /api/Store/BusinessSetCardInterests` | 设置会员卡权益 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_card_seting` | 门店 | `POST /api/Store/BusinessSetCardSeting` | 设置会员卡通用设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_cloud_index_banner` | 门店 | `POST /api/Store/BusinessSetCloudIndexBanner` | 修改云店首页Banner | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_cloud_introduction_img` | 门店 | `POST /api/Store/BusinessSetCloudIntroductionImg` | 设置云店首页介绍图 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_custom_controls` | 门店 | `POST /api/Store/BusinessSetCustomControls` | 商家设置自定义会员控件信息(会员资料设置页) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_is_birthday_user_count` | 门店 | `POST /api/Store/BusinessSetIsBirthdayUserCount` | 设置首页显示生日会员数量 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_print` | 门店 | `POST /api/Store/BusinessSetPrint` | 商家添加打印机 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_refund_protection` | 门店 | `POST /api/Store/BusinessSetRefundProtection` | 设置退款保护 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_store_info` | 门店 | `POST /api/Store/BusinessSetStoreInfo` | 设置商户信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_set_store_pay_account` | 门店 | `POST /api/Store/BusinessSetStorePayAccount` | 设置店铺收账号 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.business_store_image_list` | 门店 | `POST /api/Store/BusinessStoreImageList` | 获取店铺图片列表详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.business_update_custom_controls` | 门店 | `POST /api/Store/BusinessUpdateCustomControls` | 商家修改自定义会员控件(会员资料设置页) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.edit_store_user_tag_name` | 门店 | `POST /api/Store/EditStoreUserTagName` | 编辑店铺会员标签名称 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.edit_store_users_info` | 门店 | `POST /api/Store/EditStoreUsersInfo` | 修改用户信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.get_axc_merchant_remain` | 门店 | `POST /api/Store/GetAxcMerchantRemain` | 获取安心充充值余额信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.get_operate_info` | 门店 | `POST /api/Store/GetOperateInfo` | 显示营业时间 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.get_search_user` | 门店 | `POST /api/Store/GetSearchUser` | 尾号搜索信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.get_store_index_page_style` | 门店 | `POST /api/Store/GetStoreIndexPageStyle` | 获取首页样式 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.get_store_open_platform_account` | 门店 | `POST /api/Store/GetStoreOpenPlatformAccount` | 获取三方平台账号列表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.get_store_receive_address` | 门店 | `POST /api/Store/GetStoreReceiveAddress` | 获取店铺收货地址 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.get_store_type` | 门店 | `POST /api/Store/GetStoreType` | 获取店铺行业信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.get_store_type_v2` | 门店 | `POST /api/Store/GetStoreTypeV2` | 获取店铺行业信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.get_store_user_tag_list` | 门店 | `POST /api/Store/GetStoreUserTagList` | 获取店铺会员标签 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.get_store_workbench` | 门店 | `POST /api/Store/GetStoreWorkbench` | 获取工作台列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store.init_demo_store` | 门店 | `POST /api/Store/InitDemoStore` | 初始化 演示店铺 初始化完成 Data=true,初始化失败 Data=false | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_cloud_index_recomand_food` | 门店 | `POST /api/Store/SetCloudIndexRecomandFood` | 设置云店首页推荐餐品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_cloud_index_recomand_product` | 门店 | `POST /api/Store/SetCloudIndexRecomandProduct` | 设置云店首页推荐商品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_index_modules_customer_service` | 门店 | `POST /api/Store/SetIndexModulesCustomerService` | 设置云店客服 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_index_modules_state` | 门店 | `POST /api/Store/SetIndexModulesState` | 设置云店首页模块是否显示 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_operate_info` | 门店 | `POST /api/Store/SetOperateInfo` | 设置营业时间 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_store_complete_guide` | 门店 | `POST /api/Store/SetStoreCompleteGuide` | 设置店铺完成新手引导 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_store_complete_guide_operate_after` | 门店 | `POST /api/Store/SetStoreCompleteGuideOperateAfter` | 设置店铺完成新手引导后的第一步操作记录 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_store_index_page_style` | 门店 | `POST /api/Store/SetStoreIndexPageStyle` | 设置首页样式 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_store_is_performance` | 门店 | `POST /api/Store/SetStoreIsPerformance` | 设置店铺绩效开关 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.set_table_cards` | 门店 | `POST /api/Store/SetTableCards` | 商家扫码绑定台牌 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.store_cutover` | 门店 | `POST /api/Store/StoreCutover` | 切换店铺 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.update_store_is_user_img_or_nick` | 门店 | `POST /api/Store/UpdateStoreIsUserImgOrNick` | 设置用户授权信息开关 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store.update_store_register_method` | 门店 | `POST /api/Store/UpdateStoreRegisterMethod` | 设置店铺会员注册顺序 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_added.add_store_added_trade` | 门店 | `POST /api/StoreAdded/AddStoreAddedTrade` | 新增订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_added.get_store_added_services` | 门店 | `POST /api/StoreAdded/GetStoreAddedServices` | 获取店铺增值列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_added.get_store_added_services_by_soft_discount` | 门店 | `POST /api/StoreAdded/GetStoreAddedServicesBySoftDiscount` | 获取店铺增值列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_added.get_store_added_trade_list` | 门店 | `POST /api/StoreAdded/GetStoreAddedTradeList` | 获取订单列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_classes.add_classes_course` | 门店 | `POST /api/StoreClasses/AddClassesCourse` | 新增排课计划 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.add_classes_user` | 门店 | `POST /api/StoreClasses/AddClassesUser` | 新增学员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.add_classes_user_vacation` | 门店 | `POST /api/StoreClasses/AddClassesUserVacation` | 新增请假 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.add_store_classes` | 门店 | `POST /api/StoreClasses/AddStoreClasses` | 新增班级 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.delete_classes_course` | 门店 | `POST /api/StoreClasses/DeleteClassesCourse` | 删除排课计划 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.delete_classes_user` | 门店 | `POST /api/StoreClasses/DeleteClassesUser` | 删除学员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.delete_classes_user_vacation` | 门店 | `POST /api/StoreClasses/DeleteClassesUserVacation` | 删除请假 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.delete_store_classes` | 门店 | `POST /api/StoreClasses/DeleteStoreClasses` | 删除班级 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.get_class_user_list` | 门店 | `POST /api/StoreClasses/GetClassUserList` | 获取学员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_classes.get_classes_user_vacation` | 门店 | `POST /api/StoreClasses/GetClassesUserVacation` | 获取学员请假列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_classes.get_classes_user_vacation_info` | 门店 | `POST /api/StoreClasses/GetClassesUserVacationInfo` | 获取请假详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_classes.get_store_classes` | 门店 | `POST /api/StoreClasses/GetStoreClasses` | 获取单个班级 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_classes.get_store_classes_list` | 门店 | `POST /api/StoreClasses/GetStoreClassesList` | 获取班级 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_classes.set_classes_user_vacation_check` | 门店 | `POST /api/StoreClasses/SetClassesUserVacationCheck` | 新增请假检查 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.update_classes_course` | 门店 | `POST /api/StoreClasses/UpdateClassesCourse` | 修改排课计划 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.update_store_classes` | 门店 | `POST /api/StoreClasses/UpdateStoreClasses` | 修改班级 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_classes.update_store_classes_state` | 门店 | `POST /api/StoreClasses/UpdateStoreClassesState` | 修改班级结课状态 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_customer_mobile.business_store_customer_delete_list` | 门店 | `POST /api/StoreCustomerMobile/BusinessStoreCustomerDeleteList` | 批量删除 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_customer_mobile.business_store_customer_mobile_add_list` | 门店 | `POST /api/StoreCustomerMobile/BusinessStoreCustomerMobileAddList` | 批量添加 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_customer_mobile.business_store_customer_mobile_get_list` | 门店 | `POST /api/StoreCustomerMobile/BusinessStoreCustomerMobileGetList` | 列表查询 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_growth.create_subscription_qr_code` | 门店 | `POST /api/StoreGrowth/CreateSubscriptionQrCode` | 为指定正式版本生成官方小程序订购码。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_growth.get_subscription_overview` | 门店 | `POST /api/StoreGrowth/GetSubscriptionOverview` | 获取当前门店可查看和可办理的正式版本订购信息。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_growth.get_workspace` | 门店 | `POST /api/StoreGrowth/GetWorkspace` | 获取当前门店的增长工作区快照。 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_growth.open_migration_customer_service` | 门店 | `POST /api/StoreGrowth/OpenMigrationCustomerService` | 为当前课小秘产品和已登录门店生成企业微信官方客服会话地址。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_growth.record_client_event` | 门店 | `POST /api/StoreGrowth/RecordClientEvent` | 记录 YueKe PC 允许上报的商家转化事件。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_growth.set_activation_track_skipped` | 门店 | `POST /api/StoreGrowth/SetActivationTrackSkipped` | 设置或撤销当前门店某条经营路径的跳过状态。 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_material.add_store_material` | 门店 | `POST /api/StoreMaterial/AddStoreMaterial` | 新增素材 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_material.add_store_material_category` | 门店 | `POST /api/StoreMaterial/AddStoreMaterialCategory` | 新增素材分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_material.delete_store_material` | 门店 | `POST /api/StoreMaterial/DeleteStoreMaterial` | 删除素材 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_material.delete_store_material_category` | 门店 | `POST /api/StoreMaterial/DeleteStoreMaterialCategory` | 删除素材分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_material.get_store_material_category_data_list` | 门店 | `POST /api/StoreMaterial/GetStoreMaterialCategoryDataList` | 查询素材分类及分类下素材 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_material.get_store_material_category_list` | 门店 | `POST /api/StoreMaterial/GetStoreMaterialCategoryList` | 查询素材分类 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_material.get_store_material_list` | 门店 | `POST /api/StoreMaterial/GetStoreMaterialList` | 查询素材 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_material.update_store_material` | 门店 | `POST /api/StoreMaterial/UpdateStoreMaterial` | 修改素材 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_material.update_store_material_category` | 门店 | `POST /api/StoreMaterial/UpdateStoreMaterialCategory` | 修改素材分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_place.add_store_place` | 门店 | `POST /api/StorePlace/AddStorePlace` | 添加场地 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_place.delete_store_place` | 门店 | `POST /api/StorePlace/DeleteStorePlace` | 删除场地 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_place.exists_place_reservation` | 门店 | `POST /api/StorePlace/ExistsPlaceReservation` | 通过时间段查询是否有预约 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_place.get_place_lessons` | 门店 | `POST /api/StorePlace/GetPlaceLessons` | 获取场地关联课程 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_place.get_reservation_place` | 门店 | `POST /api/StorePlace/GetReservationPlace` | 获取预约场地 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StorePlaceCloseSettingProvider.GetStorePlaceCloseSettingByStoreId |
| `crmapi.store_place.get_store_place_info` | 门店 | `POST /api/StorePlace/GetStorePlaceInfo` | 获取场地详细 | 需要登录 | C | 查询路径包含疑似写入或外部副作用调用，必须人工复核：StorePlaceCloseInfoProvider.GetStorePlaceCloseInfosByPlaceId |
| `crmapi.store_place.get_store_place_list` | 门店 | `POST /api/StorePlace/GetStorePlaceList` | 获取场地列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_place.update_store_place` | 门店 | `POST /api/StorePlace/UpdateStorePlace` | 修改场地 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_product.business_add_store_product` | 门店 | `POST /api/StoreProduct/BusinessAddStoreProduct` | 添加价目表(商品) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_product.business_get_pay_store_product` | 门店 | `POST /api/StoreProduct/BusinessGetPayStoreProduct` | 获取价目表(商品)列表 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_product.business_get_store_product` | 门店 | `POST /api/StoreProduct/BusinessGetStoreProduct` | 获取价目表(商品)列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_product.business_get_store_product_by_id` | 门店 | `POST /api/StoreProduct/BusinessGetStoreProductById` | 获取价目表(商品)详细 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_product.business_update_store_product` | 门店 | `POST /api/StoreProduct/BusinessUpdateStoreProduct` | 修改价目表(商品) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_product.delete_store_product` | 门店 | `POST /api/StoreProduct/DeleteStoreProduct` | 删除价目表(商品) | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.business_set_store_info_v2` | 门店 | `POST /api/StoreSet/BusinessSetStoreInfoV2` | 设置商户信息V2 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.delete_store_vacation` | 门店 | `POST /api/StoreSet/DeleteStoreVacation` | 删除店铺放假时间 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.get_display_set` | 门店 | `POST /api/StoreSet/GetDisplaySet` | 获取显示设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.get_store_new_operating_info` | 门店 | `POST /api/StoreSet/GetStoreNewOperatingInfo` | 获取店铺新手引导信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_set.get_store_set_tag` | 门店 | `POST /api/StoreSet/GetStoreSetTag` | 获取店铺设置标记 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.get_store_vacation` | 门店 | `POST /api/StoreSet/GetStoreVacation` | 获取店铺放假时间 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_set.get_store_vacation_set` | 门店 | `POST /api/StoreSet/GetStoreVacationSet` | 获取店铺放假时间设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.set_display_set` | 门店 | `POST /api/StoreSet/SetDisplaySet` | 设置显示设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.set_store_vacation` | 门店 | `POST /api/StoreSet/SetStoreVacation` | 设置放假时间 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.skip_store_new_operating` | 门店 | `POST /api/StoreSet/SkipStoreNewOperating` | 跳过新手引导 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_set.update_store_vacation_set` | 门店 | `POST /api/StoreSet/UpdateStoreVacationSet` | 修改店铺放假时间设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_tag.add_store_tag` | 门店 | `POST /api/StoreTag/AddStoreTag` | 新增店铺会员标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_tag.delete_store_tag` | 门店 | `POST /api/StoreTag/DeleteStoreTag` | 删除店铺会员标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_tag.edit_store_tag` | 门店 | `POST /api/StoreTag/EditStoreTag` | 修改店铺会员标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.store_tag.get_store_tag_list` | 门店 | `POST /api/StoreTag/GetStoreTagList` | 获取会员标签列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.store_tag.set_store_group_sort` | 门店 | `POST /api/StoreTag/SetStoreGroupSort` | 设置会员标签分组 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.add_reservation_controls` | 预约 | `POST /api/Reservation/AddReservationControls` | 新增自定义控件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.add_reservation_time_seting` | 预约 | `POST /api/Reservation/AddReservationTimeSeting` | 新增预约时间段 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.business_add_reservation` | 预约 | `POST /api/Reservation/BusinessAddReservation` | 新增预约 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.business_cancel_reservation` | 预约 | `POST /api/Reservation/BusinessCancelReservation` | 取消预约 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.business_get_is_reservation` | 预约 | `POST /api/Reservation/BusinessGetIsReservation` | 取消预约 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.business_get_reservation_by_id` | 预约 | `POST /api/Reservation/BusinessGetReservationById` | 获取预约详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.business_get_reservation_list` | 预约 | `POST /api/Reservation/BusinessGetReservationList` | 获取已约列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.business_get_reservation_summary` | 预约 | `POST /api/Reservation/BusinessGetReservationSummary` | 店铺获取预约首页汇总 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.business_get_store_lessons_reservation_list` | 预约 | `POST /api/Reservation/BusinessGetStoreLessonsReservationList` | 获取已约课程列表(旧) | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.business_get_user_cards_by_lessons_id` | 预约 | `POST /api/Reservation/BusinessGetUserCardsByLessonsId` | 根据预约团课ID获取用户会员卡列表详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.business_set_is_reservation` | 预约 | `POST /api/Reservation/BusinessSetIsReservation` | 取消预约 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.business_update_reservation` | 预约 | `POST /api/Reservation/BusinessUpdateReservation` | 修改预约 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.confirm_reservation` | 预约 | `POST /api/Reservation/ConfirmReservation` | 确认预约 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.del_reservation_controls` | 预约 | `POST /api/Reservation/DelReservationControls` | 删除自定义控件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.del_reservation_time_seting` | 预约 | `POST /api/Reservation/DelReservationTimeSeting` | 删除预约时间段 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.get_course_list` | 预约 | `POST /api/Reservation/GetCourseList` | 获取课程列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_business_hours` | 预约 | `POST /api/Reservation/GetReservationBusinessHours` | 获取预约门店的营业时间，供预约课表遍历左侧时间段用 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_calender` | 预约 | `POST /api/Reservation/GetReservationCalender` | 获取日历不可预约时间 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_controls` | 预约 | `POST /api/Reservation/GetReservationControls` | 获取自定义控件 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_controls_list` | 预约 | `POST /api/Reservation/GetReservationControlsList` | 获取自定义控件列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_lessons_by_staff_id` | 预约 | `POST /api/Reservation/GetReservationLessonsByStaffId` | 根据技师ID 和 起止时间查询技师预约课表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_lessons_sum` | 预约 | `POST /api/Reservation/GetReservationLessonsSum` | 获取新版课程日历预约课程 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_people_sum` | 预约 | `POST /api/Reservation/GetReservationPeopleSum` | 获取日历预约人数 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_service_item_list` | 预约 | `POST /api/Reservation/GetReservationServiceItemList` | 获取项目列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_reservation_time_seting` | 预约 | `POST /api/Reservation/GetReservationTimeSeting` | 获取预约时间段 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.get_work_staff_list` | 预约 | `POST /api/Reservation/GetWorkStaffList` | 工作技师列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation.set_reservation_controls` | 预约 | `POST /api/Reservation/SetReservationControls` | 设置自定义控件 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation.update_reservation_business_remark` | 预约 | `POST /api/Reservation/UpdateReservationBusinessRemark` | 修改预约商家备注 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation_set.add_reservation_tag` | 预约 | `POST /api/ReservationSet/AddReservationTag` | 新增预约标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation_set.delete_reservation_tag` | 预约 | `POST /api/ReservationSet/DeleteReservationTag` | 删除预约标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation_set.edit_course_category` | 预约 | `POST /api/ReservationSet/EditCourseCategory` | 编辑商品分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation_set.get_course_search_tag` | 预约 | `POST /api/ReservationSet/GetCourseSearchTag` | 预约课目搜索标签列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation_set.get_reservation_seting` | 预约 | `POST /api/ReservationSet/GetReservationSeting` | 获取预约设置 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation_set.get_reservation_tag` | 预约 | `POST /api/ReservationSet/GetReservationTag` | 预约相关标签列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.reservation_set.set_group_lessons_seting` | 预约 | `POST /api/ReservationSet/SetGroupLessonsSeting` | 修改团课设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation_set.set_private_lessons_seting` | 预约 | `POST /api/ReservationSet/SetPrivateLessonsSeting` | 修改私教设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.reservation_set.set_reservation_seting` | 预约 | `POST /api/ReservationSet/SetReservationSeting` | 修改预约设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.add_food` | 餐饮订单 | `POST /api/Food/AddFood` | 添加食品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.add_food_category` | 餐饮订单 | `POST /api/Food/AddFoodCategory` | 创建商品分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.add_food_tag` | 餐饮订单 | `POST /api/Food/AddFoodTag` | 创建食品标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.cancel_order` | 餐饮订单 | `POST /api/Food/CancelOrder` | 取消订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.change_food_table` | 餐饮订单 | `POST /api/Food/ChangeFoodTable` | 换台 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.check_user_is_rider` | 餐饮订单 | `POST /api/Food/CheckUserIsRider` | 是否是骑手 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.close_food_table` | 餐饮订单 | `POST /api/Food/CloseFoodTable` | B端关台 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.confirm_rider` | 餐饮订单 | `POST /api/Food/ConfirmRider` | 获取配送人员是否有订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.create_food_order` | 餐饮订单 | `POST /api/Food/CreateFoodOrder` | 创建订单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.del_food` | 餐饮订单 | `POST /api/Food/DelFood` | 删除 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.del_food_category` | 餐饮订单 | `POST /api/Food/DelFoodCategory` | 删除食品分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.del_food_tag` | 餐饮订单 | `POST /api/Food/DelFoodTag` | 删除食品标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.del_rider` | 餐饮订单 | `POST /api/Food/DelRider` | 删除配送人员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_food` | 餐饮订单 | `POST /api/Food/EditFood` | 编辑食品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_food_category` | 餐饮订单 | `POST /api/Food/EditFoodCategory` | 编辑商品分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_food_category_info` | 餐饮订单 | `POST /api/Food/EditFoodCategoryInfo` | 编辑食品分类信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_food_setting` | 餐饮订单 | `POST /api/Food/EditFoodSetting` | 编辑点单基本设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_food_tag` | 餐饮订单 | `POST /api/Food/EditFoodTag` | 编辑食品标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_print_device_food_tag` | 餐饮订单 | `POST /api/Food/EditPrintDeviceFoodTag` | 编辑打印机标签 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_print_device_setting` | 餐饮订单 | `POST /api/Food/EditPrintDeviceSetting` | 编辑打印机设置 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.edit_rider` | 餐饮订单 | `POST /api/Food/EditRider` | 编辑配送员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_cart_add` | 餐饮订单 | `POST /api/Food/FoodCartAdd` | 添加菜品到购物车 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_cart_clear` | 餐饮订单 | `POST /api/Food/FoodCartClear` | 清空购物车 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_cart_remove` | 餐饮订单 | `POST /api/Food/FoodCartRemove` | 移除购物车子项 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_cart_update` | 餐饮订单 | `POST /api/Food/FoodCartUpdate` | 修改购物车商品数量 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_on_off` | 餐饮订单 | `POST /api/Food/FoodOnOff` | 上/下架食品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_search` | 餐饮订单 | `POST /api/Food/FoodSearch` | 搜索食品 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.food_table_add` | 餐饮订单 | `POST /api/Food/FoodTableAdd` | 添加桌台 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_table_delete` | 餐饮订单 | `POST /api/Food/FoodTableDelete` | 删除桌台 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.food_table_edit` | 餐饮订单 | `POST /api/Food/FoodTableEdit` | 修改桌台信息 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.get_food_cart` | 餐饮订单 | `POST /api/Food/GetFoodCart` | 获取点餐购物车 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_category_list` | 餐饮订单 | `POST /api/Food/GetFoodCategoryList` | 获取食品分类 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_inforation` | 餐饮订单 | `POST /api/Food/GetFoodInforation` | 获取食品详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_list` | 餐饮订单 | `POST /api/Food/GetFoodList` | 获取食品列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_order_detail` | 餐饮订单 | `POST /api/Food/GetFoodOrderDetail` | 获取订单详情 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_order_list` | 餐饮订单 | `POST /api/Food/GetFoodOrderList` | 获取订单列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_setting` | 餐饮订单 | `POST /api/Food/GetFoodSetting` | 获取点单基本设置 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_sku_list` | 餐饮订单 | `POST /api/Food/GetFoodSkuList` | 获取规格列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_table_info` | 餐饮订单 | `POST /api/Food/GetFoodTableInfo` | 获取桌台信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_table_list` | 餐饮订单 | `POST /api/Food/GetFoodTableList` | 获取餐台列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_table_qr_code` | 餐饮订单 | `POST /api/Food/GetFoodTableQrCode` | 获取餐台二维码 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_table_statistics` | 餐饮订单 | `POST /api/Food/GetFoodTableStatistics` | 餐台数据（首页） | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_food_tag_list` | 餐饮订单 | `POST /api/Food/GetFoodTagList` | 获取食品标签 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_new_order_information` | 餐饮订单 | `POST /api/Food/GetNewOrderInformation` | 首页获取新订单信息 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_print_device_setting` | 餐饮订单 | `POST /api/Food/GetPrintDeviceSetting` | 获取打印机设置 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.get_rider_list` | 餐饮订单 | `POST /api/Food/GetRiderList` | 获取配送人员列表 | 需要登录 | B | 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| `crmapi.food.open_food_table` | 餐饮订单 | `POST /api/Food/OpenFoodTable` | B端开台 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.order_complete` | 餐饮订单 | `POST /api/Food/OrderComplete` | 订单送达 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.order_delivery` | 餐饮订单 | `POST /api/Food/OrderDelivery` | 订单配送/更改配送员 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.pay_food_order` | 餐饮订单 | `POST /api/Food/PayFoodOrder` | 订单支付 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.print_pay_order` | 餐饮订单 | `POST /api/Food/PrintPayOrder` | 打印结账单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.print_pre_order` | 餐饮订单 | `POST /api/Food/PrintPreOrder` | 打印预结单 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.return_food` | 餐饮订单 | `POST /api/Food/ReturnFood` | 退菜 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.set_food_order_discount` | 餐饮订单 | `POST /api/Food/SetFoodOrderDiscount` | 设置订单折扣 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.set_food_out` | 餐饮订单 | `POST /api/Food/SetFoodOut` | 订单出餐完成 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.set_food_scene` | 餐饮订单 | `POST /api/Food/SetFoodScene` | 设置点餐场景 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.sort_food` | 餐饮订单 | `POST /api/Food/SortFood` | 排序食品 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |
| `crmapi.food.sort_food_category` | 餐饮订单 | `POST /api/Food/SortFoodCategory` | 排序食品分类 | 需要登录 | D | Action 名称和摘要不足以证明只读，默认禁止。 |

## 可筛选工具详细契约

### `crmapi.activity.business_activity_forwarder_data_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Activity 模块 |
| 用途 | 活动报告 转发者数据列表 |
| 使用时机 | 在商家视角中核对“活动报告 转发者数据列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Activity/BusinessActivityForwarderDataList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessActivityForwarderDataListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessActivityForwarderDataListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ActivityController.cs:380` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ActivityId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 活动ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessActivityForwarderDataListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Uid` | `int` | 转发者ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 转发者姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].IsVip` | `int` | 是否是会员 0 否 1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserImg` | `string` | 转发者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].NewViewers` | `long` | 新客浏览人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].NewUserGetCount` | `long` | 新客领取张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].NewUserUsedCount` | `long` | 新客已使用张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SelfGetCount` | `long` | 自己获得数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SelfUsedCount` | `long` | 自己使用数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].WaitGetCount` | `long` | 待领取 张 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ActivityProvider.BusinessActivityForwarderDataList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.activity.business_activity_new_user_data_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Activity 模块 |
| 用途 | 活动报告 新客数据列表 |
| 使用时机 | 在商家视角中核对“活动报告 新客数据列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Activity/BusinessActivityNewUserDataList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessActivityNewUserDataListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessActivityNewUserDataListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ActivityController.cs:405` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ActivityId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 活动ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsSelectAll` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否查询当前活动所有新客 false时查询 转发人转发效果列表；普通业务字段；可按问题需要提供 |
| `RecomUid` | `int` | 参与组合校验 | 绑定=ApiController推断；if(!model.IsSelectAll && model.RecomUid < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 转发者ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessActivityNewUserDataListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ActivityId` | `int` | 活动ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Uid` | `int` | 新客ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 新客姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CreateDate` | `string` | 新客浏览时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].NewUserGetCount` | `long` | 新客领取张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].NewUserUsedCount` | `long` | 新客使用张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsVip` | `int` | 是否是会员 0 否 1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LogType` | `bool` | 操作类型 是否是仅浏览 1 是 0 否 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`!model.IsSelectAll && model.RecomUid < 1`；`resultMsg.State`
- 一层业务调用：`ActivityProvider.BusinessActivityNewUserDataList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`；`固定提示：当查询转发人转发效果时，转发人Uid不能<0！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.activity.business_get_activity`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Activity 模块 |
| 用途 | 获取活动详情 |
| 使用时机 | 在商家视角中核对“获取活动详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Activity/BusinessGetActivity` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetActivityResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetActivityResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ActivityController.cs:169` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ActivityId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 优惠券ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 活动id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Title` | `string` | 活动标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.BgImg` | `string` | 活动背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOneReward` | `int` | 是否开启新客奖励 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTwoReward` | `int` | 是否开启转发人奖励 | 普通业务字段 | 可按问题需要提供 |
| `Data.OneReceiveLimit` | `int` | 新客奖励领取限制 0无限制 1新客 | 普通业务字段 | 可按问题需要提供 |
| `Data.OneIsMobile` | `bool` | 领取是否索要手机号码 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ShareLimit` | `int` | 转发限制，0允许转发，1不允许转发 | 普通业务字段 | 可按问题需要提供 |
| `Data.TwoReceiveLimit` | `int` | 转发人奖励限制 0 只要转发就可以得到 1有人领取才可以得到 2使用卷后才可以得到 | 普通业务字段 | 可按问题需要提供 |
| `Data.TwoReceiveCount` | `int` | 转发人奖励数量 0 仅限一份 1满足限制条件 无限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.TwoIsMobile` | `bool` | 转发是否索要手机号码 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ActivityRules` | `string` | 活动规则 | 普通业务字段 | 可按问题需要提供 |
| `Data.BeginDate` | `string` | 活动开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.EndDate` | `string` | 活动结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsRewardUnite` | `bool` | 是否奖励统一 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 -1删除 0暂停 1正常 2未开始 3结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.EndReason` | `int` | 结束原因 0默认 1到期 2优惠券发放完毕 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons` | `List<ActivityCouponViewModel>` | 优惠券详细 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].AcType` | `int` | 类型 0活动主动领券，1分享后领券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponCount` | `int` | 已领取数量 ***** 在创建活动，修改活动 接口中 该字段表示 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`；`data.Coupons?.Count > 0`
- 一层业务调用：`ActivityProvider.ActivityGetById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.activity.business_get_activity_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Activity 模块 |
| 用途 | 获取活动管理数据列表 |
| 使用时机 | 在商家视角中核对“获取活动管理数据列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Activity/BusinessGetActivityList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<BusinessGetActivityListResponseModel>>`；包装 `DataResult`；Data `PageData<BusinessGetActivityListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ActivityController.cs:66` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetActivityListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | id 活动id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Title` | `string` | 活动标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BgImg` | `string` | 活动背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BeginDate` | `string` | 活动开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].EndDate` | `string` | 活动结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 状态 -1删除 0暂停 1正常 2未开始 3结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Pageviews` | `long` | 浏览量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ForwardCoupon` | `long` | 转发送券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].NewUserCoupon` | `long` | 新客送券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsOneReward` | `bool` | 是否开启新客奖励 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsTwoReward` | `bool` | 是否开启转发人奖励 1是 0否 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ActivityProvider.BusinessGetActivityList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.activity.business_get_share_qr_code`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Activity 模块 |
| 用途 | 生成活动分享二维码 |
| 使用时机 | 在商家视角中核对“生成活动分享二维码”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Activity/BusinessGetShareQrCode` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<ActivityQrCodeShareResponseModel>>`；包装 `Task/DataResult`；Data `ActivityQrCodeShareResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ActivityController.cs:436` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ActivityId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.ActivityId<1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 活动ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Receive` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `ActivityType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 活动类型 0裂变 1抽奖；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`model.ActivityId<1`；`resultMsg.State`；`Constant.Environment.Equals(EnvironmentEnum.PRO.GetEnumText())`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`；`固定提示：活动ID不能为空！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.activity.get_activity_template_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Activity 模块 |
| 用途 | 获取活动背景图列表 |
| 使用时机 | 在商家视角中核对“获取活动背景图列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Activity/GetActivityTemplateList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetActivityTemplateListResponseModel>>`；包装 `Task/DataResult`；Data `GetActivityTemplateListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ActivityController.cs:27` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ActivityType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 活动类型 0裂变 1抽奖；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ActivityImages` | `List<string>` | 活动背景图 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`ctModel != null`
- 一层业务调用：`ActivityTemplateProvider.GetActivityTemplateAll`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.agency_matter.get_agency_matter_count`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AgencyMatter 模块 |
| 用途 | 根据门店ID，获取门店下面待办事项数量 |
| 使用时机 | 在商家视角中核对“根据门店ID，获取门店下面待办事项数量”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AgencyMatter/GetAgencyMatterCount` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<AgencyMatterItemsCountResponseModel>>`；包装 `Task/DataResult`；Data `AgencyMatterItemsCountResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AgencyMatterController.cs:80` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(info.StoreId < 1 \\|\\| info.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(info.StoreId < 1 \\|\\| info.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.MatterCount` | `int` | 代办数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.MatterCountForCard` | `int` | 会员卡审核代办数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`info.StoreId < 1 \|\| info.Uid < 1`
- 一层业务调用：`AgencyMatterProvider.GetMatterCountByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.agency_matter.get_agency_matter_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AgencyMatter 模块 |
| 用途 | 获取待审核列表 |
| 使用时机 | 在商家视角中核对“获取待审核列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AgencyMatter/GetAgencyMatterList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<AgencyMatterPageViewResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<AgencyMatterPageViewResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AgencyMatterController.cs:24` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | [必填]每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | [必填]请求页数；普通业务字段；可按问题需要提供 |
| `HandleType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | [非必填，默认是0]审核类型查询 0 全部，1发起审核 ，2财务核销审核；普通业务字段；可按问题需要提供 |
| `SponsorName` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | [非必填]发起人姓名(支持模糊查询)；普通业务字段；可按问题需要提供 |
| `UserName` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 当前会话提供并临时使用；不得持久化到模型历史 | [非必填]门店会员姓名(模糊查询)；个人信息；仅在当前授权场景按最小范围提供 |
| `AuditStatus` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | [非必填，默认为0]审核状态 0全部，1审核通过 2驳回；普通业务字段；可按问题需要提供 |
| `StartAuditTime` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | [非必填]按审核时间段查询的开始时间点（日期精确到天，格式为 2020-12-12）；普通业务字段；可按问题需要提供 |
| `EndAuditTime` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | [非必填]按审核时间段查询的结束时间点（日期精确到天，格式为 2020-12-12）；普通业务字段；可按问题需要提供 |
| `AgencyType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 查询数据类型：1为优惠券，2为会员卡；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<AgencyMatterPageViewResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MatterId` | `int` | 代办事项ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].SponsorName` | `string` | 发起人姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SponsorUid` | `int` | 发起人ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].MatterTime` | `string` | 代办发起 时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserId` | `int` | 会员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 会员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].FocusField` | `string` | 会员特殊标识（可以是车牌、生日、住址等） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].HandleType` | `int` | 代办事项类型 1、发起 2、核销 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].AgentName` | `string` | 审核人姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].AgentId` | `int` | 审核人ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].AgentState` | `int` | 审核状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | 审核备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CouponList` | `List<MatterCouponViewModel>` | 优惠券基本信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].Id` | `int` | id 主键Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CouponList[].CouponType` | `int` | coupon_type 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponTitle` | `string` | coupon_title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponImg` | `string` | coupon_img 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponIcon` | `string` | 图标 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponBgImg` | `string` | coupon_bg_img 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].IsUseVip` | `bool` | is_use_vip 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].IsMore` | `bool` | is_more 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].IsBuyCard` | `bool` | is_buy_card 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].IsAuditing` | `bool` | is_buy_card 是否需要发放审核优惠券，默认是0(不需审核)，1为需要审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].IsUseAudit` | `bool` | is_use_audit 使用优惠券是否需要审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].UseMinMoney` | `decimal` | use_min_money 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponValue` | `decimal` | coupon_value 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].OriginalPrice` | `decimal` | original_price 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponSource` | `int` | coupon_source 创建源 来源 0其他 1活动 2 优惠中心 3店长赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponCount` | `int` | coupon_count 优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].CouponDescription` | `string` | coupon_description 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].BeginDate` | `string` | begin_date 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].EndDate` | `string` | end_date 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].RangeDate` | `string` | 有效周期 天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponList[].RangeDateRead` | `string` | 优惠券有效期说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards` | `List<StorePrepaidCardViewModel>` | 会员子卡实体集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].PrepaidCardId` | `int` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].CardImg` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardType` | `int` | 类型：0计次，1储值 2时限 3权益 4安心充 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardValue` | `decimal` | 卡价值 次/金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardGivingValue` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardCount` | `int` | 库存剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardSellCount` | `long` | card_sell_count 销售数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].ValidType` | `int` | 失效日期类型: 0 相对日期（购卡后N天； 1 绝对日期（指定失效日期） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].InvalidDate` | `string` | 失效日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].ValidityDate` | `int` | 有效期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].IsBuyOnce` | `bool` | 是否只能购买一次 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].State` | `int` | 状态 1在售 0停售 -1删除 2售罄 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].OpenCardType` | `int` | 开卡方式 0购买即开卡，1首次使用开卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].StopCardDays` | `int` | 停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].OpenCardDaysMax` | `int` | 最大延迟开卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].IsPayAfterReceive` | `bool` | 是否支付后领取 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardInstructions` | `string` | 会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].IsPromptRights` | `bool` | 是否强制提示会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Services` | `List<StorePrepaidCardServiceItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Services[].PrepaidCardId` | `int` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Services[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Services[].CardValue` | `decimal` | 服务项 卡价值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Services[].IsGift` | `bool` | 是否是赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons` | `List<CouponCenterCouponViewModel>` | 购卡送的券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponCount` | `int` | 已领取数量 ***** 在创建，修改 接口中 该字段表示 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].CouponSum` | `int` | 活动优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].Coupons[].ItemId` | `int` | 活动项目ID（预留） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].IsAudit` | `bool` | 卡是否需要审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].ServiceItems` | `List<string>` | 审核卡消费项目明细 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CheckItemId` | `int` | 代办事项明细ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].CardTimeList` | `List<StorePrepaidCardViewCardTimeModel>` | 会员卡有效期集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ChildCards[].CardTimeList[].ValidType` | `int` | 失效日期类型: 0 相对日期（购卡后N天)； 1 绝对日期（指定失效日期） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].InvalidDate` | `string` | 失效日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].ValidityDate` | `int` | 有效期单位天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].MaxCount` | `int` | 最大可售出数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].CardCount` | `int` | 剩余卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].IsBuyOnce` | `bool` | 是否只能购买一次 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].GivePrice` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].StartDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].EndDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CardTimeList[].CreateDate` | `DateTime` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].IsCardChild` | `bool` | 是否包含子卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].RemainderDay` | `int` | 剩余天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].IsCustomPrice` | `bool` | 是否开启自定义金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CustomPriceMin` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CustomPriceMax` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].CustomPriceGiveRatio` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ChildCards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`model.PageIndex.Equals(0)`；`model.PageSize.Equals(0)`；`resultMsg.State`
- 一层业务调用：`AgencyMatterProvider.GetAgencyMatterList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`；`固定提示：缺少PageIndex传参`；`固定提示：缺少PageSize传参`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.agent.get_agent_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Agent 模块 |
| 用途 | 获取用户代理信息 |
| 使用时机 | 在商家视角中核对“获取用户代理信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Agent/GetAgentInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：PromotionCodeProvider.PromotionCodeAdd |
| 返回 | `Task<DataResult<GetAgentInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetAgentInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AgentController.cs:27` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.AgentId` | `int` | 代理Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AgentName` | `string` | 代理名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionCode` | `string` | 推广码 | 普通业务字段 | 可按问题需要提供 |
| `Data.AgentBalance` | `string` | 账户余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReviewState` | `int` | 审核状态 -1未申请， 0审核中 1审核通过 2审核失败 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReviewInfo` | `string` | 审核文字说明 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0`；`resultMsg.State`；`agm?.Id > 0`；`agm.State == 1`；`!(pcm?.Id > 0)`
- 一层业务调用：`AgentProvider.GetAgentByUid`、`PromotionCodeProvider.GetPromotionCodeByUid`、`PromotionCodeProvider.PromotionCodeAdd`
- 疑似副作用：`PromotionCodeProvider.PromotionCodeAdd`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.agent.get_promotion_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Agent 模块 |
| 用途 | 获取推广收益列表 |
| 使用时机 | 在商家视角中核对“获取推广收益列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Agent/GetPromotionList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：SoftOrderProvider.GetFirstPayOrder |
| 返回 | `Task<DataResult<GetPromotionListResponseModel>>`；包装 `Task/DataResult`；Data `GetPromotionListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AgentController.cs:160` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCount` | `long` | 商家数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionDatas` | `List<PromotionInfoViewModel>` | 收益列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionDatas[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PromotionDatas[].StoreLogo` | `string` | 商家Logo | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionDatas[].StoreName` | `string` | 店名 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionDatas[].UserMobile` | `string` | 手机 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PromotionDatas[].Revenue` | `string` | 预计收益 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionDatas[].State` | `int` | 审核状态 0待订购 1订购确认（返现条件达成）2待确认订购（已购买但未达7天） | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionDatas[].StateInfo` | `string` | 状态文字说明 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0`；`resultMsg.State`；`stores?.Count > 0`；`sm.SoftVersion != 0 && sm.SoftVersion != 1 && sm.SoftVersion != 100 && sm.ServiceEndDate.Value.Subtract(item.CreateDate).TotalDays > 360`；`sm != null && som!=null`；`DateTime.Now.Subtract(som.CreateDate).TotalDays >= 7`
- 一层业务调用：`UserRecommendProvider.GetUserRecommendByRuid`、`StoreProvider.GetStoreByIdAsync`、`UsersInfoProvider.UsersInfoGetByUid`、`SoftOrderProvider.GetFirstPayOrder`
- 疑似副作用：`SoftOrderProvider.GetFirstPayOrder`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.agent.gete_income_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Agent 模块 |
| 用途 | 获取提现列表 |
| 使用时机 | 在商家视角中核对“获取提现列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Agent/GeteIncomeList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GeteIncomeListResponseModel>>`；包装 `Task/DataResult`；Data `GeteIncomeListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AgentController.cs:228` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IncomeDatas` | `List<AgentIncomeInfoViewModel>` | 收益列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.IncomeDatas[].IncomeId` | `int` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IncomeDatas[].IncomePrice` | `string` | 提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.IncomeDatas[].CreateDate` | `string` | 时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.IncomeDatas[].State` | `int` | 审核状态 0待审核 1提现成功 | 普通业务字段 | 可按问题需要提供 |
| `Data.IncomeDatas[].StateInfo` | `string` | 状态文字说明 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0`；`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.app_aes_get`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 查询 AES 密钥 |
| 使用时机 | 在商家视角中核对“查询 AES 密钥”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AlipayThirdPlatform/AppAesGet` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<string>>`；包装 `Task/DataResult`；Data `string` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:192` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 商家小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `string` | 标量返回值；具体业务含义以 Action 摘要为准 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.AppAesGet`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.axc_merchantremaining_query`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 商家余额查询 |
| 使用时机 | 在商家视角中核对“商家余额查询”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `GET /api/AlipayThirdPlatform/AxcMerchantremainingQuery` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `object`；包装 `无已识别包装`；Data `object` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:557` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `storeId` | `int` | Action 形参 | ApiController推断 | 服务端注入：已确认门店 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `object` | 运行时对象，字段结构无法由方法签名静态确定 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.AxcMerchantremainingQuery`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.axc_profile_get`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 安心充卡样查询 |
| 使用时机 | 在商家视角中核对“安心充卡样查询”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `GET /api/AlipayThirdPlatform/AxcProfileGet` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `object`；包装 `无已识别包装`；Data `object` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:492` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appId` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `object` | 运行时对象，字段结构无法由方法签名静态确定 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.AxcProfileGet`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.axc_promotion_plan_query`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 安心充方案查询 |
| 使用时机 | 在商家视角中核对“安心充方案查询”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `GET /api/AlipayThirdPlatform/AxcPromotionPlanQuery` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `object`；包装 `无已识别包装`；Data `object` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:570` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appId` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `promotionPlanId` | `string` | Action 形参 | ApiController推断 | 必须来自同一会话上游 API 结果或服务端对象引用 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `object` | 运行时对象，字段结构无法由方法签名静态确定 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.AxcPromotionPlanQuery`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.category_query`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 小程序类目查询 |
| 使用时机 | 在商家视角中核对“小程序类目查询”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AlipayThirdPlatform/CategoryQuery` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<string>>`；包装 `Task/DataResult`；Data `string` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:239` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| — | — | 无 Action 业务参数 | — | — | — |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `string` | 标量返回值；具体业务含义以 Action 摘要为准 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.CategoryQuery`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.expand_order_query`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 商户申请单查询 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AlipayThirdPlatform/ExpandOrderQuery` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<string>>`；包装 `Task/DataResult`；Data `string` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:609` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appId` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `orderId` | `string` | Action 形参 | ApiController推断 | 必须来自同一会话上游 API 结果或服务端对象引用 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `string` | 标量返回值；具体业务含义以 Action 摘要为准 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.ExpandOrderQuery`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.experience_query`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 小程序体验版状态查询接口 |
| 使用时机 | 在商家视角中核对“小程序体验版状态查询接口”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AlipayThirdPlatform/ExperienceQuery` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<string>>`；包装 `Task/DataResult`；Data `string` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:91` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 商家小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppVersion` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 商家小程序版本号。；普通业务字段；可按问题需要提供 |
| `BundleId` | `string` | 可选/有默认值 | 绑定=ApiController推断；默认值="com.alipay.alipaywallet" | 必须来自同一会话上游 API 结果或服务端对象引用 | 小程序客户端类型，默认为支付宝端。支持如下客户端： com.alipay.alipaywallet：支付宝端； com.alibaba.android.rimet：DINGDING端； com.amap.app：高德端； com.alibaba.ailabs.genie.webapps：天猫精灵端； com.alipay.iot.xpaas：支付宝IoT端。；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `string` | 标量返回值；具体业务含义以 Action 摘要为准 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.ExperienceQuery`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.topic_subscribe_query`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 【源码推断】TopicSubscribeQuery 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。 |
| 使用时机 | 在商家视角中核对“【源码推断】TopicSubscribeQuery 的 XML 摘要缺失，按 Action 名仅能判断为候选查询。”对应的当前返回结果。 由于 XML 摘要缺失，只有请求/响应字段也能证明目标事实时才可调用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AlipayThirdPlatform/TopicSubscribeQuery` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `object`；包装 `无已识别包装`；Data `object` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:478` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appId` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `userId` | `string` | Action 形参 | ApiController推断 | 必须来自同一会话上游 API 结果或服务端对象引用 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `topic` | `string` | Action 形参 | ApiController推断 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `object` | 运行时对象，字段结构无法由方法签名静态确定 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.TopicSubscribeQuery`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alipay_third_platform.version_detail_query`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AlipayThirdPlatform 模块 |
| 用途 | 小程序版本查询 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AlipayThirdPlatform/VersionDetailQuery` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<object>>`；包装 `Task/DataResult`；Data `object` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AlipayThirdPlatformController.cs:582` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appId` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `version` | `string` | Action 形参 | ApiController推断 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `object` | 运行时对象，字段结构无法由方法签名静态确定 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alliance.get_alliance_member`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Alliance 模块 |
| 用途 | 获取联盟内成员 |
| 使用时机 | 在商家视角中核对“获取联盟内成员”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Alliance/GetAllianceMember` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetAllianceMemberResponseModel>>`；包装 `Task/DataResult`；Data `GetAllianceMemberResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AllianceController.cs:228` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId<0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores` | `List<AllianceStoreResponseModel>` | 管理员列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreId` | `int` | 店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Stores[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreLogo` | `string` | 用户头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Address` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].StoreMobile` | `string` | 店铺联系电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].StoreState` | `string` | 店铺状态信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreIdentity` | `int` | 身份 0创始人，1成员 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId<0`；`resultMsg.State`；`sa?.Id > 0`
- 一层业务调用：`StoreAllianceProvider.GetStoreAllianceByStoreId`、`StoreAllianceProvider.GetStoreAllianceListByAllianceId`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`；`固定提示：您还没有加入任何联盟！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.alliance.join_alliance_check`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Alliance 模块 |
| 用途 | 判断用户是否可以加入联盟（扫码） |
| 使用时机 | 在商家视角中核对“判断用户是否可以加入联盟（扫码）”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Alliance/JoinAllianceCheck` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<JoinAllianceCheckResponseModel>>`；包装 `Task/DataResult`；Data `JoinAllianceCheckResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AllianceController.cs:74` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId <1 \\|\\| string.IsNullOrEmpty(model.QrCodeStr)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId <1 \\|\\| string.IsNullOrEmpty(model.QrCodeStr)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `QrCodeStr` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId <1 \\|\\| string.IsNullOrEmpty(model.QrCodeStr)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 二维码参数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreLogo` | `string` | 店铺Logo | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsJoin` | `int` | 1：可以加入 0邀请码无效 -1，已加入该联盟 -2已加入其他联盟 -3 无法邀请自己 | 普通业务字段 | 可按问题需要提供 |
| `Data.Reason` | `string` | 不能加入的原因 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId <1 \|\| string.IsNullOrEmpty(model.QrCodeStr)`；`resultMsg.State`；`!RedisManager.ExistKey(StoreageDbDefinition.DataThemeInfo, "alliance:" + qcm.UId + qcm.TT)`；`qcm.SId==model.StoreId`；`isTrue`；`sa?.Id>0`；`sa.AllianceId==ssa?.AllianceId`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`RedisManager.ExistKey`、`StoreAllianceProvider.GetStoreAllianceByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.article.article_get_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Article 模块 |
| 用途 | 获取文章详情 新 |
| 使用时机 | 在商家视角中核对“获取文章详情 新”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Article/ArticleGetInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<ArticleGetInfoResponseModel>>`；包装 `Task/DataResult`；Data `ArticleGetInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ArticleController.cs:151` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ArticleId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.ArticleId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 文章id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 主键唯一标识 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Title` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTop` | `bool` | 是否置顶 | 普通业务字段 | 可按问题需要提供 |
| `Data.Keyword` | `string` | 关键词 | 普通业务字段 | 可按问题需要提供 |
| `Data.ArticleDescribe` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Author` | `string` | 作者 | 普通业务字段 | 可按问题需要提供 |
| `Data.Abstract` | `string` | 摘要 | 普通业务字段 | 可按问题需要提供 |
| `Data.Content` | `string` | 内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.ArticleImg` | `string` | 文章图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreTypeName` | `string` | 行业名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.ArticleId < 1`；`resultMsg.State`；`articleModel == null`
- 一层业务调用：`ArticleProvider.ArticleGetInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 101`；`固定提示：参数不正确`；`固定提示：当前文章不存在或已被下架`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.article.article_get_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Article 模块 |
| 用途 | 获取文章列表 新 |
| 使用时机 | 在商家视角中核对“获取文章列表 新”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Article/ArticleGetList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<ArticleGetListResponseModel>>`；包装 `DataResult`；Data `PageData<ArticleGetListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ArticleController.cs:115` |
| C/B 对照 | capi.article.article_get_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；默认值=1；if(model.PageIndex == 0) | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；默认值=8；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页显示行数；普通业务字段；可按问题需要提供 |
| `StoreTypeId` | `string` | 可选/有默认值 | 绑定=ApiController推断；默认值="0" | 必须来自同一会话上游 API 结果或服务端对象引用 | 行业类型id 0表示全部 目前传0；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ArticleTypeId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 文章类型 0表示全部，1-官网新闻，2-优选案例；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<ArticleGetListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | id 主键唯一标识 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Title` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ArticleImg` | `string` | 文章图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsTop` | `bool` | 是否置顶 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Url` | `string` | 文章路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UrlType` | `int` | 标识当前文章状态；0-内容，1-url跳转 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.PageSize == 0`；`model.PageIndex == 0`；`resultMsg.State`
- 一层业务调用：`ArticleProvider.ArticleGetList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`固定提示：请求数据数量不正确`；`固定提示：页码不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.article.get_article_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Article 模块 |
| 用途 | 获取文章详细 |
| 使用时机 | 在商家视角中核对“获取文章详细”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Article/GetArticleInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetArticleInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetArticleInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ArticleController.cs:22` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Id < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 文章ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Id` | `int` | id 主键唯一标识 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ArticleTypeId` | `int` | 文章类型Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Title` | `string` | title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTop` | `bool` | is_top 是否置顶 | 普通业务字段 | 可按问题需要提供 |
| `Data.Keyword` | `string` | keyword 关键词 | 普通业务字段 | 可按问题需要提供 |
| `Data.ArticleDescribe` | `string` | describe 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Author` | `string` | author 作者 | 普通业务字段 | 可按问题需要提供 |
| `Data.Abstract` | `string` | abstract 摘要 | 普通业务字段 | 可按问题需要提供 |
| `Data.Content` | `string` | content 内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.OrderBy` | `int` | order_by 排序字段 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | state 状态 -1 删除 0暂停发布 1发布 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateDate` | `DateTime` | create_date 创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Id < 1`；`resMsg.State`
- 一层业务调用：`ArticleProvider.GetArticleInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.article.get_article_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Article 模块 |
| 用途 | 获取文章列表 |
| 使用时机 | 在商家视角中核对“获取文章列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Article/GetArticleList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<GetArticleListResponseModel>>`；包装 `DataResult`；Data `PageData<GetArticleListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ArticleController.cs:63` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `PageIndex` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=1 | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=8 | AI 可在服务端上限内选择 | 每页显示行数；普通业务字段；可按问题需要提供 |
| `ArticleTypeId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 文章类型ID 0表示查询所有；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetArticleListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | id 主键唯一标识 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ArticleTypeId` | `int` | 文章类型Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ArticleTypeName` | `string` | 文章类型Id | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Title` | `string` | title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsTop` | `bool` | is_top 是否置顶 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Keyword` | `string` | keyword 关键词 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ArticleDescribe` | `string` | describe 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Author` | `string` | author 作者 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Abstract` | `string` | abstract 摘要 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Content` | `string` | content 内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OrderBy` | `int` | order_by 排序字段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UpdateDate` | `string` | update_date 修改时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | create_date 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ArticleImg` | `string` | 文章图片 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resMsg.State`
- 一层业务调用：`ArticleProvider.GetArticleList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.article.get_article_type_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Article 模块 |
| 用途 | 获取文章类型 |
| 使用时机 | 在商家视角中核对“获取文章类型”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Article/GetArticleTypeList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetArticleTypeListResponseModel>>`；包装 `Task/DataResult`；Data `GetArticleTypeListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ArticleController.cs:83` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `model` | `GetArticleTypeListRequestModel` | Action 形参 | ApiController推断 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Articles` | `List<GetArticleTypeInfoModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Articles[].Id` | `int` | 文章类型ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Articles[].TypeName` | `string` | 类型名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resMsg.State`
- 一层业务调用：`ArticleTypeProvider.GetArticleTypeList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.ask_for_items.business_get_agreement_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AskForItems 模块 |
| 用途 | 获取电子合同信息 |
| 使用时机 | 在商家视角中核对“获取电子合同信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AskForItems/BusinessGetAgreementInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetAgreementInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetAgreementInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AskForItemsController.cs:207` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 && model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 && model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpressState` | `int` | 状态 是否已经发送 0待发送 1已发送 -1未申请 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiveEmail` | `string` | 领取邮箱 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 && model.StoreId < 1`；`resMsg.State`；`agreement?.Id > 0`
- 一层业务调用：`AskForItemsProvider.BusinessGetAgreementInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.ask_for_items.business_get_ask_for_items_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AskForItems 模块 |
| 用途 | 获取物料发货信息 |
| 使用时机 | 在商家视角中核对“获取物料发货信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AskForItems/BusinessGetAskForItemsInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetAskForItemsInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetAskForItemsInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AskForItemsController.cs:21` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 && model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 && model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AskForType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 物料类别 0台牌 1合同；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpressState` | `int` | 快递状态 0未发送 1已发送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Consignee` | `string` | 收件人 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiveMobile` | `string` | 收货人电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReceivePovince` | `string` | 收货人-省 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiveCity` | `string` | 收货人-市 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiveDistrict` | `string` | 收货人-区、县 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiveAddress` | `string` | 收货详细地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ExpressName` | `string` | 快递名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpressNo` | `string` | 快递编号 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpressDate` | `string` | 快递寄出时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 && model.StoreId < 1`；`resMsg.State`
- 一层业务调用：`AskForItemsProvider.GetAskForItemsInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.ask_for_items.business_get_store_user_mobile`

| 项目 | 内容 |
| --- | --- |
| 业务域 | AskForItems 模块 |
| 用途 | 获取店铺用户默认手机号 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AskForItems/BusinessGetStoreUserMobile` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreUserMobileResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreUserMobileResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AskForItemsController.cs:107` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserMobile` | `string` | 用户手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 && model.Uid < 1`；`resMsg.State`
- 一层业务调用：`UsersProvider.GetUserMobileByUid`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.banner.banner_get_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Banner 模块 |
| 用途 | banner列表 |
| 使用时机 | 在商家视角中核对“banner列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Banner/BannerGetList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BannerGetListResponseModel>>`；包装 `Task/DataResult`；Data `BannerGetListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BannerController.cs:22` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BannerTypeId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | banner入口类型 首页-1；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList` | `List<BannerViewModel>` | banner集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.BannerList[].Img` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].LinkType` | `int` | 跳转类型；0-无，1-app内部，2-外链,3视频 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].LinkUrl` | `string` | 跳转地址；link_type为1需要定义标识，为2则是跳转地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].AppId` | `string` | AppId | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.NoticeList` | `List<NoticeViewModel>` | notice集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.NoticeList[].Content` | `string` | 内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].Level` | `int` | 等级；0-默认，1-常规，2-优先，3-紧急 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].LinkType` | `int` | 跳转类型；0-无，1-app内部，2-外链 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].LinkUrl` | `string` | 跳转地址；link_type为1需要定义标识，为2则是跳转地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`sm.SoftVersion==1 \|\| sm.CreateDate.AddDays(3)>DateTime.Now`；`!string.IsNullOrEmpty(sm.StoreTypeId) && storeTypeIds.Contains(sm.StoreTypeId)`；`sm.Id > 119496 && softVersioin.Contains(sm.SoftVersion.ToString())`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`BannerProvider.BannerGetList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.banner.banner_get_list_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Banner 模块 |
| 用途 | banner列表 |
| 使用时机 | 在商家视角中核对“banner列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Banner/BannerGetListV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BannerGetListResponseModel>>`；包装 `Task/DataResult`；Data `BannerGetListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BannerController.cs:79` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BannerTypeId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | banner入口类型 首页-1；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList` | `List<BannerViewModel>` | banner集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.BannerList[].Img` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].LinkType` | `int` | 跳转类型；0-无，1-app内部，2-外链,3视频 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].LinkUrl` | `string` | 跳转地址；link_type为1需要定义标识，为2则是跳转地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].AppId` | `string` | AppId | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.NoticeList` | `List<NoticeViewModel>` | notice集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.NoticeList[].Content` | `string` | 内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].Level` | `int` | 等级；0-默认，1-常规，2-优先，3-紧急 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].LinkType` | `int` | 跳转类型；0-无，1-app内部，2-外链 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoticeList[].LinkUrl` | `string` | 跳转地址；link_type为1需要定义标识，为2则是跳转地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`sm.SoftVersion==1 \|\| sm.CreateDate.AddDays(3)>DateTime.Now`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`BannerProvider.BannerGetList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_areas`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 获取省市数据 |
| 使用时机 | 在商家视角中核对“获取省市数据”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetAreas` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetAreasResponseViewModel>>`；包装 `Task/DataResult`；Data `GetAreasResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:70` |
| C/B 对照 | capi.base.get_areas |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `RootId` | `string` | 可选/有默认值 | 绑定=ApiController推断；默认值="0" | 必须来自同一会话上游 API 结果或服务端对象引用 | 父类id 取省传0；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.AreasData` | `List<AreasData>` | 地区实体 | 普通业务字段 | 可按问题需要提供 |
| `Data.AreasData[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.AreasData[].ClassId` | `string` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ProvincesProvider.ProvincesGetByRootId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_bank`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 获取银行数据 |
| 使用时机 | 在商家视角中核对“获取银行数据”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetBank` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetBankResponseModel>>`；包装 `Task/DataResult`；Data `GetBankResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:97` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| — | — | 无 Action 业务参数 | — | — | — |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.BankData` | `List<BankData>` | 地区实体 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankData[].BankName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankData[].BankCode` | `string` | code | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`BankProvider.GetBank`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_bank_branch`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 获取银行分行数据 |
| 使用时机 | 在商家视角中核对“获取银行分行数据”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetBankBranch` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetBankBranchResponseModel>>`；包装 `Task/DataResult`；Data `GetBankBranchResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:124` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `BankCode` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.BankCode) \\|\\| string.IsNullOrEmpty(model.ProvinceId) \\|\\| string.IsNullOrEmpty(model.CityId)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 银行编码；普通业务字段；可按问题需要提供 |
| `ProvinceId` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.BankCode) \\|\\| string.IsNullOrEmpty(model.ProvinceId) \\|\\| string.IsNullOrEmpty(model.CityId)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 省Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CityId` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.BankCode) \\|\\| string.IsNullOrEmpty(model.ProvinceId) \\|\\| string.IsNullOrEmpty(model.CityId)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 城市 id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `DistrictId` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 区 ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.BankBranchData` | `List<BankBranchData>` | 支行数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankBranchData[].BankBranchName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankBranchData[].BankBranchCode` | `string` | 支行Code | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`string.IsNullOrEmpty(model.BankCode) \|\| string.IsNullOrEmpty(model.ProvinceId) \|\| string.IsNullOrEmpty(model.CityId)`；`resultMsg.State`
- 一层业务调用：`BankBranchProvider.GetBankBranchList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_bank_name`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 通过银行卡图片获取 总行名称 |
| 使用时机 | 在商家视角中核对“通过银行卡图片获取 总行名称”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetBankName` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetBankNameResponseModel>>`；包装 `Task/DataResult`；Data `GetBankNameResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:179` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 && model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 && model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BankCardImg` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.BankCardImg)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 银行图片；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankNameCode` | `string` | 总行名称Code | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNeedBankBranch` | `bool` | 是否需要调用支行 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 && model.StoreId == 0`；`string.IsNullOrEmpty(model.BankCardImg)`；`resultMsg.State`；`string.IsNullOrEmpty(data?.BankNameCode)`
- 一层业务调用：`BaseProvider.GetBankName`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 501`；`固定提示：参数不正确`；`固定提示：未识别成功`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_default_store_agreement`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 获取默认合同协议 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetDefaultStoreAgreement` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CommonResponseViewModel>>`；包装 `Task/DataResult`；Data `CommonResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:375` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0`；`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_store_class`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 获取店铺分类 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetStoreClass` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreClassResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreClassResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:42` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `RootId` | `string` | 可选/有默认值 | 绑定=ApiController推断；默认值="0" | 必须来自同一会话上游 API 结果或服务端对象引用 | 父类id 一级分类传0；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.StoreClassData` | `List<StoreClassData>` | 分类实体 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreClassData[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreClassData[].ClassId` | `string` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`StoreClassProvider.GetStoreClassByRootId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_store_source`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 获取信息来源 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetStoreSource` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreSourceResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreSourceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:259` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `RootId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 父类id 一级分类传0；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.StoreSourceData` | `List<StoreSourceData>` | 来源实体 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreSourceData[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreSourceData[].SourceId` | `string` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.base.get_week_data`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Base 模块 |
| 用途 | 获取星期数据 |
| 使用时机 | 在商家视角中核对“获取星期数据”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Base/GetWeekData` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetWeekDataResponseViewModel>>`；包装 `DataResult`；Data `List<GetWeekDataResponseViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/BaseController.cs:220` |
| C/B 对照 | capi.base.get_week_data |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].WeekInfo` | `string` | 星期内容 | 普通业务字段 | 可按问题需要提供 |
| `Data[].WeekStart` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].WeekEnd` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0`；`resultMsg.State`
- 一层业务调用：`UsersInfoProvider.UsersInfoGetByUid`、`DateTimeHelper.GetGroupWeekByDateRange`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.commission.get_admin_cash_out_logs`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Commission 模块 |
| 用途 | 审核人员获取佣金提现申请记录 |
| 使用时机 | 在商家视角中核对“审核人员获取佣金提现申请记录”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Commission/GetAdminCashOutLogs` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataList<CommissionCashoutLogResponseModel>>`；包装 `Task/DataList`；Data `CommissionCashoutLogResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommissionController.cs:131` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Uid` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].Receiver` | `string` | 收款人 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].ReceiveAccount` | `string` | 收款账号 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReceiveChannel` | `string` | 收款渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CashoutPrice` | `decimal` | 申请提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SparePrice` | `decimal` | 剩余可提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CreateDate` | `DateTime` | 申请日期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ApplyUser` | `string` | 申请人 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`CommissionCashoutLogProvider.GetAdminCommissionCashoutLogs`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.commission.get_commission_rank`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Commission 模块 |
| 用途 | 佣金总榜 |
| 使用时机 | 在商家视角中核对“佣金总榜”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Commission/GetCommissionRank` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<GetCommissionRankResponseModel>>>`；包装 `Task/DataResult`；Data `List<GetCommissionRankResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommissionController.cs:69` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Index` | `int` | 序号 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UserId` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].UserName` | `string` | 用户名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].HeadImg` | `string` | 头像 | 普通业务字段 | 可按问题需要提供 |
| `Data[].TotalCommission` | `decimal` | 总佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ConsumptionCommissionLogProvider.GetCommissionRank`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.commission.get_consumption_rank`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Commission 模块 |
| 用途 | 消费总榜 |
| 使用时机 | 商家核对会员消费、充值、核减、返还、退款前置事实或操作后余额时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Commission/GetConsumptionRank` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<GetCommissionRankResponseModel>>>`；包装 `Task/DataResult`；Data `List<GetCommissionRankResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommissionController.cs:85` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Index` | `int` | 序号 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UserId` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].UserName` | `string` | 用户名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].HeadImg` | `string` | 头像 | 普通业务字段 | 可按问题需要提供 |
| `Data[].TotalCommission` | `decimal` | 总佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ConsumptionCommissionLogProvider.GetConsumptionRank`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.commission.get_user_commission_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Commission 模块 |
| 用途 | 获取用户佣金信息 |
| 使用时机 | 在商家视角中核对“获取用户佣金信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Commission/GetUserCommissionInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetUserCommissionInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetUserCommissionInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommissionController.cs:43` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0 \\|\\| request.PromoteUserId <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0 \\|\\| request.PromoteUserId <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PromoteUserId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(request.StoreId <= 0 \\|\\| request.Uid <= 0 \\|\\| request.PromoteUserId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 推广人ID、C端可不填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Uid` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreId` | `int` | 商家ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Commission` | `decimal` | 可提现佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionCommission` | `decimal` | 已提现佣金 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId <= 0 \|\| request.Uid <= 0 \|\| request.PromoteUserId <= 0`
- 一层业务调用：`UserCardProvider.GetUserCardByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数有误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.commission.get_user_commission_log_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Commission 模块 |
| 用途 | 获取用户佣金消费明细 |
| 使用时机 | 在商家视角中核对“获取用户佣金消费明细”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Commission/GetUserCommissionLogList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataList<GetUserCommissionLogListRespnseModel>>`；包装 `Task/DataList`；Data `GetUserCommissionLogListRespnseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommissionController.cs:22` |
| C/B 对照 | capi.commission.get_user_commission_log_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0 \\|\\| request.PromoteUserId <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0 \\|\\| request.PromoteUserId <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Type` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 类型：0全部 ，1收入，2支出；普通业务字段；可按问题需要提供 |
| `PromoteUserId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(request.StoreId <= 0 \\|\\| request.Uid <= 0 \\|\\| request.PromoteUserId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 推广人ID、C端可不填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=1 | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=20 | AI 可在服务端上限内选择 | 分页大小；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].ConsumptionId` | `string` | 财务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Source` | `int` | 消费类型 | 普通业务字段 | 可按问题需要提供 |
| `Data[].StrSource` | `string` | 消费类型 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ConsumptionTag` | `int` | 消费类型 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ConsumptionValue` | `decimal` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PayWay` | `int` | 支付方式 | 普通业务字段 | 可按问题需要提供 |
| `Data[].AfterValue` | `decimal` | 支付后剩余佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CreateDate` | `DateTime` | 时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId <= 0 \|\| request.Uid <= 0 \|\| request.PromoteUserId <= 0`
- 一层业务调用：`ConsumptionCommissionLogProvider.GetUserCommissionLogList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数有误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.common.check_new_version`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Common 模块 |
| 用途 | 检查版本更新 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Common/CheckNewVersion` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CheckNewVersionResponseModel>>`；包装 `Task/DataResult`；Data `CheckNewVersionResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommonController.cs:819` |
| C/B 对照 | capi.common.check_new_version |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(requestModel.Uid == 0 \\|\\| string.IsNullOrEmpty(requestModel.LocalVersion) \\|\\| requestModel.SystemType == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LocalVersion` | `String` | 代码校验必填 | 绑定=ApiController推断；if(requestModel.Uid == 0 \\|\\| string.IsNullOrEmpty(requestModel.LocalVersion) \\|\\| requestModel.SystemType == 0) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 设备本地版本号；普通业务字段；可按问题需要提供 |
| `SystemType` | `int` | 代码校验必填 | 绑定=ApiController推断；if(requestModel.Uid == 0 \\|\\| string.IsNullOrEmpty(requestModel.LocalVersion) \\|\\| requestModel.SystemType == 0) | AI 只能使用文档确认的枚举值 | 系统类型：1 Android，2 IOS；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.VersionNo` | `string` | 版本编号 | 普通业务字段 | 可按问题需要提供 |
| `Data.VersionName` | `string` | version_name 版本名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.VersionInfo` | `string` | 版本说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.DownUrl` | `string` | 下载地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMust` | `int` | is_must 是否强制更新 0 否 1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNewVersion` | `bool` | 是否有新版本 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`requestModel.Uid == 0 \|\| string.IsNullOrEmpty(requestModel.LocalVersion) \|\| requestModel.SystemType == 0`；`resultMsg.State`；`vm != null && vm.Id > 0`；`v1 < v2 && vm.IsBeat == 0`
- 一层业务调用：`VersionLogProvider.GetVersionLogByPlatform`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.common.check_state`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Common 模块 |
| 用途 | 校验用户状态(Y) |
| 使用时机 | 在商家视角中核对“校验用户状态(Y)”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Common/CheckState` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CheckStateResponseModel>>`；包装 `Task/DataResult`；Data `CheckStateResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommonController.cs:766` |
| C/B 对照 | capi.common.check_state |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(requestModel.Uid == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LocalVersion` | `String` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(requestModel.LocalVersion)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 设备本地版本号；普通业务字段；可按问题需要提供 |
| `SystemType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 系统类型：1 Android，2 IOS；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.NewMessageCount` | `int` | 新的系统消息数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNewSettingMessage` | `bool` | 是否有新的系统设置消息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`requestModel.Uid == 0`；`resultMsg.State`；`urm != null && urm.Id > 0`；`string.IsNullOrEmpty(requestModel.LocalVersion)`；`vm.Id > 0 && v1 < v2 && vm.IsBeat == 0`
- 一层业务调用：`UserRemindProvider.UserRemindGetByUid`、`VersionLogProvider.GetVersionLogByPlatform`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.common.get_oss_conf`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Common 模块 |
| 用途 | 获取OSS配置信息 |
| 使用时机 | 在商家视角中核对“获取OSS配置信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Common/GetOssConf` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetOssConfResponseViewModel>>`；包装 `Task/DataResult`；Data `GetOssConfResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommonController.cs:883` |
| C/B 对照 | capi.common.get_oss_conf |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UseType` | `int` | 模型特性声明必填 | 绑定=ApiController推断；[Required] | AI 只能使用文档确认的枚举值 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `FileType` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.FileType == 0) | AI 只能使用文档确认的枚举值 | 文件类型 1图片 2视频 3文档；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UploadFileDirectory` | `string` | 文件上传目录 | 普通业务字段 | 可按问题需要提供 |
| `Data.OssConf` | `StsTockenV2Model` | OSS配置 | 普通业务字段 | 可按问题需要提供 |
| `Data.OssConf.UploadUrl` | `String` | 上传地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.OssConf.SignatureVersion` | `String` | 指定签名的版本和算法 | 普通业务字段 | 可按问题需要提供 |
| `Data.OssConf.Credential` | `String` | 指明派生密钥的参数集 | 密钥/凭据 | 禁止原样进入模型 |
| `Data.OssConf.RequestDate` | `String` | 请求时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.OssConf.Signature` | `String` | 签名信息 | 密钥/凭据 | 禁止原样进入模型 |
| `Data.OssConf.SecurityToken` | `String` | 安全令牌 | 密钥/凭据 | 禁止原样进入模型 |
| `Data.OssConf.Policy` | `String` | policy 表单域 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0`；`resultMsg.State`；`model.FileType == 0`；`model.FileType == 1`
- 一层业务调用：`STSHelper.GetStsTokenV2`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.common.get_promotion_url`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Common 模块 |
| 用途 | 获取推广链接 |
| 使用时机 | 在商家视角中核对“获取推广链接”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Common/GetPromotionUrl` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `A` — 无请求参数且静态调用链未发现明显写入，仍需部署前只读验收。 |
| 返回 | `Task<DataResult<CommonResponseViewModel>>`；包装 `Task/DataResult`；Data `CommonResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CommonController.cs:1059` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| — | — | 无 Action 业务参数 | — | — | — |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.content.get_index_banner`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Content 模块 |
| 用途 | 获取首页Banner |
| 使用时机 | 在商家视角中核对“获取首页Banner”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Content/GetIndexBanner` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<IndexBananerResponseModel>>`；包装 `Task/DataResult`；Data `IndexBananerResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ContentController.cs:23` |
| C/B 对照 | capi.content.get_index_banner |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Location` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 位置 0首页头部 1首页下部 2启动页；普通业务字段；可按问题需要提供 |
| `Count` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 数量(可不填)；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banners` | `List<IndexBannerInfoResponseModel>` | 图片集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banners[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Banners[].ImgSrc` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banners[].Link` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banners[].ActionType` | `int` | 动作类型 0-Html 1-Acitivity | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`IndexBannerProvider.GetIndexBanner`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.costly_activity.get_costly_activity`

| 项目 | 内容 |
| --- | --- |
| 业务域 | CostlyActivity 模块 |
| 用途 | 进入营销活动 |
| 使用时机 | 在商家视角中核对“进入营销活动”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CostlyActivity/GetCostlyActivity` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCostlyActivityResponseModel>>`；包装 `Task/DataResult`；Data `GetCostlyActivityResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CostlyActivityController.cs:151` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 营销活动列表id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Img` | `List<string>` | 图片集合 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Id < 0`；`resultMsg.State`
- 一层业务调用：`CostlyActivityProvider.GetCostlyActivity`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请传入指定参数`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.costly_activity.get_costly_activity_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | CostlyActivity 模块 |
| 用途 | 获取营销活动列表 |
| 使用时机 | 在商家视角中核对“获取营销活动列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CostlyActivity/GetCostlyActivityList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetCostlyActivityListResponseModel>>`；包装 `DataResult`；Data `List<GetCostlyActivityListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CostlyActivityController.cs:133` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Name` | `string` | 活动名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].NutGold` | `int` | 坚果币 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Describe` | `string` | 活动描述 | 普通业务字段 | 可按问题需要提供 |
| `Data[].RightTopTag` | `List<string>` | 活动右上角标签 | 普通业务字段 | 可按问题需要提供 |
| `Data[].BottomTag` | `List<string>` | 下方标签 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`CostlyActivityProvider.GetCostlyActivityList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.costly_activity.get_now_costly_activity_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | CostlyActivity 模块 |
| 用途 | 获取当前活动列表 |
| 使用时机 | 在商家视角中核对“获取当前活动列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CostlyActivity/GetNowCostlyActivityList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetNowCostlyActivityListResponseModel>>`；包装 `Task/DataResult`；Data `GetNowCostlyActivityListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CostlyActivityController.cs:97` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Num` | `int` | 活动序号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Name` | `string` | 活动名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Describe` | `string` | 活动描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 活动状态 2-执行中，3-待开始，4-暂停，5已完成 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.costly_activity.get_nut_gold_information`

| 项目 | 内容 |
| --- | --- |
| 业务域 | CostlyActivity 模块 |
| 用途 | 获取坚果币余额信息 |
| 使用时机 | 在商家视角中核对“获取坚果币余额信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CostlyActivity/GetNutGoldInformation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetNutGoldInformationResponseModel>>`；包装 `Task/DataResult`；Data `GetNutGoldInformationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CostlyActivityController.cs:22` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.NutGold` | `int` | 坚果币 | 普通业务字段 | 可按问题需要提供 |
| `Data.Img` | `string` | 店铺图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Name` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.VipCardPosterGold` | `int` | 会员宣传海报价格 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`；`m != null`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`StoreProvider.GetStoreNutGold`、`CostlyActivityProvider.GetVipCardPosterGold`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 500`；`固定提示：店铺id不存在`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.costly_activity.get_vip_card_poster_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | CostlyActivity 模块 |
| 用途 | 获取会员卡海报列表 |
| 使用时机 | 在商家视角中核对“获取会员卡海报列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CostlyActivity/GetVipCardPosterList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetVipCardPosterListResponseModel>>`；包装 `Task/DataResult`；Data `GetVipCardPosterListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CostlyActivityController.cs:114` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Img` | `List<string>` | 图片集合 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`CostlyActivityProvider.GetVipCardPosterList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.custom_controls.business_get_custom_controls`

| 项目 | 内容 |
| --- | --- |
| 业务域 | CustomControls 模块 |
| 用途 | 商家获取自定义会员控件信息(会员资料设置页) |
| 使用时机 | 在商家视角中核对“商家获取自定义会员控件信息(会员资料设置页)”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CustomControls/BusinessGetCustomControls` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：CustomControlsProvider.AddCustomControls, StoreSetingProvider.StoreSetingByUpdateSet |
| 返回 | `Task<DataResult<BusinessGetCustomControlsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCustomControlsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CustomControlsController.cs:31` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls` | `List<CustomControlsViewModel>` | 系统公共控件 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].StoreId` | `int` | 店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].IsMust` | `int` | is_mus 是否必填 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].IsCustom` | `bool` | 是否可以删除修改 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items` | `List<CustomControlsItemViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].Items[].Cid` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls` | `List<StoreControlsViewModel>` | 店铺自定义控件 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreControls[].Cid` | `int` | 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreControls[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].IsMust` | `int` | 是否必填 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`cms.All(x => x.Id != smodel.Cid)`；`cm?.Id > 0`；`eachList?.Id > 0`；`cs != null && cs.IsPrivate`；`storecs == null`；`rcmsModel != null`
- 一层业务调用：`CustomControlsProvider.BusinessGetCustomControlsList`、`StoreControlsProvider.BusinessGetStoreControlsList`、`CustomControlsProvider.BusinessGetCustomControlsById`、`CustomControlsProvider.BusinessGetCustomControlsByStoreId`、`CustomControlsProvider.AddCustomControls`、`CustomControlsItemProvider.CustomControlsItemGetByCid`、`StoreSetingProvider.StoreSetingByUpdateSet`
- 疑似副作用：`CustomControlsProvider.AddCustomControls`、`StoreSetingProvider.StoreSetingByUpdateSet`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.custom_controls.business_get_store_controls`

| 项目 | 内容 |
| --- | --- |
| 业务域 | CustomControls 模块 |
| 用途 | 获取店铺用户自定义控件（会员编辑资料页） |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CustomControls/BusinessGetStoreControls` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreControlsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreControlsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CustomControlsController.cs:420` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Controls` | `List<StoreUserControlsViewModel>` | 用户信息集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].IsMust` | `int` | 是否必填 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlsValidations` | `List<CustomControlsValidationViewModel>` | 控件验证规则 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlsValidations[].ValidationFormula` | `string` | 验证公式 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlsValidations[].ValidationInstructions` | `string` | 不通过提示信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items` | `List<CustomControlsItemViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Controls[].Items[].Cid` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Controls[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreControlsProvider.BusinessGetStoreControls`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.distribution.get_store_distribution_setting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Distribution 模块 |
| 用途 | 获取分销设置 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Distribution/GetStoreDistributionSetting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetDistributionSettingResponseModel>>`；包装 `Task/DataResult`；Data `GetDistributionSettingResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/DistributionController.cs:20` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsDistribution` | `bool` | 是否开通了分销 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExtend` | `bool` | 是否开通了推广 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPromotionCodeVisable` | `bool` | 推广码是否可见 0 不可见 ； 1 可见 ; | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPromotBoxVisable` | `bool` | 分佣弹框是否提示 true 提示 false 不提示 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommissionValidDate` | `int` | 分佣有效天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromotionType` | `int` | 推广类型 1 全员推广 ； 2指定推广； | 普通业务字段 | 可按问题需要提供 |
| `Data.ExtendContent` | `string` | 推广说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsBuyCommission` | `bool` | 购卡分佣开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsConsumeCommission` | `bool` | 消费分佣开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommissionStartMoney` | `decimal` | 分佣起始金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCommission` | `bool` | 佣金开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCommissionConsume` | `bool` | 佣金消费开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCommissionCash` | `bool` | 佣金提现开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommissionType` | `int` | 分佣类型 0单次分佣 ； 1 永久分佣 ； 2限时分佣 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsForeverCommission` | `bool` | 永久分佣开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommissionMinCashMoney` | `decimal` | 佣金提现最低金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommissionCashCount` | `int` | 佣金每天提现次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.BuyCommissionRate` | `decimal` | 购卡分佣比例 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExtendCommissionRate` | `decimal` | 推广分佣比例 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumeCommissionRate` | `decimal` | 消费分佣比例 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterCount` | `int` | 指定推广员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCommissionLook` | `bool` | 佣金是否可以查看 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`DistributionSettingProvider.GetStoreDistributionSetting`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.distribution.get_store_promoter_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Distribution 模块 |
| 用途 | 获取店铺推广员列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Distribution/GetStorePromoterList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStorePromoterResponseModel>>`；包装 `Task/DataResult`；Data `GetStorePromoterResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/DistributionController.cs:120` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PromoterCount` | `int` | 推官员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList` | `List<StorePromoterGroupModel>` | 推官员分组列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].NameGroup` | `string` | 分组 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].PromoterList` | `List<StorePromoterVipViewModel>` | 推官员列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].PromoterList[].Uid` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PromoterGroupList[].PromoterList[].NameGroup` | `string` | 会员分组 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].PromoterList[].UserName` | `string` | 会员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PromoterGroupList[].PromoterList[].CardPrice` | `decimal` | 卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].PromoterList[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PromoterGroupList[].PromoterList[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].PromoterList[].ValidityDate` | `DateTime?` | 会员截至日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].PromoterList[].IsPromoter` | `bool` | 是否是店铺推广员 | 普通业务字段 | 可按问题需要提供 |
| `Data.PromoterGroupList[].PromoterList[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PromoterGroupList[].PromoterList[].CardId` | `int` | 卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`PromoterProvider.GetStorePromoterList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.distribution.get_store_vip_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Distribution 模块 |
| 用途 | 获取店铺会员列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Distribution/GetStoreVipList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<StorePromoterGroupModel>>`；包装 `DataResult`；Data `List<StorePromoterGroupModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/DistributionController.cs:134` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键词；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].NameGroup` | `string` | 分组 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoterList` | `List<StorePromoterVipViewModel>` | 推官员列表 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoterList[].Uid` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].PromoterList[].NameGroup` | `string` | 会员分组 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoterList[].UserName` | `string` | 会员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].PromoterList[].CardPrice` | `decimal` | 卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoterList[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].PromoterList[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoterList[].ValidityDate` | `DateTime?` | 会员截至日期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoterList[].IsPromoter` | `bool` | 是否是店铺推广员 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoterList[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].PromoterList[].CardId` | `int` | 卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`!string.IsNullOrEmpty(request.KeyWord)`
- 一层业务调用：`PromoterProvider.GetStoreVipList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lottery.business_get_lottery_by_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Lottery 模块 |
| 用途 | 获取活动详情 |
| 使用时机 | 在商家视角中核对“获取活动详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lottery/BusinessGetLotteryById` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetLotteryByIdResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetLotteryByIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LotteryController.cs:159` |
| C/B 对照 | capi.lottery.get_lottery_by_id |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.LotteryId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.LotteryId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LotteryId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.LotteryId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 抽奖ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 活动id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.BgImg` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Title` | `string` | 抽奖活动名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LotteryType` | `int` | 抽奖类型 0进店，1积分，2消费后，3满额 | 普通业务字段 | 可按问题需要提供 |
| `Data.LotteryFrequencyType` | `int` | 抽奖频率限制 0每天 1累计（进店，积分） | 普通业务字段 | 可按问题需要提供 |
| `Data.LotteryFrequencyQuantity` | `int` | 抽奖频率数量 必须大于0（进店，积分） | 普通业务字段 | 可按问题需要提供 |
| `Data.ShareReward` | `int` | 转发奖励次数）(进店) | 普通业务字段 | 可按问题需要提供 |
| `Data.MoneySet` | `string` | 现金消费多少元抽一次（满额） | 普通业务字段 | 可按问题需要提供 |
| `Data.CardValueSet` | `string` | 卡消费多少元抽一次（满额） | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAddUp` | `bool` | 是否累计次数（满额） | 普通业务字段 | 可按问题需要提供 |
| `Data.Integral` | `int` | 积分多少抽一次奖(积分) | 普通业务字段 | 可按问题需要提供 |
| `Data.WinSet` | `int` | 中奖次数设置，0每次都有机会中奖，1有效期仅有一次中奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.LotteryRules` | `string` | 活动规则 | 普通业务字段 | 可按问题需要提供 |
| `Data.BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items` | `List<LotteryItemViewModel>` | 奖项 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].LotteryId` | `int` | 抽奖id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].ItemIndex` | `int` | 抽奖项位置1-8 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].IsReward` | `bool` | is_reward 是否包含奖励 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].WinProbability` | `decimal` | 中奖几率（0.3=30%） | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].RewardTitle` | `string` | 奖品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].RewardLevel` | `int` | 奖品级别 0特等奖，1-3等奖 4普通奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].RewardType` | `int` | 奖励类型0优惠券，1积分，2红包奖励，3实物奖励 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].RewardValue` | `decimal` | 奖励值（积分，红包） | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].RewardCount` | `int` | 奖励数量-1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].RewardWinCount` | `int` | 中奖数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons` | `List<CouponCenterCouponViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Items[].Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponCount` | `int` | 已领取数量 ***** 在创建，修改 接口中 该字段表示 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].CouponSum` | `int` | 活动优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].Coupons[].ItemId` | `int` | 活动项目ID（预留） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.LotteryId < 1`；`resultMsg.State`；`data.Items?.Count > 0`
- 一层业务调用：`LotteryProvider.LotteryGetById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lottery.business_get_lottery_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Lottery 模块 |
| 用途 | 获取抽奖活动列表 |
| 使用时机 | 在商家视角中核对“获取抽奖活动列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lottery/BusinessGetLotteryList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<BusinessGetLotteryListResponseModel>>`；包装 `DataResult`；Data `PageData<BusinessGetLotteryListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LotteryController.cs:128` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `LotteryType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 抽奖类型 0进店，1积分，2消费后，3满额；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetLotteryListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].BgImg` | `string` | bg_img 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Title` | `string` | title 抽奖活动名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LotteryType` | `int` | lottery_type 抽奖类型 0进店，1积分，2消费后，3满额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LotteryCount` | `int` | 已抽奖次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MoneySet` | `string` | 现金消费多少元抽一次（满额） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardValueSet` | `string` | 卡消费多少元抽一次（满额） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsAddUp` | `bool` | 是否累计次数（满额） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 状态 0暂停 1正常 2未开始 3结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].EndReason` | `int` | 结束原因 0默认 1到期 2优惠券发放完毕 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`LotteryProvider.BusinessGetLotteryList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lottery.business_get_lottery_win_user_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Lottery 模块 |
| 用途 | 中奖用户列表 |
| 使用时机 | 在商家视角中核对“中奖用户列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lottery/BusinessGetLotteryWinUserList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<BusinessGetLotteryWinUserListResponseModel>>`；包装 `DataResult`；Data `PageData<BusinessGetLotteryWinUserListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LotteryController.cs:363` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.LotteryId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.LotteryId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `LotteryId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.LotteryId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 抽奖活动ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetLotteryWinUserListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Uid` | `int` | 顾客id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LotteryItemId` | `int` | 中奖项 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 新客姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CreateDate` | `string` | 领取时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponId` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CouponState` | `int` | 优惠券状态 1未使用 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.LotteryId < 1`；`resultMsg.State`
- 一层业务调用：`LotteryProvider.BusinessGetLotteryWinUserList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.marketing_center.business_marketing_activity_get_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | MarketingCenter 模块 |
| 用途 | 通过marketingId 获取营销活动内容详细 |
| 使用时机 | 在商家视角中核对“通过marketingId 获取营销活动内容详细”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MarketingCenter/BusinessMarketingActivityGetInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessMarketingActivityGetInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessMarketingActivityGetInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MarketingCenterController.cs:427` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.MarketingId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.MarketingId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `MarketingId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.MarketingId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 营销ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing` | `MarketingActivityViewModel` | 营销活动详细内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.MarketingId` | `int` | 营销活动ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketing.MarketingCenterType` | `int` | 营销活动类型 (1开卡送, 不用),2续费送,3生日送,4满送，5群发,6营销活动，7节日祝福 8放假 9 老带新 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.WhereDesc` | `string` | 营销筛选会员条件的描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.State` | `int` | 营销发送短信的发送状态 状态 -5删除， -4取消发送 -3审核失败 -2 发送失败 - 1未审核，0未发送 1发送成功 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.StateReason` | `string` | 营销发送短信的失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.SmsSign` | `string` | 短信签名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.SmsInfo` | `string` | 营销短信内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.TemplateContent` | `string` | 短信模板内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.SendTime` | `string` | 发送时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.MarketingState` | `int` | 1正常 2营销活动已经结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.AllianceDay` | `int` | 联盟券，多少天未在本店消费 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.SendStartDate` | `string` | 支付宝商家券发券时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.SendEndDate` | `string` | 支付宝商家券发券时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.SendCount` | `int` | 已发送数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.MaxSendCount` | `int` | 最大发送数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.CouponCenterName` | `string` | 活动名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.CouponTotalCount` | `int` | 券包优惠券总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons` | `List<MarketingActivityCouponViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketing.Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketing.Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketing.Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketing.Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Marketing.Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketing.Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketing.Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponSum` | `int` | 活动优惠券设置数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponCount` | `int` | 领取数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponUsedCount` | `int` | 优惠券使用数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].BrowseCount` | `int` | 浏览人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketing.Coupons[].CouponTotalCount` | `int` | 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo` | `StoreSmsSendInfoViewModel` | 短信任务发送详细内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.TaskId` | `int` | 任务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TaskInfo.CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.SendTime` | `string` | 发送时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.TemplateId` | `int` | 模板ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TaskInfo.IsSetSendTime` | `bool` | 是否设置发送时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.SetSendTime` | `string` | 设置发送时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.TemplateContent` | `string` | 短信内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.SmsInfo` | `string` | 短信内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.SmsSign` | `string` | 短信签名 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.SuccessCount` | `int` | 成功数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.FailCount` | `int` | 失败数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.State` | `int` | 短信任务状态 -4 取消发送 -3审核失败 -2 发送失败 -1 等待短信审核，0未发送 发送完成 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.StateReason` | `string` | 失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.WhereDescs` | `List<string>` | 条件描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere` | `MarketingUserWhereViewModel` | 筛选会员条件 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.Id` | `int` | 营销筛选会员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TaskInfo.UserWhere.BalanceValueMin` | `decimal` | 余额最小值 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.BalanceValueMax` | `decimal` | 余额最大值 -1不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.ValidityValue` | `int` | 所有会员卡有效期剩余天数 -1 表示不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.LastDays` | `int` | 会员在多少天之内未消费 -1不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.QuantityMoney` | `int` | 查询是否有余额 -1 不限制 0有余额会员，1无余额会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.WhereServices` | `List<MarketingUserWhereServicesViewModel>` | 服务条件列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.WhereServices[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TaskInfo.UserWhere.WhereServices[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.WhereServices[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.WhereServices[].ItemValueMin` | `int` | 项目最小值 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.WhereServices[].ItemValueMax` | `int` | 项目最大值 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.GroupIds` | `List<int>` | 分组ID | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.CardGroupIds` | `List<int>` | 会员卡分组id | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.Validity` | `string` | 有效期0 all 1有效 2过期 -1-有效，-2-过期，>0-几天内过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.Sex` | `int` | 性别0 all 1男 2女 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.HaveMony` | `int` | 余额0 all 1有 2无 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.ConsumptionDateStart` | `string` | 消费日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.ConsumptionDateEnd` | `string` | 消费日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.OpenCardDateStart` | `string` | 开卡日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.OpenCardDateEnd` | `string` | 开卡日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.ConsumptionMonyMin` | `decimal` | 消费金额开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.ConsumptionMonyMax` | `decimal` | 消费金额结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.ConsumptionSumMin` | `decimal` | 消费次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.ConsumptionSumMax` | `decimal` | 消费次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.CardSumMin` | `decimal` | 剩余次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.CardSumMax` | `decimal` | 剩余次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.PrepaidPrice` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.CardGroupName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserWhere.GroupName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserMobiles` | `List<UserCardMobileViewModel>` | 会员集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaskInfo.UserMobiles[].Uid` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TaskInfo.UserMobiles[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TaskInfo.UserMobiles[].Mobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.TaskInfo.StoreCustomerMobiles` | `List<string>` | 店铺顾客手机号集合 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.MarketingId < 1`；`resultMsg.State`
- 一层业务调用：`CouponCenterProvider.MarketingActivityGetInfo`、`StoreSmsSendTaskProvider.GetTaskInfoByMarketingId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.marketing_center.business_marketing_birthday_get_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | MarketingCenter 模块 |
| 用途 | 获取生日营销详细 |
| 使用时机 | 在商家视角中核对“获取生日营销详细”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MarketingCenter/BusinessMarketingBirthdayGetInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessMarketingBirthdayGetInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessMarketingBirthdayGetInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MarketingCenterController.cs:626` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsInfo` | `string` | 短信内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateId` | `int` | 模板ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.State` | `int` | 生日营销的开启状态 1开启，0暂停 | 普通业务字段 | 可按问题需要提供 |
| `Data.MarketingId` | `int` | 生日营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SmsState` | `int` | 短信任务的状态 -4 取消发送 -3审核失败 -2 发送失败 - 1未审核，0未发送 1发送成功 2发送中 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsStateReason` | `string` | 短信模板的审核不通过原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons` | `List<MarketingCenterCouponViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponCount` | `int` | 已领取数量 ***** 在创建，修改 接口中 该字段表示 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSum` | `int` | 活动优惠券数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`CouponCenterProvider.MarketingBirthdayGetInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.marketing_center.business_marketing_get_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | MarketingCenter 模块 |
| 用途 | 营销列表查询 |
| 使用时机 | 在商家视角中核对“营销列表查询”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MarketingCenter/BusinessMarketingGetList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessMarketingGetListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessMarketingGetListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MarketingCenterController.cs:393` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `MarketingCenterType` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.MarketingCenterType < 1) | AI 只能使用文档确认的枚举值 | 营销活动类型 券类型 (1开卡送, 不用),2续费送,3生日送,4满送，5群发,6营销活动，7节日祝福8放假 25券包；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings` | `List<MarketingActivityViewModel>` | 营销活动列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].MarketingId` | `int` | 营销活动ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketings[].MarketingCenterType` | `int` | 营销活动类型 (1开卡送, 不用),2续费送,3生日送,4满送，5群发,6营销活动，7节日祝福 8放假 9 老带新 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].WhereDesc` | `string` | 营销筛选会员条件的描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].State` | `int` | 营销发送短信的发送状态 状态 -5删除， -4取消发送 -3审核失败 -2 发送失败 - 1未审核，0未发送 1发送成功 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].StateReason` | `string` | 营销发送短信的失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].SmsSign` | `string` | 短信签名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].SmsInfo` | `string` | 营销短信内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].TemplateContent` | `string` | 短信模板内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].SendTime` | `string` | 发送时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].MarketingState` | `int` | 1正常 2营销活动已经结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].AllianceDay` | `int` | 联盟券，多少天未在本店消费 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].SendStartDate` | `string` | 支付宝商家券发券时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].SendEndDate` | `string` | 支付宝商家券发券时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].SendCount` | `int` | 已发送数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].MaxSendCount` | `int` | 最大发送数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].CouponCenterName` | `string` | 活动名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].CouponTotalCount` | `int` | 券包优惠券总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons` | `List<MarketingActivityCouponViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketings[].Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketings[].Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketings[].Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketings[].Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Marketings[].Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketings[].Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Marketings[].Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponSum` | `int` | 活动优惠券设置数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponCount` | `int` | 领取数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponUsedCount` | `int` | 优惠券使用数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].BrowseCount` | `int` | 浏览人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Marketings[].Coupons[].CouponTotalCount` | `int` | 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`model.MarketingCenterType < 1`；`resultMsg.State`
- 一层业务调用：`CouponCenterProvider.MarketingGetList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`；`固定提示：参数错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.materiel.get_materiel_by_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Materiel 模块 |
| 用途 | 获取单个物料 |
| 使用时机 | 在商家视角中核对“获取单个物料”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Materiel/GetMaterielById` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetMaterielByIdResponseModel>>`；包装 `Task/DataResult`；Data `GetMaterielByIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MaterielController.cs:55` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PosterId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 海报Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PosterTitle` | `string` | 海报标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterImg` | `string` | poster_img 海报路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsVip` | `bool` | 是否vip | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterType` | `int` | poster_type 类型：0朋友圈海报，1物料（购卡送券）2抽奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterielType` | `int` | materiel_type 物料类型：0海报，1A4台牌 ，2易拉宝， | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterielMaterial` | `string` | materiel_material 物料材质 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterielSize` | `string` | materiel_size 规格大小 0X0 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | price 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterielUnit` | `string` | materiel_unit 单位：张 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTitle` | `bool` | is_title 是否包含title | 普通业务字段 | 可按问题需要提供 |
| `Data.CardSum` | `int` | card_sum 购卡送券，显示卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems` | `List<PosterItemViewModel>` | 海报配置内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].PosterId` | `int` | poster_id 海报ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PosterItems[].ItemType` | `int` | item_type 类型 0文字 1图片 2二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemCode` | `string` | item_code code | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemX` | `decimal` | item_x 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemY` | `decimal` | item_y 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemW` | `decimal` | item_w 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemH` | `decimal` | item_h 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].FontColor` | `string` | 字体颜色 PS #FFFFFF | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].FontName` | `string` | font_name 字体 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].FontSize` | `int` | font_size 字号 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].RowHeight` | `int` | row_heigh 行间距 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].LinePadding` | `int` | line_padding 字间距 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].IsBold` | `bool` | is_bold 是否加粗 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.PosterId < 1`；`resultMsg.State`；`post?.Id > 0`
- 一层业务调用：`PosterProvider.GeneratePoster`、`PosterItemProvider.GetPosterItemsListByPosterId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.materiel.get_materiel_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Materiel 模块 |
| 用途 | 获取物料列表 |
| 使用时机 | 在商家视角中核对“获取物料列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Materiel/GetMaterielList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<GetMaterielListResponseModel>>`；包装 `DataResult`；Data `PageData<GetMaterielListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MaterielController.cs:23` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.PosterType < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.PosterType < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PosterType` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.PosterType < 1) | AI 只能使用文档确认的枚举值 | 类型：-1全部 0朋友圈海报，1物料（购卡送券）2抽奖，3安心充物料；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetMaterielListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].PosterTitle` | `string` | 海报标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PosterImg` | `string` | 海报路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsHot` | `bool` | 是否热门 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MaterielType` | `int` | 物料类型：0海报，1A4台牌 ，2易拉宝， | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MaterielMaterial` | `string` | 物料材质 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MaterielSize` | `string` | 规格大小 0X0 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Price` | `decimal` | price 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MaterielUnit` | `string` | materiel_unit 单位：张 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.PosterType < 1`；`resultMsg.State`
- 一层业务调用：`PosterProvider.GetMaterielList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.materiel.get_materiel_trade_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Materiel 模块 |
| 用途 | 获取物料订单列表 |
| 使用时机 | 在商家视角中核对“获取物料订单列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Materiel/GetMaterielTradeList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<GetMaterielTradeListResponseModel>>`；包装 `DataResult`；Data `PageData<GetMaterielTradeListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MaterielController.cs:165` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PosterType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 类型：-1全部 ,0朋友圈海报，1物料（购卡送券）2抽奖；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetMaterielTradeListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 订单id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].State` | `int` | 订单状态：0未付款，1已付款未发货，2已发货，3交易成功,4取消 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PayDate` | `string` | 付款时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders` | `List<BusinessOrdersViewModel>` | 订单详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].Pid` | `int` | 商品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Orders[].PTitle` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PPic` | `string` | 商品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].OrderType` | `int` | order_type 订单类型 0物料 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PosterType` | `int` | poster_type 类型：朋友圈海报，1物料（购卡送券）2抽奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PosterCustomTitle` | `string` | poster_custom_title 定制title | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].Num` | `int` | 购买数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PayPrice` | `decimal` | pay_price 实付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PCardIds` | `List<int>` | 储值卡ID（购卡送券物料） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`BusinessTradeProvider.GetMaterielTradeList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.member_growth.get_follow_up_history`

| 项目 | 内容 |
| --- | --- |
| 业务域 | MemberGrowth 模块 |
| 用途 | 按时间倒序读取会员的完整跟进历史。 |
| 使用时机 | 在商家视角中核对“按时间倒序读取会员的完整跟进历史。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MemberGrowth/GetFollowUpHistory` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<MemberFollowUpHistoryResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<MemberFollowUpHistoryResponseModel>` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MemberGrowthController.cs:77` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.UserId < 0 \\|\\| model.PageIndex < 1 \\|\\| model.PageSize is < 1 or > 100) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.UserId < 0 \\|\\| model.PageIndex < 1 \\|\\| model.PageSize is < 1 or > 100) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.UserId < 0 \\|\\| model.PageIndex < 1 \\|\\| model.PageSize is < 1 or > 100) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.UserId < 0 \\|\\| model.PageIndex < 1 \\|\\| model.PageSize is < 1 or > 100) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡主键，用于读取完整且稳定的跟进历史。；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 当前绑定用户 ID，仅参与访问权限校验，不用于过滤历史。；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<MemberFollowUpHistoryResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 跟进记录主键。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Outcome` | `int` | 跟进结果，1 已联系，2 考虑中，3 未接通，4 明确拒绝。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 任务状态，0 待继续跟进，1 本次任务完成。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].NextFollowUpDate` | `DateTime?` | 本次记录约定的下次跟进时间。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | 本次跟进备注。 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CreateDate` | `DateTime` | 跟进记录创建时间。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OperatorStaffId` | `int` | 执行跟进的门店员工主键。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].OperatorStaffName` | `string` | 执行跟进的门店员工名称。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.CardId < 1 \|\| model.UserId < 0 \|\| model.PageIndex < 1 \|\| model.PageSize is < 1 or > 100`；`result.Data == null`
- 一层业务调用：`MemberGrowthWorkbenchProvider.GetFollowUpHistory`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 403`；`固定提示：参数格式错误！`；`固定提示：无权查看该会员的跟进记录，或该会员没有已完成的试听记录。`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.member_growth.get_workbench`

| 项目 | 内容 |
| --- | --- |
| 业务域 | MemberGrowth 模块 |
| 用途 | 读取指定日期范围内的增长汇总和体验会员分页列表。 |
| 使用时机 | 在商家视角中核对“读取指定日期范围内的增长汇总和体验会员分页列表。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MemberGrowth/GetWorkbench` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<MemberGrowthWorkbenchResponseModel>>`；包装 `Task/DataResult`；Data `MemberGrowthWorkbenchResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MemberGrowthController.cs:15` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PageIndex < 1 \\|\\| model.PageSize is < 1 or > 100 \\|\\| !DateTime.TryParse(model.BeginDate, out var beginDate) \\|\\| !DateTime.TryParse(model.EndDate, out var endDate) \\|\\| beginDate.Date > endDate.Date \\|\\| (endD…) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PageIndex < 1 \\|\\| model.PageSize is < 1 or > 100 \\|\\| !DateTime.TryParse(model.BeginDate, out var beginDate) \\|\\| !DateTime.TryParse(model.EndDate, out var endDate) \\|\\| beginDate.Date > endDate.Date \\|\\| (endD…) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PageIndex < 1 \\|\\| model.PageSize is < 1 or > 100 \\|\\| !DateTime.TryParse(model.BeginDate, out var beginDate) \\|\\| !DateTime.TryParse(model.EndDate, out var endDate) \\|\\| beginDate.Date > endDate.Date \\|\\| (endD…) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计开始日期，按门店本地日期解析并包含当天。；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计结束日期，按门店本地日期解析并包含当天。；普通业务字段；可按问题需要提供 |
| `Keyword` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 当前会话提供并临时使用；不得持久化到模型历史 | 会员姓名、备注名或手机号关键字。；个人信息；仅在当前授权场景按最小范围提供 |
| `Stage` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 0 全部，1 待跟进，2 已跟进，3 已转化，4 已逾期。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Summary` | `MemberGrowthSummaryResponseModel` | 与当前筛选条件相同口径的汇总指标。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.TrialCount` | `int` | 日期范围内已完成体验预约的去重会员卡数量。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.FollowedCount` | `int` | 已有至少一条跟进记录的体验会员数量。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.PendingCount` | `int` | 尚未购卡且需要继续跟进的体验会员数量。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.ConvertedCount` | `int` | 当前会员卡状态已转为有效的体验会员数量。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.OverdueCount` | `int` | 尚未购卡且最新待办时间已经逾期的会员数量。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Opportunities` | `PageData<MemberGrowthOpportunityResponseModel>` | 当前页的体验会员增长机会。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.PageIndex < 1 \|\| model.PageSize is < 1 or > 100 \|\| !DateTime.TryParse(model.BeginDate, out var beginDate) \|\| !DateTime.TryParse(model.EndDate, out var endDate) \|\| beginDate.Date > endDate.Date \|\| (endD…`
- 一层业务调用：`MemberGrowthWorkbenchProvider.GetWorkbench`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.merchant.business_certification_get_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Merchant 模块 |
| 用途 | 获取店铺认证申请详细信息 |
| 使用时机 | 在商家视角中核对“获取店铺认证申请详细信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Merchant/BusinessCertificationGetInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessCertificationGetInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessCertificationGetInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MerchantController.cs:318` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Uid` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AppId` | `string` | 小程序APPID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreSimpleName` | `string` | 商户简称 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerContactName` | `string` | 联系人 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerContactPhone` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ImgIdcardFornt` | `string` | 身份证正面 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgIdcardBack` | `string` | 身份证反面 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgBankCard` | `string` | 银行卡正面 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgCashierScene` | `string` | 前台照片/收银台照片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgPanoramic` | `string` | 店内全景照片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgShopPhoto` | `string` | 门头照 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgCorpCode` | `string` | 营业执照 *个体*企业 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgOpBankCode` | `string` | 开户许可证 企业必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.HeadBankCode` | `string` | 开户银行编码（企业） | 普通业务字段 | 可按问题需要提供 |
| `Data.BankCode` | `string` | 开户银行支行编码（企业） | 普通业务字段 | 可按问题需要提供 |
| `Data.CardNo` | `string` | 结算银行账号或者银行卡号（企业） | 普通业务字段 | 可按问题需要提供 |
| `Data.BankProvince` | `string` | 开户省（企业） | 普通业务字段 | 可按问题需要提供 |
| `Data.BankCity` | `string` | 开户市（企业） | 普通业务字段 | 可按问题需要提供 |
| `Data.BankDistrict` | `string` | 开户区 （企业） | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreType` | `int` | 商户类型 0个人 1个体 2企业 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreEmail` | `string` | 店铺邮箱 * | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.MerBankProvince` | `string` | 开户省 个人，个体必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerBankCity` | `string` | 开户市 个人，个体必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerBankDistrict` | `string` | 开户区 个人，个体必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerProvince` | `string` | 商户省 个人必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerCity` | `string` | 商户市 个人必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerDistrict` | `string` | 商户区 个人必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerAddress` | `string` | 商户所在地区 个人必填 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.AlipayAccount` | `string` | 支付宝账号 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerBankCode` | `string` | 个人开户总行编码 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerBankBranchCode` | `string` | 个人开户支行编码 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgStoreCorpCode` | `string` | 店内包含营业执照照片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgHandCorpCode` | `string` | 手持营业执照的门头照合影 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgBeneficiaryIdcardFornt` | `string` | 受益人身份证正面 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgBeneficiaryIdcardBack` | `string` | 受益人身份证反面 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgConfirmation` | `string` | 确认书 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPublicToPrivate` | `bool` | 是否是公对私 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOwner` | `bool` | 是否是法人收款 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsConfirm` | `bool` | 是否点击了已确认完成 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 审核状态 状态 0审核 1通过审核 -1 未填写 -2回退 -3未通过审核 10开通收款 | 普通业务字段 | 可按问题需要提供 |
| `Data.Note` | `string` | 审核不通过原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsUpGrade` | `bool` | 是否升级 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreFullName` | `string` | 公司名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerCardNo` | `string` | 个人个体银行卡号 | 普通业务字段 | 可按问题需要提供 |
| `Data.HeadBankName` | `string` | 公司银行名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankName` | `string` | 公司开户支行名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerBankName` | `string` | 个人个体银行名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.WechatMerNo` | `string` | 微信商户号 | 普通业务字段 | 可按问题需要提供 |
| `Data.AliPayMerNo` | `string` | 支付宝商户号 | 普通业务字段 | 可按问题需要提供 |
| `Data.WeChatMerIsCertify` | `bool` | 微信商户号是否认证 | 普通业务字段 | 可按问题需要提供 |
| `Data.AliPayMerIsCertify` | `bool` | 支付宝商户号是否认证 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 && model.Uid < 1`；`resMsg.State`
- 一层业务调用：`StoreYopConfigProvider.BusinessCertificationGetInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.merchant.business_certification_get_mer_state`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Merchant 模块 |
| 用途 | 获取签约状态 |
| 使用时机 | 在商家视角中核对“获取签约状态”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Merchant/BusinessCertificationGetMerState` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessCertificationGetMerStateResponseModel>>`；包装 `Task/DataResult`；Data `BusinessCertificationGetMerStateResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MerchantController.cs:433` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.WechatMerStateOne` | `int` | 微信收款 1已签约 3未签约 | 普通业务字段 | 可按问题需要提供 |
| `Data.WechatMerStateTwo` | `int?` | 微信收款2 1已签约 2待打款 3未签约 | 普通业务字段 | 可按问题需要提供 |
| `Data.AliPayMerState` | `int` | 支付宝收款 1已签约 3未签约 4待授权 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 && model.Uid < 1`；`resMsg.State`
- 一层业务调用：`StoreMerchantsConfigProvider.CertificationGetMerState`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.merchant.business_get_merchant_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Merchant 模块 |
| 用途 | 获取开户信息 |
| 使用时机 | 在商家视角中核对“获取开户信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Merchant/BusinessGetMerchantInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetMerchantInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetMerchantInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MerchantController.cs:31` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreId` | `int` | 店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreType` | `int` | 商户类型 0个体 1企业 2个人 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreFullName` | `string` | 商户全称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreName` | `string` | 商户简称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LegalName` | `string` | 法人姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.LegalIdCard` | `string` | 法人身份证 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.MerCertNo` | `string` | 证件号 所对应的证件类型的证件编号 | 普通业务字段 | 可按问题需要提供 |
| `Data.OrgCode` | `string` | 组织机构代码证 | 普通业务字段 | 可按问题需要提供 |
| `Data.TaxRegistCert` | `string` | 签约类型为“企业”，且证件类型为“营业执照”，则必填 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerContactName` | `string` | 商户联系人姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerContactPhone` | `string` | 商户联系人手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.MerLevel1No` | `string` | 商户一级分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerLevel2No` | `string` | 商户二级分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerProvince` | `string` | 商户省 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerCity` | `string` | 商户市 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerDistrict` | `string` | 商户区 | 普通业务字段 | 可按问题需要提供 |
| `Data.MerAddress` | `string` | 商户详细地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.AccountLicense` | `string` | 开户许可证编号 | 普通业务字段 | 可按问题需要提供 |
| `Data.HeadBankCode` | `string` | 开户银行编码 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankCode` | `string` | 开户银行支行编码 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardNo` | `string` | 结算银行账号或者银行卡号 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankProvince` | `string` | 开户省 | 普通业务字段 | 可按问题需要提供 |
| `Data.BankCity` | `string` | 开户市 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgIdcardFornt` | `string` | 身份证正面照片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgIdcardBack` | `string` | 法人身份证反面 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgCorpCode` | `string` | 营业执照照片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgTaxCode` | `string` | 税务登记证 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgOrgCode` | `string` | 组织机构代码证 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgOpBankCode` | `string` | 银行开户许可证 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgBankCard` | `string` | 结算银行卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgHandIdcard` | `string` | 手持身份证 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ImgHandBankCard` | `string` | 手持银行卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgUniCreditCode` | `string` | 统一社会信用代码证 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgShopPhoto` | `string` | 门头照 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 -2未通过 -1未审核，0审核 1通过审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Note` | `string` | 备注 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreYopConfigProvider.GetStoreYopConfigByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.merchant.business_get_merchants_config_state`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Merchant 模块 |
| 用途 | 获取店铺收款管理状态 是否认证 体验快捷收款状态 |
| 使用时机 | 在商家视角中核对“获取店铺收款管理状态 是否认证 体验快捷收款状态”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Merchant/BusinessGetMerchantsConfigState` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreCollectionStateResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreCollectionStateResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MerchantController.cs:117` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCertification` | `bool` | 是否已认证 true 认证 false 未认证. | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExperience` | `int` | 体验快捷收款收款状态 1开启 0未开通 2关闭 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 && model.Uid < 1`；`resMsg.State`
- 一层业务调用：`StoreMerchantsConfigProvider.BusinessGetMerchantsConfigState`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.merchant.business_is_need_certification`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Merchant 模块 |
| 用途 | 是否需要提示认证 |
| 使用时机 | 在商家视角中核对“是否需要提示认证”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Merchant/BusinessIsNeedCertification` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessIsNeedCertificationResponseModel>>`；包装 `Task/DataResult`；Data `BusinessIsNeedCertificationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MerchantController.cs:143` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNeedCertification` | `bool` | 是否需要认证 | 普通业务字段 | 可按问题需要提供 |
| `Data.RemainingDays` | `int` | 剩余天数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 && model.Uid < 1`；`resMsg.State`
- 一层业务调用：`StoreMerchantsConfigProvider.BusinessIsNeedCertification`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.mini_program.get_mini_program_meun_contents`

| 项目 | 内容 |
| --- | --- |
| 业务域 | MiniProgram 模块 |
| 用途 | 获取小程序菜单对应子项 |
| 使用时机 | 在商家视角中核对“获取小程序菜单对应子项”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MiniProgram/GetMiniProgramMeunContents` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetMiniProgramMeunContentsResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetMiniProgramMeunContentsResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MiniProgramController.cs:82` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex == 0) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `MeunType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 菜单类型 11单卡 21单券 22单券包 31商品 42餐品 42餐台；普通业务字段；可按问题需要提供 |
| `KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键字；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetMiniProgramMeunContentsResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ContentId` | `int` | 内容id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ContentName` | `string` | 内容名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ContentImage` | `string` | 默认卡片图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ContentInfo` | `string` | 默认简介 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MeunUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`model.PageSize == 0`；`model.PageIndex == 0`；`resultMsg.State`
- 一层业务调用：`ProductMeunProvider.GetMiniProgramMeunContents`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`固定提示：参数不正确`；`固定提示：请求数据数量不正确`；`固定提示：页码不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.mini_program.get_mini_program_meuns`

| 项目 | 内容 |
| --- | --- |
| 业务域 | MiniProgram 模块 |
| 用途 | 获取小程序菜单列表 |
| 使用时机 | 在商家视角中核对“获取小程序菜单列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MiniProgram/GetMiniProgramMeuns` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetMiniProgramMeunsResponseModel>>`；包装 `Task/DataResult`；Data `GetMiniProgramMeunsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MiniProgramController.cs:25` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns` | `List<MiniProgramMeunResponseModel>` | 小程序菜单数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunType` | `int` | 菜单类型 0链接 10卡列表 11单卡 20券列表 21单券 22单券包 30商城 31商品 40点单 41餐品 42餐台 50预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunName` | `string` | 菜单名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].IsSelected` | `bool` | 是否多选子项（true 需要调用子集接口） | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].DefaultImage` | `string` | 默认卡片图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunTitle` | `string` | 默认标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunInfo` | `string` | 默认简介 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`ssmms.Any(x => menuItem.ModuleId == x.ModuleId)`
- 一层业务调用：`ProductMeunProvider.GetProductMeunByProductType`、`StoreSoftModuleProvider.GetStoreModuleBySid`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.official_we_chat.get_auth_url`

| 项目 | 内容 |
| --- | --- |
| 业务域 | OfficialWeChat 模块 |
| 用途 | 获取授权地址 |
| 使用时机 | 在商家视角中核对“获取授权地址”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OfficialWeChat/GetAuthUrl` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetAuthUrlResponseModel>>`；包装 `Task/DataResult`；Data `GetAuthUrlResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OfficialWeChatController.cs:55` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 应用类型0三方 1自建；普通业务字段；可按问题需要提供 |
| `StoreCode` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(requestModel.StoreCode)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 自建应用StoreCode；普通业务字段；可按问题需要提供 |
| `CodeId` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | CodeId；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.AuthUrl` | `string` | 登录授权地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`string.IsNullOrEmpty(requestModel.StoreCode)`；`resultMsg.State`；`tm?.Id>0`
- 一层业务调用：`StoreProvider.GetStoreByCode`、`ThreeAuthorizerInfoProvider.GetThreeInfoByTenantId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 306`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_admin_cash_out_logs`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 审核人员获取提现申请记录 |
| 使用时机 | 在商家视角中核对“审核人员获取提现申请记录”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetAdminCashOutLogs` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataList<GetCashOutRecordsResponseModel>>`；包装 `Task/DataList`；Data `GetCashOutRecordsResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:728` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StaffId` | `int` | 员工ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].Receiver` | `string` | 收款人 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].ReceiveAccount` | `string` | 收款账号 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReceiveChannel` | `string` | 收款渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CashoutPrice` | `decimal` | 申请提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SparePrice` | `decimal` | 剩余可提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CreateDate` | `DateTime` | 申请日期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CashoutId` | `int` | 提现ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ApplyUser` | `string` | 申请人 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`StaffCashoutLogProvider.GetAdminCashOutLogs`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_cash_out_base_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取员工提现基础信息 |
| 使用时机 | 在商家视角中核对“获取员工提现基础信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetCashOutBaseInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCashOutBaseInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetCashOutBaseInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:666` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.CashoutTotalPrice` | `decimal` | 已提现总金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SpareTotalPrice` | `decimal` | 剩余可提现总金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CanCashout` | `bool` | 是否可以提现 | 普通业务字段 | 可按问题需要提供 |
| `Data.Message` | `string` | 不可提现原因 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`!setting.IsStallMode`
- 一层业务调用：`StoreSetingProvider.GetStoreSetingByStoreId`、`StaffCashoutProvider.GetCashOutBaseInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 200,`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_cash_out_records`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取提现记录列表 |
| 使用时机 | 在商家视角中核对“获取提现记录列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetCashOutRecords` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataList<GetCashOutRecordsResponseModel>>`；包装 `Task/DataList`；Data `GetCashOutRecordsResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:657` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=1 | AI 可在服务端上限内选择 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=10 | AI 可在服务端上限内选择 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StaffId` | `int` | 员工ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].Receiver` | `string` | 收款人 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].ReceiveAccount` | `string` | 收款账号 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReceiveChannel` | `string` | 收款渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CashoutPrice` | `decimal` | 申请提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SparePrice` | `decimal` | 剩余可提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CreateDate` | `DateTime` | 申请日期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CashoutId` | `int` | 提现ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ApplyUser` | `string` | 申请人 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`StaffCashoutLogProvider.GetCashOutRecords`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_consumption_performance_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取订单的详细绩效 |
| 使用时机 | 商家核对会员消费、充值、核减、返还、退款前置事实或操作后余额时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetConsumptionPerformanceInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetConsumptionPerformanceInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetConsumptionPerformanceInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:537` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ConsumptionId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 订单ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances` | `List<GetConsumptionPerformanceInfoViewModel>` | 绩效列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].ConsumptionPerformanceId` | `string` | 绩效订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].FlagColor` | `int` | 标记颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Performances[].PerformanceItemId` | `int` | 绩效统计方式 1售卡数量 2充值金额 3服务项目 4销卡金额 0 显示销卡金额和服务项目 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].Items` | `List<ConsumptionPerformanceItemViewModel>` | 订单销售的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].ConsumptionPerformanceItemId` | `string` | 绩效项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].Items[].PrepaidCard` | `StorePrepaidCardViewModel` | 订单销售的充值卡 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Performances[].Items[].PrepaidCard.PrepaidCardId` | `int` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].Items[].PrepaidCard.CardImg` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardType` | `int` | 类型：0计次，1储值 2时限 3权益 4安心充 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardValue` | `decimal` | 卡价值 次/金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardGivingValue` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardCount` | `int` | 库存剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardSellCount` | `long` | card_sell_count 销售数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.ValidType` | `int` | 失效日期类型: 0 相对日期（购卡后N天； 1 绝对日期（指定失效日期） | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.InvalidDate` | `string` | 失效日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.ValidityDate` | `int` | 有效期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsBuyOnce` | `bool` | 是否只能购买一次 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.State` | `int` | 状态 1在售 0停售 -1删除 2售罄 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.OpenCardType` | `int` | 开卡方式 0购买即开卡，1首次使用开卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.StopCardDays` | `int` | 停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.OpenCardDaysMax` | `int` | 最大延迟开卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsPayAfterReceive` | `bool` | 是否支付后领取 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CardInstructions` | `string` | 会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsPromptRights` | `bool` | 是否强制提示会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.Services` | `List<StorePrepaidCardServiceItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.Coupons` | `List<CouponCenterCouponViewModel>` | 购卡送的券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsAudit` | `bool` | 卡是否需要审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.ServiceItems` | `List<string>` | 审核卡消费项目明细 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CheckItemId` | `int` | 代办事项明细ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].Items[].PrepaidCard.CardTimeList` | `List<StorePrepaidCardViewCardTimeModel>` | 会员卡有效期集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsCardChild` | `bool` | 是否包含子卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.RemainderDay` | `int` | 剩余天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.IsCustomPrice` | `bool` | 是否开启自定义金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CustomPriceMin` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CustomPriceMax` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.CustomPriceGiveRatio` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PrepaidCard.BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].PerformancePrice` | `decimal` | 销售金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].ServiceItemId` | `int` | 服务项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].Items[].ServiceItemName` | `string` | 服务名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].ServiceItemUnit` | `string` | 服务单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].ServiceItemCount` | `int` | 服务数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].Staffs` | `List<ItemStaffViewModel>` | 员工列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].Staffs[].StaffId` | `int` | 员工ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].Items[].Staffs[].StaffName` | `string` | 员工姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Items[].Staffs[].StaffImg` | `string` | 员工头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].State` | `int` | 绩效状态 -3绩效未启用 -2 关闭设置不能修改 -1关闭设置， 0 待设置 1 已设置 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionPerformanceProvider.GetPerformanceListByConsumptionId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_last_cashout_record`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取最后一次提现记录 |
| 使用时机 | 在商家视角中核对“获取最后一次提现记录”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetLastCashoutRecord` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCashOutRecordsResponseModel>>`；包装 `Task/DataResult`；Data `GetCashOutRecordsResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:768` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StaffId` | `int` | 员工ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Receiver` | `string` | 收款人 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReceiveAccount` | `string` | 收款账号 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiveChannel` | `string` | 收款渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data.CashoutPrice` | `decimal` | 申请提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SparePrice` | `decimal` | 剩余可提现金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateDate` | `DateTime` | 申请日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.CashoutId` | `int` | 提现ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ApplyUser` | `string` | 申请人 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`StaffCashoutLogProvider.GetLastCashoutRecord`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_lessons_card_money`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取上课销卡金额统计 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetLessonsCardMoney` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetLessonsCardMoneyResponseModel>>`；包装 `Task/DataResult`；Data `GetLessonsCardMoneyResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:787` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 && model.StaffId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 && model.StaffId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 && model.StaffId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 店员Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型 0团课 1私教；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardMoney` | `decimal` | 过期会员 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 && model.StaffId == 0`
- 一层业务调用：`StoreReservationProvider.GetLessonsCardMoney`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_lessons_info_by_card`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取课程绩效用卡详情 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetLessonsInfoByCard` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetLessonsInfoByCardResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetLessonsInfoByCardResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:828` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型：0团课，1私教；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程类型：教练id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |
| `PrepaidCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetLessonsInfoByCardResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CourseId` | `long` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BeginTime` | `string` | 开课时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples` | `List<LessonsCardInfoResponseModel>` | 预约人员信息，IsFullData=true时返回 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].PeopleCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].OperationType` | `int` | 操作方式：0 用户，1管理员,3 共享用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].OperationUserName` | `string` | 操作者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].OperationUserImg` | `string` | 操作者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].Consumption` | `LessonsLogsConsumptionViewModel` | 财务信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.ConsumptionId` | `string` | 财务ID 使用后更新 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].Consumption.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.CardImg` | `string` | 卡图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.IsRefund` | `bool` | 是否退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.RefundsReason` | `string` | 如果课程取消，财务未退款 则返回失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.AfterValue` | `string` | 消费后余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.PayType` | `int` | 支付方式：0 会员卡，1 微信支付，2支付宝，3 现金 4优惠券 5兑换码 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`dataListPager?.TotalRows > 0`；`rms?.Count > 0`；`rm.CardId > 0`；`rm.Uid > 0`；`opm?.Id > 0 && opm.OperationType == 1`；`uc?.Id > 0`；`rm.ConsumptionId > 0`；`clm.ChildCardId > 0`；`clm.ConsumptionType == 1 \|\| clm.ConsumptionType == 5 \|\| clm.ConsumptionType == 6`；`clm.CardType == 3 && clm.ConsumptionValue == 0`
- 一层业务调用：`StoreLessonsProvider.GetStoreLessonsCard`、`StoreReservationProvider.GetStoreReservationByCardId`、`UserLessonsLogProvider.GetUserLessonsLogElementsByCondition`、`StoreProvider.GetStoreByIdAsync`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreProvider.GetStoreDateTime`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UsersInfoProvider.UsersInfoGetByUid`、`ConsumptionLogProvider.GetConsumptionLogById`、`UserCardChildProvider.GetUserCardChildById`、`ConsumptionLogProvider.GetConsumptionTypeInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_staff_all_performance_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取店铺某一个员工绩效列表 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetStaffAllPerformanceList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffAllPerformanceListResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffAllPerformanceListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:247` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StatisticsDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计月份 PS：2020-03；普通业务字段；可按问题需要提供 |
| `StatisticsStartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计时间开始 格式 "2023-01-01"；普通业务字段；可按问题需要提供 |
| `StatisticsEndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计时间结束 格式 "2023-01-01"；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances` | `List<ConsumptionPerformanceViewModel>` | 绩效列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].ConsumptionPerformanceId` | `long` | 绩效订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].ConsumptionId` | `long` | 消费ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].UserId` | `int` | 消费用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].UserName` | `string` | 用户名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Performances[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Performances[].IsVip` | `bool` | 是否是VIP | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].ConsumptionDate` | `string` | 消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].PerformanceItemId` | `int` | 绩效类型 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].PerformancePrice` | `decimal` | 订单的产生的绩效金额 非项目中的 实际值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Descriptions` | `List<StaffPerformanceDescriptionViewModel>` | 订单服务的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Descriptions[].PrepaidCardId` | `int` | 充值卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Performances[].Descriptions[].Description` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Descriptions[].Value` | `string` | 描述的值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Performances[].RemarkPrice` | `string` | 备注金额 高于 或者低于 | 普通业务字段 | 可按问题需要提供 |
| `Data.Performances[].FlagColor` | `int` | 标记颜色 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionPerformanceProvider.GetStaffAllPerformanceList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_staff_performance_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取店铺某一个员工绩效列表 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetStaffPerformanceList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<ConsumptionPerformanceViewModel>>>`；包装 `Task/DataResult`；Data `PageData<ConsumptionPerformanceViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:215` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StatisticsDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计月份 PS：2020-03；普通业务字段；可按问题需要提供 |
| `StatisticsStartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计时间开始 格式 "2023-01-01"；普通业务字段；可按问题需要提供 |
| `StatisticsEndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计时间结束 格式 "2023-01-01"；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页显示行数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<ConsumptionPerformanceViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionPerformanceId` | `long` | 绩效订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ConsumptionId` | `long` | 消费ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `int` | 消费用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 用户名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].IsVip` | `bool` | 是否是VIP | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionDate` | `string` | 消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PerformanceItemId` | `int` | 绩效类型 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].PerformancePrice` | `decimal` | 订单的产生的绩效金额 非项目中的 实际值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Descriptions` | `List<StaffPerformanceDescriptionViewModel>` | 订单服务的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Descriptions[].PrepaidCardId` | `int` | 充值卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Descriptions[].Description` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Descriptions[].Value` | `string` | 描述的值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].RemarkPrice` | `string` | 备注金额 高于 或者低于 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FlagColor` | `int` | 标记颜色 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionPerformanceProvider.GetStaffPerformanceList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_store_performance_items`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取店铺统计绩效方式 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetStorePerformanceItems` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStorePerformanceItemsResponseModel>>`；包装 `Task/DataResult`；Data `GetStorePerformanceItemsResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:90` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePerItems` | `List<StorePerformanceItemViewModel>` | 店铺统计绩效方式 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePerItems[].ItemId` | `int` | 绩效方式ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StorePerItems[].ItemType` | `int` | 绩效方式类型 0销售绩效 1工作绩效 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePerItems[].ItemName` | `string` | 绩效方式名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePerItems[].IsChecked` | `bool` | 是否已经选中 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StorePerformanceItemProvider.GetStorePerformanceItems`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.performance.get_store_performance_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Performance 模块 |
| 用途 | 获取店铺所有员工绩效列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Performance/GetStorePerformanceList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStorePerformanceListResponseModel>>`；包装 `Task/DataResult`；Data `GetStorePerformanceListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PerformanceController.cs:127` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StatisticsDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计月份 PS：2020-03；普通业务字段；可按问题需要提供 |
| `StatisticsStartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计时间开始 格式 "2023-01-01"；普通业务字段；可按问题需要提供 |
| `StatisticsEndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 统计时间结束 格式 "2023-01-01"；普通业务字段；可按问题需要提供 |
| `IsLessons` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否课程绩效；普通业务字段；可按问题需要提供 |
| `OrderBy` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 排序方式 0不排序 1团课倒序 2私教倒序 3销售金额倒序；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs` | `List<StaffPerformanceStatisticsViewModel>` | 员工列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffId` | `int` | 员工ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StaffName` | `string` | 员工姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffImg` | `string` | 员工头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].Descriptions` | `List<StaffPerformanceDescriptionViewModel>` | 描述列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].Descriptions[].PrepaidCardId` | `int` | 充值卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].Descriptions[].Description` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].Descriptions[].Value` | `string` | 描述的值 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`role1 == null \|\| role1.State == 0`；`staff != null`；`setting.IsStallMode`；`role2 == null \|\| role2.State == 0`；`datas?.Count > 0`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserById`、`StoreProvider.GetStoreByIdAsync`、`ConsumptionPerformanceItemProvider.GetStorePerformanceList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.poster.get_copywriting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Poster 模块 |
| 用途 | 获取文案 |
| 使用时机 | 在商家视角中核对“获取文案”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Poster/GetCopywriting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCopywritingResponseModel>>`；包装 `Task/DataResult`；Data `GetCopywritingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PosterController.cs:106` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CopywritingId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 当前文案ID（可以不传）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PosterId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 海报Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CopywritingId` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WritingContent` | `string` | 文案内容 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.PosterId < 1`；`resultMsg.State`；`cw.Count>0`
- 一层业务调用：`CopywritingProvider.GeCopywritingListByPosterId`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.poster.get_poster_by_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Poster 模块 |
| 用途 | 获取单个海报 |
| 使用时机 | 在商家视角中核对“获取单个海报”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Poster/GetPosterById` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetPosterByIdResponseModel>>`；包装 `Task/DataResult`；Data `GetPosterByIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PosterController.cs:53` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PosterId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PosterId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 海报Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PosterTitle` | `string` | 海报标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterImg` | `string` | poster_img 海报路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsFree` | `bool` | 是否免费 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsVip` | `bool` | 是否vip | 普通业务字段 | 可按问题需要提供 |
| `Data.IsHot` | `bool` | 是否热门 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags` | `List<string>` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems` | `List<PosterItemViewModel>` | 海报配置内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].PosterId` | `int` | poster_id 海报ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PosterItems[].ItemType` | `int` | item_type 类型 0文字 1图片 2二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemCode` | `string` | item_code code | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemX` | `decimal` | item_x 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemY` | `decimal` | item_y 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemW` | `decimal` | item_w 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].ItemH` | `decimal` | item_h 坐标X轴 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].FontColor` | `string` | 字体颜色 PS #FFFFFF | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].FontName` | `string` | font_name 字体 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].FontSize` | `int` | font_size 字号 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].RowHeight` | `int` | row_heigh 行间距 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].LinePadding` | `int` | line_padding 字间距 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterItems[].IsBold` | `bool` | is_bold 是否加粗 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.PosterId < 1`；`resultMsg.State`；`post?.Id>0`；`posterTypes?.Count > 0`；`types.StoreTypeId != "001"`
- 一层业务调用：`PosterProvider.GeneratePoster`、`PosterStoreTypeProvider.PosterStoreTypeGetList`、`StoreTypeProvider.GetStoreTypeName`、`PosterItemProvider.GetPosterItemsListByPosterId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.poster.get_poster_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Poster 模块 |
| 用途 | 获取海报列表 |
| 使用时机 | 在商家视角中核对“获取海报列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Poster/GetPosterList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetPosterListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetPosterListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PosterController.cs:24` |
| C/B 对照 | capi.poster.get_poster_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PostType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 海报类型 0朋友圈，10转卡；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetPosterListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].PosterTitle` | `string` | 海报标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PosterImg` | `string` | poster_img 海报路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsFree` | `bool` | 是否免费 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsVip` | `bool` | 是否vip | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsHot` | `bool` | 是否热门 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Tags` | `List<string>` | 标签 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`PosterProvider.GePosterList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.poster.get_poster_qr_code`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Poster 模块 |
| 用途 | 获取海报二维码 |
| 使用时机 | 在商家视角中核对“获取海报二维码”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Poster/GetPosterQrCode` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetPosterQrCodeResponseModel>>`；包装 `Task/DataResult`；Data `GetPosterQrCodeResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PosterController.cs:140` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PosterQrCode` | `string` | 海报二维码 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`；`sem?.Id > 0 && !string.IsNullOrEmpty(sem.PosterQrCode)`
- 一层业务调用：`StoreExtendProvider.GetStoreExtendByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.promote.business_get_promote_user_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Promote 模块 |
| 用途 | 获取推广人员信息列表 |
| 使用时机 | 在商家视角中核对“获取推广人员信息列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Promote/BusinessGetPromoteUserList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<BusinessGetPromoteUserListResponseModel>>>`；包装 `Task/DataResult`；Data `List<BusinessGetPromoteUserListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PromoteController.cs:19` |
| C/B 对照 | capi.promote.get_promote_user_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PromoteUserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 推广人ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Keyword` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 关键字；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].NameGroup` | `string` | 字母分组 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers` | `List<PromoteUserViewModel>` | 推广人列表 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].UserId` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].PromoteUsers[].HeadImg` | `string` | 头像 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].UserName` | `string` | 用户名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].PromoteUsers[].UserStoreName` | `string` | 店铺用户名 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].RemarkName` | `string` | 备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].PromoteUsers[].IdentityTag` | `string` | 身份标识：普、VIP | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].CreateDate` | `string` | 时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].ConsumptionCommission` | `decimal` | 已提佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].Commission` | `decimal` | 佣金余额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].NameGroup` | `string` | 分组 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PromoteUsers[].UserMobile` | `string` | 手机 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].PromoteUsers[].CardTag` | `string` | 卡标签 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId <= 0 \|\| request.Uid <= 0`
- 一层业务调用：`PromoteUsersProvider.BusinessGetPromoteUserList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数有误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.promote.get_promote_report`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Promote 模块 |
| 用途 | 获取推广信息报表 |
| 使用时机 | 在商家视角中核对“获取推广信息报表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Promote/GetPromoteReport` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetPromoteReportResponseModel>>`；包装 `Task/DataResult`；Data `GetPromoteReportResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/PromoteController.cs:40` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PeopleCount` | `long` | 分销人员总数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCommission` | `decimal` | 总佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Commission` | `decimal` | 未提佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionCommission` | `decimal` | 已提佣金 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId <= 0 \|\| request.Uid <= 0`
- 一层业务调用：`PromoteUsersProvider.GetPromoteReport`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数有误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.recommend.business_get_recommend_qr_code`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Recommend 模块 |
| 用途 | 当面邀请二维码 |
| 使用时机 | 在商家视角中核对“当面邀请二维码”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Recommend/BusinessGetRecommendQrCode` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetRecommendQrCodeResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetRecommendQrCodeResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/RecommendController.cs:108` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.QrCode` | `string` | 二维码路径 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`
- 一层业务调用：`UserRecommendProvider.BusinessGetRecommendQrCode`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.recommend.get_recommend_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Recommend 模块 |
| 用途 | 获取邀请数据统计信息（暂时不用） |
| 使用时机 | 在商家视角中核对“获取邀请数据统计信息（暂时不用）”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Recommend/GetRecommendStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetRecommendStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `GetRecommendStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/RecommendController.cs:24` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCount` | `long` | 商家数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.RebatePriceSum` | `decimal` | 返利金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.recommend.get_recommend_store_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Recommend 模块 |
| 用途 | 获取邀请店铺列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Recommend/GetRecommendStoreList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetRecommendStoreListResponseModel>>`；包装 `Task/DataResult`；Data `GetRecommendStoreListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/RecommendController.cs:55` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCount` | `long` | 商家数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.RebatePriceSum` | `decimal` | 返利金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores` | `List<RecommendStoreInfoModel>` | 店铺列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreId` | `int` | 商家数量 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Stores[].StoreName` | `string` | 商家名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreLogo` | `string` | 商家Logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].IsCertification` | `bool` | 是否已认证 0否1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].IsBuy` | `bool` | 是否已订购 0否1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].RebatePrice` | `decimal` | 返利金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].IsRebate` | `bool` | 是否给客户发了返利 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].State` | `int` | 状态 0无效，1已生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Rcontent` | `string` | 备注 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`stores?.Count>0`
- 一层业务调用：`UserRecommendProvider.GetUserRecommendByRuid`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.search.get_serarch_hot`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Search 模块 |
| 用途 | 获取搜索热词 |
| 使用时机 | 在商家视角中核对“获取搜索热词”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Search/GetSerarchHot` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<String>>`；包装 `DataResult`；Data `List<String>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SearchController.cs:16` |
| C/B 对照 | capi.search.get_serarch_hot |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[]` | `String` | 标量集合；具体业务含义以 Action 摘要为准 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`SearchProvider.GetSerachHotWord`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.service_item.get_is_service`

| 项目 | 内容 |
| --- | --- |
| 业务域 | ServiceItem 模块 |
| 用途 | 获取是否有服务 |
| 使用时机 | 在商家视角中核对“获取是否有服务”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ServiceItem/GetIsService` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetIsServiceResponseModel>>`；包装 `Task/DataResult`；Data `GetIsServiceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ServiceItemController.cs:675` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsService` | `bool` | 是否有服务 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreServiceItemProvider.GetIsService`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.service_item.get_service_category_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | ServiceItem 模块 |
| 用途 | 获取服务分类 |
| 使用时机 | 在商家视角中核对“获取服务分类”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ServiceItem/GetServiceCategoryList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetServiceCategoryListResponseModel>>`；包装 `DataResult`；Data `List<GetServiceCategoryListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ServiceItemController.cs:382` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`ServiceItemCategoryProvider.GetServiceCategoryList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.service_item.get_service_item_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | ServiceItem 模块 |
| 用途 | 获取项目详情 |
| 使用时机 | 在商家视角中核对“获取项目详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ServiceItem/GetServiceItemInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetServiceItemInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetServiceItemInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ServiceItemController.cs:97` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.ItemId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.ItemId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ItemId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.ItemId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 项目Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsReservation` | `bool` | 是否支持预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemInfo` | `string` | 项目描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleSum` | `int` | 同一时间段可预约人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemDuration` | `int` | 项目时长 | 普通业务字段 | 可按问题需要提供 |
| `Data.AssociationCard` | `int` | 关联储值卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemClassId` | `string` | 项目分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ItemClassName` | `string` | 项目分类ID | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemPrice` | `decimal` | 项目价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemVipPrice` | `decimal` | 会员价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPay` | `bool` | 是否参与支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemImg` | `string` | 项目图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsEnableDelete` | `bool` | 是否允许删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.CategoryList` | `List<int>` | 分类集合 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.ItemId < 1`；`resultMsg.State`；`ret?.Id > 0`；`itemClassModel != null`
- 一层业务调用：`StoreServiceItemProvider.GetServiceItemInfo`、`ItemClassProvider.GetItemClassModelById`、`ServiceItemCategoryRelationProvider.GetServiceCategoryRelationList`、`StorePrepaidCardServiceItemProvider.ExistService`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.service_item.get_service_item_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | ServiceItem 模块 |
| 用途 | 获取项目列表 |
| 使用时机 | 在商家视角中核对“获取项目列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ServiceItem/GetServiceItemList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetServiceItemListResponseModel>>`；包装 `Task/DataResult`；Data `GetServiceItemListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ServiceItemController.cs:23` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `RequestDataType` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=0 | AI 只能使用文档确认的枚举值 | 查询数据类型 0 预约列表， 1价目表服务列表；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items` | `List<ServiceItemViewModel>` | 项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].ItemInfo` | `string` | 项目描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].IsReservation` | `bool` | 是否支持预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].ItemClassId` | `string` | 项目分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Items[].ItemClassName` | `string` | 项目分类ID | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].ItemPrice` | `decimal` | 项目价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].ItemVipPrice` | `decimal` | 会员价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].IsPay` | `bool` | 是否参与支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].ItemImg` | `string` | 项目图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Items[].IsEnableDelete` | `bool` | 是否允许删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreServiceItemProvider.GetServiceItemList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.service_item.get_service_item_staff`

| 项目 | 内容 |
| --- | --- |
| 业务域 | ServiceItem 模块 |
| 用途 | 获取项目店员预约开启状态 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ServiceItem/GetServiceItemStaff` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<ServiceItemStaffViewModel>>>`；包装 `Task/DataResult`；Data `List<ServiceItemStaffViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ServiceItemController.cs:150` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId < 1 \\|\\| request.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId < 1 \\|\\| request.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ItemId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 项目id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].StaffId` | `int` | 员工id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].UserName` | `string` | 员工名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].State` | `int` | 项目预约状态（0 关闭 1 开启） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId < 1 \|\| request.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StaffServiceItemProvider.GetServiceItemStaffByItemId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.service_item.get_service_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | ServiceItem 模块 |
| 用途 | 获取服务列表 |
| 使用时机 | 在商家视角中核对“获取服务列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ServiceItem/GetServiceList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetServiceListResponseModel>>`；包装 `DataResult`；Data `List<GetServiceListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ServiceItemController.cs:538` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ClassId` | `int` | 分类id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ClassName` | `string` | 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Count` | `int` | 个数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList` | `List<ServiceListViewModel>` | 服务列表 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ServiceList[].ItemName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].ItemImg` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].SellNum` | `int` | 销量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].ItemPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].ItemVipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].State` | `int` | 状态；0-下架，1-上架 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].OnOffState` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ServiceList[].VipDisCount` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreServiceItemProvider.GetServiceList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.sms_center.business_get_sign_name_length`

| 项目 | 内容 |
| --- | --- |
| 业务域 | SmsCenter 模块 |
| 用途 | 获取短信签名长度 |
| 使用时机 | 在商家视角中核对“获取短信签名长度”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/SmsCenter/BusinessGetSignNameLength` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSignNameLengthResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSignNameLengthResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SmsCenterController.cs:288` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignNameLength` | `int` | 签名长度 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreAddedServicesProvider.GetSignName`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.sms_center.business_get_sms_config`

| 项目 | 内容 |
| --- | --- |
| 业务域 | SmsCenter 模块 |
| 用途 | 获取短信预览内容配置信息 |
| 使用时机 | 在商家视角中核对“获取短信预览内容配置信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/SmsCenter/BusinessGetSmsConfig` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSmsConfigResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSmsConfigResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SmsCenterController.cs:252` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignName` | `string` | 签名 | 普通业务字段 | 可按问题需要提供 |
| `Data.ShoreUrl` | `string` | 短链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConstValue` | `string` | 短信内容常量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreAddedServicesProvider.GetSignName`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.sms_center.get_sms_added_service_order`

| 项目 | 内容 |
| --- | --- |
| 业务域 | SmsCenter 模块 |
| 用途 | 短信购买记录 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/SmsCenter/GetSmsAddedServiceOrder` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetSmsAddedServiceOrderResponseModel>>`；包装 `Task/DataResult`；Data `GetSmsAddedServiceOrderResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SmsCenterController.cs:122` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Orders` | `List<StoreAddedServiceOrderViewModel>` | 短信充值列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Orders[].Id` | `int` | 自增主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Orders[].ServiceType` | `int` | 增值服务类型 0短信 | 普通业务字段 | 可按问题需要提供 |
| `Data.Orders[].Title` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Orders[].ItemQuantity` | `int` | 数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Orders[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Orders[].EndDate` | `string` | 到期日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Orders[].CreateDate` | `string` | 创建时间 订单创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreAddedServiceOrderProvider.GetSmsAddedServiceOrder`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.sms_center.get_sms_added_services`

| 项目 | 内容 |
| --- | --- |
| 业务域 | SmsCenter 模块 |
| 用途 | 获取短信充值列表 |
| 使用时机 | 在商家视角中核对“获取短信充值列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/SmsCenter/GetSmsAddedServices` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetSmsAddedServicesResponseModel>>`；包装 `Task/DataResult`；Data `GetSmsAddedServicesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SmsCenterController.cs:56` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList` | `List<AddedServiceViewModel>` | 短信充值列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SmsAddedServiceList[].ServiceType` | `int` | 类型 0短信 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].Title` | `string` | title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].ChildTitle` | `string` | child_title 子标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].PosterId` | `int` | poster_id 海报ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SmsAddedServiceList[].ServiceUnit` | `string` | service_unit 单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].ServiceImg` | `string` | service_img 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].ItemQuantity` | `int` | 数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].EndDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].ServiceInfo` | `string` | service_info 介绍 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].IsShip` | `bool` | is_ship 是否需要发货 | 普通业务字段 | 可按问题需要提供 |
| `Data.SmsAddedServiceList[].InfoImg` | `List<string>` | 详情图片 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`AddedServicesProvider.GetSmsAddedServices`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.sms_center.get_store_sms_balance`

| 项目 | 内容 |
| --- | --- |
| 业务域 | SmsCenter 模块 |
| 用途 | 获取短余额 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/SmsCenter/GetStoreSmsBalance` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreSmsBalanceResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreSmsBalanceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SmsCenterController.cs:22` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreSmsBalance` | `int` | 短信余额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreSmsSendTaskProvider.GetStoreItemQuantity`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.sms_center.get_store_sms_template_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | SmsCenter 模块 |
| 用途 | 获取短信模板列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/SmsCenter/GetStoreSmsTemplateList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreSmsTemplateListResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreSmsTemplateListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SmsCenterController.cs:220` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SmsType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 短信模板用途：0 节日祝福 1群发短信 2放假通知 3优惠券活动 4续费营销 5老顾客激活 6生日营销 7预约；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates` | `List<StoreSmsTemplateViewModel>` | 模板列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Id` | `int` | 店铺短信模板 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Templates[].TemplateContent` | `string` | 模板内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].State` | `int` | -1未通过，0审核中，1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreSmsTemplateProvider.StoreSmsTemplateGetList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.business_ge_program_control_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取所有模板系统控件 |
| 使用时机 | 在商家视角中核对“获取所有模板系统控件”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/BusinessGeProgramControlList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGeProgramControlListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGeProgramControlListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:475` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ProgramTemplateId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 原始模板Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TemplateType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 模板类型 0主页；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ControlDatas` | `List<ProgramControlResponseModel>` | 控件数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.ControlDatas[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ControlDatas[].ControlType` | `int` | 控件类型 0其他 1功能项 2自定义功能 3控件 4数据列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.ControlDatas[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ControlDatas[].ControlTitle` | `string` | 控件标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.ControlDatas[].ShowRow` | `int` | 每行显示数据数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ControlDatas[].ControlImg` | `string` | 预览图 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`ProgramControlProvider.GetProgramControl`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数格式错误!`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.business_get_program_template_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取店铺模板列表 |
| 使用时机 | 在商家视角中核对“获取店铺模板列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/BusinessGetProgramTemplateList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetProgramTemplateListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetProgramTemplateListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:31` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TemplateType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 模板类型 0主页；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas` | `List<ProgramTemplateResponseViewModel>` | 模板数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Id` | `long` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].TemplateType` | `int` | template_type 模板类型 0主页 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].TemplateTitle` | `string` | template_title 模板标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].TemplateInfo` | `string` | template_info 模板介绍 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].TemplateImg` | `string` | template_img 预览图 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].UsedCount` | `int` | used_count 使用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].IsUsed` | `bool` | 是否试用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreTemplateProvider.GetStoreTemplateByTemplateType`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数格式错误!`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.business_get_store_template_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取店铺模板列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/BusinessGetStoreTemplateList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreTemplateListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreTemplateListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:137` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TemplateType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 模板类型 0主页；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas` | `List<StoreTemplateResponseViewModel>` | 模板数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Id` | `long` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].TemplateType` | `int` | template_type 模板类型 0主页 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].TemplateTitle` | `string` | template_title 模板标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].TemplateInfo` | `string` | template_info 模板介绍 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].TemplateImg` | `string` | template_img 预览图 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].State` | `int` | 状态 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数格式错误!`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.business_get_template`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取页面模板 |
| 使用时机 | 在商家视角中核对“获取页面模板”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/BusinessGetTemplate` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetTemplateResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetTemplateResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:279` |
| C/B 对照 | capi.template.get_template |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TemplateType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 模板类型 0主页；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `long` | 模板Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateId` | `long` | 原始模板ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateType` | `int` | template_type 模板类型 0主页 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateTitle` | `string` | template_title 模板标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateInfo` | `string` | template_info 模板介绍 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateImg` | `string` | template_img 预览图 | 普通业务字段 | 可按问题需要提供 |
| `Data.BackgroundColor` | `string` | background_color 背景色 | 普通业务字段 | 可按问题需要提供 |
| `Data.BackgroundImg` | `string` | background_img 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateFont` | `string` | template_font 通用字体 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateFontSize` | `int` | template_font_size 字号 | 普通业务字段 | 可按问题需要提供 |
| `Data.LetterSpacing` | `int` | letter_spacing 字间距 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCustomerService` | `bool` | is_customer_service 是否显示客服 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShare` | `bool` | is_share 是否显示分享按钮 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas` | `List<StoreTemplateControlViewModel>` | 模板数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Id` | `long` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].ControlId` | `long` | control_id 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].StyleId` | `long` | style_id 样式id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].DataType` | `int` | control_type 控件类型 0默认 1商家数据 2用户数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].ControlType` | `int` | control_type 控件类型 0其他 1功能项 2自定义功能 3控件 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].ControlName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].ControlTitle` | `string` | control_title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].IsEdit` | `bool` | is_edit 是否可以修改 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].IsShow` | `bool` | is_show 是否显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].ShowRow` | `int` | show_row 每行显示数据数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].State` | `int` | state 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs` | `List<StoreTemplateInputViewModel>` | 标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].Id` | `long` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].Inputs[].StyleId` | `long` | 样式id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].Inputs[].DataType` | `int` | data_type 数据类型 0不需赋值 1需要 2数据源 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].DataStyle` | `int` | 数据为数据源时显示情况 0不处理，1有数据显示 2无数据显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].InputType` | `string` | input_type 标签类型 img / swiper / scroll / div / span / input / text | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].DataName` | `string` | 数据项名称 StoreName | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].DataPro` | `string` | 数据项名称 StorePro | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].DataValue` | `string` | 默认数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].InputX` | `int` | input_x 坐标点X | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].InputY` | `int` | input_y 坐标点Y | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].InputWidth` | `int` | input_width 控件宽度 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].InputHeight` | `int` | input_height 控件高度 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].IsEdit` | `bool` | is_edit 是否可编辑 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].JumpTitle` | `string` | 跳转标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].JumpType` | `int` | jump_type 跳转类型 0无跳转 1网页 2本小程序 3其他小程序 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].JumpAppId` | `string` | jump_app_id 三方小程序appid,跳转类型为3的时候填写 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TemplateDatas[].Inputs[].JumpUrl` | `string` | jump_url 跳转最终地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].ChildInputs` | `List<StoreTemplateInputViewModel>` | 子项标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].DataSource` | `string` | 数据源 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateDatas[].Inputs[].State` | `int` | state 状态 -1删除 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreTemplateProvider.GetStoreTemplateByTemplateType`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数格式错误!`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.business_get_template_data`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取页面模板数据 |
| 使用时机 | 在商家视角中核对“获取页面模板数据”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/BusinessGetTemplateData` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetTemplateDataResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetTemplateDataResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:315` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreTemplateId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 模板id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreControlId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 控件id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `DataType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | data_type 数据类型 0魔方导航 1服务图标 1学员信息；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas` | `List<StoreTemplateDataViewModel>` | 数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Id` | `long` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Datas[].DataType` | `int` | data_type 数据类型 0魔方导航 1服务图标 1学员信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].DataImg` | `string` | data_img 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].DataTitle` | `string` | data_title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].JumpTitle` | `string` | 跳转标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].JumpType` | `int` | jump_type 跳转类型 0无跳转 1网页 2本小程序 3其他小程序 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].JumpAppId` | `string` | jump_app_id 三方小程序appid,跳转类型为3的时候填写 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Datas[].JumpUrl` | `string` | jump_url 跳转最终地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreTemplateDataProvider.GetStoreTemplateDataByDataType`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数格式错误!`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.get_mini_program_meun_contents`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取小程序菜单对应子项 |
| 使用时机 | 在商家视角中核对“获取小程序菜单对应子项”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/GetMiniProgramMeunContents` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetMiniProgramMeunContentsResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetMiniProgramMeunContentsResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:764` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex == 0) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `MeunType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 菜单类型 11单卡 21单券 22单券包 31商品 42餐品 42餐台；普通业务字段；可按问题需要提供 |
| `KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键字；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetMiniProgramMeunContentsResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ContentId` | `int` | 内容id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ContentName` | `string` | 内容名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ContentImage` | `string` | 默认卡片图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ContentInfo` | `string` | 默认简介 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MeunUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`model.PageSize == 0`；`model.PageIndex == 0`；`resultMsg.State`
- 一层业务调用：`ProductMeunProvider.GetMiniProgramMeunContents`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`固定提示：参数不正确`；`固定提示：请求数据数量不正确`；`固定提示：页码不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.get_mini_program_meuns`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取小程序菜单列表 |
| 使用时机 | 在商家视角中核对“获取小程序菜单列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/GetMiniProgramMeuns` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetMiniProgramMeunsResponseModel>>`；包装 `Task/DataResult`；Data `GetMiniProgramMeunsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:706` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns` | `List<MiniProgramMeunResponseModel>` | 小程序菜单数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunType` | `int` | 菜单类型 0链接 10卡列表 11单卡 20券列表 21单券 22单券包 30商城 31商品 40点单 41餐品 42餐台 50预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunName` | `string` | 菜单名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].IsSelected` | `bool` | 是否多选子项（true 需要调用子集接口） | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].DefaultImage` | `string` | 默认卡片图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunTitle` | `string` | 默认标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunInfo` | `string` | 默认简介 | 普通业务字段 | 可按问题需要提供 |
| `Data.Meuns[].MeunUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`ssmms.Any(x => menuItem.ModuleId == x.ModuleId)`
- 一层业务调用：`ProductMeunProvider.GetProductMeunByProductType`、`StoreSoftModuleProvider.GetStoreModuleBySid`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.template.get_template_banner`

| 项目 | 内容 |
| --- | --- |
| 业务域 | Template 模块 |
| 用途 | 获取模板banner图库 |
| 使用时机 | 在商家视角中核对“获取模板banner图库”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Template/GetTemplateBanner` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetTemplateBannerResponseModel>>`；包装 `Task/DataResult`；Data `GetTemplateBannerResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/TemplateController.cs:806` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TageId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 标签id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateBanners` | `List<GetTemplateBannerViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateBanners[].ImgUrl` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.TemplateBanners[].TemplateTag` | `int` | 模板标记分类（用于区分模板头图） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`stm != null && stm.Id > 0`
- 一层业务调用：`StoreTemplateProvider.GetStoreTemplateFirstOrDefaultByCondition`、`TemplateImageProvider.GetTemplateImages`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.api_get_authorizer_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取授权方账号信息 |
| 使用时机 | 在商家视角中核对“获取授权方账号信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/ApiGetAuthorizerInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxApiGetAuthorizerInfoResponse`；包装 `无已识别包装`；Data `WxApiGetAuthorizerInfoResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:339` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.authorization_info` | `WxAuthorizationInfo` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorization_info.authorizer_appid` | `string` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.authorization_info.authorizer_access_token` | `string` | 源码属性注释缺失 | 密钥/凭据 | 禁止原样进入模型 |
| `Data.authorization_info.expires_in` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorization_info.authorizer_refresh_token` | `string` | 源码属性注释缺失 | 密钥/凭据 | 禁止原样进入模型 |
| `Data.authorization_info.func_info` | `object` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info` | `WxAuthorizerInfo` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.nick_name` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.authorizer_info.head_img` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.service_type_info` | `object` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.verify_type_info` | `object` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.user_name` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.authorizer_info.principal_name` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.alias` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.business_info` | `object` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.qrcode_url` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.MiniProgramInfo` | `MiniProgramInfo` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.MiniProgramInfo.network` | `object` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.authorizer_info.MiniProgramInfo.categories` | `object[]` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ThreeAuthorizerInfoProvider.ApiGetAuthorizerInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_all_categories`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取可以设置的所有类目 |
| 使用时机 | 在商家视角中核对“获取可以设置的所有类目”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetAllCategories` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxGetAllCategoriesResponse`；包装 `无已识别包装`；Data `WxGetAllCategoriesResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:419` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list` | `WxCategoryList` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories` | `WxCategories[]` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.categories_list.categories[].name` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].level` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].father` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].children` | `int[]` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].sensitive_type` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].qualify` | `WxCategoriesQualify` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].qualify.exter_list` | `List<WxCategoriesQualifyExtList>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories_list.categories[].qualify.remark` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_all_private_template`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取已添加至帐号下所有模板列表 |
| 使用时机 | 在商家视角中核对“获取已添加至帐号下所有模板列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetAllPrivateTemplate` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxGetAllPrivateTemplateResponse`；包装 `无已识别包装`；Data `WxGetAllPrivateTemplateResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:292` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.template_list` | `WxMessageTemplate[]` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].template_id` | `string` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.template_list[].title` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].primary_industry` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].deputy_industry` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].content` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].example` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ThreeAuthorizerInfoProvider.GetAllPrivateTemplate`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_authorizer_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取APP授权信息 |
| 使用时机 | 在商家视角中核对“获取APP授权信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetAuthorizerInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<WxCommonResponse>>`；包装 `Task/DataResult`；Data `WxCommonResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:670` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appId` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_category`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取已设置的所有类目 |
| 使用时机 | 在商家视角中核对“获取已设置的所有类目”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetCategory` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxGetCategoryResponse`；包装 `无已识别包装`；Data `WxGetCategoryResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:431` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.limit` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.quota` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.category_limit` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories` | `List<WxGetCategoryItem>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories[].first` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories[].first_name` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories[].second` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories[].second_name` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories[].audit_status` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.categories[].audit_reason` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_industry`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取三方行业 |
| 使用时机 | 在商家视角中核对“获取三方行业”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetIndustry` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxGetIndustryResponse`；包装 `无已识别包装`；Data `WxGetIndustryResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:260` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.primary_industry` | `WxGetIndustryClass` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.primary_industry.first_class` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.primary_industry.second_class` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.secondary_industry` | `WxGetIndustryClass` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.secondary_industry.first_class` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.secondary_industry.second_class` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ThreeAuthorizerInfoProvider.GetIndustry`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_privacy_interface`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取API审核结果 |
| 使用时机 | 在商家视角中核对“获取API审核结果”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetPrivacyInterface` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `GetPrivacyInterfaceResponseModel`；包装 `无已识别包装`；Data `GetPrivacyInterfaceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:609` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces` | `List<PrivacyInterfaceViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].ApiName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].ApiChName` | `string` | api 中文名 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].ApiDesc` | `string` | Api描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].ApplyTime` | `string` | 申请时间 ，该字段发起申请后才会有 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].Status` | `string` | 接口状态，该字段发起申请后才会有 状态码 说明 1 待申请开通 2 无权限 3 申请中 4 申请失败 5 已开通 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].AuditId` | `string` | 申请单号，该字段发起申请后才会有 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrivacyInterfaces[].FailReason` | `string` | 申请被驳回原因或者无权限，该字段申请驳回时才会有 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].ApiLink` | `string` | api文档链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivacyInterfaces[].GroupName` | `string` | 分组名 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_privacy_setting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取小程序用户隐私保护指引 |
| 使用时机 | 在商家视角中核对“获取小程序用户隐私保护指引”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetPrivacySetting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `string`；包装 `无已识别包装`；Data `string` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:492` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `privacyVer` | `int` | Action 形参 | ApiController推断 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data` | `string` | 标量返回值；具体业务含义以 Action 摘要为准 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_query_nickname`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 查询改名审核状态 |
| 使用时机 | 在商家视角中核对“查询改名审核状态”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetQueryNickname` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `GetQueryNicknameResponse`；包装 `无已识别包装`；Data `GetQueryNicknameResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:544` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `audit_id` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.nickname` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.audit_stat` | `string` | 审核状态，1：审核中，2：审核失败，3：审核成功 | 普通业务字段 | 可按问题需要提供 |
| `Data.fail_reason` | `string` | 失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.create_time` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.audit_time` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_template_draft_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取代码草稿列表 |
| 使用时机 | 在商家视角中核对“获取代码草稿列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetTemplateDraftList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxGetTemplateDraftListResponse`；包装 `无已识别包装`；Data `WxGetTemplateDraftListResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:361` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| — | — | 无 Action 业务参数 | — | — | — |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.draft_list` | `List<WxGetTemplateDraftItem>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.draft_list[].create_time` | `long` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.draft_list[].user_version` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.draft_list[].user_desc` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.draft_list[].draft_id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ThreeAuthorizerInfoProvider.GetTemplateDraftList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.get_template_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取代码模板列表 |
| 使用时机 | 在商家视角中核对“获取代码模板列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeChatThirdPlatform/GetTemplateList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxGetTemplateListResponse`；包装 `无已识别包装`；Data `WxGetTemplateListResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:381` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| — | — | 无 Action 业务参数 | — | — | — |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list` | `List<WxGetTemplateItem>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].create_time` | `long` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].user_version` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].user_desc` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].template_id` | `string` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`ThreeAuthorizerInfoProvider.GetTemplateList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.qr_code_jump_get`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取已设置的二维码规则 |
| 使用时机 | 在商家视角中核对“获取已设置的二维码规则”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `GET /api/WeChatThirdPlatform/QrCodeJumpGet` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxQrCodeJumpGetResponse`；包装 `无已识别包装`；Data `WxQrCodeJumpGetResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:469` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.qrcodejump_open` | `int` | 是否已经打开二维码跳转链接设置 | 普通业务字段 | 可按问题需要提供 |
| `Data.qrcodejump_pub_quota` | `int` | 本月还可发布的次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.list_size` | `int` | 二维码规则数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list` | `QrCodeJumpRule[]` | 二维码规则详情列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].prefix` | `string` | 二维码规则 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].permit_sub_rule` | `string` | 是否独占符合二维码前缀匹配规则的所有子规 1 为不占用，2 为占用; 详见 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].path` | `string` | 小程序功能页面 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].open_version` | `int` | 测试范围 1 开发版（配置只对开发者生效） 2 体验版（配置对管理员、体验者生效) 3 正式版（配置对开发者、管理员和体验者生效） | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].debug_url` | `string[]` | 测试链接（选填）可填写不多于 5 个用于测试的二维码完整链接，此链接必须符合已填写的二维码规则。 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].state` | `int` | 发布标志位，1 表示未发布，2 表示已发布 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.we_chat_third_platform.qr_code_jump_mime_get`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeChatThirdPlatform 模块 |
| 用途 | 获取已设置的二维码规则 |
| 使用时机 | 在商家视角中核对“获取已设置的二维码规则”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `GET /api/WeChatThirdPlatform/QrCodeJumpMimeGet` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxQrCodeJumpGetResponse`；包装 `无已识别包装`；Data `WxQrCodeJumpGetResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeChatThirdPlatformController.cs:503` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.qrcodejump_open` | `int` | 是否已经打开二维码跳转链接设置 | 普通业务字段 | 可按问题需要提供 |
| `Data.qrcodejump_pub_quota` | `int` | 本月还可发布的次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.list_size` | `int` | 二维码规则数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list` | `QrCodeJumpRule[]` | 二维码规则详情列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].prefix` | `string` | 二维码规则 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].permit_sub_rule` | `string` | 是否独占符合二维码前缀匹配规则的所有子规 1 为不占用，2 为占用; 详见 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].path` | `string` | 小程序功能页面 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].open_version` | `int` | 测试范围 1 开发版（配置只对开发者生效） 2 体验版（配置对管理员、体验者生效) 3 正式版（配置对开发者、管理员和体验者生效） | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].debug_url` | `string[]` | 测试链接（选填）可填写不多于 5 个用于测试的二维码完整链接，此链接必须符合已填写的二维码规则。 | 普通业务字段 | 可按问题需要提供 |
| `Data.rule_list[].state` | `int` | 发布标志位，1 表示未发布，2 表示已发布 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.wei_xin.check_user_is_attention_official_account`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeiXin 模块 |
| 用途 | 判断当前用户是否关注公众号 |
| 使用时机 | 在商家视角中核对“判断当前用户是否关注公众号”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeiXin/CheckUserIsAttentionOfficialAccount` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：WechatAttentionProvider.GetOpenIdListByUid |
| 返回 | `Task<DataResult<CheckUserIsAttentionOfficialAccountResponseModel>>`；包装 `Task/DataResult`；Data `CheckUserIsAttentionOfficialAccountResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeiXinController.cs:955` |
| C/B 对照 | capi.wei_xin.check_user_is_attention_official_account |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(requestModel.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAttentionOfficialAccount` | `bool` | 是否关注公众号 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`requestModel.Uid < 1`；`resultMsg.State`；`wam!=null && !string.IsNullOrEmpty(wam.OpenId)`
- 一层业务调用：`WechatAttentionProvider.GetOpenIdListByUid`
- 疑似副作用：`WechatAttentionProvider.GetOpenIdListByUid`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 306`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.wei_xin.check_wx_verify_nick_name`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeiXin 模块 |
| 用途 | 微信认证名称检测 |
| 使用时机 | 在商家视角中核对“微信认证名称检测”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeiXin/CheckWxVerifyNickName` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxCheckWxVerifyNickNameResponse`；包装 `无已识别包装`；Data `WxCheckWxVerifyNickNameResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeiXinController.cs:1115` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `nick_name` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 当前会话提供并临时使用；不得持久化到模型历史 | 源码属性注释缺失；个人信息；仅在当前授权场景按最小范围提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.hit_condition` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.wording` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.CheckWxVerifyNickName`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.wei_xin.get`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeiXin 模块 |
| 用途 | 获取公众号/小程序所绑定的开放平台帐号 |
| 使用时机 | 在商家视角中核对“获取公众号/小程序所绑定的开放平台帐号”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeiXin/Get` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxCommonResponse`；包装 `无已识别包装`；Data `WxCommonResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeiXinController.cs:1247` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `appid` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.Get`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.wei_xin.get_code_template_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeiXin 模块 |
| 用途 | 获取代码模板列表 |
| 使用时机 | 在商家视角中核对“获取代码模板列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeiXin/GetCodeTemplateList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `WxGetTemplateListResponse`；包装 `无已识别包装`；Data `WxGetTemplateListResponse` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeiXinController.cs:1175` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `appid` | `string` | Action 形参 | ApiController推断 | 服务端注入：当前产品/小程序配置 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.errcode` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.errmsg` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list` | `List<WxGetTemplateItem>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].create_time` | `long` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].user_version` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].user_desc` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.template_list[].template_id` | `string` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetCodeTemplateList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.wei_xin.get_pre_auth_code`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeiXin 模块 |
| 用途 | 获取预授权码 |
| 使用时机 | 在商家视角中核对“获取预授权码”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeiXin/GetPreAuthCode` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetPreAuthCodeResponseViewModel>>`；包装 `Task/DataResult`；Data `GetPreAuthCodeResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeiXinController.cs:1045` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| — | — | 无 Action 业务参数 | — | — | — |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PreAuthCode` | `string` | 预授权吗 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WeiXinProvider.GetPreAuthCode`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.wei_xin.get_user_info_by_work_we_chat`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeiXin 模块 |
| 用途 | 企业微信获取用户信息 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeiXin/GetUserInfoByWorkWeChat` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetUserInfoByWorkWeChatResponseModel>>`；包装 `Task/DataResult`；Data `GetUserInfoByWorkWeChatResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeiXinController.cs:443` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `UserId` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(requestModel.UserId)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 企业微信客户ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Stores` | `List<KFStoreModel>` | 店铺集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Stores[].StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].KeCount` | `int` | 客列；0-灰色，1-红色，2-绿色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Address` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].UserName` | `string` | 店主名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].UserMobile` | `string` | 店主电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].UserCount` | `long` | 会员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].CardCount` | `long` | 会员卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].IsCertification` | `bool` | 是否开通收款 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Flag` | `int` | 店铺标记 0 无标记 1标记1，2标记2，3标记3 具体代表啥还没定 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Remark` | `string` | 店铺备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].IsVisitWechat` | `bool` | 是否有过微信回访 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].IsVisitMobile` | `bool` | 是否有过电话回访 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].SoftVersion` | `string` | 软件版本 0试用 1基础 2高级 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreTypeName` | `string` | 商店分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].AgentName` | `string` | 商店所属代理名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreCreateByTenantCount` | `long` | 当前商店创建人总共参与商店的个数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].ChannelName` | `string` | 店铺渠道名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreCount` | `int` | 店铺的个数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].UserType` | `string` | 用户权限租：0超级管理员，1管理员，2运营人员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].LoginTime` | `string` | 登录时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].ServiceEndDate` | `string` | 服务到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Modules` | `string` | 模块 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`string.IsNullOrEmpty(requestModel.UserId)`；`resultMsg.State`
- 一层业务调用：`WeChatHelper.WorkWeChatGetAuthorizerToken`、`StoreProvider.GetStoreListByUnionId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 306`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.wei_xin.work_we_chat_get_js_signature`

| 项目 | 内容 |
| --- | --- |
| 业务域 | WeiXin 模块 |
| 用途 | 企业微信获取JS签名 |
| 使用时机 | 在商家视角中核对“企业微信获取JS签名”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WeiXin/WorkWeChatGetJsSignature` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetJsSignatureResponseModel>>`；包装 `Task/DataResult`；Data `GetJsSignatureResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WeiXinController.cs:407` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `model` | `GetJsSignatureRequestModel` | Action 形参 | ApiController推断 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.AppId` | `string` | AppId | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AgentId` | `string` | 应用ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Signature` | `string` | 签名 | 密钥/凭据 | 禁止原样进入模型 |

#### 代码行为与证据

- Controller 条件：`string.IsNullOrEmpty(model.Url)`；`resultMsg.State`
- 一层业务调用：`WeChatHelper.WorkWeChatGetJsSignature`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_we_chat.get_auth_url`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 企业微信 |
| 用途 | 获取授权地址 |
| 使用时机 | 在商家视角中核对“获取授权地址”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkWeChat/GetAuthUrl` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetAuthUrlResponseModel>>`；包装 `Task/DataResult`；Data `GetAuthUrlResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkWeChatController.cs:50` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 应用类型0三方 1自建；普通业务字段；可按问题需要提供 |
| `StoreCode` | `string` | 参与组合校验 | 绑定=ApiController推断；if(requestModel.AppType==1 && string.IsNullOrEmpty(requestModel.StoreCode)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 自建应用StoreCode；普通业务字段；可按问题需要提供 |
| `CodeId` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | CodeId；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.AuthUrl` | `string` | 登录授权地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`requestModel.AppType==1 && string.IsNullOrEmpty(requestModel.StoreCode)`；`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 306`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_we_chat.get_external_contact_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 企业微信 |
| 用途 | 获取配置了客户联系功能的成员列表 |
| 使用时机 | 在商家视角中核对“获取配置了客户联系功能的成员列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `GET /api/WorkWeChat/GetExternalContactList` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetExternalContactListResponseModel>>`；包装 `Task/DataResult`；Data `GetExternalContactListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkWeChatController.cs:445` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `corpId` | `string` | Action 形参 | ApiController推断 | 必须来自同一会话上游 API 结果或服务端对象引用 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `userId` | `string` | Action 形参 | ApiController推断 | 必须来自同一会话上游 API 结果或服务端对象引用 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.ErrorMessage` | `virtual string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ErrorCodeValue` | `abstract int` | errcode的 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data` | `virtual object` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ErrorCode` | `ReturnCodeWork` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExternalUserid` | `List<string>` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`WorkWeChatProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_we_chat.get_follow_user_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 企业微信 |
| 用途 | 获取配置了客户联系功能的成员列表 |
| 使用时机 | 在商家视角中核对“获取配置了客户联系功能的成员列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `GET /api/WorkWeChat/GetFollowUserList` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：WorkWechatAttentionProvider.AddWorkWechatAttentions |
| 返回 | `Task<DataResult<GetFollowUserListResponseModel>>`；包装 `Task/DataResult`；Data `GetFollowUserListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkWeChatController.cs:409` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `corpId` | `string` | Action 形参 | ApiController推断 | 必须来自同一会话上游 API 结果或服务端对象引用 | 内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.ErrorMessage` | `virtual string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ErrorCodeValue` | `abstract int` | errcode的 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data` | `virtual object` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ErrorCode` | `ReturnCodeWork` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.FollowUser` | `List<string>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`user!= "ZhangYing"`
- 一层业务调用：`WorkWeChatProvider.GetAuthorizerAccessToken`、`WorkWechatAttentionProvider.AddWorkWechatAttentions`
- 疑似副作用：`WorkWechatAttentionProvider.AddWorkWechatAttentions`。人工复核前禁止开放。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_we_chat.get_js_signature`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 企业微信 |
| 用途 | 获取Js签名 |
| 使用时机 | 在商家视角中核对“获取Js签名”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkWeChat/GetJsSignature` |
| 鉴权 | 源码继承免鉴权基类；AI 部署仍需服务级访问控制 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetJsSignatureResponseModel>>`；包装 `Task/DataResult`；Data `GetJsSignatureResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkWeChatController.cs:340` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `model` | `GetJsSignatureRequestModel` | Action 形参 | ApiController推断 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.AppId` | `string` | AppId | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AgentId` | `string` | 应用ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Signature` | `string` | 签名 | 密钥/凭据 | 禁止原样进入模型 |

#### 代码行为与证据

- Controller 条件：`string.IsNullOrEmpty(model.Url)`；`resultMsg.State`；`model.Url.Contains("#")`；`model.Type==0`
- 一层业务调用：`ThreeAuthorizerInfoProvider.GetThreeInfoByStoreId`、`WorkWeChatProvider.GetAuthorizerAccessToken`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_we_chat_code.get_code_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 企业微信 |
| 用途 | 获取单个活码详情 |
| 使用时机 | 在商家视角中核对“获取单个活码详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkWeChatCode/GetCodeInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCodeInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetCodeInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkWechatCodeController.cs:82` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.Id == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.Id == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.Id == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 主键id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CodeTitle` | `string` | 活码标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.CodeUrl` | `string` | 活码图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.BgImg` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffId` | `int` | 员工id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.MessageInfo` | `string` | 欢迎语 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags` | `List<WorkWechatStaffCodeTagViewModelModel>` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tags[].TagId` | `int` | tag_id 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tags[].TagIcon` | `string` | tag_id 标签id | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].TagName` | `string` | 标签名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments` | `List<WorkWechatStaffCodeAttachmentsViewModel>` | 附件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MsgType` | `int` | msg_type 附件消息类型 1文本 2图片 3链接 4小程序 5视频 6文件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].FileUrl` | `string` | file_url 附件资源地址 用来上传获取media_id | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].Title` | `string` | title 附件标题 3 4 5使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkPicurl` | `string` | link_picurl 链接封面图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkDesc` | `string` | link_desc 链接描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkUrl` | `string` | link_url 链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].MiniprogramMeunType` | `int` | 小程序菜单类型 0链接 10卡列表 11单卡 20券列表 21单券 22单券包 30商城 31商品 40点单 42餐品 42餐台 50预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].MiniprogramContentId` | `int` | 小程序内容id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MiniprogramAppid` | `string` | miniprogram_appid 小程序appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MiniprogramPage` | `string` | miniprogram_page 小程序page路径 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0 \|\| model.Id == 0`；`resultMsg.State`；`codeInfo?.Id > 0`；`codeInfo.IsTag`；`tags?.Count>0`；`tagsInfo?.Count>0`；`codeInfo.IsAttachment`；`attrs?.Count>0`
- 一层业务调用：`WorkWechatStaffCodeProvider.GetWorkWechatStaffCodeById`、`WorkWechatStaffCodeTagProvider.GetWorkWechatStaffCodeTagByCodeId`、`StoreProvider.GetStoreByIdAsync`、`StoreGroupProvider.GetStoreGroupByIds`、`WorkWechatStaffCodeAttachmentsProvider.GetWorkWechatStaffCodeAttachmentsByCodeId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：MsgType = attr.MsgType,`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_we_chat_code.get_codes`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 企业微信 |
| 用途 | 获取所有活码列表 |
| 使用时机 | 在商家视角中核对“获取所有活码列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkWeChatCode/GetCodes` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCodesResponseModel>>`；包装 `Task/DataResult`；Data `GetCodesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkWechatCodeController.cs:28` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes` | `List<WorkWechatStaffCodeResponseModel>` | 活码数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Codes[].CodeTitle` | `string` | 活码标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].BgImg` | `string` | bg_img 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].StaffName` | `string` | 员工名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].MessageInfo` | `string` | message_info 欢迎语 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].CodeUrl` | `string` | code_url 活码地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].CreateName` | `string` | 创建人 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`codes?.Count>0`
- 一层业务调用：`WorkWechatStaffCodeProvider.GetWorkWechatStaffCodeByStoreId`、`StoreProvider.GetStoreByIdAsync`、`TenantUserProvider.GetTenantUserByTenantId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.business_check_user_coupon_used`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 判断优惠券是否可用 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/BusinessCheckUserCouponUsed` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CouponUsedViewModel>>`；包装 `Task/DataResult`；Data `CouponUsedViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:358` |
| C/B 对照 | capi.coupon.check_user_coupon_used |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.UserId < 1 \\|\\| model.CouponIds.Count < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.UserId < 1 \\|\\| model.CouponIds.Count < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.UserId < 1 \\|\\| model.CouponIds.Count < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 店铺Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CouponIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 优惠券Ids；普通业务字段；可按问题需要提供 |
| `UsedType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 使用场景 1现金支付 2购买会员卡 3 会员卡核销 4现场使用（服务或实物兑换）；普通业务字段；可按问题需要提供 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.CardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 核销会员卡的卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TotalPrice` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 实付金额；普通业务字段；可按问题需要提供 |
| `PayModel` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.UsedType == 3 && model.PayModel == 0) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 付款模式 0实付 1应付(不对金额做任何校验)；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.IsUsed` | `bool` | 是否可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayPrice` | `decimal` | 优惠后金额（实付） | 普通业务字段 | 可按问题需要提供 |
| `Data.ErrorCode` | `int` | 1 优惠券不可多选, 2选择的优惠券中存在不可用不可用 查看CouponErrorInfo | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponErrorInfo` | `List<CouponUsedErrorInfoViewModel>` | 优惠券中的错误信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponErrorInfo[].Id` | `int` | 优惠券id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponErrorInfo[].IsUsed` | `bool` | 是否可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponErrorInfo[].StateReasonContent` | `string` | 不可用原因说明 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.UserId < 1 \|\| model.CouponIds.Count < 1`；`model.UsedType == 3 && model.PayModel == 0`；`model.CardId < 1`；`uccm?.Id == null && uccm.State < 1`；`uccm.State == 2`；`uccm.CardType == 0`；`resultMsg.State`
- 一层业务调用：`UserCardChildProvider.GetUserCardChildById`、`UserCouponProvider.CheckUsed`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`状态赋值：StatusCode = 304`；`状态赋值：StatusCode = 305`；`固定提示：参数格式错误！`；`固定提示：使用场景不正确！`；`固定提示：无效的会员卡！`；`固定提示：会员卡已过期！`；`固定提示：会员卡类型不正确！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.business_get_coupon_count`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 获取每个状态对应的优惠券总数量 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/BusinessGetCouponCount` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<IList<BusinessGetCouponCountResponseModel>>`；包装 `DataResult`；Data `IList<BusinessGetCouponCountResponseModel>` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:605` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].SearchState` | `int` | 查询状态 0:已发放 1:未使用 2:已使用 3:快到期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Count` | `int` | 优惠券总数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`UserCouponProvider.BusinessGetCouponCount`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.business_get_coupon_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 查询店铺优惠券 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/BusinessGetCouponList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetCouponListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetCouponListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:582` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.CouponTypeList.Count < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SearchState` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 查询状态 0:已发放 1:未使用 2:已使用 3:快到期；普通业务字段；可按问题需要提供 |
| `CouponTypeList` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 优惠券类型 类型 0代金券 1打折券 2服务券 3礼品券；普通业务字段；可按问题需要提供 |
| `BeginDate` | `DateTime` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询起始日期；普通业务字段；可按问题需要提供 |
| `EndDate` | `DateTime` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询结束时间；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=1 | AI 可在服务端上限内选择 | 分页页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=10 | AI 可在服务端上限内选择 | 分页大小；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetCouponListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Uid` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].OrderNo` | `string` | 订单号 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 客户 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 手机 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CouponType` | `int` | 优惠券类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponTitle` | `string` | 优惠券标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CouponValue` | `decimal` | 优惠券面值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReceiveTime` | `DateTime` | 收券时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 状态 0不可用 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FocusField` | `string` | 焦点字段 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.CouponTypeList.Count < 1`
- 一层业务调用：`UserCouponProvider.BusinessGetCouponList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.business_get_coupon_list_by_store_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 获取当前店铺的所有优惠券列表 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/BusinessGetCouponListByStoreId` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCouponListByStoreIdResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCouponListByStoreIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:177` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Uid` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AppId` | `string` | 小程序APPID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons` | `List<CouponViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`CouponProvider.GetCouponListByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.business_get_coupon_money`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 查询店铺优惠券代金券总额 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/BusinessGetCouponMoney` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCouponMoneyResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCouponMoneyResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:617` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.CouponTypeList == null \\|\\| model.CouponTypeList.Count < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SearchState` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 查询状态 0:已发放 1:未使用 2:已使用 3:快到期；普通业务字段；可按问题需要提供 |
| `CouponTypeList` | `List<int>` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.CouponTypeList == null \\|\\| model.CouponTypeList.Count < 1) | AI 只能使用文档确认的枚举值 | 优惠券类型 类型 0代金券 1打折券 2服务券 3礼品券；普通业务字段；可按问题需要提供 |
| `BeginDate` | `DateTime` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询起始日期；普通业务字段；可按问题需要提供 |
| `EndDate` | `DateTime` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询结束时间；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=1 | AI 可在服务端上限内选择 | 分页页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=10 | AI 可在服务端上限内选择 | 分页大小；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Money` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.CouponTypeList == null \|\| model.CouponTypeList.Count < 1`
- 一层业务调用：`UserCouponProvider.BusinessGetCouponMoney`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.get_user_coupon`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 获取单个优惠券 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/GetUserCoupon` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserCouponResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserCouponResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:119` |
| C/B 对照 | capi.coupon.get_user_coupon |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 用户Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CouponId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.CouponId < 1 && model.PCouponId < 1 && string.IsNullOrEmpty(model.CouponCode)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 优惠券Id（三选一）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CouponCode` | `string` | 参与组合校验 | 绑定=ApiController推断；if(model.CouponId < 1 && model.PCouponId < 1 && string.IsNullOrEmpty(model.CouponCode)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 优惠券Code（三选一）；普通业务字段；可按问题需要提供 |
| `PCouponId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.CouponId < 1 && model.PCouponId < 1 && string.IsNullOrEmpty(model.CouponCode)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 原始优惠券Id（三选一）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserId` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserName` | `string` | 用户名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserImg` | `string` | 用户logo | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Coupon` | `UserCouponViewModel` | 优惠券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Coupon.CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.ActivityId` | `int` | 活动id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.ActivityType` | `int` | 活动类型 0裂变 1抽奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponCenterId` | `int` | 优惠中心ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupon.CouponCount` | `int` | 优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponSum` | `int` | 优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.UseFrequencyCount` | `int` | 限制频率已使用张数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.StateReasonContent` | `string` | 不可用原因说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupon.CouponSourceContent` | `string` | 来源说明 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.UserId < 0`；`model.CouponId < 1 && model.PCouponId < 1 && string.IsNullOrEmpty(model.CouponCode)`；`resultMsg.State`；`coupon?.Id > 0`
- 一层业务调用：`UserCouponProvider.BusinessGetUserCoupon`、`UserCardProvider.BusinessGetUserCardInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.get_user_coupon_count`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 获取用户可用优惠券数量 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/GetUserCouponCount` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserCouponCountResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserCouponCountResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:26` |
| C/B 对照 | capi.coupon.get_user_coupon_count |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.UserId == 0 && model.CardId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.UserId == 0 && model.CardId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 用户ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCount` | `int` | 优惠券数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`model.UserId == 0 && model.CardId == 0`；`resultMsg.State`
- 一层业务调用：`UserCouponProvider.GetUserCouponCountByUserId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon.get_user_coupon_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 获取优惠券列表 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Coupon/GetUserCouponList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserCouponListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserCouponListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponController.cs:69` |
| C/B 对照 | capi.coupon.get_user_coupon_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.UserId == 0 && model.CardId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UsedType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 使用场景 1现金支付 2购买会员卡 3 会员卡核销 4现场使用（服务或实物兑换）；普通业务字段；可按问题需要提供 |
| `TotalPrice` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 实付金额；普通业务字段；可按问题需要提供 |
| `QueryState` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 查询状态 0不限制 1未生效或可用 2已用或过期；普通业务字段；可按问题需要提供 |
| `PayModel` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 付款模式 0实付 1应付(不对金额做任何校验)；普通业务字段；可按问题需要提供 |
| `UserId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.UserId == 0 && model.CardId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 用户ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons` | `List<UserCouponViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].ActivityId` | `int` | 活动id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].ActivityType` | `int` | 活动类型 0裂变 1抽奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponCenterId` | `int` | 优惠中心ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].CouponCount` | `int` | 优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSum` | `int` | 优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyCount` | `int` | 限制频率已使用张数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StateReasonContent` | `string` | 不可用原因说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSourceContent` | `string` | 来源说明 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`model.UserId == 0 && model.CardId == 0`；`resultMsg.State`
- 一层业务调用：`UserCouponProvider.GetUserCouponByUserId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon_center.business_coupon_center_coupon_report`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 优惠中心 优惠券详细页面报表 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CouponCenter/BusinessCouponCenterCouponReport` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<BusinessCouponCenterCouponReportResponseModel>>`；包装 `DataResult`；Data `PageData<BusinessCouponCenterCouponReportResponseModel>` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponCenterController.cs:520` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CouponCenterId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CouponCenterId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CouponCenterId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CouponCenterId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 优惠中心ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CouponId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 优惠券Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 当前页；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessCouponCenterCouponReportResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserName` | `string` | 用户昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsVip` | `long` | 是否是会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUsed` | `long` | 是否已经使用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.CouponCenterId < 1`；`resultMsg.State`
- 一层业务调用：`CouponCenterProvider.BusinessCouponCenterCouponReport`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon_center.business_coupon_center_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 优惠中心券 详细信息 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CouponCenter/BusinessCouponCenterInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessCouponCenterInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessCouponCenterInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponCenterController.cs:478` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CouponCenterType < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CouponCenterType < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CouponCenterType` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CouponCenterType < 1) | AI 只能使用文档确认的枚举值 | 优惠券中心优惠类型；普通业务字段；可按问题需要提供 |
| `IsByUserLevel` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.IsByUserLevel == 0) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否按会员等级 -1默认 0 否 1是；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.State` | `int` | 当前优惠中心 券类型的状态 1正常 2已停用 | 普通业务字段 | 可按问题需要提供 |
| `Data.StateReason` | `int` | 0已开始 1已暂停 2已达到发行数量上限,3优惠券截止日期已过 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsByUserLevel` | `bool` | 是否按会员等级送券 默认false | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos` | `List<BusinessCouponCenterInfoResponseInfoModel>` | 获取优惠中心券 详细信息 响应Model列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].TemplateId` | `int` | 短信模板ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].CouponCenterType` | `int` | 券类型 1开卡送,2续费送,3生日送,4满送，5群发,6营销活动，7节日祝福8放假 9老客户激活，10散客营销， 21 续费营销（新），22 联盟券，23，购卡送券，24 支付宝商家券 ，25 券包 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].IsByUserLevel` | `bool` | 是否按会员等级 1是0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].PreCardId` | `int` | 按会员等级时必填 储值卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].IsOnlyOne` | `int` | 满赠券时必填 1单次 0累十 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].ConsumptionType` | `int` | 满赠券时必填 消费方式1仅付款2卡内消费 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].MeetAmountMin` | `decimal` | 满赠券时必填 金额到达区间开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].MeetAmountMax` | `decimal` | 预留字段 金额到达区间结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].SendGroupDate` | `string` | 群发券必填 发放时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].SendGroup` | `int` | 群发券必填发放群体 0全部会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].SendGroupTimes` | `int` | 群发券必填 群发次数 1只发一次2按月次数 3按年发送 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].LessMoney` | `decimal` | 续费送券 当余额低于多少时送券 (元) | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].LessTimes` | `int` | 续费送券 当余额低于多少时送券（次） | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].ShareRegularCount` | `int` | 定向券时必填 微信分享限制几人领取 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].AllianceDay` | `int` | 联盟券，多少天未在本店消费 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].SendStartDate` | `DateTime?` | 支付宝商家券发券时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].SendEndDate` | `DateTime?` | 支付宝商家券发券时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].CouponSum` | `int` | 活动优惠券张数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].State` | `int` | 状态 -1删除 0停止使用 1正常 群发券 0默认 1,未开始 2已发送 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].MaxSendCount` | `int` | 最大发送数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].CouponCenterName` | `string` | 活动名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].SendCount` | `int` | 发送数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons` | `List<CouponCenterCouponViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponCount` | `int` | 已领取数量 ***** 在创建，修改 接口中 该字段表示 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].CouponSum` | `int` | 活动优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].Coupons[].ItemId` | `int` | 活动项目ID（预留） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].CouponCenterId` | `int` | 优惠中心 ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponCenterInfos[].TotalSendCount` | `long` | 总发放（张） | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].UsedCount` | `long` | 已使用（张） | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponCenterInfos[].NoUsedCount` | `long` | 未使用（张） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.CouponCenterType < 1`；`resultMsg.State`；`model.IsByUserLevel == -1`；`model.IsByUserLevel == 0`；`model.IsByUserLevel == 1`
- 一层业务调用：`CouponCenterProvider.BusinessCouponCenterInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon_center.business_share_coupon_get_random_param`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 分享优惠券生成随机密钥 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CouponCenter/BusinessShareCouponGetRandomParam` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：CouponPasswordProvider.CouponPasswordAdd |
| 返回 | `Task<DataResult<BusinessShareCouponGetRandomParamResponseModel>>`；包装 `Task/DataResult`；Data `BusinessShareCouponGetRandomParamResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponCenterController.cs:590` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CouponCenterId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 优惠中心ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ShareRegularCount` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 分享限制几人领取 0不限制；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.RandomParam` | `string` | 优惠券随机密钥 | 密钥/凭据 | 禁止原样进入模型 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`；`couponCenter.Coupons.Count > 0`；`couponCenter.CouponCenterType.Equals(25)`；`model.ShareRegularCount > 0 && model.ShareRegularCount > couponCenter.MaxSendCount`
- 一层业务调用：`CouponCenterProvider.CouponCenterGetModelById`、`CouponPasswordProvider.CouponPasswordAdd`
- 疑似副作用：`CouponPasswordProvider.CouponPasswordAdd`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon_link.get_coupon_link_detail`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 获取支付宝内券详情 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CouponLink/GetCouponLinkDetail` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CouponLinkViewModel>>`；包装 `Task/DataResult`；Data `CouponLinkViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponLinkController.cs:38` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 优惠券id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Name` | `string` | 券名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Type` | `int` | 券类型 0：直领券 1：售卖券 | 普通业务字段 | 可按问题需要提供 |
| `Data.FaceValue` | `decimal` | 面额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SellingPrice` | `decimal?` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponUrl` | `string` | 领券链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.Image` | `string` | 优惠券券图片 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`CouponLinkProvider.GetCouponLinkDetail`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.coupon_link.get_coupon_link_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 优惠券 |
| 用途 | 获取支付宝内券列表 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/CouponLink/GetCouponLinkList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCouponLinkListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCouponLinkListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CouponLinkController.cs:23` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.CouponLinkList` | `List<BusinessCouponLinkViewModel>` | 优惠券列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponLinkList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CouponLinkList[].Name` | `string` | 券名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponLinkList[].Type` | `int` | 券类型 0：直领券 1：售卖券 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponLinkList[].FaceValue` | `decimal` | 面额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponLinkList[].SellingPrice` | `decimal?` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponLinkList[].CouponUrl` | `string` | 领券链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponLinkList[].Image` | `string` | 优惠券券图片 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`CouponLinkProvider.GetCouponLinkList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_user_black_list.get_store_user_black_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 黑名单列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreUserBlackList/GetStoreUserBlackList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<StoreUserBlackListItemResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<StoreUserBlackListItemResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreUserBlackListController.cs:290` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid <= 0 \\|\\| model.StoreId <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid <= 0 \\|\\| model.StoreId <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize <= 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex <= 0) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 可选按会员ID筛选；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 可选按会员卡ID筛选；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PenaltyLevel` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 可选按惩罚等级筛选；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<StoreUserBlackListItemResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 黑名单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Uid` | `int` | 会员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 会员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].PenaltyLevel` | `int` | 惩罚等级 0禁止进入小程序 1禁止查看排课 2禁止约课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PenaltyDate` | `string` | 自动移出时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LogNote` | `string` | 操作备注 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 状态 -1删除 0黑名单失效 1黑名单生效中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid <= 0 \|\| model.StoreId <= 0`；`model.PageIndex <= 0`；`model.PageSize <= 0`
- 一层业务调用：`StoreUserBlackListProvider.ExpireStoreUserBlackList`、`StoreUserBlackListProvider.GetStoreUserBlackListByPage`、`UsersInfoProvider.GetUserInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_user_black_list.get_store_user_black_list_logs`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 黑名单操作日志列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreUserBlackList/GetStoreUserBlackListLogs` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<StoreUserBlackListLogItemResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<StoreUserBlackListLogItemResponseModel>` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreUserBlackListController.cs:354` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid <= 0 \\|\\| model.StoreId <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid <= 0 \\|\\| model.StoreId <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize <= 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex <= 0) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `BlackId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 可选黑名单ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 可选会员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 可选会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LogType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 可选日志类型 0加入黑名单 1移出黑名单 2修改黑名单信息；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<StoreUserBlackListLogItemResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 日志ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].BlackId` | `long` | 黑名单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Uid` | `int` | 会员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 会员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LogType` | `int` | 日志类型 0加入黑名单 1移出黑名单 2修改黑名单信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OperationType` | `int` | 操作类型 0系统操作 1管理员操作 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OperationName` | `string` | 操作人名称（系统操作返回“系统”） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LogNote` | `string` | 操作备注 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PenaltyLevel` | `int` | 惩罚等级 0禁止进入小程序 1禁止查看排课 2禁止约课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PenaltyDate` | `string` | 惩罚时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 状态 -1删除 0黑名单失效 1黑名单生效中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid <= 0 \|\| model.StoreId <= 0`；`model.PageIndex <= 0`；`model.PageSize <= 0`；`x.OperationType != 0`；`x.CreateBy>0`
- 一层业务调用：`StoreUserBlackListLogProvider.GetStoreUserBlackListLogsByPage`、`TenantUserProvider.GetTenantUserByStoreId`、`UsersInfoProvider.GetUserInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.business_get_store_user`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 加载用户信息（会员编辑资料页） |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/BusinessGetStoreUser` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreUserResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreUserResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:877` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.UserId` | `int` | 会员ID大于0则不可修改手机 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserMobile` | `string` | 会员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserInfos` | `List<StoreUserForControlViewModel>` | 用户信息集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].ControlType` | `string` | control_type 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserInfos[].IsMust` | `int` | 是否必填 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].IsShow` | `bool` | 是否必填对C端展示 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].ControlsValidations` | `List<CustomControlsValidationViewModel>` | 控件验证规则 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].ControlsValidations[].ValidationFormula` | `string` | 验证公式 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].ControlsValidations[].ValidationInstructions` | `string` | 不通过提示信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].Items` | `List<CustomControlsItemViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserInfos[].Items[].Cid` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserInfos[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserInfos[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups` | `List<UserGroupViewModel>` | 分组信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupId` | `int` | 分组Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].GroupName` | `string` | 分组名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupIcon` | `string` | 标签Icon | 普通业务字段 | 可按问题需要提供 |
| `Data.UserTagName` | `string` | 标签对应的名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.CardId < 1`；`resultMsg.State`
- 一层业务调用：`StoreUserProvider.GetStoreUser`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.business_get_u_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 会员信息统计 |
| 使用时机 | 在商家视角中核对“会员信息统计”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/BusinessGetUStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:813` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员uid；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionPrice` | `string` | 消费金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseLessonsCount` | `long` | 累计上课 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivateLessonsCount` | `long` | 累计私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Classes` | `List<UserClassesViewModel>` | 班级列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Classes[].Id` | `long` | 班级id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Classes[].ClassTitle` | `string` | 班级名字 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.CardId == 0`；`resultMsg.State`；`ucModel?.Id > 0 \|\| ucModel?.Uid > 0`；`ls!=null`；`scs?.Count>0`
- 一层业务调用：`UserCardProvider.BusinessGetUserCardInfo`、`StoreReservationProvider.GetLessonsStatistics`、`StoreClassesUserProvider.GetStoreClassesUserElementsByCondition`、`StoreClassesProvider.GetStoreClassesFirstOrDefaultByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.business_get_user_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取用户全部详细信息和名下所有会员卡（会员详情页） |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/BusinessGetUserInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:567` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.UserId < 0 && model.CardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UsedDatetime` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 用卡时间。预约传预约时间 其他不用传；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.StoreId` | `int` | 所属店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardImg` | `string` | 会员卡背景 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 会员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.CardName` | `string` | 卡名 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.WillExpireCardIntegral` | `int` | 即将过期的积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.WillExpireIntegralDate` | `int` | 即将过期的日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionIntegral` | `int` | 消费积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionPrint` | `int` | 消费印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 当前 CardId 对应的 user_card 会员身份状态：1正常，0未开卡，-1销卡， -2商家删除，2已过期，-3过期续费删除，3停卡。 | 普通业务字段 | 可按问题需要提供 |
| `Data.ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalMoney` | `decimal` | 会员消费统计 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalNum` | `long` | 会员消费次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumRenewal` | `decimal` | 充值金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumPrice` | `decimal` | 消费金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonSum` | `long` | 团课上课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivateLessonSum` | `long` | 私教上课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.MissAppointmentSum` | `long` | 爽约次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.RemarkName` | `string` | 备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsAssociatedUser` | `bool` | 是否已经关联用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOpenIntegral` | `bool` | 是否开启积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardExpiredIntegral` | `long` | 将过期积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardExpiredIntegralDate` | `string` | 积分过期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Commission` | `decimal` | 佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionCommission` | `decimal` | 提现/消费 佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards` | `List<UserCardChildViewModel>` | 当前 CardId 对应会员身份下的具体持卡列表；空数组表示该身份下没有未删除的卡。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations` | `List<StoreUserOperationByUserViewModel>` | 管理员操作记录 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionId` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreUserOperations[].StoreUserId` | `int` | 管理员Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreUserOperations[].StoreUserName` | `string` | 管理员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreUserOperations[].UserType` | `int` | 用户权限：0超级管理员，1管理员，2运营人员 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionType` | `int` | 消费方式：0 计次，1 金额，2积分，3印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionTag` | `string` | 0支出 ，1充值,2 核减，3返还，4赠送，5付款 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionValue` | `decimal` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo` | `List<StoreUserSimpleViewModel>` | 用户信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreUserInfo[].IsShow` | `bool` | 是否必填 0否 1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups` | `List<UserGroupViewModel>` | 分组信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupId` | `int` | 分组Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].GroupName` | `string` | 分组名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupIcon` | `string` | 标签Icon | 普通业务字段 | 可按问题需要提供 |
| `Data.UserTagName` | `string` | 标签对应的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCardStaff` | `UserCardStaffViewModel` | 销售顾问 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCardStaff.StaffId` | `int` | 店员Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserCardStaff.StaffImg` | `string` | 店员头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCardStaff.StaffName` | `string` | 店员名 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWay` | `SourceWayViewModel` | 来源渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWay.SourceType` | `int` | 1特定渠道 2指定会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWay.SourceWayId` | `long` | 渠道Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SourceWay.SourceWayName` | `string` | 来源渠道名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMustSignAgreement` | `bool` | 是否需要签署协议 true表示用户需要签署协议（还未签署） | 普通业务字段 | 可按问题需要提供 |
| `Data.IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`model.UserId < 0 && model.CardId < 1`；`string.IsNullOrEmpty(um.UserMobile)`；`resultMsg.State`；`model.CardId > 0`；`string.IsNullOrEmpty(ucModel.UserImg)`；`ucModel?.Id > 0 \|\| ucModel?.Uid > 0`；`sm.IsSignAgreement && !ucModel.IsSignAgreement`；`resultMsg.Data.StoreUserInfo?.Count > 0`；`item.ControlName == "user_tag_" + model.StoreId`；`item.ControlName == "user_photo_" + model.StoreId`；`!string.IsNullOrEmpty(item.ControlValue)`
- 一层业务调用：`UsersInfoProvider.UsersInfoGetByUid`、`StoreProvider.GetStoreByIdAsync`、`UserCardProvider.BusinessGetUserCardInfo`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UserCardProvider.BusinessGetUserCardBasisInfoByUid`、`StoreUserPermissionsProvider.UserInfoIsEncryption`、`UserCardChildProvider.GetUserCardChildViewByCardId`、`StoreUserProvider.GetStoreUserSimpleList`、`UserGroupProvider.GetUserGroupByCardId`、`StoreUserBlackListProvider.GetEnabledByStoreId`、`CustomUserTagProvider.GetUserTag`、`UserCardProvider.GetUserCardExpiredIntegral`、`TenantUserProvider.GetTenantUserByTenantId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 312`；`固定提示：参数格式错误！`；`固定提示：参数错误！`；`固定提示：无权限查看会员信息！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.check_user_by_mobile`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 检查体验手机号 |
| 使用时机 | 在商家视角中核对“检查体验手机号”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/CheckUserByMobile` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CheckExperienceUserResponseModel>>`；包装 `Task/DataResult`；Data `CheckExperienceUserResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:2212` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 && string.IsNullOrEmpty(model.UserMobile)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 && string.IsNullOrEmpty(model.UserMobile)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserMobile` | `string` | 参与组合校验 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 && string.IsNullOrEmpty(model.UserMobile)) | 当前会话提供并临时使用；不得持久化到模型历史 | 源码注释疑似与字段名冲突，待人工复核（原注释：课程Id）；个人信息；仅在当前授权场景按最小范围提供 |
| `Scene` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 场景 0普通获取信息 1预约校验；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.TipsType` | `int` | 提示类型：1已经是会员 2已经上过体验课 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 会员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.RemarkName` | `string` | 备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreUserInfo` | `List<StoreUserSimpleViewModel>` | 用户信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreUserInfo[].IsShow` | `bool` | 是否必填 0否 1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserTagName` | `string` | 标签对应的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCardStaff` | `UserCardStaffViewModel` | 销售顾问 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCardStaff.StaffId` | `int` | 店员Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserCardStaff.StaffImg` | `string` | 店员头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCardStaff.StaffName` | `string` | 店员名 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWay` | `SourceWayViewModel` | 来源渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWay.SourceType` | `int` | 1特定渠道 2指定会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWay.SourceWayId` | `long` | 渠道Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SourceWay.SourceWayName` | `string` | 来源渠道名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups` | `List<UserGroupViewModel>` | 分组信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupId` | `int` | 分组Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].GroupName` | `string` | 分组名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupIcon` | `string` | 标签Icon | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 && string.IsNullOrEmpty(model.UserMobile)`；`resultMsg.State`；`ucm?.Id > 0`；`string.IsNullOrEmpty(resultMsg.Data.UserImg)`；`ucm.StaffId > 0`；`tum?.Id > 0`；`ucm.SourceType > 0`；`ucm.SourceType == 1`；`swm?.Id > 0`；`ucm.SourceType == 2`；`userInfo?.Id > 0`；`model.Scene == 1`
- 一层业务调用：`UserCardProvider.BusinessGetUserCardByMobile`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UserGroupProvider.GetUserGroupByCardId`、`StoreUserProvider.GetStoreUserSimpleList`、`StoreProvider.GetStoreByIdAsync`、`TenantUserProvider.GetTenantUserByTenantId`、`StoreReservationProvider.GetStoreReservationFirstOrDefaultByCondition`、`UserCardChildProvider.GetUserCardChildByCardId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.check_user_mobile`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 检测手机号能否使用 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/CheckUserMobile` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CheckUserMobileResponseModel>>`；包装 `Task/DataResult`；Data `CheckUserMobileResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:1901` |
| C/B 对照 | capi.user.check_user_mobile |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if((model.OperationType == 1 && model.Uid == 0) \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；if((model.OperationType == 1 && model.Uid == 0) \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 店铺ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `OperationType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 应用场景：0注册 1修改；普通业务字段；可按问题需要提供 |
| `UserMobile` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.UserMobile)) | 当前会话提供并临时使用；不得持久化到模型历史 | 手机号；个人信息；仅在当前授权场景按最小范围提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsUse` | `bool` | 是否可以试用该手机号 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`(model.OperationType == 1 && model.Uid == 0) \|\| model.StoreId < 1`；`string.IsNullOrEmpty(model.UserMobile)`；`!StringOperate.IsNumber(model.UserMobile)`；`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 306`；`状态赋值：StatusCode = 307`；`固定提示：参数格式错误！`；`固定提示：手机号码不可为空`；`固定提示：手机号码格式不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_birthday_users_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取临近生日列表 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetBirthdayUsersList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetUserListV2ResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetUserListV2ResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:294` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `IsListSummary` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否仅返回会员列表展示所需的摘要数据；默认关闭以兼容旧调用方。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetUserListV2ResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 1正常 0停用 -1 销卡 -2管理员销卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsCount` | `long` | 已完成上课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards` | `List<UserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUserPhoto` | `bool` | 是否有用户的图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserTag` | `int` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Id` | `long` | 日志ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CancelInfo.CancelType` | `int` | 注销类型 0会员 1卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.OperationType` | `int` | 操作类型 0删除 1恢复 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.StoreUserName` | `string` | 店员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CreateDate` | `string` | 創建時間 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.GetBirthdayUsersList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_experience_users_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取体验人员列表 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetExperienceUsersList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetUserListV2ResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetUserListV2ResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:257` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `IsListSummary` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否仅返回会员列表展示所需的摘要数据；默认关闭以兼容旧调用方。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetUserListV2ResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 1正常 0停用 -1 销卡 -2管理员销卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsCount` | `long` | 已完成上课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards` | `List<UserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUserPhoto` | `bool` | 是否有用户的图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserTag` | `int` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Id` | `long` | 日志ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CancelInfo.CancelType` | `int` | 注销类型 0会员 1卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.OperationType` | `int` | 操作类型 0删除 1恢复 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.StoreUserName` | `string` | 店员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CreateDate` | `string` | 創建時間 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.GetExperienceUsersList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_low_balance_users_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取低余额会员列表 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetLowBalanceUsersList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetUserListV2ResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetUserListV2ResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:219` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `IsListSummary` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否仅返回会员列表展示所需的摘要数据；默认关闭以兼容旧调用方。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetUserListV2ResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 1正常 0停用 -1 销卡 -2管理员销卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsCount` | `long` | 已完成上课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards` | `List<UserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUserPhoto` | `bool` | 是否有用户的图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserTag` | `int` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Id` | `long` | 日志ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CancelInfo.CancelType` | `int` | 注销类型 0会员 1卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.OperationType` | `int` | 操作类型 0删除 1恢复 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.StoreUserName` | `string` | 店员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CreateDate` | `string` | 創建時間 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.GetLowBalanceUsersList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_user_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取用户详细信息（会员详情页） |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetUserInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：WorkWeChatProvider.ConvertAddWay |
| 返回 | `Task<DataResult<GetUserInfoResponseModel1>>`；包装 `Task/DataResult`；Data `GetUserInfoResponseModel1` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:365` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.UserId < 0 && model.CardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 会员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.WillExpireCardIntegral` | `int` | 即将过期的积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.WillExpireIntegralDate` | `int` | 即将过期的日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionIntegral` | `int` | 消费积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionPrint` | `int` | 消费印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 1正常 0停用 -1 销卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalMoney` | `decimal` | 会员消费统计 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalNum` | `long` | 会员消费次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumRenewal` | `decimal` | 充值金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumPrice` | `decimal` | 消费金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.RemarkName` | `string` | 备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsAssociatedUser` | `bool` | 是否已经关联用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOpenIntegral` | `bool` | 是否开启积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardExpiredIntegral` | `long` | 将过期积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardExpiredIntegralDate` | `string` | 积分过期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Commission` | `decimal` | 佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionCommission` | `decimal` | 提现/消费 佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards` | `List<UserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations` | `List<StoreUserOperationByUserViewModel>` | 管理员操作记录 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionId` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreUserOperations[].StoreUserId` | `int` | 管理员Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreUserOperations[].StoreUserName` | `string` | 管理员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreUserOperations[].UserType` | `int` | 用户权限：0超级管理员，1管理员，2运营人员 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionType` | `int` | 消费方式：0 计次，1 金额，2积分，3印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionTag` | `string` | 0支出 ，1充值,2 核减，3返还，4赠送，5付款 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserOperations[].ConsumptionValue` | `decimal` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo` | `List<StoreUserSimpleViewModel>` | 用户信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserInfo[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreUserInfo[].IsShow` | `bool` | 是否必填 0否 1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups` | `List<UserGroupViewModel>` | 分组信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupId` | `int` | 分组Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].GroupName` | `string` | 分组名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupIcon` | `string` | 标签Icon | 普通业务字段 | 可按问题需要提供 |
| `Data.UserTagName` | `string` | 标签对应的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffId` | `int` | 归属店员Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StaffName` | `string` | 归属店员 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAddWorkWeChat` | `bool` | 是否添加企业微信好友 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSendMessage` | `bool` | 是否可以发消息 false 代表不是你的联系人 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMustSignAgreement` | `bool` | 是否需要签署协议 true表示用户需要签署协议（还未签署） | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkWeChatInfo` | `UserWorkWeChatInfoViewModel` | 企微信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkWeChatInfo.WeChatName` | `string` | 微信昵称（来源于企业微信接口） | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkWeChatInfo.WorkWeChatAddWay` | `string` | 好友添加来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkWeChatInfo.ExternalUserId` | `string` | 外部联系人ID | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`model.UserId < 0 && model.CardId < 1`；`resultMsg.State`；`model.CardId > 0`；`string.IsNullOrEmpty(ucModel.UserImg)`；`ucModel?.Id > 0 \|\| ucModel?.Uid > 0`；`resultMsg.Data.StoreUserInfo?.Count > 0`；`item.ControlName == "user_tag_" + model.StoreId`；`item.ControlName == "user_photo_" + model.StoreId`；`!string.IsNullOrEmpty(item.ControlValue)`；`workUrs?.Count > 0`；`wechatInfo?.Id > 0`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`UserCardProvider.BusinessGetUserCardInfo`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UserCardProvider.BusinessGetUserCardBasisInfoByUid`、`StoreUserPermissionsProvider.UserInfoIsEncryption`、`UserCardChildProvider.GetUserCardChildViewByCardId`、`StoreUserProvider.GetStoreUserSimpleList`、`UserGroupProvider.GetUserGroupByCardId`、`CustomUserTagProvider.GetUserTag`、`WorkWechatUserRelationProvider.GetWorkWechatUserRelationByUid`、`WorkWechatAttentionProvider.GetWorkWechatAttentionByUid`、`TenantUserProvider.GetTenantUserByTenantId`、`UsersInfoProvider.UsersInfoGetByUid`、`WorkWeChatProvider.ConvertAddWay`、`UserCardProvider.GetUserCardExpiredIntegral`、`UserCardChildProvider.CheckChildCardPrice`
- 疑似副作用：`WorkWeChatProvider.ConvertAddWay`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`；`固定提示：参数错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_user_search_condition_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取单一会员搜索条件 |
| 使用时机 | 在商家视角中核对“获取单一会员搜索条件”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetUserSearchConditionInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetUserSearchConditionInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetUserSearchConditionInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:1100` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.Id < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.Id < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.Id < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 搜索条件Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ConditionTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionDescribe` | `string` | 搜索描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo` | `UserSearchConditionViewModel` | 搜索内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.KeyWord` | `string` | 搜索关键词 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.StaffId` | `int` | 归属店员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ConditionInfo.HaveMoney` | `int` | 余额0 all 1有 2无 3有余额未续费 10固定剩余金额，11固定剩余次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.Validity` | `int` | -1-有效，-2-过期，>0-几天内过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardState` | `int?` | 0未开卡 1开卡 3停卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.Sex` | `int` | 性别0 all 1男 2女 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.IsNoCard` | `bool` | 是否无卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.PrepaidPrice` | `decimal` | 储值的金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.OpenCardDateStart` | `string` | 开卡日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.OpenCardDateEnd` | `string` | 开卡日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionDateStart` | `string` | 消费日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionDateEnd` | `string` | 消费日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsDateStart` | `string` | 上课日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsDateEnd` | `string` | 上课日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionMoneyMin` | `decimal?` | 消费金额开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionMoneyMax` | `decimal?` | 消费金额结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsCountStart` | `int?` | 上课次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsCountEnd` | `int?` | 上课次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ActivateVipDay` | `int?` | 活跃天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.NotActivateVipDay` | `int?` | 沉睡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.TagIds` | `List<int>` | 标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.GroupIds` | `List<int>` | 分组集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.PrepaidCardIds` | `List<int>` | 储值卡Id | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardPriceMin` | `decimal?` | 剩余金额开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardPriceMax` | `decimal?` | 剩余金额结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardSumMin` | `decimal?` | 剩余次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardSumMax` | `decimal?` | 剩余次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardLessonsMin` | `decimal?` | 剩余课时开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardLessonsMax` | `decimal?` | 剩余课时结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionSumMin` | `decimal?` | 消费次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionSumMax` | `decimal?` | 消费次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionSumMin` | `decimal?` | 卡内消费次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionSumMax` | `decimal?` | 卡内消费次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ByCardMoneyMin` | `decimal?` | 累计储值开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ByCardMoneyMax` | `decimal?` | 累计储值结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionMoneyMin` | `decimal?` | 卡内消费开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionMoneyMax` | `decimal?` | 卡内消费结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionLastDateStart` | `string` | 最后消费日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionLastDateEnd` | `string` | 最后消费日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ValidityDateStart` | `string` | 有效期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ValidityDateEnd` | `string` | 有效期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardIntegralMin` | `int?` | 剩余积分开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardIntegralMax` | `int?` | 剩余积分结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShare` | `bool` | 是否所有人可见 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 -1删除 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.Id < 1`；`resultMsg.State`；`condition?.Id > 0`
- 一层业务调用：`UserSearchConditionProvider.GetUserSearchConditionById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_user_search_condition_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取会员搜索条件列表 |
| 使用时机 | 在商家视角中核对“获取会员搜索条件列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetUserSearchConditionList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataList<GetUserSearchConditionListResponseModel>>`；包装 `Task/DataList`；Data `GetUserSearchConditionListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:1064` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ConditionTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ConditionDescribe` | `string` | 搜索描述 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsShare` | `bool` | 是否所有人可见 | 普通业务字段 | 可按问题需要提供 |
| `Data[].State` | `int` | 状态 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserSearchConditionProvider.GetUserSearchConditionList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_user_sign_agreement_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 签署合同 |
| 使用时机 | 在商家视角中核对“签署合同”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetUserSignAgreementInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetUserSignAgreementInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetUserSignAgreementInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:1732` |
| C/B 对照 | capi.user.get_user_sign_agreement_info |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreAgreement` | `string` | 会员协议 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSignAgreement` | `bool` | 是否签署 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignImage` | `string` | 签字图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignDate` | `string` | 签字时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.CardId < 1`；`uam?.CardId > 0`；`sam == null \|\| sam.Id == 0`；`sam?.Id > 0`
- 一层业务调用：`UserCardAgreementProvider.GetUserCardAgreementFirstOrDefaultByCondition`、`StoreAgreementProvider.GetStoreAgreementFirstOrDefaultByCondition`、`StoreAgreementProvider.GetNewStoreAgreement`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_users_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取会员列表 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetUsersList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataList<GetUserListResponseModel>>`；包装 `Task/DataList`；Data `GetUserListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:144` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `SearchCondition` | `UserSearchConditionViewModel` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索条件；普通业务字段；可按问题需要提供 |
| `SearchCondition.KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键词；普通业务字段；可按问题需要提供 |
| `SearchCondition.StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 归属店员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SearchCondition.HaveMoney` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 余额0 all 1有 2无 3有余额未续费 10固定剩余金额，11固定剩余次数；普通业务字段；可按问题需要提供 |
| `SearchCondition.Validity` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | -1-有效，-2-过期，>0-几天内过期；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardState` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 只能使用文档确认的枚举值 | 0未开卡 1开卡 3停卡；普通业务字段；可按问题需要提供 |
| `SearchCondition.Sex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 性别0 all 1男 2女；普通业务字段；可按问题需要提供 |
| `SearchCondition.IsNoCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否无卡；普通业务字段；可按问题需要提供 |
| `SearchCondition.PrepaidPrice` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 储值的金额；普通业务字段；可按问题需要提供 |
| `SearchCondition.OpenCardDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.OpenCardDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionMoneyMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionMoneyMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsCountStart` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsCountEnd` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ActivateVipDay` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 活跃天数；普通业务字段；可按问题需要提供 |
| `SearchCondition.NotActivateVipDay` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 沉睡天数；普通业务字段；可按问题需要提供 |
| `SearchCondition.TagIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 只能使用文档确认的枚举值 | 标签集合；普通业务字段；可按问题需要提供 |
| `SearchCondition.GroupIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 分组集合；普通业务字段；可按问题需要提供 |
| `SearchCondition.PrepaidCardIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 储值卡Id；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardPriceMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余金额开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardPriceMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余金额结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardLessonsMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余课时开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardLessonsMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余课时结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ByCardMoneyMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 累计储值开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ByCardMoneyMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 累计储值结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionMoneyMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionMoneyMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionLastDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 最后消费日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionLastDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 最后消费日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ValidityDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 有效期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ValidityDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 有效期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardIntegralMin` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardIntegralMax` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分结束；普通业务字段；可按问题需要提供 |
| `DataType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 数据类型 0全部 1已加企微好友，0未加企微好友；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].TotalMoney` | `string` | 剩余总金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].TotalTimes` | `string` | 剩余总次数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UserTag` | `string` | 用户的标签 类型；1-红，2-蓝，3-绿，4-粉，0-灰 | 普通业务字段 | 可按问题需要提供 |
| `Data[].StaffName` | `string` | 归属店员 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsAddWorkWeChat` | `bool` | 是否添加企业微信好友 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsSendMessage` | `bool` | 是否可以发消息 false 代表不是你的联系人 | 普通业务字段 | 可按问题需要提供 |
| `Data[].WorkWeChatInfo` | `UserWorkWeChatInfoViewModel` | 企微信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].WorkWeChatInfo.WeChatName` | `string` | 微信昵称（来源于企业微信接口） | 普通业务字段 | 可按问题需要提供 |
| `Data[].WorkWeChatInfo.WorkWeChatAddWay` | `string` | 好友添加来源 | 普通业务字段 | 可按问题需要提供 |
| `Data[].WorkWeChatInfo.ExternalUserId` | `string` | 外部联系人ID | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_users_list_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取会员列表V2 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetUsersListV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetUserListV2ResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetUserListV2ResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:179` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `IsListSummary` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否仅返回会员列表展示所需的摘要数据；默认关闭以兼容旧调用方。；普通业务字段；可按问题需要提供 |
| `SearchCondition` | `UserSearchConditionViewModel` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索条件；普通业务字段；可按问题需要提供 |
| `SearchCondition.KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键词；普通业务字段；可按问题需要提供 |
| `SearchCondition.StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 归属店员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SearchCondition.HaveMoney` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 余额0 all 1有 2无 3有余额未续费 10固定剩余金额，11固定剩余次数；普通业务字段；可按问题需要提供 |
| `SearchCondition.Validity` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | -1-有效，-2-过期，>0-几天内过期；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardState` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 只能使用文档确认的枚举值 | 0未开卡 1开卡 3停卡；普通业务字段；可按问题需要提供 |
| `SearchCondition.Sex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 性别0 all 1男 2女；普通业务字段；可按问题需要提供 |
| `SearchCondition.IsNoCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否无卡；普通业务字段；可按问题需要提供 |
| `SearchCondition.PrepaidPrice` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 储值的金额；普通业务字段；可按问题需要提供 |
| `SearchCondition.OpenCardDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.OpenCardDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionMoneyMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionMoneyMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsCountStart` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.LessonsCountEnd` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 上课次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ActivateVipDay` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 活跃天数；普通业务字段；可按问题需要提供 |
| `SearchCondition.NotActivateVipDay` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 沉睡天数；普通业务字段；可按问题需要提供 |
| `SearchCondition.TagIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 只能使用文档确认的枚举值 | 标签集合；普通业务字段；可按问题需要提供 |
| `SearchCondition.GroupIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 分组集合；普通业务字段；可按问题需要提供 |
| `SearchCondition.PrepaidCardIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 储值卡Id；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardPriceMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余金额开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardPriceMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余金额结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardLessonsMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余课时开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardLessonsMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余课时结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费次数开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费次数结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ByCardMoneyMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 累计储值开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ByCardMoneyMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 累计储值结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionMoneyMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardConsumptionMoneyMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 卡内消费结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionLastDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 最后消费日期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ConsumptionLastDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 最后消费日期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.ValidityDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 有效期开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.ValidityDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 有效期结束；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardIntegralMin` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分开始；普通业务字段；可按问题需要提供 |
| `SearchCondition.CardIntegralMax` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分结束；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetUserListV2ResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 1正常 0停用 -1 销卡 -2管理员销卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsCount` | `long` | 已完成上课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards` | `List<UserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUserPhoto` | `bool` | 是否有用户的图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserTag` | `int` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Id` | `long` | 日志ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CancelInfo.CancelType` | `int` | 注销类型 0会员 1卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.OperationType` | `int` | 操作类型 0删除 1恢复 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.StoreUserName` | `string` | 店员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CreateDate` | `string` | 創建時間 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`!string.IsNullOrEmpty(model.SearchCondition.KeyWord)`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.BusinessGetUserCardsV2`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user.get_users_sum`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取会员数量统计 |
| 使用时机 | 在商家视角中核对“获取会员数量统计”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/User/GetUsersSum` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetUserSumResponseModel>>`；包装 `Task/DataResult`；Data `GetUserSumResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserController.cs:331` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.AllUserSum` | `long` | 全部会员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.ContactUserSum` | `long` | 以加好友数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoContactUserSum` | `long` | 未加好友数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.CrmGetUserSum`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user_group.get_group`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取会员分组 |
| 使用时机 | 在商家视角中核对“获取会员分组”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/UserGroup/GetGroup` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetGroupResponseModel>>`；包装 `Task/DataResult`；Data `GetGroupResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserGroupController.cs:61` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.GroupId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.GroupId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `GroupId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.GroupId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 分组Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsCardGroup` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否是卡分组；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupId` | `int` | 分组Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.GroupName` | `string` | 分组名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users` | `List<GroupUsersViewModel>` | 会员卡列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].UserId` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].UserName` | `string` | 会员名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].UserImg` | `string` | 头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0 \|\| model.GroupId < 1`；`resultMsg.State`；`model.IsCardGroup`；`groupInfo?.Id > 0`
- 一层业务调用：`StoreCardProvider.GetStoreCardByGroupInfo`、`StoreGroupProvider.GetStoreGroupById`、`UserGroupProvider.GetUserInfoByGroupId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.user_group.get_group_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员与身份 |
| 用途 | 获取商家会员分组列表 |
| 使用时机 | 在商家视角中核对“获取商家会员分组列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/UserGroup/GetGroupList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetGroupListResponseModel>>`；包装 `Task/DataResult`；Data `GetGroupListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/UserGroupController.cs:23` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsStatisticsPeople` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否统计人数，如无需统计人数可不传；普通业务字段；可按问题需要提供 |
| `IsHideCardGroup` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否隐藏卡分组；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups` | `List<StoreGroupViewModel>` | 分组列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].Id` | `int` | 分组Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].TagGroupId` | `int` | tag_group_id 标签总id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].GroupIcon` | `string` | 分组Icon | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupName` | `string` | 分组名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].PeopleSum` | `long` | 总人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].PriceSum` | `decimal` | 总余额 仅储值卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardType` | `int` | 卡类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].Items` | `IList<CardItemSummaryViewModel>` | 项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].Items[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].Items[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].Items[].Value` | `decimal` | 次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo` | `GetStoreCardByGroupListResponseModel` | 会员卡分组的附加信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.PrepaidCardId` | `int` | 卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].CardGroupInfo.PeopleCount` | `long` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.CardName` | `string` | 卡名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.CardImg` | `string` | 卡图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.ValidityDate` | `int` | 有效期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.Price` | `decimal` | 金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.DiscountPrice` | `decimal` | 折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.CardDiscount` | `decimal` | 卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.CardType` | `int` | 卡类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.CardValue` | `decimal` | 卡价值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].CardGroupInfo.CardGivingValue` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].UnitPrice` | `decimal` | 单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].PrepaidCardId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`
- 一层业务调用：`StoreGroupProvider.GetStoreGroupByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_card_info_by_number`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 通过会员卡号 获取CardId 和UID |
| 使用时机 | 在商家视角中核对“通过会员卡号 获取CardId 和UID”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetCardInfoByNumber` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCardInfoByNumberResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCardInfoByNumberResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:1071` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardNumber` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.CardNumber)) | 当前会话提供并临时使用；不得持久化到模型历史 | 会员卡编号；个人信息；仅在当前授权场景按最小范围提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardId` | `int` | 会员 卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserId` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ChildCardId` | `int` | 子卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`string.IsNullOrEmpty(model.CardNumber)`；`resultMsg.State`；`childCardModel?.CardId > 0`
- 一层业务调用：`UserCardChildProvider.GetCardInfoByNumber`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数不正确`；`固定提示：会员卡不存在`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_message_template`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取短信模板 |
| 使用时机 | 在商家视角中核对“获取短信模板”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetMessageTemplate` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetMessageTemplateResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetMessageTemplateResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:691` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChildCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员子卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserMobile` | `string` | 客户手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserName` | `string` | 用户名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LinkUrl` | `string` | 短信模板短链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendMessageDate` | `string` | 发送短信日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSentMessage` | `bool` | 是否已经发送短信 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.ChildCardId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.BusinessGetMessageTemplate`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_user_card_balance`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取会员卡余额信息（调整余额页面） |
| 使用时机 | 已定位会员和卡后，核对商家端单张卡详情、余额、操作记录或可管理状态。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUserCardBalance` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserCardBalanceResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserCardBalanceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:280` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.ChildCardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 主卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChildCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CardId < 1 \\|\\| model.ChildCardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 子卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 会员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Card` | `UserCardChildViewModel` | 会员卡信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Card.PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Card.CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Card.CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Card.Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Card.UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.CardId < 1 \|\| model.ChildCardId < 1`；`resultMsg.State`；`ucModel?.Id > 0`
- 一层业务调用：`UserCardProvider.BusinessGetUserCardBasisInfo`、`StoreUserPermissionsProvider.UserInfoIsEncryption`、`UserCardChildProvider.GetViewUserCardChildById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数格式错误！`；`固定提示：会员卡不存在！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_user_card_by_key`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 根据兑换秘钥获取会员卡 |
| 使用时机 | 已定位会员和卡后，核对商家端单张卡详情、余额、操作记录或可管理状态。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUserCardByKey` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserCardByKeyResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserCardByKeyResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:211` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| string.IsNullOrEmpty(model.SecretKey)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SecretKey` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| string.IsNullOrEmpty(model.SecretKey)) | 不得由模型提供 | 秘钥；密钥/凭据；禁止原样进入模型 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserId` | `int` | uid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardChildId` | `int` | 子卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PreCardId` | `int` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| string.IsNullOrEmpty(model.SecretKey)`；`resultMsg.State`；`!(spm?.Id > 0)`；`spm.State == 1`；`spm.State == 2 && spm.StoreId != model.StoreId`
- 一层业务调用：`StorePhysicalCardProvider.GetStorePhysicalCardByKey`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 303`；`状态赋值：StatusCode = 304`；`状态赋值：StatusCode = 305`；`固定提示：参数不正确`；`固定提示：二维码无效`；`固定提示：该卡未被使用`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_user_card_child_qr_code`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取单个会员子卡的二维码 |
| 使用时机 | 已定位会员和卡后，核对商家端单张卡详情、余额、操作记录或可管理状态。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUserCardChildQrCode` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserCardChildQrCodeResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserCardChildQrCodeResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:789` |
| C/B 对照 | capi.card.get_user_card_child_qr_code |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChildCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员子卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.QrCode` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.ChildCardId < 1`；`resultMsg.State`；`Constant.Environment.Equals(EnvironmentEnum.PRO.GetEnumText())`；`Constant.Environment.Equals(EnvironmentEnum.DEMO.GetEnumText())`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_user_card_operation_log`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 停卡记录（时效卡） |
| 使用时机 | 已定位会员和卡后，核对商家端单张卡详情、余额、操作记录或可管理状态。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUserCardOperationLog` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUserCardOperationLogResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUserCardOperationLogResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:841` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChildCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ChildCardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 停的子卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs` | `List<UserCardOperationLogViewModel>` | 停卡操作记录 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].UserCardOperationLogId` | `int` | 时限卡停卡操作ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Logs[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Logs[].ChildCardId` | `int` | 会员子卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Logs[].StopFrequency` | `string` | 停卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].StopCardDays` | `string` | 停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].StopDescribe` | `string` | 停卡描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].LogDates` | `List<UserCardOperationLogDateViewModel>` | 停卡时间 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].LogDates[].LogDate` | `string` | 日志时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].LogDates[].LogType` | `string` | 日志类型 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.ChildCardId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardOperationLogProvider.GetUserCardOperationLog`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_user_cards`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取用户会员卡列表(会员首页,筛选页) |
| 使用时机 | 商家核对会员卡列表、筛选结果、余额、有效期和卡状态时使用；复现单个顾客页面还需对照 C 端接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUserCards` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetUserCardsResponseViewModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetUserCardsResponseViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:32` |
| C/B 对照 | capi.card.get_user_cards |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CardState` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 卡状态 0全部 1卡过期但有余额，2卡有效且有余额，3消费完未续费；普通业务字段；可按问题需要提供 |
| `KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键词；普通业务字段；可按问题需要提供 |
| `Validity` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 有效期0 all 1有效 2过期 -1-有效，-2-过期，>0-几天内过期；普通业务字段；可按问题需要提供 |
| `Sex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 性别0 all 1男 2女；普通业务字段；可按问题需要提供 |
| `HaveMony` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 余额0 all 1有 2无；普通业务字段；可按问题需要提供 |
| `ConsumptionDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期开始；普通业务字段；可按问题需要提供 |
| `ConsumptionDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期结束；普通业务字段；可按问题需要提供 |
| `OpenCardDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期开始；普通业务字段；可按问题需要提供 |
| `OpenCardDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期结束；普通业务字段；可按问题需要提供 |
| `ConsumptionMonyMin` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额开始；普通业务字段；可按问题需要提供 |
| `ConsumptionMonyMax` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额结束；普通业务字段；可按问题需要提供 |
| `ConsumptionSumMin` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数开始；普通业务字段；可按问题需要提供 |
| `ConsumptionSumMax` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数结束；普通业务字段；可按问题需要提供 |
| `CardSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数开始；普通业务字段；可按问题需要提供 |
| `CardSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数结束；普通业务字段；可按问题需要提供 |
| `CardIntegralMin` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分开始；普通业务字段；可按问题需要提供 |
| `CardIntegralMax` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分结束；普通业务字段；可按问题需要提供 |
| `GroupIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 分组Id；普通业务字段；可按问题需要提供 |
| `CardGroupIds` | `List<int>` | 可选/有默认值 | 绑定=ApiController推断；默认值=new List<int>() | AI 可按业务问题提供；仍需服务端 Schema 校验 | 会员卡分组id；普通业务字段；可按问题需要提供 |
| `WhereServices` | `List<ViewModel.MarketingCenter.MarketingUserWhereServicesViewModel>` | 可选/有默认值 | 绑定=ApiController推断；默认值=new List<ViewModel.MarketingCenter.MarketingUserWhereServicesViewModel>() | AI 可按业务问题提供；仍需服务端 Schema 校验 | 服务条件列表；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 项目ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `WhereServices[].ItemName` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目名称；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemUnit` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目单位；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemValueMin` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=-1 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目最小值；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemValueMax` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=-1 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目最大值；普通业务字段；可按问题需要提供 |
| `SearchPlace` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 查询的位置 0-好友，1-续费，2-老客，3-短信；普通业务字段；可按问题需要提供 |
| `PrepaidPrice` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 储值的金额；普通业务字段；可按问题需要提供 |
| `IsNoCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否无卡；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetUserCardsResponseViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 1正常 0停用 -1 销卡 -2管理员销卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards` | `List<UserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUserPhoto` | `bool` | 是否有用户的图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserTag` | `int` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Id` | `long` | 日志ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CancelInfo.CancelType` | `int` | 注销类型 0会员 1卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.OperationType` | `int` | 操作类型 0删除 1恢复 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.StoreUserName` | `string` | 店员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CreateDate` | `string` | 創建時間 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CancelInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CancelInfo.CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`!string.IsNullOrEmpty(model.KeyWord)`；`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.BusinessGetUserCards`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_user_cards_by_marketing`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 筛选会员 |
| 使用时机 | 商家核对会员卡列表、筛选结果、余额、有效期和卡状态时使用；复现单个顾客页面还需对照 C 端接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUserCardsByMarketing` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<UserInfoJJViewModel>>>`；包装 `Task/DataResult`；Data `PageData<UserInfoJJViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:67` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CardState` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 卡状态 0全部 1卡过期但有余额，2卡有效且有余额，3消费完未续费；普通业务字段；可按问题需要提供 |
| `KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键词；普通业务字段；可按问题需要提供 |
| `Validity` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 有效期0 all 1有效 2过期 -1-有效，-2-过期，>0-几天内过期；普通业务字段；可按问题需要提供 |
| `Sex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 性别0 all 1男 2女；普通业务字段；可按问题需要提供 |
| `HaveMony` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 余额0 all 1有 2无；普通业务字段；可按问题需要提供 |
| `ConsumptionDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期开始；普通业务字段；可按问题需要提供 |
| `ConsumptionDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期结束；普通业务字段；可按问题需要提供 |
| `OpenCardDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期开始；普通业务字段；可按问题需要提供 |
| `OpenCardDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期结束；普通业务字段；可按问题需要提供 |
| `ConsumptionMonyMin` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额开始；普通业务字段；可按问题需要提供 |
| `ConsumptionMonyMax` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额结束；普通业务字段；可按问题需要提供 |
| `ConsumptionSumMin` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数开始；普通业务字段；可按问题需要提供 |
| `ConsumptionSumMax` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数结束；普通业务字段；可按问题需要提供 |
| `CardSumMin` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数开始；普通业务字段；可按问题需要提供 |
| `CardSumMax` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数结束；普通业务字段；可按问题需要提供 |
| `CardIntegralMin` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分开始；普通业务字段；可按问题需要提供 |
| `CardIntegralMax` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分结束；普通业务字段；可按问题需要提供 |
| `GroupIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 分组Id；普通业务字段；可按问题需要提供 |
| `CardGroupIds` | `List<int>` | 可选/有默认值 | 绑定=ApiController推断；默认值=new List<int>() | AI 可按业务问题提供；仍需服务端 Schema 校验 | 会员卡分组id；普通业务字段；可按问题需要提供 |
| `WhereServices` | `List<ViewModel.MarketingCenter.MarketingUserWhereServicesViewModel>` | 可选/有默认值 | 绑定=ApiController推断；默认值=new List<ViewModel.MarketingCenter.MarketingUserWhereServicesViewModel>() | AI 可按业务问题提供；仍需服务端 Schema 校验 | 服务条件列表；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 项目ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `WhereServices[].ItemName` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目名称；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemUnit` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目单位；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemValueMin` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=-1 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目最小值；普通业务字段；可按问题需要提供 |
| `WhereServices[].ItemValueMax` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=-1 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目最大值；普通业务字段；可按问题需要提供 |
| `SearchPlace` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 查询的位置 0-好友，1-续费，2-老客，3-短信；普通业务字段；可按问题需要提供 |
| `PrepaidPrice` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 储值的金额；普通业务字段；可按问题需要提供 |
| `IsNoCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否无卡；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<UserInfoJJViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Uid` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 用户昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Services` | `List<UserCardServiceItemJJViewModel>` | 项目信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`!string.IsNullOrEmpty(model.KeyWord)`；`model.Uid == 0 \|\| model.StoreId < 1`；`model.PageSize == 0`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.BusinessGetUserCardsByMarketing`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_user_cards_by_source_way`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取会员渠道 |
| 使用时机 | 商家核对会员卡列表、筛选结果、余额、有效期和卡状态时使用；复现单个顾客页面还需对照 C 端接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUserCardsBySourceWay` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetUserCardsBySourceWayResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetUserCardsBySourceWayResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:1673` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SourceType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | source_type 来源类型 0无 1特定渠道 1其他会员推荐；普通业务字段；可按问题需要提供 |
| `SourceWayId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 渠道Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetUserCardsBySourceWayResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Uid` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 用户昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].SourceType` | `int` | source_type 来源类型 0无 1特定渠道 1其他会员推荐 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SourceWayId` | `long` | 渠道Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].SourceWayName` | `string` | 来源渠道名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`model.PageSize == 0`；`resultMsg.State`；`ums != null && ums.Data?.Count > 0`；`cm.CardId > 0`；`cm.Uid > 0`；`cm.SourceType == 1`；`swm?.Id > 0`；`cm.SourceType == 2`
- 一层业务调用：`UserCardProvider.BusinessGetUserCardsByStore`、`StoreUserSourceWayExtendProvider.GetStoreUserSourceWayExtendElementsByCondition`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UsersInfoProvider.UsersInfoGetByUid`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.business_get_users`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取会员列表 |
| 使用时机 | 在商家视角中核对“获取会员列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/BusinessGetUsers` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetUsersResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetUsersResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:614` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardState` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 卡状态 0全部 1卡过期但有余额，2卡有效且有余额，3消费完未续费；普通业务字段；可按问题需要提供 |
| `KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键词；普通业务字段；可按问题需要提供 |
| `Validity` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 有效期0 all 1有效 2过期；普通业务字段；可按问题需要提供 |
| `Sex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 性别0 all 1男 2女；普通业务字段；可按问题需要提供 |
| `HaveMony` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 余额0 all 1有 2无；普通业务字段；可按问题需要提供 |
| `ConsumptionDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期开始；普通业务字段；可按问题需要提供 |
| `ConsumptionDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消费日期结束；普通业务字段；可按问题需要提供 |
| `OpenCardDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期开始；普通业务字段；可按问题需要提供 |
| `OpenCardDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开卡日期结束；普通业务字段；可按问题需要提供 |
| `ConsumptionMonyMin` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额开始；普通业务字段；可按问题需要提供 |
| `ConsumptionMonyMax` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费金额结束；普通业务字段；可按问题需要提供 |
| `ConsumptionSumMin` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数开始；普通业务字段；可按问题需要提供 |
| `ConsumptionSumMax` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 消费次数结束；普通业务字段；可按问题需要提供 |
| `CardSumMin` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数开始；普通业务字段；可按问题需要提供 |
| `CardSumMax` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余次数结束；普通业务字段；可按问题需要提供 |
| `CardIntegralMin` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分开始；普通业务字段；可按问题需要提供 |
| `CardIntegralMax` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 剩余积分结束；普通业务字段；可按问题需要提供 |
| `GroupIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 分组Id；普通业务字段；可按问题需要提供 |
| `CardGroupIds` | `List<int>` | 可选/有默认值 | 绑定=ApiController推断；默认值=new List<int>() | AI 可按业务问题提供；仍需服务端 Schema 校验 | 会员卡分组id；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas` | `List<BusinessGetUsersItemModel>` | 用户分组数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].NameGroup` | `string` | 会员名首字母 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users` | `List<BusinessGetUsersJJViewModel>` | 会员列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Datas[].Users[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Datas[].Users[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Datas[].Users[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Datas[].Users[].TotalMoney` | `string` | 剩余总金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users[].TotalTimes` | `string` | 剩余总次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users[].CardCount` | `int` | 会员卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users[].UserTag` | `int` | 用户的标签 类型；1-红，2-蓝，3-绿，4-粉，0-灰 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users[].IsUserPhoto` | `bool` | 是否有照片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Datas[].Users[].IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.BusinessGetUsers`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.get_card_group_users_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 会员卡分组会员列表 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/GetCardGroupUsersList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<GetCardGroupUsersListResponseModel>>>`；包装 `Task/DataResult`；Data `List<GetCardGroupUsersListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:1191` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].NameGroup` | `string` | 会员名首字母 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List` | `List<GetCardGroupUsersViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].List[].StoreId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].List[].CardId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].List[].Uid` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].List[].CardPrice` | `decimal` | 剩余的次数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].ValidityDate` | `DateTime?` | 有效期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].ValidityDateStr` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].CardType` | `int` | 类型：0计次，1储值 2 限时卡 3权益卡 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].CardTag` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].RemarkName` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].List[].NameGroup` | `string` | 首字母 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].PriceSum` | `decimal` | 剩余的金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].List[].UserImg` | `string` | 头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.PrepaidCardId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.GetCardGroupUsersList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.get_source_way`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 会员来源渠道列表 |
| 使用时机 | 在商家视角中核对“会员来源渠道列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/GetSourceWay` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetSourceWayResponseModel>>`；包装 `Task/DataResult`；Data `GetSourceWayResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:1229` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWays` | `List<SourceWayResponseModel>` | 来源数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWays[].Id` | `long` | 来源渠道ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SourceWays[].SourceWayName` | `string` | 来源渠道名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceWays[].IsCanDelete` | `bool` | 是否可以删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`sourceWays?.Count > 0`
- 一层业务调用：`StoreUserSourceWayExtendProvider.GetStoreUserSourceWayExtendElementsByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.get_source_way_user_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 统计来源渠道 |
| 使用时机 | 在商家视角中核对“统计来源渠道”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/GetSourceWayUserStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetSourceWayUserStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `GetSourceWayUserStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:1575` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalData` | `List<SourceWayUserStatisticalResponseModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalData[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalData[].SourceType` | `int` | source_type 来源类型 0无 1特定渠道 1其他会员推荐 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalData[].SourceWayId` | `long` | 渠道Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StatisticalData[].SourceWayName` | `string` | 来源渠道名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalData[].UserSum` | `long` | 会员数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`cardStatistical?.Count > 0`；`sourceWays?.Count > 0`；`um != null`；`item.SourceWayId > 0`；`noWay != null && noWay.UserSum>0`
- 一层业务调用：`UserCardProvider.GetSourceWayUserStatistical`、`StoreUserSourceWayExtendProvider.GetStoreUserSourceWayExtendElementsByCondition`、`UserCardProvider.BusinessGetUserCardBasisInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.card.get_staff_user_card_count`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取会籍顾问下会员数量 |
| 使用时机 | 已定位会员和卡后，核对商家端单张卡详情、余额、操作记录或可管理状态。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Card/GetStaffUserCardCount` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffUserCardCountResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffUserCardCountResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CardController.cs:1533` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.StaffId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.StaffId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.StaffId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 店员Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCount` | `long` | 会籍顾问下店员数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.StaffId < 1`；`resultMsg.State`；`TenantUserProvider.Instance.GetTenantUserByStoreId(model.StoreId).Any(x => x.Id == model.StaffId)`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`UserCardProvider.GetStaffUserCardCount`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.business_get_prepaid_card_qr_code`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取购买储值卡的二维码 |
| 使用时机 | 在商家视角中核对“获取购买储值卡的二维码”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/BusinessGetPrepaidCardQrCode` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetPrepaidCardQrCodeResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetPrepaidCardQrCodeResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:1370` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCardQrCode` | `string` | 购买储值卡二维码路径 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.PrepaidCardId < 1`；`resultMsg.State`；`Constant.Environment.Equals(EnvironmentEnum.PRO.GetEnumText())`；`Constant.Environment.Equals(EnvironmentEnum.DEMO.GetEnumText())`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.business_get_store_card`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取商户默认会员卡 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/BusinessGetStoreCard` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreCardResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreCardResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:25` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardId` | `int` | 卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardName` | `string` | 卡片名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`scModel?.Id > 0`
- 一层业务调用：`StoreCardProvider.GetStoreCardViewModelByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.business_get_store_prepaid_card`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取商家单个充值卡 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/BusinessGetStorePrepaidCard` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStorePrepaidCardResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStorePrepaidCardResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:300` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.PrepaidCardId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.PrepaidCardId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.PrepaidCardId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardImg` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardValue` | `decimal` | 卡价值 次/金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardGivingValue` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardCount` | `int` | 库存剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaxCount` | `int` | 最大销售数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.OpenCardType` | `int` | 开卡方式 0购买即开卡，1首次使用开卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.StopCardDays` | `int` | 每次最多停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.OpenCardDaysMax` | `int` | 最大延迟开卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPayAfterReceive` | `bool` | 是否支付后领取 | 普通业务字段 | 可按问题需要提供 |
| `Data.ValidType` | `int` | 失效日期类型: 0 相对日期（购卡后N天； 1 绝对日期（指定失效日期） | 普通业务字段 | 可按问题需要提供 |
| `Data.InvalidDate` | `string` | 失效日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.ValidityDate` | `int` | 有效期 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 1在售 0停售 -1删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsBuyOnce` | `bool` | 是否只能购买一次 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.SingleMaxFrequency` | `int` | 单次最大次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.DayMaxFrequency` | `int` | 每日最大核销次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.DayMaxReservation` | `int` | day_max_frequency 每日最大预约次数 0不限 (1限制代约.) | 普通业务字段 | 可按问题需要提供 |
| `Data.WeekMaxReservation` | `int` | week_max_frequency 每周最大预约次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.MouthMaxReservation` | `int` | mouth_max_frequency 每月最大预约次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardInstructions` | `string` | 会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPromptRights` | `bool` | 是否强制提示会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services` | `List<StorePrepaidCardServiceItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].PrepaidCardId` | `int` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].CardValue` | `decimal` | 服务项 卡价值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].IsGift` | `bool` | 是否是赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAuditing` | `bool` | 该会员卡是否需要审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.ShareQrCode` | `string` | 领卡二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCustomPrice` | `bool` | 是否开启自定义金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomPriceMin` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomPriceMax` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomPriceGiveRatio` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.TransferMaxCount` | `int` | 转让卡次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList` | `List<CardTimeEditModel>` | 会员卡有效期集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardTimeList[].ValidType` | `int` | 失效日期类型: 0 相对日期（购卡后N天)； 1 绝对日期（指定失效日期） | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].InvalidDate` | `string` | 失效日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].ValidityDate` | `int` | 有效期单位天 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].MaxCount` | `int` | 最大可售出数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].CardCount` | `int` | 卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].IsBuyOnce` | `bool` | 是否只能购买一次 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].State` | `int` | 状态；1-在售，0-停售，-1-删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].GivePrice` | `decimal` | 赠送金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].StartDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardTimeList[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsLimitTime` | `bool` | 是否限制可用时段 | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedData` | `List<UsedWeekViewModel>` | 允许使用时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedData[].Id` | `long` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UsedData[].UsedWeekInfo` | `string` | 营业星期（1,2,7） | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedData[].UsedTimeInfo` | `List<UsedTimeViewModel>` | 营业星期（1,2,7） | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedData[].UsedTimeInfo[].Id` | `long` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UsedData[].UsedTimeInfo[].BeginTime` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedData[].UsedTimeInfo[].EndTime` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0 \|\| model.PrepaidCardId == 0`；`resultMsg.State`；`scModel?.Id > 0`；`item.EndDate.Date < DateTime.Now.Date \|\| item.StartDate.Date > DateTime.Now.Date`；`i==1`；`scModel.IsLimitTime`；`weeks?.Count() > 0`
- 一层业务调用：`StorePrepaidCardProvider.GetStorePrepaidCardAll`、`StorePrepaidCardChildProvider.GetStorePrepaidCardChildList`、`StorePrepaidCardServiceItemProvider.GetStorePerpaidCardServices`、`WeiXinProvider.GetStoreMpQrCode`、`CardUsedWeekProvider.GetCardUsedWeekElementsByCondition`、`CardUsedTimeProvider.GetCardUsedTimeElementsByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.business_get_store_prepaid_card_sales_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取商家单个充值卡销售汇总信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/BusinessGetStorePrepaidCardSalesInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetStorePrepaidCardSalesInfoResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetStorePrepaidCardSalesInfoResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:1297` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex == 0) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetStorePrepaidCardSalesInfoResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserId` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 用户名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 备注姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 用户手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CreateDate` | `string` | 购买时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SalesMoney` | `string` | 购买金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`model.PageSize == 0`；`model.PageIndex == 0`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.StorePrepaidCardSalesInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`固定提示：参数不正确`；`固定提示：请求数据数量不正确`；`固定提示：页码不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.business_get_store_prepaid_card_sales_summary`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取商家单个充值卡销售汇总信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/BusinessGetStorePrepaidCardSalesSummary` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStorePrepaidCardSalesSummaryResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStorePrepaidCardSalesSummaryResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:1266` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.PrepaidCardId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.PrepaidCardId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId == 0 \\|\\| model.PrepaidCardId == 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SalesCount` | `long` | 销售数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SalesMoney` | `decimal` | 销售金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0 \|\| model.PrepaidCardId == 0`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.StorePrepaidCardSalesSummary`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.business_get_store_prepaid_cards`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取商家所有充值卡 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/BusinessGetStorePrepaidCards` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StorePrepaidCardProvider.StorePrepaidCardStateUpdate |
| 返回 | `Task<DataResult<BusinessGetStorePrepaidCardsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStorePrepaidCardsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:69` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreName` | `string` | store_name 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreLogo` | `string` | store_logo 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards` | `List<StorePrepaidCardViewModel>` | 充值卡列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].PrepaidCardId` | `int` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].CardImg` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardType` | `int` | 类型：0计次，1储值 2时限 3权益 4安心充 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardValue` | `decimal` | 卡价值 次/金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardGivingValue` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardCount` | `int` | 库存剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardSellCount` | `long` | card_sell_count 销售数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].ValidType` | `int` | 失效日期类型: 0 相对日期（购卡后N天； 1 绝对日期（指定失效日期） | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].InvalidDate` | `string` | 失效日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].ValidityDate` | `int` | 有效期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].IsBuyOnce` | `bool` | 是否只能购买一次 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].State` | `int` | 状态 1在售 0停售 -1删除 2售罄 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].OpenCardType` | `int` | 开卡方式 0购买即开卡，1首次使用开卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].StopCardDays` | `int` | 停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].OpenCardDaysMax` | `int` | 最大延迟开卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].IsPayAfterReceive` | `bool` | 是否支付后领取 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardInstructions` | `string` | 会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].IsPromptRights` | `bool` | 是否强制提示会员权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Services` | `List<StorePrepaidCardServiceItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Services[].PrepaidCardId` | `int` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Services[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Services[].CardValue` | `decimal` | 服务项 卡价值 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Services[].IsGift` | `bool` | 是否是赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons` | `List<CouponCenterCouponViewModel>` | 购卡送的券 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PrepaidCards[].Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponCount` | `int` | 已领取数量 ***** 在创建，修改 接口中 该字段表示 优惠券总发行量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].CouponSum` | `int` | 活动优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].Coupons[].ItemId` | `int` | 活动项目ID（预留） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].IsAudit` | `bool` | 卡是否需要审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].ServiceItems` | `List<string>` | 审核卡消费项目明细 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CheckItemId` | `int` | 代办事项明细ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].CardTimeList` | `List<StorePrepaidCardViewCardTimeModel>` | 会员卡有效期集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrepaidCards[].CardTimeList[].ValidType` | `int` | 失效日期类型: 0 相对日期（购卡后N天)； 1 绝对日期（指定失效日期） | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].InvalidDate` | `string` | 失效日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].ValidityDate` | `int` | 有效期单位天 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].MaxCount` | `int` | 最大可售出数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].CardCount` | `int` | 剩余卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].IsBuyOnce` | `bool` | 是否只能购买一次 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].GivePrice` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].StartDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].EndDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CardTimeList[].CreateDate` | `DateTime` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].IsCardChild` | `bool` | 是否包含子卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].RemainderDay` | `int` | 剩余天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].IsCustomPrice` | `bool` | 是否开启自定义金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CustomPriceMin` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CustomPriceMax` | `decimal` | 自定义储值最低 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].CustomPriceGiveRatio` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`models != null && models.Count > 0`；`item.CardType == 3 \|\| item.CardType == 4`；`item.IsCardChild && cardChildList.Count > 0`；`cardInfo != null`；`card.CardType == 4`；`bindCourses != null`
- 一层业务调用：`UsersInfoProvider.UsersInfoGetByUid`、`StoreProvider.GetStoreByIdAsync`、`StorePrepaidCardProvider.GetStorePrepaidCardByStoreId`、`StorePrepaidCardChildProvider.GetStorePrepaidCardChildList`、`StorePrepaidCardProvider.StorePrepaidCardStateUpdate`、`StorePrepaidCardServiceItemProvider.GetStorePerpaidCardServices`、`StoreCourseCardRelationProvider.GetStoreCourseCardRelationElementsByCondition`
- 疑似副作用：`StorePrepaidCardProvider.StorePrepaidCardStateUpdate`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.business_get_store_service_items`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取服务项目列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/BusinessGetStoreServiceItems` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreServiceItemsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreServiceItemsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:1340` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services` | `List<StoreServiceItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].ItemId` | `int` | 服务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].ItemUnit` | `string` | 服务名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreServiceItemProvider.BusinessGetStoreServiceItems`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.get_store_card_by_group_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取会员卡分组详情 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/GetStoreCardByGroupInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<GetStoreCardByGroupInfoResponseModel>>>`；包装 `Task/DataResult`；Data `List<GetStoreCardByGroupInfoResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:1443` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UserImg` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].UserName` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].UserId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].CardId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreCardProvider.GetStoreCardByGroupInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_card.get_store_card_by_group_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 会员卡与课卡 |
| 用途 | 获取会员卡分组列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCard/GetStoreCardByGroupList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<GetStoreCardByGroupListResponseModel>>>`；包装 `Task/DataResult`；Data `List<GetStoreCardByGroupListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCardController.cs:1418` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PrepaidCardId` | `int` | 卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].PeopleCount` | `long` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardName` | `string` | 卡名 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardImg` | `string` | 卡图片 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ValidityDate` | `int` | 有效期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Price` | `decimal` | 金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].DiscountPrice` | `decimal` | 折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardDiscount` | `decimal` | 卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardType` | `int` | 卡类型 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardValue` | `decimal` | 卡价值 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CardGivingValue` | `decimal` | 赠送 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreCardProvider.GetStoreCardByGroupList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.employee.get_store_employees`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工 |
| 用途 | 获取所有员工信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Employee/GetStoreEmployees` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreEmployeesResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreEmployeesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/EmployeeController.cs:20` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId<1 \\|\\| model.Uid<1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId<1 \\|\\| model.Uid<1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Employees` | `List<StoreStaffViewModel>` | 店内所有员工列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Employees[].StaffId` | `int` | 技师Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Employees[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Employees[].StaffName` | `string` | 技师姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Employees[].JobTitle` | `string` | 职称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Employees[].IsVacation` | `bool` | 是否休假 | 普通业务字段 | 可按问题需要提供 |
| `Data.Employees[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Employees[].Tips` | `string` | 提示语 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId<1 \|\| model.Uid<1`；`resultMsg.State`
- 一层业务调用：`TenantUserProvider.GetStoreEmployees`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.business_get_admin_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取管理员详细信息 |
| 使用时机 | 在商家视角中核对“获取管理员详细信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/BusinessGetAdminInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetAdminInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetAdminInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:1123` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1\\|\\| model.StaffId<1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1\\|\\| model.StaffId<1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1\\|\\| model.StaffId<1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工的ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffId` | `long` | 管理员id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserId` | `long` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserName` | `string` | 用户名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 用户手机 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserSex` | `int` | 用户性别 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivilegeGroupId` | `long` | 权限组id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PrivilegeGroupName` | `string` | 权限分组名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffTags` | `List<ReservationTagViewModel>` | 教练标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffTags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StaffTags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.SpecialiseTags` | `List<ReservationTagViewModel>` | 擅长标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.SpecialiseTags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SpecialiseTags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffExperience` | `string` | 店员经历 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgList` | `List<string>` | 图片集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsRecommend` | `bool` | 是否推荐，用作首页展示 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 管理员状态 1在职 0离职 -1删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1\|\| model.StaffId<1`；`resultMsg.State`；`adminInfo?.Id>0`；`staffInfo != null`；`user?.Id>0`；`adminInfo.PrivilegeGroupId>0`；`privilegeGroupInfo != null`；`staffTags?.Count>0`；`baseTags?.Count() > 0`；`experienceTags?.Count() > 0`；`staffImages?.Count>0`；`await StoreUserPermissionsProvider.Instance.UserInfoIsEncryption(model.StoreId, model.Uid)`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`UsersInfoProvider.UsersInfoGetByUid`、`TenantUserInfoProvider.GetTenantUserInfo`、`TenantUserTagRelationProvider.GetTenantUserTagRelationElementsByCondition`、`TenantUserImageProvider.GetTenantUserImageElementsByCondition`、`PrivilegeGroupProvider.GetPrivilegeGroupFirstOrDefaultByCondition`、`StoreReservationTagProvider.GetTagsByStoreId`、`StoreUserPermissionsProvider.UserInfoIsEncryption`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.business_get_admin_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取管理员列表 |
| 使用时机 | 在商家视角中核对“获取管理员列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/BusinessGetAdminList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetAdminListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetAdminListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:241` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsGetVacation` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否获取放假信息；普通业务字段；可按问题需要提供 |
| `AdminType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 0所有 1只看教练 2只看通知权限；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users` | `List<BusinessAdminResponseModel>` | 管理员列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].StaffId` | `long` | 员工的ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].UserName` | `string` | 昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].StoreUserName` | `string` | 店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].UserType` | `int` | 用户权限租：0超级管理员，1管理员，2运营人员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].IsAdmin` | `bool` | 是否是管理员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].IsReservation` | `bool` | 是否是教练 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].StaffTags` | `List<ReservationTagViewModel>` | 教练标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].StaffTags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].StaffTags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].SpecialiseTags` | `List<ReservationTagViewModel>` | 擅长标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].SpecialiseTags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].SpecialiseTags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].State` | `int` | 1在职 0离职 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].VacationState` | `int` | 放假状态 0正常上班 1待开始 3放假中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].VacationBegin` | `string` | 放假开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].VacationEnd` | `string` | 放假结束 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`model.IsGetVacation`；`staffTags?.Count>0`；`baseTags?.Count() > 0`；`experienceTags?.Count() > 0`；`model.IsGetVacation && vacations?.Count > 0`；`vacations.Any(x => x.State == 3 && x.StaffId==item.Id)`；`vacations.Any(x => x.State == 1 && x.StaffId==item.Id)`；`staffVacation?.Id>0`
- 一层业务调用：`StaffVacationProvider.GetStaffVacationAll`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreProvider.GetStoreByIdAsync`、`TenantUserTagRelationProvider.GetTenantUserTagRelationElementsByCondition`、`UsersInfoProvider.UsersInfoGetByUid`、`StoreReservationTagProvider.GetTagsByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.business_get_admin_permissions`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取管理员权限 |
| 使用时机 | 在商家视角中核对“获取管理员权限”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/BusinessGetAdminPermissions` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetAdminPermissionsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetAdminPermissionsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:934` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.Userid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Userid` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.Userid < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 管理员Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PrivilegeGroupId` | `long` | 角色id 1店长 2管理员 3教练 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AdminPermissions` | `List<StoreUserPermissionsViewModel>` | 管理员权限集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.AdminPermissions[].Pid` | `int` | 权限id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AdminPermissions[].PName` | `string` | p_name 权限名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.AdminPermissions[].PController` | `string` | p_controller 权限控制器 | 普通业务字段 | 可按问题需要提供 |
| `Data.AdminPermissions[].PInfo` | `string` | p_info 权限说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.AdminPermissions[].State` | `int?` | 状态 0不可用 1可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.AdminPermissions[].PRootId` | `int` | 上级权限ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AdminPermissions[].Permissions` | `List<StoreUserPermissionsViewModel>` | 子权限 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.Userid < 1`；`resultMsg.State`；`model.StoreId > 0`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.business_get_privilege_group`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取权限组 |
| 使用时机 | 在商家视角中核对“获取权限组”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/BusinessGetPrivilegeGroup` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetPrivilegeGroupResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetPrivilegeGroupResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:1747` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups` | `List<BusinessGetPrivilegeGroupViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].Id` | `long` | 权限组id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Groups[].Title` | `string` | 权限组标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Groups[].GroupInfo` | `string` | 权限组介绍 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`PrivilegeGroupProvider.GetPrivilegeGroupElementsByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.del_staff_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 删除员工（旧） |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/DelStaffInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<DelStaffInfoResponseModel>>`；包装 `Task/DataResult`；Data `DelStaffInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:149` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 删除员工的ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`TenantUserProvider.DelStaffInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.get_staff_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 技师详情（旧） |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/GetStaffInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:58` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId<1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId<1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId<1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 技师ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffId` | `int` | 技师ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserName` | `string` | 微信昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StaffName` | `string` | 技师姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserImg` | `string` | 技师头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.JobTitle` | `string` | 职称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffVacations` | `List<StaffVacation>` | 请假列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffVacations[].StaffVacationId` | `int` | 请假的ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StaffVacations[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffVacations[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffVacations[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StaffVacations[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services` | `List<StoreServiceItem>` | 店铺可预约项目显示列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].Remark` | `string` | 该员工 关联此项目的描述 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Services[].State` | `int` | 0 停止预约， 1 开启预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Weeks` | `List<StaffOperateWeek>` | 店铺技师上班列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Weeks[].WeekInfo` | `int` | 星期 1-7 | 普通业务字段 | 可按问题需要提供 |
| `Data.Weeks[].State` | `int` | 0 不上班， 1正常上班 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.StaffId<1`；`resultMsg.State`
- 一层业务调用：`TenantUserProvider.GetStaffInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.get_staff_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取技师列表（旧） |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/GetStaffList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffListResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:32` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页显示行数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Staffs` | `List<GetStaffInfoModel>` | 技师列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffId` | `int` | 员工（技师）ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].UserImg` | `string` | 技师头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserName` | `string` | 技师姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].JobTitle` | `string` | 职称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`TenantUserProvider.GetStaffList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.get_staff_operate_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取员工工作时间 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/GetStaffOperateInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffOperateInfoResponseV2Model>>`；包装 `Task/DataResult`；Data `GetStaffOperateInfoResponseV2Model` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:2540` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `OperateId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 工作Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateWeeks` | `List<StaffOperateWeekViewModel>` | 工作日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateWeeks[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OperateWeeks[].WeekInfo` | `int` | 营业日期 （星期1-7） 数字类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateWeeks[].State` | `int` | 0 非营业时间，1正常营业 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateTimes` | `List<StaffOperateTimeViewModel>` | 工作时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateTimes[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OperateTimes[].BeginTime` | `string` | 营业时间开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateTimes[].EndTime` | `string` | 营业时间 结束时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.StaffId < 1`；`resultMsg.State`
- 一层业务调用：`StaffOperateWeekProvider.GetStaffOperateInfoV2`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.get_staff_operate_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取员工工作时间 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/GetStaffOperateList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffOperateListResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffOperateListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:2511` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StaffId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateDatas` | `List<StaffOperateViewModel>` | 工作日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateDatas[].OperateId` | `long` | 工作Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OperateDatas[].OperateWeeks` | `List<StaffOperateWeekViewModel>` | 工作日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateDatas[].OperateWeeks[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OperateDatas[].OperateWeeks[].WeekInfo` | `int` | 营业日期 （星期1-7） 数字类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateDatas[].OperateWeeks[].State` | `int` | 0 非营业时间，1正常营业 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateDatas[].OperateTimes` | `List<StaffOperateTimeViewModel>` | 工作时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateDatas[].OperateTimes[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OperateDatas[].OperateTimes[].BeginTime` | `string` | 营业时间开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperateDatas[].OperateTimes[].EndTime` | `string` | 营业时间 结束时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.StaffId < 1`；`resultMsg.State`
- 一层业务调用：`StaffOperateWeekProvider.GetStaffOperateList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.get_staff_vacation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取管理员请假列表 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/GetStaffVacation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffVacationResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffVacationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:1878` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工的ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsMore` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否更多；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMore` | `bool` | 是否有更多 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations` | `List<StaffVacationViewModel>` | 放假信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationId` | `long` | 放假id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Vacations[].VacationTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationSubTitle` | `string` | 子标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationState` | `int` | 1未开始 2已过期 3放假中 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`svms?.Count>0`；`!model.IsMore`；`yxVms?.Count()>0`；`sxVms?.Count()>0`；`sxVms.Count>1`；`item.State == 1 && item.BeginDate < DateTime.Now`；`item.State==1 && item.BeginDate.ToString("yyyy-MM-dd")==item.EndDate.ToString("yyyy-MM-dd")`；`vacations?.Count > 0`；`vacations.Any(x=>x.VacationState==1)`；`vacations.Any(x=>x.VacationState==3)`
- 一层业务调用：`StaffVacationProvider.GetStaffVacationsByStaffId`、`DateTimeHelper.GetWeekDay`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.get_staff_vacation_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取请假详情 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/GetStaffVacationInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffVacationInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffVacationInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:2050` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.VacationId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.VacationId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工的ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `VacationId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.VacationId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 放假id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo` | `VacationInfoViewModel` | 放假具体信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo.Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.VacationInfo.BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo.EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.VacationId < 1`；`resultMsg.State`；`vm?.Id>0`
- 一层业务调用：`StaffVacationProvider.GetStaffVacationById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.get_store_staff_vacation_calendar`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 获取与指定日期范围相交的门店员工请假记录。 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/GetStoreStaffVacationCalendar` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreStaffVacationCalendarResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreStaffVacationCalendarResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:1820` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| !DateTime.TryParse(model.BeginDate, CultureInfo.InvariantCulture, DateTimeStyles.None, out beginDate) \\|\\| !DateTime.TryParse(model.EndDate, CultureInfo.InvariantCulture, DateTimeStyles.None, out endDate) \\|\\| endDate…) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| !DateTime.TryParse(model.BeginDate, CultureInfo.InvariantCulture, DateTimeStyles.None, out beginDate) \\|\\| !DateTime.TryParse(model.EndDate, CultureInfo.InvariantCulture, DateTimeStyles.None, out endDate) \\|\\| endDate…) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询开始时间，包含该时刻。；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询结束时间，不包含该时刻。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations` | `List<StoreStaffVacationCalendarItemModel>` | 与查询范围相交的请假记录。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationId` | `long` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Vacations[].StaffId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Vacations[].StaffName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].BeginDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].EndDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].Remark` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Vacations[].VacationState` | `int` | 1待开始，2已结束，3请假中。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| !DateTime.TryParse(model.BeginDate, CultureInfo.InvariantCulture, DateTimeStyles.None, out beginDate) \|\| !DateTime.TryParse(model.EndDate, CultureInfo.InvariantCulture, DateTimeStyles.None, out endDate) \|\| endDate…`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`StaffVacationProvider.GetStaffVacationCalendar`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请假日历查询范围格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.staff.staff_quit_job_check`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 员工与教练 |
| 用途 | 管理员离职检查 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Staff/StaffQuitJobCheck` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<StaffQuitJobCheckResponseModel>>`；包装 `Task/DataResult`；Data `StaffQuitJobCheckResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StaffController.cs:1517` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工的ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserCount` | `long` | 会籍顾问下店员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseCount` | `long` | 课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonsCount` | `long` | 团课数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivateLessonsCount` | `long` | 私教数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`adminInfo?.Id>0`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`StoreCourseProvider.GetCourseStaffCount`、`StoreLessonsProvider.GetLessonsStaffCount`、`UserCardProvider.GetStaffUserCardCount`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product.check_product_sku_pno`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 检查规格货号是否可用 |
| 使用时机 | 在商家视角中核对“检查规格货号是否可用”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Product/CheckProductSkuPno` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<EditProductResponseModel>>`；包装 `Task/DataResult`；Data `EditProductResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductController.cs:697` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 商品id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SkuList` | `List<EditSkuListModel>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 规格列表集合；普通业务字段；可按问题需要提供 |
| `SkuList[].Id` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 主键id（新增即传入为0）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SkuList[].IsExchange` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否兑换商品；普通业务字段；可按问题需要提供 |
| `SkuList[].ExchangeIntegral` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 兑换积分；普通业务字段；可按问题需要提供 |
| `SkuList[].SkuAttrPrice` | `decimal` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 规格属性价格；普通业务字段；可按问题需要提供 |
| `SkuList[].SkuAttrVipPrice` | `decimal?` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 规格属性会员价；普通业务字段；可按问题需要提供 |
| `SkuList[].Quantity` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 库存；普通业务字段；可按问题需要提供 |
| `SkuList[].State` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 只能使用文档确认的枚举值 | 状态；-1-删除，1-正常；普通业务字段；可按问题需要提供 |
| `SkuList[].Pno` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 货号；普通业务字段；可按问题需要提供 |
| `SkuList[].IsPurchaseLimit` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可在服务端上限内选择 | 是否限购；普通业务字段；可按问题需要提供 |
| `SkuList[].PurchaseLimitType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可在服务端上限内选择 | 限购方式 0永久 1天；普通业务字段；可按问题需要提供 |
| `SkuList[].PurchaseLimitSum` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可在服务端上限内选择 | 限购数量；普通业务字段；可按问题需要提供 |
| `SkuList[].SkuAttrList` | `List<EditSkuAttrListModel>` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 规格属性集合；普通业务字段；可按问题需要提供 |
| `SkuList[].SkuAttrList[].SkuId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 规格id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SkuList[].SkuAttrList[].SkuName` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 规格名称；普通业务字段；可按问题需要提供 |
| `SkuList[].SkuAttrList[].SkuAttrName` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 规格属性名称；普通业务字段；可按问题需要提供 |
| `Pno` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 货号；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 0 \|\| model.StoreId < 0`；`!(model.SkuList?.Count > 0)`；`resultMsg.State`；`!string.IsNullOrEmpty(item.Pno)`；`model.SkuList.Where(x => x.Pno == item.Pno)?.Count() > 1`；`model.Id > 0`；`psam?.ProductId > 0 && psam.ProductId != model.Id`；`psam?.ProductId > 0 && psam.Id > 5 && psam.Id != item.Id`；`psam?.ProductId > 0`；`result == -2000`；`result == -2001`；`result == -2002`
- 一层业务调用：`ProductSkuAttrDetailProvider.GetProductByPno`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 307`；`固定提示：参数格式错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product.get_product_by_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 商品详情 |
| 使用时机 | 在商家视角中核对“商品详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Product/GetProductById` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetProductByIdResponseModel>>`；包装 `Task/DataResult`；Data `GetProductByIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductController.cs:364` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 商品id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 商品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ImgList` | `List<ImgListModel>` | 商品图片集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgList[].Id` | `int` | 图片id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ImgList[].Img` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgList[].OrderBy` | `int` | 排序数字，对应图片顺序； 第一张则1，依此类推 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImgList[].State` | `int` | 状态；-1-删除，1-正常（新上传图片时也传1） | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductName` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo` | `string` | 商品描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExpress` | `int` | 是否需要快递服务；0-不需要，1-需要 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList` | `List<int>` | 商品分类集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductTagList` | `List<string>` | 商品标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList` | `List<EditSkuListModel>` | 规格列表集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].Id` | `int` | 主键id（新增即传入为0） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuAttrPrice` | `decimal` | 规格属性价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuAttrVipPrice` | `decimal?` | 规格属性会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].State` | `int` | 状态；-1-删除，1-正常 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].Pno` | `string` | 货号 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].IsPurchaseLimit` | `bool` | 是否限购 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].PurchaseLimitType` | `int` | 限购方式 0永久 1天 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].PurchaseLimitSum` | `int` | 限购数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuAttrList` | `List<EditSkuAttrListModel>` | 规格属性集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuAttrList[].SkuId` | `int` | 规格id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].SkuAttrList[].SkuName` | `string` | 规格名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuAttrList[].SkuAttrName` | `string` | 规格属性名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 0下架 1上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.Detail` | `string` | 商品详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.EnjoyVipDiscount` | `bool` | 享受会员折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Pno` | `string` | 货号 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPurchaseLimit` | `bool` | 是否限购 | 普通业务字段 | 可按问题需要提供 |
| `Data.PurchaseLimitType` | `int` | 限购方式 0永久 1天 | 普通业务字段 | 可按问题需要提供 |
| `Data.PurchaseLimitSum` | `int` | 限购数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Id < 0`；`resultMsg.State`；`m != null`
- 一层业务调用：`ProductProvider.GetProductById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：请上传商品id`；`固定提示：当前商品不存在`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product.get_sku_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 获取规格列表 |
| 使用时机 | 在商家视角中核对“获取规格列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Product/GetSkuList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetSkuListResponseModel>>`；包装 `DataResult`；Data `List<GetSkuListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductController.cs:400` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ProductId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 商品id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 规格id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].SkuName` | `string` | 规格名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`SkuAttrProvider.GetSkuList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product.products_search`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 搜索商品 |
| 使用时机 | 在商家视角中核对“搜索商品”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Product/ProductsSearch` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<ProductsSearchResponseViewModel>>`；包装 `Task/DataResult`；Data `ProductsSearchResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductController.cs:248` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid<1 \\|\\| model.StoreId<1)；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid<1 \\|\\| model.StoreId<1)；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ClassId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 商品分类ID,0为全部商品；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `KeyWord` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.KeyWord)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 关键词；普通业务字段；可按问题需要提供 |
| `ProductType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 商品类型 0全部商品 1只买商品 2兑换商品；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList` | `List<ProductViewModel>` | 门店分类商品集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].Id` | `int` | 商品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductsList[].ImgStr` | `string` | 商品图片集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].ProductName` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].ProductInfo` | `string` | 商品描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].State` | `int` | 0下架 1上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].ProductTagList` | `List<string>` | 商品标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].SellNum` | `int` | 销量 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].IsPurchaseLimit` | `bool` | 是否限购 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].PurchaseLimitType` | `int` | 限购方式 0永久 1天 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsList[].PurchaseLimitSum` | `int` | 限购数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid<1 \|\| model.StoreId<1`；`string.IsNullOrEmpty(model.KeyWord)`；`resultMsg.State`；`model.Uid < 1 \|\| model.StoreId < 1`
- 一层业务调用：`ProductProvider.ProductsSearch`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product_center.get_business_products_class_by_store_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 根据门店ID获取B端商品分类集合 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ProductCenter/GetBusinessProductsClassByStoreId` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<ProductsClassResponseViewModel>>`；包装 `Task/DataResult`；Data `ProductsClassResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductCenterController.cs:192` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsClassItems` | `List<ProductsClassModel>` | 门店商品分类集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsClassItems[].Id` | `int` | id ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductsClassItems[].ClassName` | `string` | class_name 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsClassItems[].ClassImg` | `string` | 分类图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsClassItems[].RootId` | `string` | root_id 父级ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductsClassItems[].StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductsClassItems[].IsSystem` | `bool` | 是否是系统分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductsClassItems[].State` | `int` | state 状态 1正常 0停用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`
- 一层业务调用：`ProductClassProvider.GetClassListByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product_center.get_business_products_list_by_store_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 根据门店ID获取门店下面对应的商品列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ProductCenter/GetBusinessProductsListByStoreId` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<ProductsListResponseViewModel>>`；包装 `Task/DataResult`；Data `ProductsListResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductCenterController.cs:239` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList` | `List<ProductClassViewModel>` | 商品分类集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].Id` | `int` | id ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductClassList[].ClassName` | `string` | class_name 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ClassId` | `int` | class_id 分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductClassList[].ClassImg` | `string` | 分类图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].IsSystem` | `bool` | 是否是系统分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].State` | `int` | state 状态 1正常 0停用 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductTotal` | `int` | 分类下商品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].Sort` | `int` | 分类排序索引 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList` | `List<ProductViewModel>` | 门店分类商品集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].Id` | `int` | 商品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductClassList[].ProductsList[].ImgStr` | `string` | 商品图片集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].ProductName` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].ProductInfo` | `string` | 商品描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].State` | `int` | 0下架 1上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].ProductTagList` | `List<string>` | 商品标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].SellNum` | `int` | 销量 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].IsPurchaseLimit` | `bool` | 是否限购 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].PurchaseLimitType` | `int` | 限购方式 0永久 1天 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList[].ProductsList[].PurchaseLimitSum` | `int` | 限购数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`
- 一层业务调用：`ProductProvider.GetProductListByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product_center.get_business_products_message_by_product_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 根据商品ID获取商品信息 |
| 使用时机 | 在商家视角中核对“根据商品ID获取商品信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ProductCenter/GetBusinessProductsMessageByProductId` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<ProductsMessageResponseViewModel>>`；包装 `Task/DataResult`；Data `ProductsMessageResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductCenterController.cs:265` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ProductId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ProductId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ProductId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.ProductId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 商品ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 商品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ImgList` | `List<string>` | 商品图片集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductName` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo` | `string` | 商品描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExpress` | `int` | 是否需要快递服务；0-不需要，1-需要 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductClassList` | `List<string>` | 商品分类集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductTagList` | `List<string>` | 商品标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList` | `List<ProductsMessageSkuListModel>` | 规格列表集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].Id` | `int` | 规格id（新增即传入0） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].SkuName` | `ProductsMessageSkuNameModel` | 规格名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuName.Id` | `int` | 规格id（新增即传入0） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].SkuName.SkuName` | `string` | 规格名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuName.SkuAttrList` | `List<ProductsMessageSkuAttrListModel>` | 规格属性集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuName.SkuAttrList[].Id` | `int` | 规格属性的id（新增即传入0） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].SkuName.SkuAttrList[].SkuAttrName` | `string` | 规格属性名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuAttrList` | `List<ProductsMessageSkuAttrListModel>` | 规格属性集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuAttrList[].Id` | `int` | 规格属性的id（新增即传入0） | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].SkuAttrList[].SkuAttrName` | `string` | 规格属性名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 0下架 1上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.Detail` | `string` | 商品详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList` | `List<ProductMessageSkuAttrDetailListModel>` | 商品规格属性详情集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuAttrDetailList[].ProductSkuIds` | `string` | id组合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].VipPrice` | `decimal` | 会员价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].IsPurchaseLimit` | `bool` | 是否限购 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].PurchaseLimitType` | `int` | 限购方式 0永久 1天 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].PurchaseLimitSum` | `int` | 限购数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuAttrDetailList[].PurchaseLimitBySum` | `int` | 已购数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsVip` | `bool` | 是否是vip | 普通业务字段 | 可按问题需要提供 |
| `Data.EnjoyVipDiscount` | `bool` | 享受会员折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Pno` | `string` | 货号 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsHaveSku` | `bool` | 是否包含多SKU | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPurchaseLimit` | `bool` | 是否限购 | 普通业务字段 | 可按问题需要提供 |
| `Data.PurchaseLimitType` | `int` | 限购方式 0永久 1天 | 普通业务字段 | 可按问题需要提供 |
| `Data.PurchaseLimitSum` | `int` | 限购数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PurchaseLimitBySum` | `int` | 已购数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.ProductId < 1`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product_center.get_product_class_ico_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 获取商品分类图标 |
| 使用时机 | 在商家视角中核对“获取商品分类图标”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ProductCenter/GetProductClassIcoList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetProductClassIcoResponseViewModel>>`；包装 `Task/DataResult`；Data `GetProductClassIcoResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductCenterController.cs:545` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IcoList` | `List<IcoListModel>` | 分类图标集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.IcoList[].IcoDesc` | `string` | 图标描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.IcoList[].IcoUrl` | `string` | 图标路径 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`
- 一层业务调用：`ProductClassProvider.GetProductClassIcoList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product_center.get_store_shop_lnventory_warning_count`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 获取商城库存商品数量 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ProductCenter/GetStoreShopLnventoryWarningCount` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreShopSetingProvider.GetStoreShopSetByStoreId |
| 返回 | `Task<DataResult<GetStoreShopLnventoryWarningCountResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreShopLnventoryWarningCountResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductCenterController.cs:482` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductCount` | `int` | 库存预警的商品数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`shstm.IsOpenLnventoryWarning`
- 一层业务调用：`StoreShopSetingProvider.GetStoreShopSetByStoreId`、`ProductProvider.GetStoreShopLnventoryWarningCount`
- 疑似副作用：`StoreShopSetingProvider.GetStoreShopSetByStoreId`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product_center.get_store_shop_lnventory_warning_product`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 获取商城库存预警的商品 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ProductCenter/GetStoreShopLnventoryWarningProduct` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetStoreShopLnventoryWarningProductResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetStoreShopLnventoryWarningProductResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductCenterController.cs:514` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\|model.PageIndex<1 \\|\\| model.PageSize<1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\|model.PageIndex<1 \\|\\| model.PageSize<1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\|model.PageIndex<1 \\|\\| model.PageSize<1) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\|model.PageIndex<1 \\|\\| model.PageSize<1) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetStoreShopLnventoryWarningProductResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | 商品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Pno` | `string` | 货号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductImg` | `string` | 商品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductName` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductUnit` | `string` | 商品单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsOnSale` | `bool` | 是否可以上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 0下架 1上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SkuAttrDetailList` | `List<LnventoryWarningSkuAttrDetailViewModel>` | 商品规格属性详情集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SkuAttrDetailList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].SkuAttrDetailList[].Pno` | `string` | 货号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SkuAttrDetailList[].ProductSkuIds` | `string` | id组合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SkuAttrDetailList[].ProductSkuNames` | `string` | Name组合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SkuAttrDetailList[].Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\|model.PageIndex<1 \|\| model.PageSize<1`；`resultMsg.State`
- 一层业务调用：`ProductProvider.GetStoreShopLnventoryWarningProduct`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.product_center.get_store_shop_lnventory_warning_seting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 商品 |
| 用途 | 获取商城库存预警设置 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ProductCenter/GetStoreShopLnventoryWarningSeting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreShopSetingProvider.GetStoreShopSetByStoreId |
| 返回 | `Task<DataResult<GetStoreShopLnventoryWarningSetingResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreShopLnventoryWarningSetingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ProductCenterController.cs:416` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | id 逐渐ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreId` | `int` | store_id 门店ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IsOpenLnventoryWarning` | `bool` | 是否开启库存预警 | 普通业务字段 | 可按问题需要提供 |
| `Data.LnventoryQuantity` | `int` | 预警库存数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsLnventoryWechatNotice` | `bool` | 是否开启库存预警微信提醒 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsLnventorySmsNotice` | `bool` | 是否开启库存预警短信提醒 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`
- 一层业务调用：`StoreShopSetingProvider.GetStoreShopSetByStoreId`
- 疑似副作用：`StoreShopSetingProvider.GetStoreShopSetByStoreId`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_bench.get_dashboard_overview`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 工作台 |
| 用途 | 获取 PC 运营首页聚合数据。 数据范围严格来自当前登录员工在当前门店的权限，不接受前端声明 StaffId。 |
| 使用时机 | 在商家视角中核对“获取 PC 运营首页聚合数据。 数据范围严格来自当前登录员工在当前门店的权限，不接受前端声明 StaffId。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkBench/GetDashboardOverview` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetDashboardOverviewResponseModel>>`；包装 `Task/DataResult`；Data `GetDashboardOverviewResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkBenchController.cs:221` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model == null \\|\\| model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| authenticatedUid < 1 \\|\\| model.Uid != authenticatedUid \\|\\| !DateTime.TryParse(model.StartDate, out var startDate) \\|\\| !DateTime.TryParse(model.EndDate, out var endDate) \\|\\| endDate < startDate \…) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model == null \\|\\| model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| authenticatedUid < 1 \\|\\| model.Uid != authenticatedUid \\|\\| !DateTime.TryParse(model.StartDate, out var startDate) \\|\\| !DateTime.TryParse(model.EndDate, out var endDate) \\|\\| endDate < startDate \…) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 门店自然日开始时间。；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 门店自然日结束时间。；普通业务字段；可按问题需要提供 |
| `EventPageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 首页最新预约返回条数，服务端会限制在安全范围内。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.GeneratedAt` | `string` | 服务端按门店时间生成数据的时间点。 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreId` | `int` | 本次响应对应的门店，前端用它阻止切店后的旧响应覆盖新门店。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Scope` | `DashboardScopeResponseModel` | 当前员工角色与数据范围。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Scope.PrivilegeGroupId` | `int` | 1 店长、2 店员、3 教练；沿用现有权限组口径。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Scope.CourseScope` | `string` | 课程范围：store 或 self。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Scope.StatisticsScope` | `string` | 统计范围：store 或 self。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Scope.CanViewFinancials` | `bool` | 是否允许展示门店财务和退款对象明细。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary` | `DashboardSummaryResponseModel` | 首页汇总指标。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.CourseCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.ReservationCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.AttendanceCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.WaitlistCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.OpenTaskCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.RefundFailureCount` | `long` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.PendingAttendanceCount` | `long` | 当前日期内仍未签到且课次允许签到的实际人数，多人同行预约按同行人数累计。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.ExpiringMemberCount` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.BirthdayMemberCount` | `long` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.Revenue` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Summary.RevenueVisible` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary` | `DashboardPreviousSummaryResponseModel` | 昨日同一门店自然日汇总，仅包含可可靠回算的日指标。 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary.Date` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary.CourseCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary.ReservationCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary.AttendanceCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary.WaitlistCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary.Revenue` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.PreviousSummary.RevenueVisible` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses` | `List<DashboardCourseResponseModel>` | 当日课程运行列表。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CourseId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].ClassesId` | `long` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CourseType` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseStar` | `int` | 课次难度，沿用 0–10 整数协议，奇数表示半星。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseColor` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseImage` | `string` | 课目配置的真实背景图；首页不使用课程色生成替代纹理。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].ImageStyle` | `int` | 图片样式：0-明亮，1-暗黑；供前端选择可读的文字遮罩。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BeginTime` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].EndTime` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Capacity` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].MinimumPeople` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].IsAutoCancel` | `bool` | 当前课次是否启用最低人数自动取消；前端据此区分普通人数不足与开课风险。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].State` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].PlaceId` | `long` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].PlaceName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].ReservedCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].AttendanceCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].WaitlistCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].PrepaidCardExceptionCount` | `int` | 当前课次关联预约中的预付卡异常数量。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Staffs` | `List<CourseStaffViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Courses[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Attendees` | `List<DashboardCourseAttendeeResponseModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Attendees[].ReservationId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Attendees[].UserId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Attendees[].UserName` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Courses[].Attendees[].UserImage` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Attendees[].PeopleCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Attendees[].State` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecentReservations` | `List<DashboardRecentReservationResponseModel>` | 与预约记录页同源的当日最新预约，按记录创建时间倒序。 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecentReservations[].ReservationId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RecentReservations[].UserName` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.RecentReservations[].UserImage` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecentReservations[].CourseName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecentReservations[].CourseType` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecentReservations[].CreatedAt` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecentReservations[].State` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks` | `List<DashboardTaskResponseModel>` | 已按严重程度和临近时间排序的对象级待办。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].Key` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].Kind` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].Severity` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].Count` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].LessonsId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tasks[].ReservationId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tasks[].MemberId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tasks[].MemberName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].CourseName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].BeginTime` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].Delta` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].ResourceName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tasks[].Reason` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model == null \|\| model.StoreId < 1 \|\| model.Uid < 1 \|\| authenticatedUid < 1 \|\| model.Uid != authenticatedUid \|\| !DateTime.TryParse(model.StartDate, out var startDate) \|\| !DateTime.TryParse(model.EndDate, out var endDate) \|\| endDate < startDate \…`；`tenantUser == null && !isSystemAdministrator`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`AdminUserProvider.GetAdminUserUidListAsync`、`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`DashboardOverviewProvider.GetDashboardOverview`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_bench.get_reservation_attention_counts`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 工作台 |
| 用途 | 获取当前门店当天的候补与待签到记录数。 身份、预约处理权限和课程数据范围均来自当前已验证会话，不接受前端传入员工范围。 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkBench/GetReservationAttentionCounts` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationAttentionCountsResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationAttentionCountsResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkBenchController.cs:162` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model == null \\|\\| model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| authenticatedUid < 1 \\|\\| model.Uid != authenticatedUid) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model == null \\|\\| model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| authenticatedUid < 1 \\|\\| model.Uid != authenticatedUid) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreId` | `int` | 本次响应所属门店，用于前端阻止切店后的旧响应覆盖新门店。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.GeneratedAt` | `string` | 服务端按门店当地时间生成计数的时间点。 | 普通业务字段 | 可按问题需要提供 |
| `Data.WaitlistRecordCount` | `int` | 当天预约状态为候补（State=10）的记录数。 | 普通业务字段 | 可按问题需要提供 |
| `Data.PendingCheckInRecordCount` | `int` | 当天正式预约仍未签到，且课次处于待上课、上课中或已完课的记录数。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model == null \|\| model.StoreId < 1 \|\| model.Uid < 1 \|\| authenticatedUid < 1 \|\| model.Uid != authenticatedUid`；`tenantUser == null && !isSystemAdministrator`；`!isSystemAdministrator && reservationProcessing?.State != 1`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`AdminUserProvider.GetAdminUserUidListAsync`、`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`DashboardOverviewProvider.GetReservationAttentionCounts`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_bench.get_store_workbench`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 工作台 |
| 用途 | 获取门店工作台列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkBench/GetStoreWorkbench` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreWorkbenchV2ResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreWorkbenchV2ResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkBenchController.cs:116` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas` | `List<WorkBenchViewModel>` | 工作台数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkBenchDatas[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkBenchDatas[].ModuleId` | `int` | 模块id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkBenchDatas[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].Module` | `string` | module 模块值 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].Type` | `int` | 类型；0-小程序内部，1-外链，2-外部小程序，3-不跳转 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].Path` | `string` | path 跳转的内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].IsUsed` | `bool` | 是否可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].WorkBenchImg` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].IsShowNum` | `bool` | 是否显示数字 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].Num` | `int` | 数字 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchDatas[].Sign` | `bool` | 状态 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`wbs?.Count>0`
- 一层业务调用：`StoreWorkbenchProvider.GetStoreWorkbenchV3`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.work_bench.get_work_bench_all`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 工作台 |
| 用途 | 获取所有工作台入口 |
| 使用时机 | 在商家视角中核对“获取所有工作台入口”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/WorkBench/GetWorkBenchAll` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetWorkBenchAllResponseModel>>`；包装 `Task/DataResult`；Data `GetWorkBenchAllResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/WorkBenchController.cs:29` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas` | `List<WorkBenchClassViewModel>` | 工作台数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkBenchClassDatas[].ClassName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].IsUsed` | `bool` | 是否可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas` | `List<WorkBenchViewModel>` | 工作台数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].ModuleId` | `int` | 模块id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].Module` | `string` | module 模块值 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].Type` | `int` | 类型；0-小程序内部，1-外链，2-外部小程序，3-不跳转 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].Path` | `string` | path 跳转的内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].IsUsed` | `bool` | 是否可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].WorkBenchImg` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].IsShowNum` | `bool` | 是否显示数字 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].Num` | `int` | 数字 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkBenchClassDatas[].WorkBenchDatas[].Sign` | `bool` | 状态 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId <1 \|\| model.Uid < 1`；`resultMsg.State`；`ssmms.Any(x=>classItem.ModuleId==x.ModuleId)`；`workBenchitem.Id==6 && !sm.IsUseAudit`；`workBenchitem.Id==7 && !sm.IsStallMode`；`ssmms.Any(x => workBenchitem.ModuleId == x.ModuleId)`
- 一层业务调用：`WorkbenchProvider.GetWorkBenchClass`、`WorkbenchProvider.GetWorkBench`、`StoreSoftModuleProvider.GetStoreModuleBySid`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.message.get_work_we_chat_msg_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消息 |
| 用途 | 获取商家单个消息 |
| 使用时机 | 在商家视角中核对“获取商家单个消息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Message/GetWorkWeChatMsgInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetWorkWeChatMsgInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetWorkWeChatMsgInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MessageController.cs:286` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 消息Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 消息Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TempId` | `int` | 模板id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.MsgType` | `int` | 消息类型 0主动发送 1被动发送 | 普通业务字段 | 可按问题需要提供 |
| `Data.MsgInfo` | `string` | 消息内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendTime` | `string` | type=0 群发时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.SearchConditionId` | `int` | 搜索条件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ConditionInfo` | `UserSearchConditionViewModel` | 搜索内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.KeyWord` | `string` | 搜索关键词 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.StaffId` | `int` | 归属店员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ConditionInfo.HaveMoney` | `int` | 余额0 all 1有 2无 3有余额未续费 10固定剩余金额，11固定剩余次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.Validity` | `int` | -1-有效，-2-过期，>0-几天内过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardState` | `int?` | 0未开卡 1开卡 3停卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.Sex` | `int` | 性别0 all 1男 2女 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.IsNoCard` | `bool` | 是否无卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.PrepaidPrice` | `decimal` | 储值的金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.OpenCardDateStart` | `string` | 开卡日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.OpenCardDateEnd` | `string` | 开卡日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionDateStart` | `string` | 消费日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionDateEnd` | `string` | 消费日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsDateStart` | `string` | 上课日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsDateEnd` | `string` | 上课日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionMoneyMin` | `decimal?` | 消费金额开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionMoneyMax` | `decimal?` | 消费金额结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsCountStart` | `int?` | 上课次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.LessonsCountEnd` | `int?` | 上课次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ActivateVipDay` | `int?` | 活跃天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.NotActivateVipDay` | `int?` | 沉睡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.TagIds` | `List<int>` | 标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.GroupIds` | `List<int>` | 分组集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.PrepaidCardIds` | `List<int>` | 储值卡Id | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardPriceMin` | `decimal?` | 剩余金额开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardPriceMax` | `decimal?` | 剩余金额结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardSumMin` | `decimal?` | 剩余次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardSumMax` | `decimal?` | 剩余次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardLessonsMin` | `decimal?` | 剩余课时开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardLessonsMax` | `decimal?` | 剩余课时结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionSumMin` | `decimal?` | 消费次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionSumMax` | `decimal?` | 消费次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionSumMin` | `decimal?` | 卡内消费次数开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionSumMax` | `decimal?` | 卡内消费次数结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ByCardMoneyMin` | `decimal?` | 累计储值开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ByCardMoneyMax` | `decimal?` | 累计储值结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionMoneyMin` | `decimal?` | 卡内消费开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardConsumptionMoneyMax` | `decimal?` | 卡内消费结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionLastDateStart` | `string` | 最后消费日期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ConsumptionLastDateEnd` | `string` | 最后消费日期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ValidityDateStart` | `string` | 有效期开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.ValidityDateEnd` | `string` | 有效期结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardIntegralMin` | `int?` | 剩余积分开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionInfo.CardIntegralMax` | `int?` | 剩余积分结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConditionDescribe` | `string` | 搜索描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendSum` | `int` | 发送次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendPeopleSum` | `int` | 发送人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 0未发送 1启用 2发送中 3已发送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments` | `List<WorkWeChatMsgAttachmentViewModel>` | 附件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MsgType` | `int` | 附件消息类型 1文本 2图片 3链接 4小程序 5视频 6文件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].FileUrl` | `string` | 附件资源地址 用来上传获取media_id | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].Title` | `string` | 附件标题 3 4 5使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkPicurl` | `string` | 链接封面图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkDesc` | `string` | 链接描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].MiniprogramMeunType` | `int` | 小程序菜单类型 0链接 10卡列表 11单卡 20券列表 21单券 22单券包 30商城 31商品 40点单 42餐品 42餐台 50预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].MiniprogramContentId` | `int` | 小程序内容id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MiniprogramAppid` | `string` | miniprogram_appid 小程序appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MiniprogramPage` | `string` | 小程序page路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].State` | `int` | 状态 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`msgTemp?.Id > 0`；`tma?.Count > 0`
- 一层业务调用：`WorkWechatMsgProvider.GetWorkWechatMsgById`、`WorkWechatMsgAttachmentsProvider.WorkWechatMsgAttachmentsGetByMsgId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：MsgType = msgTemp.MsgType,`；`状态赋值：MsgType = msgAttr.MsgType,`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.message.get_work_we_chat_msg_template_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消息 |
| 用途 | 获取商家单个消息模板 |
| 使用时机 | 在商家视角中核对“获取商家单个消息模板”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Message/GetWorkWeChatMsgTemplateInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetWorkWeChatMsgTemplateInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetWorkWeChatMsgTemplateInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MessageController.cs:103` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 模板Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 模板Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TempName` | `string` | 模板名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.TempInfo` | `string` | 模板内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 -1删除 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments` | `List<WorkWeChatMsgTemplateAttachmentViewModel>` | 附件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MsgType` | `int` | 附件消息类型 1文本 2图片 3链接 4小程序 5视频 6文件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].FileUrl` | `string` | 附件资源地址 用来上传获取media_id 图片文件 小程序封面图 视频文件 文件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].Title` | `string` | 附件标题 3 4 5使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkPicurl` | `string` | 链接封面图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkDesc` | `string` | 链接描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].LinkUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].MiniprogramAppid` | `string` | 小程序appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].MiniprogramPage` | `string` | 小程序page路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].MiniprogramMeunType` | `int` | 小程序菜单类型 0链接 10卡列表 11单卡 20券列表 21单券 22单券包 30商城 31商品 40点单 42餐品 42餐台 50预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Attachments[].MiniprogramContentId` | `int` | 小程序内容id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Attachments[].State` | `int` | 状态 -1删除 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`msgTemp?.Id > 0`；`tma?.Count > 0`
- 一层业务调用：`WorkWechatMsgTemplateProvider.GetWorkWechatMsgTemplateById`、`WorkWechatMsgTemplateAttachmentsProvider.GetWorkWechatMsgTemplateAttachmentsByTempId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：MsgType = msgAttr.MsgType,`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.message.get_work_we_chat_msg_templates`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消息 |
| 用途 | 获取商家所有消息模板 |
| 使用时机 | 在商家视角中核对“获取商家所有消息模板”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Message/GetWorkWeChatMsgTemplates` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetWorkWeChatMsgTemplatesResponseModel>>`；包装 `Task/DataResult`；Data `GetWorkWeChatMsgTemplatesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MessageController.cs:32` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates` | `List<WorkWeChatMsgTemplateViewModel>` | 模板数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Templates[].TempName` | `string` | 模板名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].TempInfo` | `string` | 模板内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].State` | `int` | 状态 -1删除 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments` | `List<WorkWeChatMsgTemplateAttachmentViewModel>` | 附件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Templates[].Attachments[].MsgType` | `int` | 附件消息类型 1文本 2图片 3链接 4小程序 5视频 6文件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].FileUrl` | `string` | 附件资源地址 用来上传获取media_id 图片文件 小程序封面图 视频文件 文件 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].Title` | `string` | 附件标题 3 4 5使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].LinkPicurl` | `string` | 链接封面图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].LinkDesc` | `string` | 链接描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].LinkUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].MiniprogramAppid` | `string` | 小程序appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Templates[].Attachments[].MiniprogramPage` | `string` | 小程序page路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].MiniprogramMeunType` | `int` | 小程序菜单类型 0链接 10卡列表 11单卡 20券列表 21单券 22单券包 30商城 31商品 40点单 42餐品 42餐台 50预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Attachments[].MiniprogramContentId` | `int` | 小程序内容id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Templates[].Attachments[].State` | `int` | 状态 -1删除 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`tms?.Count>0`；`tma?.Count>0`
- 一层业务调用：`WorkWechatMsgTemplateProvider.GetWorkWechatMsgTemplates`、`WorkWechatMsgTemplateAttachmentsProvider.GetWorkWechatMsgTemplateAttachments`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：MsgType = msgAttr.MsgType,`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.message.get_work_we_chat_msgs`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消息 |
| 用途 | 获取商家所有群发消息 |
| 使用时机 | 在商家视角中核对“获取商家所有群发消息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Message/GetWorkWeChatMsgs` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataList<GetWorkWeChatMsgsResponseModel>>`；包装 `Task/DataList`；Data `GetWorkWeChatMsgsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MessageController.cs:257` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `MsgType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 消息类型 0主动发送 1被动发送；普通业务字段；可按问题需要提供 |
| `SendDateStart` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消息发送时间开始；普通业务字段；可按问题需要提供 |
| `SendDateEnd` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 消息发送时间结束；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 消息Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].TempId` | `int` | 模板id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].MsgType` | `int` | 消息类型 0主动发送 1被动发送 | 普通业务字段 | 可按问题需要提供 |
| `Data[].MsgInfo` | `string` | 消息内容 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SendTime` | `string` | type=0 群发时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsTimingSend` | `bool` | type=0 是否定时发送 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SearchConditionId` | `int` | 搜索条件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].SearchConditionTitle` | `string` | 搜索条件标题 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SearchConditionDescribe` | `string` | 搜索条件描述 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SendSum` | `int` | 发送次数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SendPeopleSum` | `int` | 发送人数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].State` | `int` | 状态 0禁用 1启用（定时发送） 2发送中 3已发送 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments` | `List<WorkWeChatMsgAttachmentViewModel>` | 附件 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Attachments[].MsgType` | `int` | 附件消息类型 1文本 2图片 3链接 4小程序 5视频 6文件 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].FileUrl` | `string` | 附件资源地址 用来上传获取media_id | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].Title` | `string` | 附件标题 3 4 5使用 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].LinkPicurl` | `string` | 链接封面图 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].LinkDesc` | `string` | 链接描述 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].LinkUrl` | `string` | 链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].MiniprogramMeunType` | `int` | 小程序菜单类型 0链接 10卡列表 11单卡 20券列表 21单券 22单券包 30商城 31商品 40点单 42餐品 42餐台 50预约 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].MiniprogramContentId` | `int` | 小程序内容id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Attachments[].MiniprogramAppid` | `string` | miniprogram_appid 小程序appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Attachments[].MiniprogramPage` | `string` | 小程序page路径 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Attachments[].State` | `int` | 状态 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`
- 一层业务调用：`WorkWechatMsgProvider.GetWorkWechatMsgs`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.consumption.business_get_consumption_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消费与核销 |
| 用途 | 获取单个消费明细信息 |
| 使用时机 | 商家核对会员消费、充值、核减、返还、退款前置事实或操作后余额时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Consumption/BusinessGetConsumptionInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetConsumptionInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetConsumptionInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ConsumptionController.cs:25` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ConsumptionId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 主键id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.ConsumptionId` | `string` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreName` | `string` | 门店名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardType` | `int` | 类型：0计次，1储值 2 限时卡 3权益卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 用户手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.RemarkName` | `string` | 备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PayType` | `string` | 支付名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayTypeInt` | `int` | 支付方式 0 线上 1 现金 2POST 3个人码微信 4个人码支付宝 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardDiscount` | `string` | 支付方式 0 线上 1 现金 2POST 3个人码微信 4个人码支付宝 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionType` | `int` | 消费方式：0 计次，1 金额，2积分，3印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperationType` | `int` | 操作方式：0 用户，1管理员 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserName` | `string` | 操作店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ConsumptionTag` | `int` | 0支出 ，1充值,2 核减，3返还，4赠送，5付款 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionTagValue` | `string` | 交易类型 刷卡 返还…… | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionValue` | `string` | 消费值（统计数据：充值金额） | 普通业务字段 | 可按问题需要提供 |
| `Data.OtherValue` | `decimal` | 备用字段，例如次卡充值时充值的次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.AfterValue` | `string` | 操作后值（统计数据 消费次数） | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateDate` | `string` | 创建时间（统计数据 年） | 普通业务字段 | 可按问题需要提供 |
| `Data.CardName` | `string` | 卡号名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.TotalPrice` | `string` | 应付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCoupon` | `int` | 是否使用优惠卷 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsIntegral` | `bool` | 是否使用积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsProduct` | `bool` | 是否包含商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsService` | `bool` | 是否包含服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsShowRemark` | `bool` | 是否向顾客展示备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.RemarkFiles` | `List<FileViewModel>` | 文件列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.RemarkFiles[].Id` | `string` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RemarkFiles[].FilePath` | `string` | file_path 文件路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrintNo` | `int` | 打印机编号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons` | `List<UserCouponViewModel>` | 优惠券信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Id` | `int` | 优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].CouponId` | `int` | 原始优惠券Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Uid` | `int` | 用户Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].StoreName` | `string` | 店铺名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StoreAddress` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Coupons[].CouponType` | `int` | 类型 0代金券 1打折券 2服务券 3礼品券 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponImg` | `string` | 优惠券背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponIcon` | `string` | 赠品图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseAudit` | `bool` | 优惠券使用是否需要使用审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSource` | `int` | 创建来源 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponBgImg` | `string` | 优惠券卡包背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsUseVip` | `bool` | 是否会员可用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsMore` | `bool` | 是否可以同时使用多张 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsBuyCard` | `bool` | 是否只用于购买会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseMinMoney` | `decimal` | 最低消费限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponValue` | `decimal` | 优惠卷面值（金额、折扣、现价） | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].WxCouponCode` | `string` | 优惠卷Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponDescription` | `string` | 优惠卷说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StartRangeDate` | `int` | 领取后第N天生效 0立即生效 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDate` | `string` | 有效周期 格式 ps:01-02-03 表示1年2个月3天 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeDateRead` | `string` | 有效周期 的展示描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].RangeType` | `int` | 有效期类型 0固定周期，1起始结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyType` | `int` | 使用频率限制 0每天 1每周 2每月 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyQuantity` | `int` | 使用频率数量0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsAuditing` | `bool` | 发放和使用优惠券是否需要审核 true 需要审核 false 无需审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].State` | `int` | 状态 -1删除 0暂停发放 1正常发放 2发放完毕 (用户)状态 -1不可用 0未生效 1正常 2已用 3已过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponRemaining` | `int` | 优惠券剩余数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].MarketingId` | `int` | 营销ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services` | `List<CouponServiceItemViewModel>` | 优惠券服务 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].IsExpired` | `bool` | 是否过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].ActivityId` | `int` | 活动id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].ActivityType` | `int` | 活动类型 0裂变 1抽奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponCenterId` | `int` | 优惠中心ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Coupons[].CouponCount` | `int` | 优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSum` | `int` | 优惠券数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].UseFrequencyCount` | `int` | 限制频率已使用张数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].StateReasonContent` | `string` | 不可用原因说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Coupons[].CouponSourceContent` | `string` | 来源说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Service` | `List<ConsumptionServiceItemViewModel>` | 服务信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Service[].ItemId` | `int` | 项目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Service[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Service[].ConsumptionValue` | `decimal` | consumption_value 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Service[].AfterValue` | `decimal` | after_value 操作后值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Service[].PayPrice` | `decimal` | 实付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Service[].ItemUnit` | `string` | 服务单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products` | `List<ConsumptionProductViewModel>` | 商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductId` | `int` | 商品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Products[].ProductTitle` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].SkuName` | `string` | Sku名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductImg` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductSum` | `int` | 商品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductUnit` | `int` | 商品单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].PayPrice` | `string` | 实付总金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.AfterIntegral` | `string` | 剩余积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShop` | `bool` | 是否商城 | 普通业务字段 | 可按问题需要提供 |
| `Data.PostFee` | `string` | 快递费 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderId` | `int` | 点餐订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IsCommission` | `string` | 是否使用佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.AfterCommission` | `string` | 剩余佣金 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsBookkeeping` | `bool` | 是否记账 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsVip` | `bool` | 是否是会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAxc` | `bool` | 是否安心充 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShowRefundButton` | `bool` | 是否显示退款按钮 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsRefunds` | `bool` | 是否已退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceId` | `string` | 退款原订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RefundPrice` | `string` | 退款金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.RefundsReason` | `string` | 退款失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShowDelete` | `bool` | 是否可以删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetConsumptionLogById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.consumption.business_get_consumption_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消费与核销 |
| 用途 | 获取消费明细列表(首页) |
| 使用时机 | 商家核对会员消费、充值、核减、返还、退款前置事实或操作后余额时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Consumption/BusinessGetConsumptionList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetConsumptionListResponseViewModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetConsumptionListResponseViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ConsumptionController.cs:56` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex == 0) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `IsIncome` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否是收款记录；普通业务字段；可按问题需要提供 |
| `PayType` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 支付方式:1 微信支付，2支付宝，3 线下(可为空)；普通业务字段；可按问题需要提供 |
| `PayWay` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 支付渠道 0 线上 1 现金 2POST 3个人码微信 4个人码支付宝(可为空)；普通业务字段；可按问题需要提供 |
| `PCardId` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡Id(可为空)（SelectType 2，5不可为空）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserType` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 用户群Type 1新客 2老客 3会员(可为空)；普通业务字段；可按问题需要提供 |
| `SelectType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 查询类型 0全部，1收入，2售卡，3续费，4手动添加流水，5卡核销分析，6顾客刷卡 7管理员核减；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetConsumptionListResponseViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionId` | `string` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Userid` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 会员卡id 0为散户 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 2时限卡 3权益卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].PayType` | `int` | 支付方式：0 会员卡，1 微信支付，2支付宝，3 现金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionType` | `int` | 消费方式：0 计次，1 金额，2积分，3印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OperationType` | `int` | 操作方式：0 用户，1管理员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StoreUserName` | `string` | 操作店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].ConsumptionTag` | `int` | consumption_tag 0支出 ，1充值,2 核减，3返还，4赠送，5付款,6初始化,7商家录入,8过期(积分),9优惠券,10商户提现 11 调整有效期 20 限时卡停卡 21限时卡开卡 22 限时卡复卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionTagValue` | `string` | 0支出 ，1充值,2 核减，3返还，4赠送，5付款,6商家初始化,10商户提现 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OtherValue` | `decimal` | 备用字段，例如次卡充值时充值的次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].AfterValue` | `string` | 操作后值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionSum` | `int` | 消费次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateYear` | `string` | 创建时间(年) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].DataType` | `int` | 数据类型0列表 1统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 0未到账，1已到账 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Note` | `string` | 备注 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsCoupon` | `int` | 是否使用优惠卷 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsIntegral` | `bool` | 是否使用积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsBookkeeping` | `bool` | 是否记账 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].IsPerformance` | `bool` | 是否是绩效订单 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PerformanceState` | `int` | -1关闭设置 0 待设置 1 已设置 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems` | `List<ConsumptionServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ItemId` | `int` | 项目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ServiceItems[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ConsumptionValue` | `decimal` | consumption_value 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].AfterValue` | `decimal` | after_value 操作后值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].PayPrice` | `decimal` | 实付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ItemUnit` | `string` | 服务单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products` | `List<ConsumptionProductViewModel>` | 商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductId` | `int` | 商品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Products[].ProductTitle` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].SkuName` | `string` | Sku名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductImg` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductSum` | `int` | 商品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductUnit` | `int` | 商品单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].PayPrice` | `string` | 实付总金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsHaveInfo` | `bool` | 是否有小票 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsOfflineOrder` | `bool` | 是否是线下订单 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsAxc` | `bool` | 是否安心充订单 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsRefunds` | `bool` | 是否已退款 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`model.PageSize == 0`；`model.PageIndex == 0`；`resultMsg.State`；`role == null \|\| role.State == 0`；`staff != null`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserById`、`ConsumptionLogProvider.BusinessGetConsumptionLogList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`固定提示：参数不正确`；`固定提示：请求数据数量不正确`；`固定提示：页码不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.consumption.business_get_consumption_rank`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消费与核销 |
| 用途 | 获取消费排名 |
| 使用时机 | 商家核对会员消费、充值、核减、返还、退款前置事实或操作后余额时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Consumption/BusinessGetConsumptionRank` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetConsumptionRankResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetConsumptionRankResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ConsumptionController.cs:1280` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询时间 开始；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询时间 结束；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetConsumptionRankResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Uid` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 用户名字 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].TotalPrice` | `decimal` | 消费金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| string.IsNullOrEmpty(model.BeginDate) \|\| string.IsNullOrEmpty(model.EndDate)`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.GetConsumptionRank`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.consumption.business_get_user_consumption_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 消费与核销 |
| 用途 | 获取会员消费明细列表(会员详情页) |
| 使用时机 | 商家核对会员消费、充值、核减、返还、退款前置事实或操作后余额时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Consumption/BusinessGetUserConsumptionList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetUserConsumptionListResponseViewModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetUserConsumptionListResponseViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ConsumptionController.cs:124` |
| C/B 对照 | capi.consumption.get_user_consumption_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChildCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 子卡ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SearchType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 查询类型 0所有 1开卡停卡 2流水明细；普通业务字段；可按问题需要提供 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex == 0) | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetUserConsumptionListResponseViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ConsumptionId` | `string` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].StoreName` | `string` | 门店名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PayType` | `int` | 支付方式：0 会员卡，1 微信支付，2支付宝，3 现金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 2时限卡 3权益卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionType` | `int` | 消费方式：0 计次，1 金额，2积分，3印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OperationType` | `int` | 操作方式：0 用户，1管理员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionImg` | `string` | 足迹头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StoreUserName` | `string` | 操作店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].ConsumptionTag` | `int` | consumption_tag 0支出 ，1充值,2 核减，3返还，4赠送，5付款,6初始化,7商家录入,8过期(积分),9优惠券,10商户提现 11 调整有效期 20 限时卡停卡 21限时卡开卡 22 限时卡复卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionTagValue` | `string` | 0支出 ，1充值,2 核减，3返还，4赠送，5付款,6商家初始化,10商户提现 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OtherValue` | `decimal` | 备用字段，例如次卡充值时充值的次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].AfterValue` | `string` | 操作后值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateYear` | `string` | 创建时间(年) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].DataType` | `int` | 数据类型0列表 1统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsCoupon` | `int` | 是否使用优惠卷 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsIntegral` | `bool` | 是否使用积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsPerformance` | `bool` | 是否是绩效订单 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PerformanceState` | `int` | -1关闭设置 0 待设置 1 已设置 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].ServiceItems` | `List<ConsumptionServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ItemId` | `int` | 项目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ServiceItems[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ConsumptionValue` | `decimal` | consumption_value 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].AfterValue` | `decimal` | after_value 操作后值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].PayPrice` | `decimal` | 实付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ItemUnit` | `string` | 服务单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products` | `List<ConsumptionProductViewModel>` | 商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductId` | `int` | 商品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Products[].ProductTitle` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].SkuName` | `string` | Sku名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductImg` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductSum` | `int` | 商品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].ProductUnit` | `int` | 商品单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Products[].PayPrice` | `string` | 实付总金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsHaveInfo` | `bool` | 是否有小票 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsOfflineOrder` | `bool` | 是否是线下订单 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsBookkeeping` | `bool` | 是否记账 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsAxc` | `bool` | 是否安心充订单 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsRefunds` | `bool` | 是否已退款 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`model.PageSize == 0`；`model.PageIndex == 0`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetUserConsumptionLogList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`固定提示：参数不正确`；`固定提示：请求数据数量不正确`；`固定提示：页码不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.mobile_management.get_binding_status`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 移动端绑定 |
| 用途 | 查询当前登录员工发起的微信绑定进度。 UID 与设备摘要必须同时匹配，其他已登录账号不能读取或领取该会话结果。 |
| 使用时机 | 在商家视角中核对“查询当前登录员工发起的微信绑定进度。 UID 与设备摘要必须同时匹配，其他已登录账号不能读取或领取该会话结果。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MobileManagement/GetBindingStatus` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<MobileManagementStatusResponseModel>`；包装 `DataResult`；Data `MobileManagementStatusResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MobileManagementController.cs:64` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BindingId` | `string` | 模型特性声明必填 | 绑定=ApiController推断；[Required] | 必须来自同一会话上游 API 结果或服务端对象引用 | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `DeviceId` | `string` | 代码校验必填 | 绑定=ApiController推断；[Required]；if(uid < 1 \\|\\| string.IsNullOrWhiteSpace(model?.BindingId) \\|\\| string.IsNullOrWhiteSpace(model.DeviceId)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 必须与创建二维码时使用的浏览器设备标识一致。；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Status` | `string` | UNBOUND、BOUND、WAITING、SCANNED、CONFLICT、REJECTED 或 EXPIRED。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Message` | `string` | 可直接展示给当前用户的状态说明。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`uid < 1 \|\| string.IsNullOrWhiteSpace(model?.BindingId) \|\| string.IsNullOrWhiteSpace(model.DeviceId)`；`pollCount > 60`
- 一层业务调用：`RedisManager.IncrementToRedisDb`、`YueKeMobileManagementProvider.GetBindingStatus`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.mobile_management.get_status`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 移动端绑定 |
| 用途 | 获取当前登录员工的移动端开通状态。 |
| 使用时机 | 在商家视角中核对“获取当前登录员工的移动端开通状态。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/MobileManagement/GetStatus` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<MobileManagementStatusResponseModel>`；包装 `DataResult`；Data `MobileManagementStatusResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/MobileManagementController.cs:20` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Status` | `string` | UNBOUND、BOUND、WAITING、SCANNED、CONFLICT、REJECTED 或 EXPIRED。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Message` | `string` | 可直接展示给当前用户的状态说明。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`YueKeMobileManagementProvider.GetCurrentStatus`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_card_analyse`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 会员卡分析 |
| 使用时机 | 在商家视角中核对“会员卡分析”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetCardAnalyse` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCardAnalyseResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCardAnalyseResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:905` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas` | `List<CardAnalyseDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].CardId` | `int` | 储值卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardAnalyseDatas[].CardName` | `string` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].CardImg` | `string` | 储值卡图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].CardType` | `int` | 类型：0计次，1储值 2时效卡 3权益卡 4安心充卡 6课时卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].Price` | `decimal` | 售价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].TotalPrice` | `decimal` | 售价总和 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].SellCount` | `long` | 销售数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].PeopleCount` | `long` | 持卡人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardAnalyseDatas[].CardState` | `int` | -1删除 0停售 1在售 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StorePrepaidCardProvider.BusinessGetCardAnalyse`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_card_reservation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 上课用卡分析 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetCardReservation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCardReservationResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCardReservationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:946` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 店员id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas` | `List<CardReservationDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].CardId` | `int` | 储值卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardReservationDatas[].CardName` | `string` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].CardImg` | `string` | 储值卡图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].CardType` | `int` | 类型：0计次，1储值 2时效卡 3权益卡 4安心充卡 6课时卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].TotalCount` | `long` | 用卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].Proportion` | `double` | 使用占比 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].CardState` | `int` | -1删除 0停售 1在售 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StorePrepaidCardProvider.BusinessGetCardReservation`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_card_reservation_course`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 上课用卡分析 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetCardReservationCourse` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCardReservationCourseResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCardReservationCourseResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:987` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId==0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId==0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `long` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.PrepaidCardId==0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationCourseDatas` | `List<BusinessGetCardReservationCourseDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationCourseDatas[].Id` | `long` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardReservationCourseDatas[].DataImage` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationCourseDatas[].DataName` | `string` | 数据名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationCourseDatas[].CourseType` | `int` | 课目类似 0团课 1私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationCourseDatas[].TotalCount` | `long` | 试听课总人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.PrepaidCardId==0`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetCardReservationCourse`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_expiring`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 临期会员统计信息 |
| 使用时机 | 在商家视角中核对“临期会员统计信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetExpiring` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetExpiringResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetExpiringResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:785` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Conditions` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 条件 0按星期统计 1按月统计；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpiringDatas` | `List<ExpiringDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpiringDatas[].DateIndex` | `long` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpiringDatas[].StartDate` | `string` | 条件开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpiringDatas[].EndDate` | `string` | 条件结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExpiringDatas[].UserCount` | `long` | 过期会员 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardChildProvider.BusinessGetExpiring`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_lessons_data_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 上课信息统计 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLessonsDataStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreLessonsProvider.BusinessGetCancelRate, StoreLessonsProvider.BusinessGetOpenGroupLessonsProportion |
| 返回 | `Task<DataResult<BusinessGetLessonsDataStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetLessonsDataStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:1068` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `DateType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 数据类型 0团课人次，1团课节数，2私教节数，3团课约满率，4 团课取消率， 5团课开课率，6团课满班率；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas` | `List<LessonsDataStatisticalDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StatisticalDatas[].DataImage` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].DataName` | `string` | 数据名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].Proportion` | `decimal` | 占比 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].DataCount` | `long` | 统计总数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreLessonsProvider.BusinessGetGroupLessonsPeople`、`StoreLessonsProvider.BusinessGetGroupLessonsCount`、`StoreLessonsProvider.BusinessGetPrivateLessonsCount`、`StoreLessonsProvider.BusinessGetPeopleRate`、`StoreLessonsProvider.BusinessGetCancelRate`、`StoreLessonsProvider.BusinessGetOpenGroupLessonsProportion`、`StoreLessonsProvider.BusinessGetOccupancyRate`
- 疑似副作用：`StoreLessonsProvider.BusinessGetCancelRate`、`StoreLessonsProvider.BusinessGetOpenGroupLessonsProportion`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_lessons_people`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 每日上课人数统计 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLessonsPeople` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetLessonsPeopleResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetLessonsPeopleResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:1027` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PeopleDatas` | `List<LessonsPeopleDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.PeopleDatas[].CalendarDate` | `string` | 日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.PeopleDatas[].PeopleCount` | `long` | 人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.BusinessGetLessonsPeople`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_lessons_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户课程预约统计信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLessonsStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetLessonsStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetLessonsStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:712` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 店员Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型 0团课 1私教 2班课 10团课和班课；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `IsShowPrivateLessonsName` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否显示私教课名称；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData` | `List<StoreLessonsStatisticalModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].CourseId` | `int` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SchedulingData[].CourseType` | `int` | 课目分类 0团课 1私教 2班课 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].CourseName` | `string` | 课目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].StaffId` | `int` | 教练id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SchedulingData[].StaffName` | `string` | 教练名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].StaffImg` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].LessonsCount` | `long` | 排课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].LessonsPeopleCount` | `long` | 上课人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreLessonsProvider.BusinessGetLessonsStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_lessons_time_heatmap`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取团课、班课按星期和三小时时段聚合的预约人数利用率。 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLessonsTimeHeatmap` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<BusinessGetLessonsTimeHeatmapResponseModel>`；包装 `DataResult`；Data `BusinessGetLessonsTimeHeatmapResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:752` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| !DateTime.TryParse(model.BeginDate, out var beginDate) \\|\\| !DateTime.TryParse(model.EndDate, out var endDate) \\|\\| beginDate.Date > endDate.Date) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| !DateTime.TryParse(model.BeginDate, out var beginDate) \\|\\| !DateTime.TryParse(model.EndDate, out var endDate) \\|\\| beginDate.Date > endDate.Date) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始日期，格式 yyyy-MM-dd。；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束日期，格式 yyyy-MM-dd。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas` | `List<LessonsTimeHeatmapDataModel>` | 按星期和三小时时段聚合的课程统计。 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].WeekDay` | `int` | 星期索引，0 表示周一，6 表示周日。 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].StartHour` | `int` | 三小时时段的开始小时，例如 18 表示 18:00-21:00。 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].LessonsCount` | `long` | 排课节数。 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].ReservationCount` | `long` | 有效预约人次。 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CapacityCount` | `long` | 可预约总容量。 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].OccupancyRate` | `decimal` | 预约人数利用率，单位为百分比。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| !DateTime.TryParse(model.BeginDate, out var beginDate) \|\| !DateTime.TryParse(model.EndDate, out var endDate) \|\| beginDate.Date > endDate.Date`
- 一层业务调用：`StoreLessonsProvider.BusinessGetLessonsTimeHeatmap`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_liability_card_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 负债卡信息 |
| 使用时机 | 在商家视角中核对“负债卡信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLiabilityCardInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetLiabilityCardInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetLiabilityCardInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:1208` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsAllCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否包含过期卡；普通业务字段；可按问题需要提供 |
| `LiabilityDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 负债月份 yyyy-MM；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrepaidCardId` | `int` | 储值卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardName` | `string` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardImg` | `string` | 储值卡图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardType` | `int` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalPeopleCount` | `long` | 持卡人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCardCount` | `long` | 持卡张数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCardPrice` | `decimal` | 剩余数量总计：次卡为次、储值卡为金额、周期卡为天、课时卡为课时 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalGivePrice` | `decimal` | 剩余赠送数量总计；单位与对应卡类型一致 | 普通业务字段 | 可按问题需要提供 |
| `Data.LiabilityPrice` | `decimal` | 负债金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services` | `List<LiabilityCardInfoDataModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].TotalCardPrice` | `decimal` | 剩余金额总计 | 普通业务字段 | 可按问题需要提供 |
| `Data.Services[].TotalGivePrice` | `decimal` | 剩余赠金总计 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardChildLiabilityProvider.BusinessGetLiabilityCardInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_liability_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 负债信息 |
| 使用时机 | 在商家视角中核对“负债信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLiabilityInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetLiabilityInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetLiabilityInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:1172` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 卡类型：0次卡、1储值卡、3周期卡、6课时卡；普通业务字段；可按问题需要提供 |
| `IsAllCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否包含过期卡；普通业务字段；可按问题需要提供 |
| `LiabilityDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 负债月份 yyyy-MM；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalPeopleCount` | `long` | 持卡人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCardCount` | `long` | 持卡张数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCardPrice` | `decimal` | 剩余数量总计；按卡类型分别表示次、金额、天或课时，全部卡类型时不可直接跨单位比较 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalGivePrice` | `decimal` | 剩余赠送数量总计；单位与对应卡类型一致 | 普通业务字段 | 可按问题需要提供 |
| `Data.LiabilityPrice` | `decimal` | 负债金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas` | `List<LiabilityInfoDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].PrepaidCardId` | `int` | 储值卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StatisticalDatas[].CardName` | `string` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CardImg` | `string` | 储值卡图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CardType` | `int` | 类型：0计次，1储值 2时效卡 3权益卡 4安心充卡 6课时卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].TotalPeopleCount` | `long` | 持卡人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].TotalCardCount` | `long` | 持卡张数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].TotalCardPrice` | `decimal` | 剩余数量总计：次卡为次、储值卡为金额、周期卡为天、课时卡为课时 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].TotalGivePrice` | `decimal` | 剩余赠送数量总计；单位与对应卡类型一致 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].LiabilityPrice` | `decimal` | 负债金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CardState` | `int` | -1删除 0停售 1在售 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardChildLiabilityProvider.BusinessGetLiabilityInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_liability_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 负债统计 |
| 使用时机 | 在商家视角中核对“负债统计”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLiabilityStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetLiabilityStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetLiabilityStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:1131` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 卡类型：0次卡、1储值卡、3周期卡、6课时卡；普通业务字段；可按问题需要提供 |
| `IsAllCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否包含过期卡；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas` | `List<LiabilityDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CalendarDate` | `string` | 日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].LiabilityPrice` | `decimal` | 负债金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardChildLiabilityProvider.BusinessGetLiabilityStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_liability_user_card_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 负债卡会员列表 |
| 使用时机 | 已定位会员和卡后，核对商家端单张卡详情、余额、操作记录或可管理状态。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetLiabilityUserCardInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetLiabilityUserCardInfoResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetLiabilityUserCardInfoResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:1243` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `KeyWord` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 搜索关键词；普通业务字段；可按问题需要提供 |
| `PrepaidCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsAllCard` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否包含过期卡；普通业务字段；可按问题需要提供 |
| `LiabilityDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 负债月份 yyyy-MM；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetLiabilityUserCardInfoResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 会员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardPrice` | `decimal` | 剩余数量：次卡为次、储值卡为金额、周期卡为天、课时卡为课时 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardGivePrice` | `decimal` | 剩余赠送数量；单位与对应卡类型一致 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services` | `List<LiabilityCardInfoDataModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services[].TotalCardPrice` | `decimal` | 剩余金额总计 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Services[].TotalGivePrice` | `decimal` | 剩余赠金总计 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardChildLiabilityProvider.BusinessGetLiabilityUserCardInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_income_report_table`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺提现统计报表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreIncomeReportTable` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetStoreIncomeReportTableResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetStoreIncomeReportTableResponseModel>` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:569` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |
| `Conditions` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 条件 0按日统计 1按月统计；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetStoreIncomeReportTableResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Year` | `string` | 年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Introduction` | `string` | 信息说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SumMony` | `decimal` | 总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PayCount` | `long` | 收入笔数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetStoreIncomeReportTable`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_income_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 收款管理统计 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreIncomeStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreIncomeStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreIncomeStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:530` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumMony` | `decimal` | 总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayCount` | `long` | 收入笔数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetStoreIncomeStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_people_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺顾客统计信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStorePeopleStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStorePeopleStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStorePeopleStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:316` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas` | `List<StorePeopleStatisticalModel>` | 顾客统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].TypeName` | `string` | 用户群名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].UserType` | `long` | 用户群Type 1新客 2老客 3会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].PeopleSum` | `long` | 人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetStorePeopleStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_report_table`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺统计报表（报表页） |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreReportTable` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetStoreReportTableResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetStoreReportTableResponseModel>` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:402` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |
| `Conditions` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 条件 0按日统计 1按月统计 2按周；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `FillEmptyPeriods` | `bool?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 是否补齐筛选区间内没有流水的日期；不传时保持旧端的补齐行为；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetStoreReportTableResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Year` | `string` | 年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Introduction` | `string` | 信息说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SumMony` | `decimal` | 总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SumOpenCard` | `long` | 总计开卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SumRenewal` | `long` | 总计续费人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SumNonVipPeople` | `long` | 散客人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SumVipPeople` | `long` | 总计会员人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RefundsMoney` | `decimal` | 退款金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RefundsOrderSum` | `long` | 退款次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].DataType` | `int` | 数据类型 0普通列表数据 1合计数据 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetStoreReportTable`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_report_table_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺统计报表（报表页） |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreReportTableV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetStoreReportTableV2ResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetStoreReportTableV2ResponseModel>` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:439` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |
| `Conditions` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 条件 0按日统计 1按月统计 2按周；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `FillEmptyPeriods` | `bool?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 是否补齐筛选区间内没有流水的日期；不传时保持旧端的补齐行为；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetStoreReportTableV2ResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Year` | `string` | 年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Introduction` | `string` | 信息说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SumMoney` | `decimal` | 总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SellCardMoney` | `decimal` | 售卡收入 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OtherMoney` | `decimal` | 其他收入金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RefundsMoney` | `decimal` | 退款金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SellCardSum` | `long` | 售卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PayCount` | `long` | 收入交易笔数，仅统计已记账的售卡和其他收入流水 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].DataType` | `int` | 数据类型 0普通列表数据 1合计数据 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetStoreReportTableV2`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_reservation_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺统计报表（报表页） |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreReservationStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreReservationStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreReservationStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:476` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumMoney` | `decimal` | 总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.WillExpiringMembers` | `decimal` | 即将到期的会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonsCount` | `long` | 团课数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivateLessonsCount` | `long` | 私教数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonsPeopleCount` | `long` | 团课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonsPeopleSignCount` | `long` | 团课签到人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivateLessonsPeopleCount` | `long` | 私教人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivateLessonsPeopleSignCount` | `long` | 私教签到人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LastUserLessonsLogId` | `long` | 最后课程操作记录 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RefundFailureLessonsSum` | `long` | 预约退款失败订单数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignSum` | `long` | 签到预约数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.BirthdayUserCount` | `long` | 生日会员数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`role == null \|\| role.State == 0`；`staff != null`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserById`、`ConsumptionLogProvider.BusinessGetStoreReservationStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_sell_card_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺售卡统计信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreSellCardStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreSellCardStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreSellCardStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:273` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas` | `List<StoreSellCardStatisticalModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CardName` | `string` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CardId` | `int` | 储值卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StatisticalDatas[].ConsumptionCount` | `long` | 消费笔数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].StatisticalMoney` | `decimal` | 金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetStoreSellCardStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺统计信息（首页 报表页） |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:117` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumMony` | `decimal` | 总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumVipMony` | `decimal` | 会员总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumVipPeople` | `long` | 总计会员人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumOpenCard` | `long` | 总计开卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumRenewal` | `long` | 总计续费人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumNonVipMony` | `decimal` | 非会员总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumNonVipPeople` | `long` | 散客人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumOldNonVipPeople` | `long` | 多次散客 | 普通业务字段 | 可按问题需要提供 |
| `Data.SellCardMoney` | `decimal` | 售卡收入 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardConsumptionMoney` | `decimal` | 卡内消费 | 普通业务字段 | 可按问题需要提供 |
| `Data.RefundsMoney` | `decimal` | 退款金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.RefundsOrderSum` | `long` | 退款次数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`role == null \|\| role.State == 0`；`staff != null`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserById`、`ConsumptionLogProvider.BusinessGetStoreStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_statistical_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺统计信息（首页 报表页） |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreStatisticalV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreStatisticalV2ResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreStatisticalV2ResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:173` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumMoney` | `decimal` | 总计金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SellCardMoney` | `decimal` | 售卡收入 | 普通业务字段 | 可按问题需要提供 |
| `Data.OtherMoney` | `decimal` | 其他收入金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.RefundsMoney` | `decimal` | 退款金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SellCardSum` | `long` | 售卡数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.NewVipSum` | `long` | 新会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.RenewalVipSum` | `long` | 续费会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayCount` | `long` | 收入交易笔数，仅统计已记账的售卡和其他收入流水 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`role == null \|\| role.State == 0`；`staff != null`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserById`、`ConsumptionLogProvider.BusinessGetStoreStatisticalV2`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.business_get_store_used_card_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 获取商户店铺销卡统计信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/BusinessGetStoreUsedCardStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreUsedCardStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreUsedCardStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:359` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StartDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 条件结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas` | `List<StoreUsedCardStatisticalModel>` | 顾客统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CardId` | `int` | 储值卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StatisticalDatas[].CardName` | `string` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].CardType` | `int` | 类型：0计次，1储值 2时效卡 3权益卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].UsedSum` | `long` | 使用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StatisticalDatas[].UsedMoney` | `decimal` | 核销金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ConsumptionLogProvider.BusinessGetStoreUsedCardStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.get_lessons_calendar_scheduling`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 上课统计信息（日历） |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/GetLessonsCalendarScheduling` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetLessonsCalendarSchedulingResponseModel>>`；包装 `Task/DataResult`；Data `GetLessonsCalendarSchedulingResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:69` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 店员ID 可不传；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型 0团课 1私教；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `Conditions` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 条件 0按日统计 1按月统计 2周；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.BeginDate` | `string` | 返回数据的起始日期(店铺注册日期) | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData` | `List<LessonsCalendarSchedulingResponseModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].DateInfo` | `string` | 日期 返回到月 按日统计则返回“yyyy-mm” | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].Introduction` | `string` | 信息说明 按日 返回具体日期 按周 返回具体 哪周（1-5） | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].SchedulingInfo` | `long` | 统计信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| !StringOperate.IsDate(model.BeginDate) \|\| !StringOperate.IsDate(model.EndDate)`；`resultMsg.State`；`DateTime.Parse(model.EndDate).Date<=sm.CreateDate.Date`；`DateTime.Parse(model.BeginDate).Date<sm.CreateDate.Date`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`StoreLessonsProvider.GetLessonsCalendarScheduling`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.operating_reports.get_staff_card_reservation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 经营报表 |
| 用途 | 上课用卡统计 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/OperatingReports/GetStaffCardReservation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffCardReservationResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffCardReservationResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/OperatingReportsController.cs:1280` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate) \\|\\| model.StaffId<1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate) \\|\\| model.StaffId<1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate) \\|\\| model.StaffId<1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 店员id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型 0团课 1私教 不传 所有；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate) \\|\\| model.StaffId<1) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate) \\|\\| model.StaffId<1) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas` | `List<StaffCardReservationDataModel>` | 统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].PrepaidCardId` | `int` | 储值卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardReservationDatas[].CardName` | `string` | 储值卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].CardImg` | `string` | 储值卡图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].CardType` | `int` | 类型：0计次，1储值 2时效卡 3权益卡 4安心充卡 6课时卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].TotalCount` | `long` | 用卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardReservationDatas[].TotalPrice` | `decimal` | 总计核销金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| string.IsNullOrEmpty(model.BeginDate) \|\| string.IsNullOrEmpty(model.EndDate) \|\| model.StaffId<1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetStaffCardReservation`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.shop_order.business_get_shop_order_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 订单 |
| 用途 | 商家端查询订单列表 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ShopOrder/BusinessGetShopOrderList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataList<BusinessGetShopOrderListResponseModel>`；包装 `DataList`；Data `BusinessGetShopOrderListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ShopOrderController.cs:21` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0 \\|\\| !statusArray.Contains(model.Status)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0 \\|\\| !statusArray.Contains(model.Status)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=1 | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=20 | AI 可在服务端上限内选择 | 每页条数；普通业务字段；可按问题需要提供 |
| `Keywords` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 当前会话提供并临时使用；不得持久化到模型历史 | 关键词，可为空 订单号、姓名、手机号等；个人信息；仅在当前授权场景按最小范围提供 |
| `Status` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 订单状态：-1 全部，0 等待买家付款；1 等待卖家发货 2 卖家已发货 3 订单完成 4 订单关闭；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Uid` | `int` | uid 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ReceiverName` | `string` | receiver_name 收货人的姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].ReceiverMobile` | `string` | receiver_mobile 收货人的手机号码 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].ConsumptionId` | `string` | 订单编号 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].IsExchange` | `bool` | 是否兑换 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Payment` | `decimal` | 实付款 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Status` | `int` | 订单状态 | 普通业务字段 | 可按问题需要提供 |
| `Data[].TakeTime` | `string` | 自提时间（例：10:00） | 普通业务字段 | 可按问题需要提供 |
| `Data[].OrderType` | `int` | 0邮寄 1配送 2自提 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PickupCode` | `string` | 自提提货码 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CreateDate` | `DateTime` | 下单时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ProductCount` | `int` | 商品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products` | `List<ShopOrderProductModel>` | 商品列表 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Products[].StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Products[].Uid` | `int` | uid 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Products[].ShopOrderId` | `int` | shop_order_id 订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Products[].ProductId` | `int` | product_id 商品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Products[].SkuId` | `int` | sku_id 商品SkuID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Products[].Pno` | `string` | 货号 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].ProductName` | `string` | product_name 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].ProductImg` | `string` | product_img 商品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].ProductCount` | `int` | product_num 数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].ProductPrice` | `decimal` | product_price 商品价格 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].SkuString` | `string` | sku_string SKU描述 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].SkuValue` | `string` | sku_value SKU值 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].CreateDate` | `DateTime` | create_date | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].CreateBy` | `int` | create_by | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].UpdateDate` | `DateTime` | update_date | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].UpdateBy` | `int` | update_by | 普通业务字段 | 可按问题需要提供 |
| `Data[].Products[].TenantId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.StoreId <= 0 \|\| model.Uid <= 0 \|\| !statusArray.Contains(model.Status)`
- 一层业务调用：`ShopOrderProvider.BusinessGetShopOrderList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数有误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.shop_order.get_shop_order_detail`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 订单 |
| 用途 | 获取订单详情 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ShopOrder/GetShopOrderDetail` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetShopOrderDetailResponseModel>>`；包装 `Task/DataResult`；Data `GetShopOrderDetailResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ShopOrderController.cs:47` |
| C/B 对照 | capi.shop_order.get_shop_order_detail |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0 \\|\\| model.ShopOrderId <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；if(model.StoreId <= 0 \\|\\| model.Uid <= 0 \\|\\| model.ShopOrderId <= 0) | 服务端注入：已确认门店 | 店铺ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ShopOrderId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId <= 0 \\|\\| model.Uid <= 0 \\|\\| model.ShopOrderId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 商城订单ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Uid` | `int` | uid 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.BuyerHeadImg` | `string` | 买家头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.PicPath` | `string` | pic_path 商品图片绝对途径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Title` | `string` | title 订单标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Status` | `int` | status 订单状态：-2退款，0 等待买家付款；1 等待卖家发货 2 卖家已发货 3 订单完成 4 订单关闭 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExchange` | `bool` | 是否兑换 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductCount` | `int` | product_number 商品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.NeedPayment` | `decimal` | need_payment 应付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Payment` | `decimal` | payment 实付金额。精确到2位小数;单位:元。如:200.07，表示:200元7分 | 普通业务字段 | 可按问题需要提供 |
| `Data.NeedPost` | `bool` | need_post 是否需要邮寄 | 普通业务字段 | 可按问题需要提供 |
| `Data.PostFee` | `decimal` | post_fee 邮费。精确到2位小数;单位:元。如:200.07，表示:200元7分 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiverName` | `string` | receiver_name 收货人的姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReceiverAddress` | `string` | receiver_address 收货人的详细地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReceiverZip` | `string` | receiver_zip 收货人的邮编 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReceiverMobile` | `string` | receiver_mobile 收货人的手机号码 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PostCompany` | `string` | post_company 快递公司 | 普通业务字段 | 可按问题需要提供 |
| `Data.PostOrderNo` | `string` | post_order_no 快递单号 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Remark` | `string` | remark 买家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.BusinessRemark` | `string` | business_remark 卖家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.BuyerName` | `string` | buyer_name 买家名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayMethod` | `int` | pay_method 支付方式：0 会员卡支付、1微信支付、2支付宝支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateDate` | `DateTime` | create_date 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayDate` | `DateTime` | pay_date 支付时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.PostDate` | `DateTime` | post_date 发货时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpdateDate` | `DateTime` | update_date 更新时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateBy` | `int` | create_by 创建人 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpdateBy` | `int` | update_by 更新人 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionId` | `string` | consumption_id 财务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IntegralDeduct` | `decimal` | 积分减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommissionDeduct` | `decimal` | 佣金减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardDeduct` | `decimal` | 权益卡、储值卡减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponDeduct` | `decimal` | 优惠券减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products` | `List<ShopOrderProductModel>` | 商品列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Products[].StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Products[].Uid` | `int` | uid 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Products[].ShopOrderId` | `int` | shop_order_id 订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Products[].ProductId` | `int` | product_id 商品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Products[].SkuId` | `int` | sku_id 商品SkuID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Products[].Pno` | `string` | 货号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductName` | `string` | product_name 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductImg` | `string` | product_img 商品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductCount` | `int` | product_num 数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].ProductPrice` | `decimal` | product_price 商品价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].SkuString` | `string` | sku_string SKU描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].SkuValue` | `string` | sku_value SKU值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].CreateDate` | `DateTime` | create_date | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].CreateBy` | `int` | create_by | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].UpdateDate` | `DateTime` | update_date | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].UpdateBy` | `int` | update_by | 普通业务字段 | 可按问题需要提供 |
| `Data.Products[].TenantId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IsShowRefundButton` | `bool` | 是否显示退款按钮 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsRefunds` | `bool` | 是否已退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceId` | `string` | 退款原订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RefundPrice` | `decimal` | 退款金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserName` | `string` | 操作店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.TakeTime` | `string` | 自提时间（例：10:00） | 普通业务字段 | 可按问题需要提供 |
| `Data.OrderType` | `int` | 0邮寄 1配送 2自提 | 普通业务字段 | 可按问题需要提供 |
| `Data.PickupCode` | `string` | 自提提货码 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId <= 0 \|\| model.Uid <= 0 \|\| model.ShopOrderId <= 0`
- 一层业务调用：`ShopOrderProvider.GetShopOrderDetail`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 200`；`固定提示：请求参数有误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.business_get_lessons_rank`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取上课排名 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/BusinessGetLessonsRank` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetLessonsRankResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetLessonsRankResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:4335` |
| C/B 对照 | capi.lessons.get_lessons_rank |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询时间 开始；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询时间 结束；普通业务字段；可按问题需要提供 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型 0团课 1私教；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetLessonsRankResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `int` | 会员id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Uid` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 用户名字 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsCount` | `long` | 上课次数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| string.IsNullOrEmpty(model.BeginDate) \|\| string.IsNullOrEmpty(model.EndDate)`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetLessonsRank`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.check_user_reservation_seting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取商家预约设置 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/CheckUserReservationSeting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StoreReservationProvider.GetUserCancelSum |
| 返回 | `Task<DataResult<CheckUserReservationSetingResponseModel>>`；包装 `Task/DataResult`；Data `CheckUserReservationSetingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:2177` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOnlyStaff` | `bool` | 私教是否只能约指定教练 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserStaffId` | `long` | 登录人的销售顾问 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserCancelSum` | `long` | 用户在周期内已取消次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelCycle` | `int` | 0天 1周 2月 当N | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelSum` | `int` | 取消N次 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelAppointmentDays` | `int` | N天不允许预约 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`；`ucModel?.Id > 0`；`resultMsg.Data.CancelSum > 0`
- 一层业务调用：`StoreReservationSetingProvider.GetReservationSeting`、`UserCardProvider.BusinessGetUserCardInfo`、`StoreReservationProvider.GetUserCancelSum`
- 疑似副作用：`StoreReservationProvider.GetUserCancelSum`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_always_lessons_user`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取常用会员 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetAlwaysLessonsUser` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetLessonsRankResponseModel>>`；包装 `Task/DataResult`；Data `GetLessonsRankResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:4260` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 查询月份；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.RankData` | `List<LessonsRankViewModel>` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.RankData[].Rank` | `long` | 排名 | 普通业务字段 | 可按问题需要提供 |
| `Data.RankData[].CardId` | `int` | 会员id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RankData[].Uid` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RankData[].UserName` | `string` | 用户名字 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.RankData[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.RankData[].LessonsCount` | `long` | 上课次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.RankData[].IsInBlackList` | `bool` | 是否在黑名单中（仅生效中） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetAlwaysLessonsUser`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_course_scheduling`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 按日期范围读取门店排课日历，可分别返回排课时间、排课数量或预约人数。 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetCourseScheduling` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCourseSchedulingResponseModel>>`；包装 `Task/DataResult`；Data `GetCourseSchedulingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:43` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.DataType is < 1 or > 3 \\|\\| model.Conditions is < 0 or > 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.DataType is < 1 or > 3 \\|\\| model.Conditions is < 0 or > 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课Id 可不传；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 教练ID 可不传；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型：0团课，1私教 可不传；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `Conditions` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 聚合粒度：0 按日统计，1 按月统计；普通业务字段；可按问题需要提供 |
| `DataType` | `int` | 模型特性声明必填 | 绑定=ApiController推断；[Required] | AI 只能使用文档确认的枚举值 | 返回维度：1 排课时间，2 排课数量，3 预约人数；必须明确提供，不能使用默认值 0；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData` | `List<CourseSchedulingResponseModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].Date` | `string` | 日期 按日统计则返回“yyyy-mm-dd” 按月则返回“yyyy-mm” | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].SchedulingData` | `List<string>` | 排课数据 多条具体时间信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].DataSum` | `long` | 排课数/预约人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.DataType is < 1 or > 3 \|\| model.Conditions is < 0 or > 1`；`resultMsg.State`；`lms?.Count > 0`；`model.Conditions == 1`
- 一层业务调用：`StoreLessonsProvider.GetStoreLessonsTime`、`StoreLessonsProvider.GetStoreLessonsSum`、`StoreReservationProvider.GetStoreReservationPeopleSum`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取单个课程信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessons` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetLessonsResponseModel>>`；包装 `Task/DataResult`；Data `GetLessonsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:1180` |
| C/B 对照 | capi.lessons.get_lessons |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.LessonsId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.LessonsId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LessonsId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.LessonsId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseId` | `long` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseImage` | `string` | 课程图片id | 普通业务字段 | 可按问题需要提供 |
| `Data.ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseName` | `string` | 课目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.BeginDate` | `string` | 开始时间 yyyy-MM-dd hh:mm | 普通业务字段 | 可按问题需要提供 |
| `Data.EndDate` | `string` | 结束时间 yyyy-MM-dd hh:mm | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.TeachCount` | `int` | 最大授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.MinPeople` | `int` | 最小授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseStar` | `int` | 难度星级1-10 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignDate` | `string` | 签到时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelDate` | `string` | 取消时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAutoCancel` | `bool` | 是否自动停课 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态：0未开课，1已开课，2取消 3人数不足 5完课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials` | `List<StoreMaterialViewModel>` | 关联素材 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Id` | `int` | 素材ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].CategoryId` | `int` | 素材分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].CategoryName` | `string` | 素材分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MaterialName` | `string` | 素材名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MaterialType` | `int` | 素材类型：1图片，2视频，3视频号链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FilePath` | `string` | 素材路径；图片/视频为文件地址，视频号为链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileUrl` | `string` | 素材完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileName` | `string` | 文件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileExt` | `string` | 文件扩展名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileSize` | `long` | 文件大小，单位字节 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MimeType` | `string` | 文件MIME类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoCover` | `string` | 视频封面路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoCoverUrl` | `string` | 视频封面完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoNo` | `string` | 视频号标识 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoTimeLength` | `int` | 视频时长，单位秒 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Width` | `int` | 素材宽度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Height` | `int` | 素材高度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Materials[].OrderBy` | `int` | 排序值，值越大越靠前 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].State` | `int` | 状态：1启用，0停用，-1删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.LessonsId < 1`；`resultMsg.State`；`lms?.Id > 0`；`lms.CourseType == 0 \|\| lms.CourseType == 2`；`cm?.Id > 0`；`lms.CoursePlaceId > 0`；`pm?.Id > 0`；`courseStaffs?.Count > 0`；`staffModel?.Id > 0`；`lms.SignDate != null`；`lms.CancelDate != null`；`lms.CourseImageId > 0`
- 一层业务调用：`StoreLessonsProvider.GetStoreLessonsFirstOrDefaultByCondition`、`StoreCourseProvider.GetStoreCourses`、`StorePlaceProvider.GetStorePlaceById`、`StoreLessonsStaffProvider.GetStoreLessonsStaffElementsByCondition`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreMaterialProvider.GetLessonsMaterials`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取课程列表(排课) |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessonsList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetLessonsListResponseModel>>`；包装 `Task/DataResult`；Data `GetLessonsListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:257` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课目id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型：0团课，1私教；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程类型：教练id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |
| `IsFullData` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否需要更多的数据（约课信息）；普通业务字段；可按问题需要提供 |
| `IsReservationData` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否需要预约信息 约课人数 是否候补；普通业务字段；可按问题需要提供 |
| `ClassTagId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 分类id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `State` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 状态：0未开课，1完课，2取消 3人数不足 5上课中 10停课；普通业务字段；可按问题需要提供 |
| `IsSign` | `bool?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否签到；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData` | `List<LessonsResponseModel>` | 课程信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].CourseId` | `long` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].ClassesId` | `long` | 班级id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseOriginalName` | `string` | 原始课目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseImage` | `string` | 课程图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseStar` | `int` | 难度星级1-10 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].BeginTime` | `string` | 开课时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].EndTime` | `string` | 开课时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePeopleCount` | `int` | 最大授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].MinPeople` | `int` | 最小授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].IsReserve` | `bool` | 是否有候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].WaitlistCount` | `int` | 候补人数；供课表汇总视图读取，不要求返回完整预约人员明细。 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].IsAutoCancel` | `bool` | 是否自动触发取消 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].IsMake` | `bool` | 是否补课 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].State` | `int` | 状态；状态：0未开课，1完课，2取消 3（取消）人数不足 5上课中 10停课 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].VacationCount` | `long` | 请假人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].ClassTag` | `StoreReservationTagViewModel` | 分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].ClassTag.Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].ClassTag.TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples` | `List<LessonsInfoResponseModel>` | 预约人员信息，IsFullData=true时返回 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].LessonsPeoples[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].LessonsPeoples[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].LessonsPeoples[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].IsVacation` | `bool` | 是否请假 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].State` | `int` | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5完课 10候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].SignDate` | `string` | 签到时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].SignFile` | `string` | 签到文件（签字签到会有） | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].StateReason` | `string` | 状态原因 主要针对取消状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].PeopleCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].UpdateDate` | `string` | 最后更新时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].OperationType` | `int` | 操作方式：0 用户，1管理员,3 共享用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].OperationUserName` | `string` | 操作者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].OperationUserImg` | `string` | 操作者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].IsPay` | `bool` | 是否已支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption` | `LessonsLogsConsumptionViewModel` | 财务信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.ConsumptionId` | `string` | 财务ID 使用后更新 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.CardImg` | `string` | 卡图 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.IsRefund` | `bool` | 是否退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.RefundsReason` | `string` | 如果课程取消，财务未退款 则返回失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.AfterValue` | `string` | 消费后余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].Consumption.PayType` | `int` | 支付方式：0 会员卡，1 微信支付，2支付宝，3 现金 4优惠券 5兑换码 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos` | `List<GetReservationInfosModel>` | 预约信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos[].IsMust` | `int` | 是否必填项 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].LessonsPeoples[].ReservationInfos[].Items` | `List<GetReservationItemControlsListViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseTools` | `List<StoreReservationTagViewModel>` | 课程工具 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CourseTools[].Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsData[].CourseTools[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePrice` | `StoreCoursePriceViewModel` | 单次付费信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePrice.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePrice.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePrice.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePrice.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsData[].CoursePrice.State` | `int` | 状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`role == null \|\| role.State == 0`；`lms?.Count > 0`；`(model.IsFullData \|\| model.IsReservationData) && batchContext.ReservationCount > 0`；`rms?.Count > 0`；`model.IsFullData`；`lessonsItem.State == 0 \|\| lessonsItem.State == 5 \|\| lessonsItem.State == 1`；`rm.CardId > 0`；`userVacations.Any(x => x.CardId == rm.CardId) \|\| rm.IsVacation`；`rm.Uid > 0`；`model.IsReservationData`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreLessonsProvider.GetStoreLessonsTime`、`UserVacationProvider.GetUserVacationElementsByCondition`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UsersInfoProvider.UsersInfoGetByUid`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons_list_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取课程列表(排课 分页版) |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessonsListV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<LessonsResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<LessonsResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:407` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型：0团课，1私教，2班课 10 团课班课；普通业务字段；可按问题需要提供 |
| `ClassId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 班级id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsMake` | `bool?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否补课；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 教练id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |
| `IsFullData` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否需要更多的数据（约课信息）；普通业务字段；可按问题需要提供 |
| `IsReservationData` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否需要预约信息 约课人数 是否候补；普通业务字段；可按问题需要提供 |
| `ClassTagId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 分类id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PrepaidCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 储值卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `State` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 状态：0未开课，1完课，2取消 3人数不足 5上课中 10停课；普通业务字段；可按问题需要提供 |
| `OrderBy` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 排序 0开课时间倒序 1开课时间正序；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<LessonsResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CourseId` | `long` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ClassesId` | `long` | 班级id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseOriginalName` | `string` | 原始课目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseImage` | `string` | 课程图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseStar` | `int` | 难度星级1-10 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BeginTime` | `string` | 开课时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].EndTime` | `string` | 开课时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePeopleCount` | `int` | 最大授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MinPeople` | `int` | 最小授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsReserve` | `bool` | 是否有候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].WaitlistCount` | `int` | 候补人数；供课表汇总视图读取，不要求返回完整预约人员明细。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsAutoCancel` | `bool` | 是否自动触发取消 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsMake` | `bool` | 是否补课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 状态；状态：0未开课，1完课，2取消 3（取消）人数不足 5上课中 10停课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].VacationCount` | `long` | 请假人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ClassTag` | `StoreReservationTagViewModel` | 分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ClassTag.Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ClassTag.TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples` | `List<LessonsInfoResponseModel>` | 预约人员信息，IsFullData=true时返回 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].IsVacation` | `bool` | 是否请假 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].State` | `int` | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5完课 10候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].SignDate` | `string` | 签到时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].SignFile` | `string` | 签到文件（签字签到会有） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].StateReason` | `string` | 状态原因 主要针对取消状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].PeopleCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].UpdateDate` | `string` | 最后更新时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].OperationType` | `int` | 操作方式：0 用户，1管理员,3 共享用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].OperationUserName` | `string` | 操作者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].OperationUserImg` | `string` | 操作者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].IsPay` | `bool` | 是否已支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption` | `LessonsLogsConsumptionViewModel` | 财务信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.ConsumptionId` | `string` | 财务ID 使用后更新 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsPeoples[].Consumption.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.CardImg` | `string` | 卡图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.IsRefund` | `bool` | 是否退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.RefundsReason` | `string` | 如果课程取消，财务未退款 则返回失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.AfterValue` | `string` | 消费后余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].Consumption.PayType` | `int` | 支付方式：0 会员卡，1 微信支付，2支付宝，3 现金 4优惠券 5兑换码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos` | `List<GetReservationInfosModel>` | 预约信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos[].IsMust` | `int` | 是否必填项 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsPeoples[].ReservationInfos[].Items` | `List<GetReservationItemControlsListViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseTools` | `List<StoreReservationTagViewModel>` | 课程工具 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseTools[].Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CourseTools[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePrice` | `StoreCoursePriceViewModel` | 单次付费信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePrice.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePrice.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePrice.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePrice.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePrice.State` | `int` | 状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`role == null \|\| role.State == 0`；`dataListPager?.TotalRows > 0`；`(model.IsFullData \|\| model.IsReservationData) && batchContext.ReservationCount > 0`；`rms?.Count > 0`；`model.IsFullData`；`lessonsItem.State == 0 \|\| lessonsItem.State == 5 \|\| lessonsItem.State == 1`；`rm.CardId > 0`；`userVacations.Any(x => x.CardId == rm.CardId) \|\| rm.IsVacation`；`rm.Uid > 0`；`opm?.Id > 0 && opm.OperationType == 1`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreLessonsProvider.GetStoreLessonsTime`、`StoreProvider.GetStoreByIdAsync`、`UserLessonsLogProvider.GetUserLessonsLogElementsByCondition`、`UserVacationProvider.GetUserVacationElementsByCondition`、`StoreProvider.GetStoreDateTime`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UsersInfoProvider.UsersInfoGetByUid`、`ConsumptionLogProvider.GetConsumptionLogById`、`UserCardChildProvider.GetUserCardChildById`、`ConsumptionLogProvider.GetConsumptionTypeInfo`、`StoreReservationControlsProvider.GetReservationControlsList`、`StoreReservationDetailedProvider.GetReservationInfoList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons_logs`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取约课log |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessonsLogs` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetLessonsLogsResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetLessonsLogsResponseModel>` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:4297` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型：0团课，1私教；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程类型：教练id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetLessonsLogsResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ReservationId` | `long` | 预约id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].IsExperience` | `bool` | 是否是体验课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessonsName` | `string` | 课程名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseType` | `int` | 课程类型 0团课 1私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardId` | `long` | 卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `long` | 顾客id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserName` | `string` | 顾客名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserImg` | `string` | 顾客头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].LogType` | `int` | 上课日志类型 0预约 1修改预约,3候补， 2取消预约，10签到 11取消签到 20旷课 21取消旷课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StateReason` | `string` | 如果是取消预约 这里是取消原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsRepeal` | `bool` | 是否撤销 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OperationType` | `int` | 操作方式：0 用户，1管理员,3 共享用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OperationUserName` | `string` | 操作者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].OperationUserImg` | `string` | 操作者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].OperationDate` | `string` | 操作者时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption` | `LessonsLogsConsumptionViewModel` | 财务信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption.ConsumptionId` | `string` | 财务ID 使用后更新 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Consumption.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption.CardImg` | `string` | 卡图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption.IsRefund` | `bool` | 是否退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption.RefundsReason` | `string` | 如果课程取消，财务未退款 则返回失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption.ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption.AfterValue` | `string` | 消费后余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Consumption.PayType` | `int` | 支付方式：0 会员卡，1 微信支付，2支付宝，3 现金 4优惠券 5兑换码 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`role == null \|\| role.State == 0`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserByStoreId`、`UserLessonsLogProvider.GetUserLessonsLogs`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons_reservation_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取课程预约信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessonsReservationInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetLessonsReservationInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetLessonsReservationInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:2905` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.LessonsId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.LessonsId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LessonsId` | `long` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.LessonsId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples` | `List<LessonsInfoResponseModel>` | 预约人员信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsPeoples[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsPeoples[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsPeoples[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].IsVacation` | `bool` | 是否请假 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].State` | `int` | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5完课 10候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].SignDate` | `string` | 签到时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].SignFile` | `string` | 签到文件（签字签到会有） | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].StateReason` | `string` | 状态原因 主要针对取消状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].PeopleCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].UpdateDate` | `string` | 最后更新时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].OperationType` | `int` | 操作方式：0 用户，1管理员,3 共享用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].OperationUserName` | `string` | 操作者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].OperationUserImg` | `string` | 操作者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].IsPay` | `bool` | 是否已支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption` | `LessonsLogsConsumptionViewModel` | 财务信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption.ConsumptionId` | `string` | 财务ID 使用后更新 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsPeoples[].Consumption.CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption.CardImg` | `string` | 卡图 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption.IsRefund` | `bool` | 是否退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption.RefundsReason` | `string` | 如果课程取消，财务未退款 则返回失败原因 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption.ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption.AfterValue` | `string` | 消费后余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].Consumption.PayType` | `int` | 支付方式：0 会员卡，1 微信支付，2支付宝，3 现金 4优惠券 5兑换码 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos` | `List<GetReservationInfosModel>` | 预约信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsPeoples[].ReservationInfos[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].IsMust` | `int` | 是否必填项 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].Items` | `List<GetReservationItemControlsListViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsPeoples[].ReservationInfos[].Items[].CommonReservationControlsId` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsPeoples[].ReservationInfos[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsPeoples[].ReservationInfos[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.LessonsId < 1`；`resultMsg.State`；`rms?.Count() > 0`；`lessonsItem.State == 0 \|\| lessonsItem.State == 5 \|\| lessonsItem.State == 1`；`rm.CardId > 0`；`userVacations.Any(x => x.CardId == rm.CardId) \|\| rm.IsVacation`；`rm.Uid > 0`；`opm?.Id > 0 && opm.OperationType == 1`；`uc?.Id > 0`；`rm.ConsumptionId > 0`；`cm.ChildCardId > 0`；`cm.ConsumptionType is 1 or 5 or 6`
- 一层业务调用：`StoreReservationProvider.GetStoreReservationElementsByCondition`、`UserLessonsLogProvider.GetUserLessonsLogElementsByCondition`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreProvider.GetStoreByIdAsync`、`StoreLessonsProvider.GetStoreLessonsFirstOrDefaultByCondition`、`StoreProvider.GetStoreDateTime`、`UserVacationProvider.GetUserVacationElementsByCondition`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UsersInfoProvider.UsersInfoGetByUid`、`ConsumptionLogProvider.GetConsumptionLogById`、`UserCardChildProvider.GetUserCardChildById`、`ConsumptionLogProvider.GetConsumptionTypeInfo`、`StoreReservationControlsProvider.GetReservationControlsList`、`StoreReservationDetailedProvider.GetReservationInfoList`、`CommonReservationControlsItemProvider.GetCommonReservationControlsItemList`、`StoreReservationControlsProvider.GetStoreReservationControlsByInstructtions`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons_reservation_list_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取已约列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessonsReservationListV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetReservationListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetReservationListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:3781` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间；普通业务字段；可按问题需要提供 |
| `ReservationEndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间；普通业务字段；可按问题需要提供 |
| `State` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 状态 -1已取消，0未确认，1以确定 -10全部；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetReservationListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationYear` | `string` | 预约时间（年） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationDate` | `string` | 预约时间（日期部分 09-23） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationWeek` | `string` | 预约时间（星期部分 周一） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations` | `List<ReservationDataViewModel>` | 预约数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].ReservationTime` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationTimeEnd` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].StaffName` | `string` | 技师名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ItemsName` | `List<string>` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].PlaceName` | `string` | 场地名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].State` | `int` | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 5上课中 10候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].LessonsId` | `int` | 课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].CourseDesc` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].TeachCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].CourseType` | `int` | 课程类型：0私教，1团课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationProportion` | `string` | 约课占比 格式为：已预约人数/授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].IsReservationDetailed` | `bool` | 是否有预约详情内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.BusinessGetStoreReservationList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons_timetable_logs`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 返回当前门店最近的课表生成记录；未部署日志表时安全返回空列表。 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessonsTimetableLogs` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<TimetableLogsResponseModel>`；包装 `DataResult`；Data `TimetableLogsResponseModel` |
| 数据时效 | 历史/记录型数据；具体保留范围以接口代码为准 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:1869` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Count` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=20 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs` | `List<TimetableLogViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].Id` | `long` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Logs[].BeginDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].EndDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].TemplateCode` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].TemplateVersion` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].Format` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].FileUrl` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].LessonCount` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].State` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].ErrorMessage` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Logs[].CreateDate` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`
- 一层业务调用：`StoreLessonsDownloadLogProvider.GetRecent`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_lessons_timetable_templates`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取可用课表模板。模板元数据与业务数据分离，便于后续新增商家或 AI 模板来源。 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetLessonsTimetableTemplates` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<TimetableTemplatesResponseModel>`；包装 `DataResult`；Data `TimetableTemplatesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:1810` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates` | `List<TimetableTemplateViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Code` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Version` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].SchemaVersion` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Name` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Description` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].Layout` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].SourceType` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].AccentColor` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Templates[].SupportedFormats` | `List<string>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_people_reservation_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取个人预约信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetPeopleReservationInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetPeopleReservationInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetPeopleReservationInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:3129` |
| C/B 对照 | capi.lessons.get_people_reservation_info |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.ReservationId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.ReservationId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationId` | `long` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.ReservationId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 预约Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `UserId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 顾客id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationId` | `long` | 当前预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserId` | `int` | 顾客Id，用于详情内继续执行签到和笔记操作 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员主卡Id，用于详情内读取可用课卡和保存上课笔记 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.LessonsId` | `int` | 当前课次Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CourseId` | `int` | 当前课程项目Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StaffId` | `int` | 私教员工Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPay` | `bool` | 是否已经产生预约消费 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserName` | `string` | 顾客名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserImg` | `string` | 顾客头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.LessonsName` | `string` | 课程名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseImage` | `string` | 课次创建时选择的课程背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImageStyle` | `int` | 课程背景图显示风格 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffName` | `string` | 私教教练名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffImg` | `string` | 私教教练头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseType` | `int` | 课程类型 0团课 1私教 2班课 | 普通业务字段 | 可按问题需要提供 |
| `Data.BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Remark` | `string` | 备注 （用户） | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.BusinessRemark` | `string` | 备注（商家） | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5上课中 10候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperationLogs` | `List<PeopleReservationInfoResponseModel>` | 操作日志 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperationLogs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OperationLogs[].ConsumptionId` | `string` | 财务ID 使用后更新 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OperationLogs[].LogType` | `int` | 上课日志类型 0预约 1修改预约 2取消预约，10签到 11取消签到 20旷课 21取消旷课 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperationLogs[].SignFile` | `string` | 签到文件 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperationLogs[].IsRepeal` | `bool` | 是否撤销 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperationLogs[].OperationType` | `int` | 操作方式：0 用户，1管理员,3 共享用户 10系统 | 普通业务字段 | 可按问题需要提供 |
| `Data.OperationLogs[].OperationUserName` | `string` | 操作者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.OperationLogs[].OperationUserImg` | `string` | 操作者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.OperationLogs[].OperationDate` | `string` | 操作者时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Note` | `string` | 笔记内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.NoteId` | `long` | 上课笔记Id；为0表示当前预约尚未添加笔记 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Files` | `List<FileViewModel>` | 文件列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Files[].Id` | `string` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Files[].FilePath` | `string` | file_path 文件路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.LastUpdateDate` | `string` | 最后操作时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCancel` | `bool` | 是否可以取消预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.LastCancelDate` | `string` | 最晚取消时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos` | `List<GetReservationInfosModel>` | 预约信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReservationInfos[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].IsMust` | `int` | 是否必填项 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items` | `List<GetReservationItemControlsListViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationInfos[].Items[].CommonReservationControlsId` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationInfos[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.ReservationId < 1`；`resultMsg.State`；`rm?.Id > 0`；`snm?.Id > 0`；`snm.IsHaveImage`；`images.Count > 0`；`lessonsInfo.CourseImageId > 0`；`courseImage?.Id > 0`；`rm.StaffId > 0`；`staff?.Id > 0`；`rm.CardId > 0`；`rm.Uid > 0`
- 一层业务调用：`StoreReservationProvider.GetStoreReservationById`、`StoreReservationNoteProvider.GetStoreReservationNoteFirstOrDefaultByCondition`、`StoreLessonsProvider.GetStoreLessonsFirstOrDefaultByCondition`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UsersInfoProvider.UsersInfoGetByUid`、`UserLessonsLogProvider.GetUserLessonsLogElementsByCondition`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreReservationControlsProvider.GetReservationControlsList`、`StoreReservationDetailedProvider.GetReservationInfoList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_private_lessons_reservation_time_seting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取私教预约时间段 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetPrivateLessonsReservationTimeSeting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet |
| 返回 | `Task<DataResult<GetPrivateLessonsReservationTimeSetingResponseModel>>`；包装 `Task/DataResult`；Data `GetPrivateLessonsReservationTimeSetingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:2126` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 店铺Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(setModel.IsSelectCourse && model.CourseId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程ID 非必填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 技师Id 团课非必填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LessonsId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程id(修改课程预约时间传入)；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间（格式"2012-01-21"）；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes` | `List<StoreReservationTimeSetingViewModel>` | 预约时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].Id` | `int` | 时间端Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationTimes[].ReservationTime` | `string` | 预约时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].ReservationTimeEnd` | `string` | 预约时间段 结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsOccupy` | `bool` | 时间被占用 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].SurplusCount` | `int` | 剩余可预约人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`setModel.IsSelectCourse && model.CourseId <= 0`
- 一层业务调用：`StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet`、`StoreReservationTimeSetingProvider.GetReservationTimeSetingV2`
- 疑似副作用：`StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`；`固定提示：课程ID不能为空`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_staff_private_lesson_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取教练私教列表 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetStaffPrivateLessonList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffPrivateLessonListResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffPrivateLessonListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:699` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间（格式"2012-01-21"）；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| !StringOperate.IsDate(model.BeginDate) \\|\\| !StringOperate.IsDate(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间（格式"2012-01-21"）；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs` | `List<StaffPrivateLessonViewModel>` | 私教列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StoreUserName` | `string` | 店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserType` | `int` | 用户权限租：0超级管理员，1管理员，2运营人员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].IsSelectCourse` | `bool` | 预约是否可选课目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses` | `List<StaffCourseViewModel>` | 关联私教课目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StaffCourses[].CourseId` | `int` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StaffCourses[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CourseImage` | `string` | 课程图片id | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CardCount` | `int` | 支持卡的数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice` | `StoreCoursePriceViewModel` | 单次付费信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.State` | `int` | 状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].State` | `int` | 状态；0-未开放，1-已开放 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo` | `List<DatePrivateLessonsInfoViewModel>` | 时间列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].Date` | `string` | 日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].DateWeek` | `int` | 星期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].ReservationState` | `int` | 状态 1休息 2放假 3员工休息 4请假 5无课程 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples` | `List<LessonsInfoResponseModel>` | 预约人员信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].IsVacation` | `bool` | 是否请假 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].State` | `int` | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5完课 10候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].SignDate` | `string` | 签到时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].SignFile` | `string` | 签到文件（签字签到会有） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].StateReason` | `string` | 状态原因 主要针对取消状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].PeopleCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].UpdateDate` | `string` | 最后更新时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].OperationType` | `int` | 操作方式：0 用户，1管理员,3 共享用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].OperationUserName` | `string` | 操作者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].OperationUserImg` | `string` | 操作者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].IsPay` | `bool` | 是否已支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].Consumption` | `LessonsLogsConsumptionViewModel` | 财务信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].DatePrivateLessonsInfo[].LessonsPeoples[].ReservationInfos` | `List<GetReservationInfosModel>` | 预约信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| !StringOperate.IsDate(model.BeginDate) \|\| !StringOperate.IsDate(model.EndDate)`；`resultMsg.State`；`role == null \|\| role.State == 0`；`tums?.Count > 0`；`staff?.Id > 0`
- 一层业务调用：`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserByStoreId`、`StoreReservationProvider.GetWorkStaffV2`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_user_lessons_note`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取会员上课笔记详细信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetUserLessonsNote` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetUserLessonsNoteResponseModel>>`；包装 `Task/DataResult`；Data `GetUserLessonsNoteResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:3888` |
| C/B 对照 | capi.lessons.get_user_lessons_note |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 笔记id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsId` | `long` | 课程id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateUser` | `string` | 创建人 | 普通业务字段 | 可按问题需要提供 |
| `Data.BeginDate` | `string` | 课程开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseName` | `string` | 课程名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.Note` | `string` | 笔记内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Files` | `List<FileViewModel>` | 文件列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Files[].Id` | `string` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Files[].FilePath` | `string` | file_path 文件路径 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.CardId < 1`；`resultMsg.State`；`snm?.Id > 0`；`snm.IsHaveImage`；`images.Count > 0`；`snm.ReservationId > 0`；`rm?.Id > 0 && rm.LessonsId > 0`
- 一层业务调用：`StoreReservationNoteProvider.GetStoreReservationNoteFirstOrDefaultByCondition`、`StoreReservationProvider.GetStoreReservationById`、`StoreLessonsProvider.GetStoreLessonsElementById`、`TenantUserProvider.GetTenantUserByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_user_lessons_note_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取会员上课笔记列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetUserLessonsNoteList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetUserLessonsNoteListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetUserLessonsNoteListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:3853` |
| C/B 对照 | capi.lessons.get_user_lessons_note_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CardId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CardId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CardId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.CardId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetUserLessonsNoteListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 笔记id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CourseName` | `string` | 课程名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StaffName` | `string` | 店员名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsHaveImage` | `bool` | 是否包含图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Note` | `string` | 笔记内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateTime` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.CardId < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationNoteProvider.GetStoreReservationNotes`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_user_lessons_reservation_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取会员已约列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetUserLessonsReservationList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetUserLessonsReservationListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetUserLessonsReservationListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:3817` |
| C/B 对照 | capi.lessons.get_user_lessons_reservation_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChildCardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 子卡id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型 0团课 1私教 2班课；普通业务字段；可按问题需要提供 |
| `OrderBy` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 排序方式0 上课时间正序 1上课时间倒序；普通业务字段；可按问题需要提供 |
| `State` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5上课中 10候补；普通业务字段；可按问题需要提供 |
| `IsRefunds` | `bool?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否已退款；普通业务字段；可按问题需要提供 |
| `IsSign` | `bool?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否签到；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetUserLessonsReservationListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ReservationTime` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StaffName` | `string` | 技师名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].State` | `int` | -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5上课中 10候补 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 3上课中 5完课 10候补 20完课未签到 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SignDate` | `string` | 签到时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseId` | `int` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessonsId` | `int` | 课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseColor` | `string` | 课程颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseDesc` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseStar` | `int` | 难度星级1-10 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TeachCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CourseType` | `int` | 课程类型：0私教，1团课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionId` | `string` | 财务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ConsumptionValue` | `string` | 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].AfterValue` | `string` | 消费后余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsRefunds` | `bool` | 是否已退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ConsumptionState` | `int` | 财务状态 0未支付 1已支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsSetNote` | `bool` | 是否设置了笔记 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsCancel` | `bool` | 是否可以取消预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems` | `List<ConsumptionServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ItemId` | `int` | 项目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ServiceItems[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ConsumptionValue` | `decimal` | consumption_value 消费值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].AfterValue` | `decimal` | after_value 操作后值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].PayPrice` | `decimal` | 实付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceItems[].ItemUnit` | `string` | 服务单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Tags` | `List<StoreReservationTagViewModel>` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Tags[].Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Tags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ClassTag` | `StoreReservationTagViewModel` | 分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ClassTag.Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ClassTag.TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsExperience` | `bool` | 是否体验用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | 预约创建时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetUserLessonsReservationList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.get_user_lessons_scheduling`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取上课日历 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/GetUserLessonsScheduling` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetUserLessonsSchedulingResponseModel>>`；包装 `Task/DataResult`；Data `GetUserLessonsSchedulingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:4371` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 教练ID 可不传；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课程类型：0团课，1私教 2班课；普通业务字段；可按问题需要提供 |
| `BeginDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.BeginDate) \\|\\| string.IsNullOrEmpty(model.EndDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间 yyyy-MM-dd；普通业务字段；可按问题需要提供 |
| `Conditions` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 条件 0按日统计 1按月统计；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonsSum` | `long` | 团课上课统计 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrivateLessonsSum` | `long` | 私教上课统计 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassesLessonsSum` | `long` | 班课上课统计 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData` | `List<UserLessonsSchedulingResponseModel>` | 上课统计数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].Date` | `string` | 日期 按日统计则返回“yyyy-mm-dd” 按月则返回“yyyy-mm” | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].CourseType` | `int` | 课程类型：0团课，1私教 2班课 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingData[].LessonsCount` | `long` | 上课数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| string.IsNullOrEmpty(model.BeginDate) \|\| string.IsNullOrEmpty(model.EndDate)`；`resultMsg.State`
- 一层业务调用：`StoreLessonsProvider.GetUserLessonsScheduling`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.lessons.preview_lessons_timetable_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课次与排课 |
| 用途 | 获取统一课表快照预览；预览不会保存设置或生成文件。 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Lessons/PreviewLessonsTimetableV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<TimetablePreviewResponseModel>>`；包装 `Task/DataResult`；Data `TimetablePreviewResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/LessonsController.cs:1831` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `TemplateCode` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings` | `TimetableSettingsViewModel` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.Title` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.SubTitle` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.SubTitleBrand` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.HeadImage` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.CaveatTitle` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.Caveat` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.LessonsStyle` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowDate` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowCourseName` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowStaffName` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowTime` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowTag` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 只能使用文档确认的枚举值 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowStar` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowPlaceName` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsCourseClass` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |
| `Settings.IsShowStaff` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现嵌套字段显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 源码属性注释缺失；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document` | `TimetableDocumentViewModel` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.StoreId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Document.StoreName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.StoreLogo` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.StoreAddress` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Document.StoreMobile` | `string` | 源码属性注释缺失 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Document.BeginDate` | `DateTime` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.EndDate` | `DateTime` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings` | `TimetableSettingsViewModel` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.Title` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.SubTitle` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.SubTitleBrand` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.HeadImage` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.CaveatTitle` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.Caveat` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.LessonsStyle` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowDate` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowCourseName` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowStaffName` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowTime` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowTag` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowStar` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowPlaceName` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsCourseClass` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Settings.IsShowStaff` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days` | `List<TimetableDayViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Date` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].WeekDay` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons` | `List<TimetableViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].Id` | `int` | 课次 ID。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Document.Days[].Lessons[].CourseId` | `int` | 课程项目 ID。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Document.Days[].Lessons[].CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CourseImage` | `string` | 课程图片公开地址。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].ImageStyle` | `int` | 图片样式；0 明亮，1 暗黑。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].BeginDate` | `string` | 开始时间 yyyy-MM-dd hh:mm | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].EndDate` | `string` | 结束时间 yyyy-MM-dd HH:mm:ss。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CourseStar` | `int` | 课程难度星级。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CourseClassName` | `string` | 课程分类名称。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CourseTools` | `List<string>` | 课程工具名称。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].TeachCount` | `int` | 最大授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].MinPeople` | `int` | 最小授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Days[].Lessons[].State` | `int` | 状态；0未开课 1已开课 10停课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons` | `List<TimetableViewModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].Id` | `int` | 课次 ID。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Document.Lessons[].CourseId` | `int` | 课程项目 ID。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Document.Lessons[].CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CourseImage` | `string` | 课程图片公开地址。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].ImageStyle` | `int` | 图片样式；0 明亮，1 暗黑。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].BeginDate` | `string` | 开始时间 yyyy-MM-dd hh:mm | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].EndDate` | `string` | 结束时间 yyyy-MM-dd HH:mm:ss。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CourseStar` | `int` | 课程难度星级。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CourseClassName` | `string` | 课程分类名称。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CourseTools` | `List<string>` | 课程工具名称。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Document.Lessons[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Document.Lessons[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].TeachCount` | `int` | 最大授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].MinPeople` | `int` | 最小授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Document.Lessons[].CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Document.Lessons[].State` | `int` | 状态；0未开课 1已开课 10停课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template` | `TimetableTemplateViewModel` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.Code` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.Version` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.SchemaVersion` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.Name` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.Description` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.Layout` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.SourceType` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.AccentColor` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Template.SupportedFormats` | `List<string>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`!resultMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.course.get_course`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取单个课目 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Course/GetCourse` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCourseResponseModel>>`；包装 `Task/DataResult`；Data `GetCourseResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CourseController.cs:83` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CourseId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CourseId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CourseId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课目id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LessonsId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseImageId` | `long` | 课程图片id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CourseImage` | `string` | 课程图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePeopleCount` | `int` | 最大授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.MinPeople` | `int` | 最小授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseStar` | `int` | 难度星级1-10 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags` | `List<StoreReservationTagViewModel>` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassTag` | `StoreReservationTagViewModel` | 分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassTag.Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ClassTag.TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseTools` | `List<StoreReservationTagViewModel>` | 课程工具 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseTools[].Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CourseTools[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardCount` | `int` | 支持卡的数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SchedulingCount` | `long` | 排课数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePrice` | `StoreCoursePriceViewModel` | 单次付费信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePrice.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePrice.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePrice.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePrice.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePrice.State` | `int` | 状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsRecommend` | `bool` | 是否推荐，用作首页展示 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials` | `List<StoreMaterialViewModel>` | 关联素材 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Id` | `int` | 素材ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].CategoryId` | `int` | 素材分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].CategoryName` | `string` | 素材分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MaterialName` | `string` | 素材名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MaterialType` | `int` | 素材类型：1图片，2视频，3视频号链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FilePath` | `string` | 素材路径；图片/视频为文件地址，视频号为链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileUrl` | `string` | 素材完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileName` | `string` | 文件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileExt` | `string` | 文件扩展名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileSize` | `long` | 文件大小，单位字节 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MimeType` | `string` | 文件MIME类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoCover` | `string` | 视频封面路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoCoverUrl` | `string` | 视频封面完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoNo` | `string` | 视频号标识 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoTimeLength` | `int` | 视频时长，单位秒 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Width` | `int` | 素材宽度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Height` | `int` | 素材高度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Materials[].OrderBy` | `int` | 排序值，值越大越靠前 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].State` | `int` | 状态：1启用，0停用，-1删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态；0-未开放，1-已开放，-1已删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.CourseId < 1`；`resultMsg.State`；`course?.Id > 0`
- 一层业务调用：`StoreCourseProvider.GetStoreCourses`、`StoreLessonsProvider.GetSchedulingCount`、`StoreMaterialProvider.GetCourseMaterials`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.course.get_course_card_price_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取单次付费信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Course/GetCourseCardPriceInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCourseCardPriceInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetCourseCardPriceInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CourseController.cs:387` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| (model.CourseId < 1 && model.StaffId < 1)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| (model.CourseId < 1 && model.StaffId < 1)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseId` | `long` | 参与组合校验 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| (model.CourseId < 1 && model.StaffId < 1)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课目Id （私教不选课目的可不传）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 授课类型；0-团课，1-私教；普通业务字段；可按问题需要提供 |
| `StaffId` | `long` | 参与组合校验 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| (model.CourseId < 1 && model.StaffId < 1)) | 必须来自同一会话上游 API 结果或服务端对象引用 | 教练Id(私教课传入)；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `LessonsId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课id（团课排课单独设置id）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsHideDowngradeCard` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否隐藏下架的储值卡；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| (model.CourseId < 1 && model.StaffId < 1)`；`resultMsg.State`；`coursePrice?.Id>0`
- 一层业务调用：`StoreCoursePriceProvider.GetStoreCoursePriceFirstOrDefaultByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.course.get_course_images`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取课目图片 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Course/GetCourseImages` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCourseImagesResponseModel>>`；包装 `Task/DataResult`；Data `GetCourseImagesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CourseController.cs:1963` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TagId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 图片标签Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ImageType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 图片类型 0课程图片 1课表头图；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImageData` | `List<CourseImagesResponseModel>` | 课程图片数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImageData[].Id` | `long` | 图片id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ImageData[].ImageUrl` | `string` | 课程图片id | 普通业务字段 | 可按问题需要提供 |
| `Data.ImageData[].ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`model.TagId > 0`；`rmimgs?.Count > 0`
- 一层业务调用：`CourseImageTagRelationProvider.GetCourseImageTagRelationElementsByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.course.get_course_link_name`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取课目关联名称 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Course/GetCourseLinkName` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCourseLinkNameResponseModel>>`；包装 `Task/DataResult`；Data `GetCourseLinkNameResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CourseController.cs:250` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CourseId<1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CourseId<1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseId` | `long` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.CourseId<1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课目id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags` | `List<CourseLinkNameResponseModel>` | 名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tags[].CourseName` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.CourseId<1`；`resultMsg.State`；`tags?.Count > 0`
- 一层业务调用：`StoreCourseNameProvider.GetStoreCourseNameElementsByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.course.get_course_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取课目列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Course/GetCourseList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCourseListV2ResponseModel>>`；包装 `Task/DataResult`；Data `GetCourseListV2ResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CourseController.cs:34` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CourseType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 授课类型；0-团课，1-私教；普通业务字段；可按问题需要提供 |
| `State` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 课目状态 不传显示所有 0停用的 1有效的；普通业务字段；可按问题需要提供 |
| `TagIds` | `List<long>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 标签；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses` | `List<StoreCourseViewModel>` | 管理员列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseImageId` | `long` | 课程图片id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CourseMainImage` | `string` | 课程图片(长图) | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseSmallImage` | `string` | 课程图片（方图） | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Tags` | `List<StoreReservationTagViewModel>` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Tags[].Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Tags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].ClassTag` | `StoreReservationTagViewModel` | 分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].ClassTag.Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].ClassTag.TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseTools` | `List<StoreReservationTagViewModel>` | 课程工具 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseTools[].Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CourseTools[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePeopleCount` | `int` | 最大授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].MinPeople` | `int` | 最小授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseStar` | `int` | 难度星级1-10 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].IsRecommend` | `bool` | 是否推荐，用作首页展示 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CardCount` | `int` | 支持卡的数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].SchedulingCount` | `long` | 排课数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].State` | `int` | 状态；0-未开放，1-已开放，-1已删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Courses[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos` | `List<CourseCardBindInfoViewModelModel>` | 课卡绑定信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].PrepaidCardId` | `long` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].BindCardInfos[].CardType` | `int` | 类型：0计次，1储值 2时限 3权益 4安心充 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].ValidityPeriod` | `int` | （时限卡）扣减有效期 天 0不扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].CardValue` | `decimal` | 扣减卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services` | `List<CourseCardBindItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services[].ItemId` | `long` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].BindCardInfos[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services[].ItemValue` | `int` | 服务项 卡价值 0 不支持 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].State` | `int` | 关联状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePrice` | `StoreCoursePriceViewModel` | 单次付费信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePrice.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePrice.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePrice.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePrice.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePrice.State` | `int` | 状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos` | `List<StoreMaterialViewModel>` | 关联视频素材 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].Id` | `int` | 素材ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Videos[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Videos[].CategoryId` | `int` | 素材分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Videos[].CategoryName` | `string` | 素材分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].MaterialName` | `string` | 素材名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].MaterialType` | `int` | 素材类型：1图片，2视频，3视频号链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].FilePath` | `string` | 素材路径；图片/视频为文件地址，视频号为链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].FileUrl` | `string` | 素材完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].FileName` | `string` | 文件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].FileExt` | `string` | 文件扩展名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].FileSize` | `long` | 文件大小，单位字节 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].MimeType` | `string` | 文件MIME类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].VideoCover` | `string` | 视频封面路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].VideoCoverUrl` | `string` | 视频封面完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].VideoNo` | `string` | 视频号标识 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].VideoTimeLength` | `int` | 视频时长，单位秒 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].Width` | `int` | 素材宽度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].Height` | `int` | 素材高度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Courses[].Videos[].OrderBy` | `int` | 排序值，值越大越靠前 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Videos[].State` | `int` | 状态：1启用，0停用，-1删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`courseData?.Count > 0`；`course?.Id > 0`
- 一层业务调用：`StoreCourseProvider.StoreCourses`、`StoreCourseProvider.GetStoreCourses`、`StoreLessonsProvider.GetSchedulingCount`、`StoreMaterialProvider.GetCourseMaterials`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.course.get_private_lesson_staff_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取私教列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Course/GetPrivateLessonStaffList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet |
| 返回 | `Task<DataResult<GetPrivateLessonStaffListResponseModel>>`；包装 `Task/DataResult`；Data `GetPrivateLessonStaffListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/CourseController.cs:1294` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间（格式"2012-01-21"）；普通业务字段；可按问题需要提供 |
| `IsGetVacation` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否获取放假信息；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs` | `List<PrivateLessonStaffViewModel>` | 私教列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffId` | `long` | 员工的ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].UserName` | `string` | 昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StoreUserName` | `string` | 店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Staffs[].UserType` | `int` | 用户权限租：0超级管理员，1管理员，2运营人员 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].IsSelectCourse` | `bool` | 预约是否可选课目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses` | `List<StaffCourseViewModel>` | 关联私教课目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StaffCourses[].CourseId` | `int` | 课目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StaffCourses[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CourseImage` | `string` | 课程图片id | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CardCount` | `int` | 支持卡的数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice` | `StoreCoursePriceViewModel` | 单次付费信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].CoursePrice.State` | `int` | 状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffCourses[].State` | `int` | 状态；0-未开放，1-已开放 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].OperateDatas` | `List<StaffOperateViewModel>` | 工作日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].OperateDatas[].OperateId` | `long` | 工作Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].OperateDatas[].OperateWeeks` | `List<StaffOperateWeekViewModel>` | 工作日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].OperateDatas[].OperateWeeks[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].OperateDatas[].OperateWeeks[].WeekInfo` | `int` | 营业日期 （星期1-7） 数字类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].OperateDatas[].OperateWeeks[].State` | `int` | 0 非营业时间，1正常营业 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].OperateDatas[].OperateTimes` | `List<StaffOperateTimeViewModel>` | 工作时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].OperateDatas[].OperateTimes[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].OperateDatas[].OperateTimes[].BeginTime` | `string` | 营业时间开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].OperateDatas[].OperateTimes[].EndTime` | `string` | 营业时间 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffTags` | `List<ReservationTagViewModel>` | 教练标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffTags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StaffTags[].TagTitle` | `string` | 标签标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].CardCount` | `int` | 支持卡的数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].IsPartiallyUnlinked` | `bool` | 部分未关联 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].VacationState` | `int` | 放假状态 0正常上班 1待放假 3放假中 10休息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].CourseTime` | `int` | 私教授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].State` | `int` | 私教预约状态 0停课 1开课 2部分停课 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`tums?.Count > 0`；`model.IsGetVacation`；`!string.IsNullOrEmpty(model.ReservationDate)`；`setModel.IsSelectCourse && setModel.State==1`；`courseData?.Count > 0`；`courseStates?.Count > 1`；`courseStates?.Count == 1`；`courseModel?.Count > 0`；`cm?.Id > 0`；`bindCards?.Count > 0`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`StaffVacationProvider.GetStaffVacationAll`、`DateTimeHelper.GetWeek`、`StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet`、`TenantUserTagRelationProvider.GetTenantUserTagRelationElementsByCondition`、`UsersInfoProvider.UsersInfoGetByUid`、`StaffOperateWeekProvider.GetStaffOperateList`、`StoreCourseStaffProvider.GetStoreCourseStaffElementsByCondition`、`StoreCourseProvider.GetStoreCourseElementsByCondition`、`StoreReservationTagProvider.GetTagsByStoreId`
- 疑似副作用：`StaffPrivateLessonSetProvider.GetStaffPrivateLessonSet`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_course.get_store_course_by_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取单条课程信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCourse/GetStoreCourseById` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreCourseByIdResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreCourseByIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCourseController.cs:411` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Id < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 店铺课程id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 课程id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreItemModel` | `StoreItemViewModel` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreItemModel.ItemId` | `int` | 项目id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreItemModel.ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseTime` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.CoursePeopleCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.CourseRemark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.State` | `int` | 状态；0-未开放，1-已开放，-1已删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseStaffList` | `List<StoreCourseStaffByIdViewModel>` | 授课人员list集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseStaffList[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreCourseStaffList[].CourseStaffId` | `int` | 授课人员id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreCourseStaffList[].JobTitle` | `string` | 职称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseStaffList[].UserName` | `string` | 名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreCourseStaffList[].UserImg` | `string` | 头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreCourseStaffList[].State` | `int` | 0-未选中，1-选中 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseTimeList` | `List<StoreCourseTimeByIdViewModel>` | 课程时间list集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseTimeList[].Id` | `int` | 课程时间id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreCourseTimeList[].WeekInfo` | `string` | 周几， 以逗号分隔, | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseTimeList[].BeginTime` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseTimeList[].EndTime` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCourseTimeList[].TimeInterval` | `int` | 预约时间间隔 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials` | `List<StoreMaterialViewModel>` | 关联素材 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Id` | `int` | 素材ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].CategoryId` | `int` | 素材分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Materials[].CategoryName` | `string` | 素材分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MaterialName` | `string` | 素材名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MaterialType` | `int` | 素材类型：1图片，2视频，3视频号链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FilePath` | `string` | 素材路径；图片/视频为文件地址，视频号为链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileUrl` | `string` | 素材完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileName` | `string` | 文件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileExt` | `string` | 文件扩展名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].FileSize` | `long` | 文件大小，单位字节 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].MimeType` | `string` | 文件MIME类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoCover` | `string` | 视频封面路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoCoverUrl` | `string` | 视频封面完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoNo` | `string` | 视频号标识 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].VideoTimeLength` | `int` | 视频时长，单位秒 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Width` | `int` | 素材宽度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Height` | `int` | 素材高度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Materials[].OrderBy` | `int` | 排序值，值越大越靠前 | 普通业务字段 | 可按问题需要提供 |
| `Data.Materials[].State` | `int` | 状态：1启用，0停用，-1删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Id < 1`；`resultMsg.State`
- 一层业务调用：`StoreCourseProvider.GetStoreCourseById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = (int)StatusCodeEnum.ParameterError`；`固定提示：请传入课程id`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_course.get_store_course_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取课程列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCourse/GetStoreCourseList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetStoreCourseListResponseModel>>`；包装 `DataResult`；Data `List<GetStoreCourseListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCourseController.cs:22` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data[].State` | `int` | 状态；0-未开放，1-已开放，-1已删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreCourseProvider.GetStoreCourseList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_course.get_store_course_type`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 课程 |
| 用途 | 获取当前店铺团课、私教课信息 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCourse/GetStoreCourseType` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreCourseTypeResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreCourseTypeResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCourseController.cs:438` |
| C/B 对照 | capi.store_course.get_store_course_type |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTuan` | `bool` | 是否有团课 true-有，false-无 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSi` | `bool` | 是否有私教课 true-有，false-无 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`StoreCourseProvider.GetStoreCourseType`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_activity`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取软件活动 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftActivity` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftActivityResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftActivityResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:1143` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SoftType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 软件产品类型 0软件版本 1短信 2云音响；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ActivityTitle` | `string` | 活动标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.ActivitySubTitle` | `string` | 活动子标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.ActivityInfo` | `string` | 活动内容 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resMsg.State`
- 一层业务调用：静态扫描未识别到标准 Provider/Service 调用。
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_discount_coupon`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取优惠券信息 |
| 使用时机 | 商家核对优惠券配置、发放、领取、使用条件和统计时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftDiscountCoupon` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftDiscountCouponResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftDiscountCouponResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:652` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.Code)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.Code)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Code` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| string.IsNullOrEmpty(model.Code)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 优惠券秘钥；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CycleTitle` | `string` | 优惠券信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.EffectiveTime` | `string` | 生效时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 0失效、1可用、2已用 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftVersion` | `int` | 软件版本 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftVersionName` | `string` | 软件版本名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info` | `GetOrderCodeInfoResponseModel` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.ModuleList` | `List<MudoleViewModel>` | 模块的列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.ModuleList[].Id` | `int` | 模块的id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Info.ModuleList[].Name` | `string` | 模块的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.ModuleList[].Code` | `string` | 唯一 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.ModuleList[].Describe` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.ModuleList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.ModuleList[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Unit` | `int` | 单位；0-天，1-月，2-年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Day` | `int` | 天or年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.IsDiscount` | `bool` | 是否是折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Discount` | `decimal` | 折扣数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.EndDate` | `string` | 到期的时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Ext` | `FunctionExtViewModel` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Ext.VipCount` | `int` | 会员数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Ext.StorePeopleCount` | `int` | 店员数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Ext.SmsCount` | `int` | 短信数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Ext.TaiCardCount` | `int` | 台牌数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.Ext.IsGive` | `bool` | 是否是赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.Info.State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| string.IsNullOrEmpty(model.Code)`；`!(couponModel?.Id > 0)`；`resMsg.State`；`state == 0`；`state == 2`
- 一层业务调用：`SoftDiscountCouponProvider.GetSoftDiscountCouponByCode`、`StoreProvider.GetStoreByIdAsync`、`SoftOrderProvider.ConvertServiceUnit`、`SoftOrderProvider.ConvertServiceDateTime`、`SoftOrderProvider.GetSoftVersionModule`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数错误`；`固定提示：优惠券不存在`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_order`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取软件订购信息 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftOrder` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<BusinessGetSoftOrderResponseModel>>`；包装 `DataResult`；Data `PageData<BusinessGetSoftOrderResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:145` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页数量；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 当前页；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetSoftOrderResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CycleTitle` | `string` | 周期 一个月,三个月,六个月,3一年,三年,五年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceBeginDate` | `string` | 有效期开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceEndDate` | `string` | 有效期结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PayPrice` | `decimal` | 真实购买价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 购买状态 状态 1成功 0待入账 -1删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `string` | 下单时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUserSoftCoupon` | `bool` | 是否使用了淘宝优惠码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SoftVersion` | `int` | 购买后版本 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SoftVersionName` | `string` | 购买后软件版本名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BeforeSoftVersion` | `int` | 购买前版本 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].BeforeSoftVersionName` | `string` | 购买前软件版本名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsUpGrade` | `bool` | 是否是升级 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PackageName` | `string` | 套餐包名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resMsg.State`
- 一层业务调用：`SoftOrderProvider.GetSoftOrder`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_price`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取软件版本价格类目列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftPrice` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftPriceResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftPriceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:52` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SoftVersion` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 软件版本标识 1试用版本 2会员基础版 3会员标准版 4预约版 5营销版 10高级版；普通业务字段；可按问题需要提供 |
| `VersionTag` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 软件版本标识，0为原始版本 1为新版本；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices` | `List<BusinessGetSoftPriceInfoModel>` | 价格类目信息列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].Id` | `int` | 主键唯一标识 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SoftPrices[].CycleTitle` | `string` | 周期 0:一个月,1:三个月,2:六个月,3:一年,4:三年,5:五年 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].EffectiveTime` | `string` | 生效时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].GiveSmsCount` | `int` | 赠送短信数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].StoreStaffNumber` | `int` | 软件版本当前门店店员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].TableCardsNumber` | `int` | 台牌数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].MemberCount` | `int` | 会员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaxMemberCount` | `int` | 最大会员数量 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreStaffNumber` | `int` | 当前软件版本，门店店员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.TableCardsNumber` | `int` | 当前软件版本，门店赠送台牌数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.BtnState` | `int` | 0 不显示任何按钮， 1 立即订购，2立即升级 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpGradeCycle` | `string` | 升级周期 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpGradePrice` | `decimal` | 升级所需要金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Remark` | `string` | 备注内容 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ShopOriginalPrice` | `decimal` | 商城的原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.ShopDiscountPrice` | `decimal` | 商城的折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.ShopDay` | `int` | 商城的天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOriginalPrice` | `decimal` | 点餐的原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodDiscountPrice` | `decimal` | 点餐的折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodDay` | `int` | 点餐的天数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resMsg.State`
- 一层业务调用：`SoftPriceProvider.GetSoftPrice`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_server_end_date`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取店铺软建到期日期 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftServerEndDate` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftServerEndDateResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftServerEndDateResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:169` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `VersionId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.VersionId==0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 服务到期时间；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceEndDate` | `string` | 服务到期时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`model.VersionId > 0`；`!(svm?.Id > 0)`；`resMsg.State`；`model.VersionId==0`
- 一层业务调用：`StoreVersionProvider.GetStoreVersionByVersionId`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数错误`；`固定提示：未购买该版本`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_server_free_date`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取店铺软件服务免费使用时间 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftServerFreeDate` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftServerFreeDateResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftServerFreeDateResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:617` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayPreferentialBeginDate` | `string` | 试用开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayPreferentialEndDate` | `string` | 试用结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.FreeBeginDate` | `string` | 免费开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.FreeEndDate` | `string` | 免费结束时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resMsg.State`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_version_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取软件版本价格类目列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftVersionInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftVersionInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftVersionInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:24` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.VersionId<1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.VersionId<1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `VersionId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| model.VersionId<1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 软件版本标识Id 1试用版本 2会员基础版 3会员标准版 4预约版 5营销版 10高级版；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.VersionName` | `string` | 版本名称 1试用版本 2会员基础版 3会员标准版 4预约版 5营销版 10高级版 | 普通业务字段 | 可按问题需要提供 |
| `Data.VersionInfo` | `string` | 版本说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.InfoVideo` | `string` | info_video 版本视频 | 普通业务字段 | 可按问题需要提供 |
| `Data.VersionIntroduction` | `string` | 版本介绍 | 普通业务字段 | 可按问题需要提供 |
| `Data.BtnState` | `int` | 0 不显示任何按钮， 1 立即订购，2立即升级 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpGradeCycle` | `string` | 升级周期 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpGradePrice` | `decimal` | 升级所需要金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpGradeOriginalPrice` | `decimal` | 升级原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Remark` | `string` | 备注内容 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsOpen` | `bool` | 是否已开通 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceEndDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices` | `List<SoftPriceViewModel>` | 价格类目信息列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].Id` | `int` | 主键唯一标识 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SoftPrices[].CycleTitle` | `string` | 周期 0:一个月,1:三个月,2:六个月,3:一年,4:三年,5:五年 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].EffectiveTime` | `string` | 生效时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].IsRecommend` | `bool` | 是否推荐 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].SubTitle` | `string` | 小标题内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].DiscountInfo` | `string` | 折扣信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftPrices[].ActivityInfo` | `List<string>` | 活动信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| model.VersionId<1`；`resMsg.State`；`StoreProvider.Instance.CheckAgentStore(model.StoreId,model.Uid)`
- 一层业务调用：`StoreProvider.CheckAgentStore`、`SoftVersionProvider.GetSoftInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_soft_versions`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取版本列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetSoftVersions` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftVersionsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftVersionsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:850` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `VersionTag` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 软件版本标识，0为原始版本 1为新版本,5最新版；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Versions` | `List<SoftVersionViewModel>` | 软件版本列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].SoftVersion` | `int` | 软件版本标识 版本名称 1试用版本 2会员基础版 3会员标准版 4预约版 5营销版 10高级版 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].VersionName` | `string` | 版本名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].MaxMermberCount` | `int` | max_mermber_count 最高会员数量 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].MaxClerkCount` | `int` | max_clerk_count 最高店员数量 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].GiveSmsCount` | `int` | 赠送短信数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].VersionInfo` | `string` | 亮点功能 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].VersionRemark` | `string` | 备注说明 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Versions[].IsRecommend` | `bool` | 是否推荐 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resMsg.State`；`model.VersionTag.Equals(1)`
- 一层业务调用：`SoftVersionProvider.GetSoftUpgradeersions`、`SoftVersionProvider.GetSoftVersions`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.business_get_upgrade_soft_price`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取软件版本价格类目列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/BusinessGetUpgradeSoftPrice` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<BusinessGetSoftPriceResponseModel>>>`；包装 `Task/DataResult`；Data `List<BusinessGetSoftPriceResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:76` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `SoftVersion` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 软件版本标识 1试用版本 2会员基础版 3会员标准版 4预约版 5营销版 10高级版；普通业务字段；可按问题需要提供 |
| `VersionTag` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 软件版本标识，0为原始版本 1为新版本；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices` | `List<BusinessGetSoftPriceInfoModel>` | 价格类目信息列表 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].Id` | `int` | 主键唯一标识 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].SoftPrices[].CycleTitle` | `string` | 周期 0:一个月,1:三个月,2:六个月,3:一年,4:三年,5:五年 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].EffectiveTime` | `string` | 生效时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].GiveSmsCount` | `int` | 赠送短信数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].StoreStaffNumber` | `int` | 软件版本当前门店店员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].TableCardsNumber` | `int` | 台牌数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SoftPrices[].MemberCount` | `int` | 会员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].MaxMemberCount` | `int` | 最大会员数量 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data[].StoreStaffNumber` | `int` | 当前软件版本，门店店员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].TableCardsNumber` | `int` | 当前软件版本，门店赠送台牌数量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].BtnState` | `int` | 0 不显示任何按钮， 1 立即订购，2立即升级 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UpGradeCycle` | `string` | 升级周期 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UpGradePrice` | `decimal` | 升级所需要金额 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Remark` | `string` | 备注内容 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].ShopOriginalPrice` | `decimal` | 商城的原价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ShopDiscountPrice` | `decimal` | 商城的折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ShopDay` | `int` | 商城的天数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodOriginalPrice` | `decimal` | 点餐的原价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodDiscountPrice` | `decimal` | 点餐的折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodDay` | `int` | 点餐的天数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resMsg.State`
- 一层业务调用：`SoftVersionProvider.GetBusinessGetSoftPriceList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.get_module`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取模块信息 |
| 使用时机 | 在商家视角中核对“获取模块信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/GetModule` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：SoftOrderProvider.GetRenewModule, SoftOrderProvider.GetAddModule |
| 返回 | `Task<DataResult<GetModuleResponseModel>>`；包装 `Task/DataResult`；Data `GetModuleResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:929` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Type` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 1-订购，2-续费，3-添加的模块；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PackageList` | `List<PackageViewModel>` | 套餐包列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.PackageList[].Id` | `int` | 套餐包id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PackageList[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.PackageList[].ContainModuleList` | `List<int>` | 包含的模块id的集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.PackageList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.PackageList[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.PackageList[].Describe` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList` | `List<MudolesViewModel>` | 模块的列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Id` | `int` | 模块的id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ModuleList[].Name` | `string` | 模块的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Describe` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].IsBuy` | `bool` | 是否已购买 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Code` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ContainModuleList` | `List<int>` | 包含的模块id的集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext` | `FunctionExtViewModel` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.VipCount` | `int` | 会员数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.StorePeopleCount` | `int` | 店员数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.SmsCount` | `int` | 短信数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.TaiCardCount` | `int` | 台牌数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.IsGive` | `bool` | 是否是赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceEndDate` | `string` | 到期的时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Mobile` | `string` | 电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsBind` | `bool` | 是否已经绑定了 | 普通业务字段 | 可按问题需要提供 |
| `Data.YearsList` | `List<SoftYearsDiscountViewModel>` | 年 | 普通业务字段 | 可按问题需要提供 |
| `Data.YearsList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.YearsList[].Unit` | `int` | 单位；0-日，1-月，2-年 | 普通业务字段 | 可按问题需要提供 |
| `Data.YearsList[].Day` | `int` | 对应的具体值 | 普通业务字段 | 可按问题需要提供 |
| `Data.YearsList[].Discount` | `decimal` | 折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.YearsList[].State` | `int` | state | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 续费时展示的价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Day` | `int` | 天数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`；`model.Type == 1`；`model.Type == 2`；`model.Type == 3`；`store.SoftVersion != 0 && store.SoftVersion != 1 && store.SoftVersion != 2001`
- 一层业务调用：`SoftOrderProvider.GetModule`、`SoftOrderProvider.GetRenewModule`、`SoftOrderProvider.GetAddModule`、`SoftOrderProvider.GetYearsDiscountList`、`StoreProvider.GetStoreByIdAsync`、`SoftVersionPermissionProvider.GetPermissionModelBySoftVersion`
- 疑似副作用：`SoftOrderProvider.GetRenewModule`、`SoftOrderProvider.GetAddModule`。人工复核前禁止开放。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.get_order_code_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取二维码后的内容 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/GetOrderCodeInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetOrderCodeInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetOrderCodeInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:894` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Id <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 二维码id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChannelUsersId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 用户的id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList` | `List<MudoleViewModel>` | 模块的列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Id` | `int` | 模块的id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ModuleList[].Name` | `string` | 模块的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Code` | `string` | 唯一 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Describe` | `string` | 描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.ModuleList[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.Unit` | `int` | 单位；0-天，1-月，2-年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Day` | `int` | 天or年 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsDiscount` | `bool` | 是否是折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Discount` | `decimal` | 折扣数 | 普通业务字段 | 可按问题需要提供 |
| `Data.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.OriginalPrice` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.EndDate` | `string` | 到期的时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext` | `FunctionExtViewModel` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.VipCount` | `int` | 会员数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.StorePeopleCount` | `int` | 店员数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.SmsCount` | `int` | 短信数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.TaiCardCount` | `int` | 台牌数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Ext.IsGive` | `bool` | 是否是赠送 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Id <= 0`；`resultMsg.State`；`!string.IsNullOrEmpty(result.Message)`
- 一层业务调用：`SoftOrderProvider.GetOrderCodeInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不完整`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.get_qr_img_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 扫码商户扩展二维码 |
| 使用时机 | 在商家视角中核对“扫码商户扩展二维码”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/GetQrImgInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetQrImgInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetQrImgInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:1069` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ChannelUsersId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.ChannelUsersId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 用户的id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ChannelName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态；0-可以点击接受，-1不可点击接收 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.ChannelUsersId <= 0`；`resultMsg.State`
- 一层业务调用：`SoftOrderProvider.GetQrImgInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不完整`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.get_soft_server_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取店铺软件服务信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/GetSoftServerInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetSoftServerInfoResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetSoftServerInfoResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:224` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 && model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 && model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftVersion` | `int` | 1试用版、2基础版、3标准版、4预约版、5营销版、10高级版 | 普通业务字段 | 可按问题需要提供 |
| `Data.SoftVersionName` | `string` | 软件版本名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceEndDate` | `string` | 服务到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceDateDays` | `int` | 剩余服务天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsServiceEnd` | `bool` | 软件服务是否已到期 0否1是 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaxCardCount` | `int` | 制卡数量 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaxMemberCount` | `int` | 最大会员数量 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaxClerkCount` | `int` | 当前软件版本，门店店员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCertification` | `int` | 是否已认证 0否1是 0-未开通，1-审核已通过，2-审核未通过，3-审核中 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTry` | `bool` | 是否可以使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAgentStore` | `bool` | 是否是代理商户 | 普通业务字段 | 可按问题需要提供 |
| `Data.Modules` | `List<StoreSoftModuleViewModel>` | 模块列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Modules[].ModuleId` | `int` | 模块的id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Modules[].VersionId` | `int` | 对应版本的id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Modules[].ModuleName` | `string` | module_name 模块的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Modules[].ModuleCode` | `string` | module_code 唯一 | 普通业务字段 | 可按问题需要提供 |
| `Data.Modules[].State` | `int` | state 状态；1-正常，0-关闭 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions` | `List<StoreVersionViewModel>` | 版本列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].VersionId` | `int` | 版本id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Versions[].VersionName` | `string` | 版本名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].VersionType` | `int` | 类型；0-软件，1-增值服务/年，2增值服务/终身 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].ServiceEndDate` | `string` | 到期日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].IsUpdate` | `bool` | 是否可以升级 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].IsRenewal` | `bool` | 是否可以续费 | 普通业务字段 | 可按问题需要提供 |
| `Data.Versions[].State` | `int` | state 状态 1生效 2过期 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceTips` | `List<SoftServerTipsModel>` | 提醒列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceTips[].TipsType` | `int` | 提示类型 0 软件临期，1 软件过期，2可以延长试用 3免费版升级提醒 11 快捷收款临期，12，快捷收款额度预警，13 快捷收款关闭 21没有软件权限 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceTips[].TipsTitle` | `string` | 提醒标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceTips[].TipsSubTitle` | `string` | 子标题 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 && model.StoreId < 1`；`resMsg.State`；`versionInfo?.Id > 0`；`storePermission?.Id > 0`；`sm.IsCertification`；`yopModel == null \|\| yopModel.State == -4`；`yopModel.State == 1 \|\| yopModel.State == 10`；`yopModel.State == -3`；`yopModel.State == -5`；`sm.ServiceEndDate !=null`；`logCount == 1`；`resMsg.Data.Versions?.Count > 0`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`StoreSoftVersionPermissionProvider.GetStorePermissionModelByStoreId`、`SoftVersionProvider.GetVersionModelById`、`StoreYopConfigProvider.GetYopConfigModelByStoreId`、`StoreSoftModuleProvider.GetStoreModuleByStoreId`、`StoreVersionProvider.GetStoreVersionByStoreId`、`StoreProvider.CheckAgentStore`、`RedisManager.IncrementToRedisDb`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.soft.get_store_module`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 软件订购与版本 |
| 用途 | 获取店铺的模块 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Soft/GetStoreModule` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreModuleResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreModuleResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/SoftController.cs:1036` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsChannel` | `bool` | 是否有渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsBuy` | `bool` | 是否已购买过模块 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceEndDate` | `string` | 到期的时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreModuleList` | `List<StoreModuleViewModel>` | 店铺模块列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreModuleList[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreModuleList[].Code` | `string` | 唯一 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`SoftOrderProvider.GetStoreModule`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.admin_store.get_access`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 返回当前登录用户是否具备系统店铺管理权限。 |
| 使用时机 | 在商家视角中核对“返回当前登录用户是否具备系统店铺管理权限。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AdminStore/GetAccess` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<GetSystemAdminAccessResponseModel>`；包装 `DataResult`；Data `GetSystemAdminAccessResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AdminStoreController.cs:24` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.IsSystemAdmin` | `bool` | 当前签名用户是否为有效系统管理员。 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AdminUserProvider.IsActiveAdminUser`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.admin_store.get_stores`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取系统管理员可进入的店铺分页列表。 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/AdminStore/GetStores` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<SystemAdminStoreListItemModel>>`；包装 `DataResult`；Data `PageData<SystemAdminStoreListItemModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/AdminStoreController.cs:44` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 当前页，从 1 开始。；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页记录数；控制器会限制最大值，避免一次读取过多店铺。；普通业务字段；可按问题需要提供 |
| `Keyword` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 店铺 ID 或店铺名称关键字。；普通业务字段；可按问题需要提供 |
| `StoreState` | `int` | 可选/有默认值 | 绑定=ApiController推断；默认值=-1 | AI 只能使用文档确认的枚举值 | 店铺状态；-1 查询全部未删除店铺，0 暂停营业，1 正常营业。；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<SystemAdminStoreListItemModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StoreId` | `int` | 店铺 ID。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].StoreName` | `string` | 店铺名称。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StoreLogo` | `string` | 店铺 Logo 的可访问地址。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TenantId` | `int` | 店铺所属租户 ID。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].State` | `int` | 店铺状态，1 正常营业，0 暂停营业。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].SoftVersion` | `int` | 店铺软件版本。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ServiceEndDate` | `DateTime?` | 软件服务到期时间；为空表示永久。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `DateTime` | 店铺创建时间。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].UserType` | `int` | 系统管理员进入门店后使用店长级页面角色，实际身份仍是系统管理员。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PrivilegeGroupId` | `long` | 前端角色占位；权限仍由后端按系统管理员身份实时返回。 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`!TryValidateSystemAdmin(model?.Uid ?? 0, result)`
- 一层业务调用：`StoreProvider.GetSystemAdminStoreList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_demo_get_store_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 如果是Demo环境，调用DEMO店铺列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessDemoGetStoreList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessDemoStoreResponseModel>>`；包装 `Task/DataResult`；Data `BusinessDemoStoreResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1593` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| — | — | 无 Action 业务参数 | — | — | — |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.DemoStoreList` | `List<BusinessDemoStoreModel>` | 演示店铺数据列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.DemoStoreList[].Id` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.DemoStoreList[].StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.DemoStoreList[].BgImg` | `string` | 店铺背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.DemoStoreList[].WatchCount` | `int` | 观看次数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`Constant.Environment.Equals(EnvironmentEnum.DEMO.GetEnumText())`
- 一层业务调用：`StoreProvider.GetStoreList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_card_agreement`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取会员协议 |
| 使用时机 | 在商家视角中核对“获取会员协议”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetCardAgreement` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCardAgreementResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCardAgreementResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1044` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreAgreement` | `string` | 会员协议 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSignAgreement` | `bool` | 是否需要签署协议 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`scModel?.Id > 0`
- 一层业务调用：`StoreSetingProvider.StoreSetingGetAgreement`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_card_interests`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取会员卡权益 |
| 使用时机 | 在商家视角中核对“获取会员卡权益”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetCardInterests` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCardInterestsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCardInterestsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:972` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardInstructions` | `string` | 使用说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardLimit` | `string` | 使用须知 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`scModel?.Id > 0`
- 一层业务调用：`StoreCardProvider.GetCardInterestsByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_cloud_index_banner_detail`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取云店首页banner详情 |
| 使用时机 | 在商家视角中核对“获取云店首页banner详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetCloudIndexBannerDetail` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCloudIndexBannerDetailResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCloudIndexBannerDetailResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2209` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BannerId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Banner` | `CloudIndexBannerModel` | Banner详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Banner.ImgUrl` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.JumpUrl` | `string` | 跳转地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.Type` | `int` | -1 不跳转 0 网页 1 内部小程序 2 外部小程序 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.ProductImg` | `string` | 跳转商品/餐品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.AppId` | `string` | appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Banner.IsSystem` | `bool` | 是否系统 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.TemplateTag` | `int` | 模板标记分类（用于区分模板头图） | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.Sort` | `int` | 排序号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Banner.IsCustomizeBanner` | `bool` | 是否是自定义banner | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId <= 0 \|\| request.Uid <= 0`
- 一层业务调用：`StoreProvider.GetCloudIndexBannerDetail`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_cloud_store_index_modules`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取云店首页模块信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetCloudStoreIndexModules` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCloudStoreIndexModulesResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCloudStoreIndexModulesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2425` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.BannerList` | `List<CloudIndexBannerModel>` | banner列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.BannerList[].ImgUrl` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].JumpUrl` | `string` | 跳转地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].Type` | `int` | -1 不跳转 0 网页 1 内部小程序 2 外部小程序 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].ProductImg` | `string` | 跳转商品/餐品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].AppId` | `string` | appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.BannerList[].IsSystem` | `bool` | 是否系统 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].TemplateTag` | `int` | 模板标记分类（用于区分模板头图） | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].Sort` | `int` | 排序号 | 普通业务字段 | 可按问题需要提供 |
| `Data.BannerList[].IsCustomizeBanner` | `bool` | 是否是自定义banner | 普通业务字段 | 可按问题需要提供 |
| `Data.IntroductionImg` | `string` | 介绍图 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsFoodRecomand` | `bool` | 是否开启餐品推荐 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsProductRecomand` | `bool` | 是否开启商品推荐 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecomandFoodImageList` | `List<RecomandInfo>` | 推荐餐品列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecomandFoodImageList[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RecomandFoodImageList[].Imgurl` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecomandProductImageList` | `List<RecomandInfo>` | 推荐商品列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.RecomandProductImageList[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RecomandProductImageList[].Imgurl` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodModuleState` | `int` | 点单模块购买状态 0未购买 -1已过期 1 已购买 | 普通业务字段 | 可按问题需要提供 |
| `Data.ShopModuleState` | `int` | 商城模块购买状态 0未购买 -1已过期 1 已购买 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCustomerService` | `bool` | 是否展示客服 | 普通业务字段 | 可按问题需要提供 |
| `Data.EnterpriseNo` | `string` | 企业号（客服跳转企业微信） | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomerLink` | `string` | 客服链接（跳转企业微信） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId <= 0 \|\| request.Uid <= 0`；`res.State`
- 一层业务调用：`StoreProvider.GetCloudStoreIndexModules`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_current_store_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取商户信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetCurrentStoreList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetCurrentStoreListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCurrentStoreListResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2647` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedStoreCount` | `int` | 还能创建的店铺数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores` | `List<StoreInfoViewModel>` | 店铺列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Stores[].StoreCode` | `string` | 店铺Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreTypeId` | `string` | 店铺类型ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Stores[].StoreProvince` | `string` | 商户省 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreCity` | `string` | 商户市 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreDistrict` | `string` | 商户区 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Address` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].Longitude` | `string` | 定位经度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Latitude` | `string` | 定位纬度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreMobile` | `string` | 预留电话（多个按,分隔） | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].State` | `int` | state 状态，1营业，0暂停营业，-1停止运营 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].WorkingWeeks` | `List<int>` | 工作时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].UserType` | `int` | 用户权限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].WorkingTimes` | `List<string>` | 工作时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`storeModels?.Count > 0`；`sm != null && sm.Id > 0`
- 一层业务调用：`StoreProvider.GetStoreListByTenant`、`TenantProvider.GetTenantById`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_custom_controls`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 商家获取自定义会员控件信息(会员资料设置页) |
| 使用时机 | 在商家视角中核对“商家获取自定义会员控件信息(会员资料设置页)”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetCustomControls` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：CustomControlsProvider.AddCustomControls, StoreSetingProvider.StoreSetingByUpdateSet |
| 返回 | `Task<DataResult<BusinessGetCustomControlsResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetCustomControlsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:259` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls` | `List<CustomControlsViewModel>` | 系统公共控件 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].StoreId` | `int` | 店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].IsMust` | `int` | is_mus 是否必填 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].IsCustom` | `bool` | 是否可以删除修改 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items` | `List<CustomControlsItemViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].Items[].Cid` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomControls[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomControls[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls` | `List<StoreControlsViewModel>` | 店铺自定义控件 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreControls[].Cid` | `int` | 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreControls[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].IsMust` | `int` | 是否必填 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreControls[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`resultMsg.State`；`cms.All(x => x.Id != smodel.Cid)`；`cm?.Id > 0`；`eachList?.Id > 0`；`cs != null && cs.IsPrivate`；`storecs == null`；`rcmsModel != null`
- 一层业务调用：`CustomControlsProvider.BusinessGetCustomControlsList`、`StoreControlsProvider.BusinessGetStoreControlsList`、`CustomControlsProvider.BusinessGetCustomControlsById`、`CustomControlsProvider.BusinessGetCustomControlsByStoreId`、`CustomControlsProvider.AddCustomControls`、`CustomControlsItemProvider.CustomControlsItemGetByCid`、`StoreSetingProvider.StoreSetingByUpdateSet`
- 疑似副作用：`CustomControlsProvider.AddCustomControls`、`StoreSetingProvider.StoreSetingByUpdateSet`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_demo_store_qr_img`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取演示店铺的小程序二维码 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetDemoStoreQrImg` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CommonResponseViewModel>>`；包装 `Task/DataResult`；Data `CommonResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1650` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(reqModel.Uid == 0 \\|\\| reqModel.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(reqModel.Uid == 0 \\|\\| reqModel.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`reqModel.Uid == 0 \|\| reqModel.StoreId < 1`
- 一层业务调用：`StoreProvider.GetStoreXcxQrCode`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_print_by_store_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 商家添加打印机 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetPrintByStoreId` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetPrintByStoreIdResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetPrintByStoreIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1741` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrintSn` | `string` | SN | 普通业务字段 | 可按问题需要提供 |
| `Data.PrintKey` | `string` | KEY | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态1开启 0关闭 | 普通业务字段 | 可按问题需要提供 |
| `Data.PrintSum` | `int` | 打印次数 1或 2 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`pint != null`
- 一层业务调用：`PrintConfigProvider.PrintGetInfoByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_store_email`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺默认邮箱 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetStoreEmail` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreEmailResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreEmailResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1465` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 && model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserEmail` | `string` | 店铺默认邮箱 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 && model.Uid < 1`；`resMsg.State`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_store_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取商户信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetStoreInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreInfoResponseViewModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreInfoResponseViewModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:648` |
| C/B 对照 | capi.store.get_store_info |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsNotDefaultData` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 当店铺名，logo为空 是否返回默认管理员数据；普通业务字段；可按问题需要提供 |
| `SetStoreId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 要设置的StoreId；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCode` | `string` | 店铺Code | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreTypeId` | `string` | 店铺类型ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.WorkingTimeStart` | `string` | 工作时间 开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.WorkingTimeEnd` | `string` | 工作时间 结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreProvince` | `string` | 商户省 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreCity` | `string` | 商户市 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreDistrict` | `string` | 商户区 | 普通业务字段 | 可按问题需要提供 |
| `Data.Address` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Longitude` | `string` | 定位经度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Latitude` | `string` | 定位纬度 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreMobile` | `string` | 预留电话（多个按,分隔） | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.State` | `int` | state 状态，1营业，0暂停营业，-1停止运营 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShop` | `bool` | 是否开通了商城 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsIntegralDeduction` | `bool` | 是否参与积分抵扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsFood` | `bool` | 是否开通了点单 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodMode` | `int` | 点单模式 0 一人一单 1多人一单 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade` | `CustomMadeInfoViewModel` | 定制化信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.ShopAssistantCall` | `string` | 技师（店员）称呼 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.PlaceCall` | `string` | place_call 场地称呼，预约使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.PrivateLessonsTitle` | `string` | private_lessons_title 私教课标题，预约使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.ClassesLessonsTitle` | `string` | 班课标题，预约使用 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.BackgroundImage` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.IsShowCarSearch` | `bool` | 是否显示汽车搜索 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.IsPerformance` | `bool` | 是否开启了绩效功能 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.AreaSimple` | `string` | 省份简称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.AllianceType` | `int` | 联盟类型：0默认联盟。1城市联盟（泰州） | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.CurrencyName` | `string` | 货币名称 元 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.CurrencyUnit` | `string` | 货币符号 ￥$ | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.IsOfficialPay` | `bool` | 是否是官方支付渠道 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.SoftVersionTag` | `int` | 软件版本标记 0老版本 1新版本 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.IsUseAudit` | `bool` | 优惠券是否需要审核 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.AppId` | `string` | APPID 店铺小程序的APPID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomMade.QrCodeRule` | `string` | 三方小程序二维码规则 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.IsStallMode` | `bool` | 是否档口模式 | 普通业务字段 | 可按问题需要提供 |
| `Data.CustomMade.AntShopId` | `string` | 蚂蚁门店ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CustomMade.AliPayPid` | `string` | 支付宝PID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IsDistribution` | `bool` | 是否打开分销 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExtend` | `bool` | 是否开通了推广 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCommission` | `bool` | 佣金开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMultipleTable` | `bool` | 是否是多码 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTransfer` | `bool` | 是否允许转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.RegisterMethod` | `int` | 1.先填资料 2.后填资料 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsUserImgOrNick` | `bool` | 是否需要获取用户头像昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPromotionCodeVisable` | `bool` | 推广码是否可见 0 不可见 1 可见 ; | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPromotBoxVisable` | `bool` | 分佣弹框是否提示 1提示 0 不提示 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCloudStore` | `bool` | 是否是云店 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsBalanceMergers` | `bool` | 储值卡是否合并 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsElectronicCard` | `bool` | 是否创建电子卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsConfirmPass` | `bool` | 会员消费是否需要输入密码 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsConfirmPhoto` | `bool` | 是否在会员消费时验证照片 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsPayByCard` | `bool` | 是否允许顾客自主核销 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsRefundProtection` | `bool` | 是否开启退卡保护 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSignAgreement` | `bool` | 是否需要签署协议 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsVisitWechat` | `bool` | 是否添加微信 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsBirthdayUserCount` | `bool` | 是否显示近期会员生日数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`model.SetStoreId > 0`；`resultMsg.State`；`sm?.Id > 0`；`!sm.IsBuildQrCode`；`sm.IsCertification && string.IsNullOrEmpty(sm.FubeiMerChantNo)`；`sm.SoftVersion > 1`；`um?.Id > 0 && !string.IsNullOrEmpty(um.UserMobile) && sm.StoreTypeId == "005"`；`model.StoreId == 3189`；`auth != null`；`threeMp != null`；`storeSetting != null`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`StoreProvider.BuildStoreQrCode`、`UsersInfoProvider.UsersInfoGetByUid`、`AreaMobileProvider.GetAreaInfo`、`AlipayAuthInfoProvider.GetAlipayAuthInfoByStoreId`、`ThreeAuthorizerInfoProvider.GetThreeMiniProgramByStoreId`、`StoreSetingProvider.GetStoreSetingByStoreId`、`ReturnVisitProvider.ExistReturnVisit`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_store_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取商户信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetStoreList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreListResponseModel` |
| 数据时效 | 可能包含缓存结果；不能等同数据库即时状态 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2577` |
| C/B 对照 | capi.store.get_store_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedStoreCount` | `int` | 还能创建的店铺数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores` | `List<StoreInfoViewModel>` | 店铺列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreId` | `int` | 店铺Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Stores[].StoreCode` | `string` | 店铺Code | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreTypeId` | `string` | 店铺类型ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Stores[].StoreProvince` | `string` | 商户省 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreCity` | `string` | 商户市 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreDistrict` | `string` | 商户区 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Address` | `string` | 店铺地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].Longitude` | `string` | 定位经度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].Latitude` | `string` | 定位纬度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].StoreMobile` | `string` | 预留电话（多个按,分隔） | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Stores[].State` | `int` | state 状态，1营业，0暂停营业，-1停止运营 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].WorkingWeeks` | `List<int>` | 工作时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].UserType` | `int` | 用户权限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Stores[].WorkingTimes` | `List<string>` | 工作时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`storeModels?.Count > 0`；`sm != null && sm.Id > 0`
- 一层业务调用：`StoreProvider.GetStoreListByAdminId`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_store_management_data`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取商户信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetStoreManagementData` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreManagementDataResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreManagementDataResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1111` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreName` | `string` | 店铺名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreLogo` | `string` | 店铺logo | 普通业务字段 | 可按问题需要提供 |
| `Data.UserName` | `string` | 昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StoreUserName` | `string` | 店铺姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.AdminCount` | `int` | 管理员数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShowVoiceSeting` | `bool` | 是否显示语音开关 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsVoice` | `bool` | 语音开关 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`sm?.Id > 0`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`TenantUserProvider.GetTenantUserById`、`UsersInfoProvider.UsersInfoGetByUid`、`StoreUserPermissionsProvider.GetStoreUserPermissionsByUid`、`TenantUserProvider.GetTenantUserByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_store_qr_code_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺二维码列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetStoreQrCodeList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreQrCodeListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreQrCodeListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1219` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes` | `List<StoreQrCodeViewModel>` | 二维码信息列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].CodeTitle` | `string` | 二维码标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].CodeInfo` | `string` | 二维码描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].Scenes` | `int` | 场景0付款 1价目表 2购卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].AppId` | `string` | 二维码对应小程序ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Codes[].CodePathParam` | `string` | 二维码对应页面参数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].PayQrCodeImg` | `string` | 二维码图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Codes[].DownloadImg` | `string` | 下载图片地址 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`threeMp?.Id > 0`；`!sm.IsBuildQrCode`；`ssmms.Any(x => x.ModuleId == 10)`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`ThreeAuthorizerInfoProvider.GetThreeMiniProgramByTenantId`、`StoreSoftModuleProvider.GetStoreModuleBySid`、`StoreProvider.BuildStoreQrCode`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_store_seting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取商户设置信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetStoreSeting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreSetingResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreSetingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:216` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsControls` | `int` | 是否设置控件 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsLimit` | `int` | 是否设置了权益 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAgreement` | `int` | 是否设置了协议 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsInfo` | `int` | 是否设置了店铺信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCompleteGuide` | `bool` | 是否完成新手指引 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`sm != null && sm.Id > 0`
- 一层业务调用：`StoreSetingProvider.GetStoreSetingByStoreId`、`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_get_store_user_statistical`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取商户店铺会员统计信息(会员首页) |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessGetStoreUserStatistical` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreUserStatisticalResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreUserStatisticalResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:177` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId == 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumVipPeople` | `long` | 总计会员人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.NewVipPeople` | `long` | 新会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumVip` | `long` | 会员总计有效金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumVipNon` | `long` | 会员总计过期金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumNotRenewal` | `long` | 总计未续费 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumInactiveVip` | `long` | 未激活会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumLowBalanceVip` | `long` | 低余额会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumActivateVip` | `long` | 活跃会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumNotActivateVip` | `long` | 沉睡会员 | 普通业务字段 | 可按问题需要提供 |
| `Data.SumExperience` | `long` | 体验总数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId == 0`；`string.IsNullOrEmpty(um.UserMobile)`；`resultMsg.State`
- 一层业务调用：`UsersInfoProvider.UsersInfoGetByUid`、`UserCardProvider.BusinessGetStoreUserStatistical`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.business_store_image_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺图片列表详情 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/BusinessStoreImageList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessStoreImageListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessStoreImageListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2181` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(request.StoreId <= 0 \\|\\| request.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ImgType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 0 banner 1 广告位 2环境图；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Images` | `List<CloudIndexBannerModel>` | banner详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Images[].ImgUrl` | `string` | 图片地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].JumpUrl` | `string` | 跳转地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].Type` | `int` | -1 不跳转 0 网页 1 内部小程序 2 外部小程序 | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].ProductImg` | `string` | 跳转商品/餐品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].AppId` | `string` | appid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Images[].IsSystem` | `bool` | 是否系统 | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].TemplateTag` | `int` | 模板标记分类（用于区分模板头图） | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].Sort` | `int` | 排序号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Images[].IsCustomizeBanner` | `bool` | 是否是自定义banner | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`request.StoreId <= 0 \|\| request.Uid <= 0`
- 一层业务调用：`StoreProvider.GetIndexBanner`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_axc_merchant_remain`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取安心充充值余额信息 |
| 使用时机 | 在商家视角中核对“获取安心充充值余额信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetAxcMerchantRemain` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<AxcMerchantRemainResponseModel>>`；包装 `Task/DataResult`；Data `AxcMerchantRemainResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1997` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.UnusedPrincipal` | `decimal` | 消费者尚未使用的本金部分 | 普通业务字段 | 可按问题需要提供 |
| `Data.UnusedBenefit` | `decimal` | 消费者未使用的权益金部分 | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedBenefit` | `decimal` | 消费者已使用的权益金部分 | 普通业务字段 | 可按问题需要提供 |
| `Data.UsedPrincipal` | `decimal` | 消费者已使用的本金部分 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`AlipayThirdPlatformProvider.AxcMerchantremainingQuery`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_operate_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 显示营业时间 |
| 使用时机 | 在商家视角中核对“显示营业时间”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetOperateInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetOperateInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetOperateInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:874` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreOperateWeeks` | `List<StoreOperateWeekViewModel>` | 营业日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreOperateWeeks[].WeekInfo` | `int` | 营业日期 （星期1-7） 数字类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreOperateWeeks[].State` | `int` | 0 非营业时间，1正常营业 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreOperateTimes` | `List<StoreOperateTimeViewModel>` | 营业时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreOperateTimes[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreOperateTimes[].BeginTime` | `string` | 营业时间开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreOperateTimes[].EndTime` | `string` | 营业时间 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreVacations` | `List<StoreVacationViewModel>` | 放假时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreVacations[].Id` | `int` | ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreVacations[].BeginDate` | `string` | 放假时间开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreVacations[].EndDate` | `string` | 放假时间结束时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreProvider.GetOperateInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_search_user`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 尾号搜索信息 |
| 使用时机 | 商家按手机号或筛选条件定位会员、确认会员档案与门店关系，为后续卡/预约/消费查询建立上游对象引用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetSearchUser` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<GetSearchUserResponseModel>>>`；包装 `Task/DataResult`；Data `List<GetSearchUserResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2538` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId == 0 \\|\\| string.IsNullOrEmpty(model.Value)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Value` | `string` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId == 0 \\|\\| string.IsNullOrEmpty(model.Value)) | 当前会话提供并临时使用；不得持久化到模型历史 | 完整会员手机号或至少 4 位手机号尾号；服务端按当前门店或当前连锁租户范围做后缀匹配；个人信息；仅在当前授权场景按最小范围提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UserId` | `int` | 用户的id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Img` | `string` | 头像 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Mobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].UserPhoto` | `string` | 图片验证的图片 | 普通业务字段 | 可按问题需要提供 |
| `Data[].State` | `int` | 搜索展示状态，由会员身份和持卡情况派生：0-有正常可用卡，1-余额不足， 2-卡已过期，-2-会员身份被商家删除；不是 user_card.state 原值。 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CancelInfo.Id` | `long` | 日志ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].CancelInfo.CancelType` | `int` | 注销类型 0会员 1卡 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CancelInfo.OperationType` | `int` | 操作类型 0删除 1恢复 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CancelInfo.StoreUserName` | `string` | 店员名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].CancelInfo.CreateDate` | `string` | 創建時間 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CancelInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].CancelInfo.CancelInfo` | `CancelUserInfoViewModel` | 会员注销详情 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId == 0 \|\| string.IsNullOrEmpty(model.Value)`；`resultMsg.State`；`model.Value.Length < 4`
- 一层业务调用：`UserCardProvider.GetSearchUser`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_store_index_page_style`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取首页样式 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetStoreIndexPageStyle` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreIndexPageStyleResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreIndexPageStyleResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2837` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageStyle` | `int` | 0卡包样式 1云店样式 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`res.State`
- 一层业务调用：`StoreCustomSetingProvider.GetStoreCustomSetingByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_store_type`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺行业信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetStoreType` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreTypeResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreTypeResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1779` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `RootId` | `string` | 可选/有默认值 | 绑定=ApiController推断；默认值="0" | 必须来自同一会话上游 API 结果或服务端对象引用 | 父类id 取1级分类 传0；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreTypes` | `List<GetStoreTypeViewModel>` | 店铺类型集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreTypes[].StoreTypeName` | `string` | 类型名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreTypes[].StoreTypeLogo` | `string` | 类型图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreTypes[].StoreTypeId` | `string` | 类型ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreTypeProvider.GetStoreTypeAll`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_store_type_v2`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺行业信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetStoreTypeV2` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreTypeResponseV2Model>>`；包装 `Task/DataResult`；Data `GetStoreTypeResponseV2Model` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:1819` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `RootId` | `string` | 可选/有默认值 | 绑定=ApiController推断；默认值="0" | 必须来自同一会话上游 API 结果或服务端对象引用 | 父类id 取1级分类 传0；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreRootTypes` | `List<StoreTypeRootViewModel>` | 店铺类型集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreRootTypes[].StoreTypeName` | `string` | 类型名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreRootTypes[].StoreTypeId` | `string` | 类型ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreRootTypes[].StoreTypes` | `List<GetStoreTypeViewModel>` | 店铺类型集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreRootTypes[].StoreTypes[].StoreTypeName` | `string` | 类型名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreRootTypes[].StoreTypes[].StoreTypeLogo` | `string` | 类型图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreRootTypes[].StoreTypes[].StoreTypeId` | `string` | 类型ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`types?.Count > 0`
- 一层业务调用：`StoreTypeProvider.GetStoreType`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_store_user_tag_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺会员标签 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetStoreUserTagList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetStoreUserTagListResponseModel>>`；包装 `DataResult`；Data `List<GetStoreUserTagListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2453` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Name` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Type` | `int` | 类型；1-红，2-蓝，3-绿，4-粉，0-灰 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreProvider.GetStoreUserTagList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store.get_store_workbench`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取工作台列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Store/GetStoreWorkbench` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetStoreWorkbenchResponseModel>>`；包装 `DataResult`；Data `List<GetStoreWorkbenchResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreController.cs:2512` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].WorkbenchId` | `int` | 工作台id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Module` | `string` | 工作台模块 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Sign` | `bool` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Type` | `int` | 类型；0-小程序内部，1-外链，2-外部小程序，3-不跳转 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Path` | `string` | 跳转的内容 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Data` | `string` | 内容 | 普通业务字段 | 可按问题需要提供 |
| `Data[].NameEnglish` | `string` | 英文的名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].BorderColor` | `string` | 边框的颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FontChineseColor` | `string` | 中文字体色 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FontEnglishColor` | `string` | 英文字体色 | 普通业务字段 | 可按问题需要提供 |
| `Data[].BackgroundColor` | `string` | 背景色 | 普通业务字段 | 可按问题需要提供 |
| `Data[].RightBottomImg` | `string` | 右下角的图 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsShowNum` | `bool` | 是否显示数字 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Num` | `int` | 数字 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreWorkbenchProvider.GetStoreWorkbench`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_added.get_store_added_services`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺增值列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreAdded/GetStoreAddedServices` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreAddedServicesResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreAddedServicesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreAddedController.cs:26` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList` | `List<AddedServicesTypeViewModel>` | 增值列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].ServiceType` | `int` | 类型 0短信 1物料 2硬件 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].ServiceTypeName` | `string` | 类型名 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList` | `List<AddedServiceViewModel>` | 增值列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AddedServiceTypeList[].AddedServiceList[].ServiceType` | `int` | 类型 0短信 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].Title` | `string` | title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].ChildTitle` | `string` | child_title 子标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].PosterId` | `int` | poster_id 海报ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AddedServiceTypeList[].AddedServiceList[].ServiceUnit` | `string` | service_unit 单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].ServiceImg` | `string` | service_img 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].ItemQuantity` | `int` | 数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].EndDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].ServiceInfo` | `string` | service_info 介绍 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].IsShip` | `bool` | is_ship 是否需要发货 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceTypeList[].AddedServiceList[].InfoImg` | `List<string>` | 详情图片 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`Constant.Platform < 2`；`mol?.Count() > 0`
- 一层业务调用：`AddedServicesProvider.GetAddedServices`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_added.get_store_added_services_by_soft_discount`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺增值列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreAdded/GetStoreAddedServicesBySoftDiscount` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreAddedServicesBySoftDiscountResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreAddedServicesBySoftDiscountResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreAddedController.cs:93` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList` | `List<AddedServiceViewModel>` | 增值列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AddedServiceList[].ServiceType` | `int` | 类型 0短信 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].Title` | `string` | title 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].ChildTitle` | `string` | child_title 子标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].PosterId` | `int` | poster_id 海报ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.AddedServiceList[].ServiceUnit` | `string` | service_unit 单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].ServiceImg` | `string` | service_img 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].ItemQuantity` | `int` | 数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].EndDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].ServiceInfo` | `string` | service_info 介绍 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].IsShip` | `bool` | is_ship 是否需要发货 | 普通业务字段 | 可按问题需要提供 |
| `Data.AddedServiceList[].InfoImg` | `List<string>` | 详情图片 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`AddedServicesProvider.GetAddedServicesBySoftDiscount`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_added.get_store_added_trade_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取订单列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreAdded/GetStoreAddedTradeList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<GetStoreAddedTradeListResponseModel>>`；包装 `DataResult`；Data `PageData<GetStoreAddedTradeListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreAddedController.cs:203` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetStoreAddedTradeListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 订单id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].State` | `int` | 订单状态：0未付款，1已付款未发货，2已发货，3交易成功,4取消 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PayDate` | `string` | 付款时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PayPrice` | `decimal` | pay_price 实际支付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ItemQuantity` | `int` | 短信数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders` | `List<BusinessOrdersViewModel>` | 订单详情 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].Pid` | `int` | 商品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Orders[].PTitle` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PPic` | `string` | 商品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].OrderType` | `int` | order_type 订单类型 0物料 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PosterType` | `int` | poster_type 类型：朋友圈海报，1物料（购卡送券）2抽奖 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PosterCustomTitle` | `string` | poster_custom_title 定制title | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].Num` | `int` | 购买数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PayPrice` | `decimal` | pay_price 实付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Orders[].PCardIds` | `List<int>` | 储值卡ID（购卡送券物料） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`BusinessTradeProvider.GetStoreAddedTradeList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_classes.get_class_user_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取学员列表 |
| 使用时机 | 在商家视角中核对“获取学员列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreClasses/GetClassUserList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetClassUserListResponseModel>>`；包装 `Task/DataResult`；Data `GetClassUserListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreClassesController.cs:377` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ClassesId` | `long` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 班级id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users` | `List<ClassUserRequestModel>` | 返回数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].CreateDate` | `string` | 入班时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards` | `List<UserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Users[].Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardNormalPrice` | `decimal` | card_normal_price 正金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardGivePrice` | `decimal` | card_give_price 赠金 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardDiscountPrice` | `decimal` | card_discount_price 已优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].CardMergerDiscountPrice` | `decimal` | card_merger_discount_price 合并后未使用优惠金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 4转让中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].OpenDate` | `string` | 开卡时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].WeekMaxFrequency` | `int` | week_max_frequency 每周最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].MouthMaxFrequency` | `int` | mouth_max_frequency 每月最大核销次数 0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].WeekConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].MouthConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].IsAllianceCard` | `bool` | 是否为联盟卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services` | `List<UserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Users[].Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].IsTransferCard` | `bool` | 是否开启转让卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].IsTransferCardValue` | `bool` | 是否开启转让卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].IsUsedDateTime` | `bool` | 是否在使用时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].BindCourseSum` | `long` | 绑定课目数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].UseMaxCount` | `decimal` | 最大使用次数 -1不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].Cards[].UseCount` | `decimal` | 已用次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].State` | `int` | -1退班 0停课 1正常 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].UserVacationState` | `int` | 请假状态 0正常 1待开始 3放假中 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].VacationBegin` | `string` | 放假开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].VacationEnd` | `string` | 放假结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.Users[].LessonsSum` | `long` | 累计上课次数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`UserCardProvider.BusinessGetClassesUsers`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_classes.get_classes_user_vacation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取学员请假列表 |
| 使用时机 | 在商家视角中核对“获取学员请假列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreClasses/GetClassesUserVacation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetClassesUserVacationResponseModel>>`；包装 `Task/DataResult`；Data `GetClassesUserVacationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreClassesController.cs:790` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsMore` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否更多；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMore` | `bool` | 是否有更多 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations` | `List<ClassesUserVacationViewModel>` | 放假信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationId` | `long` | 请假id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Vacations[].VacationTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationSubTitle` | `string` | 子标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationState` | `int` | 1未开始 2已过期 3放假中 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`svms?.Count > 0`；`!model.IsMore`；`yxVms?.Count() > 0`；`sxVms?.Count() > 0`；`sxVms.Count > 1`；`item.State == 1 && item.BeginDate < DateTime.Now`；`item.State == 1 && item.BeginDate.ToString("yyyy-MM-dd") == item.EndDate.ToString("yyyy-MM-dd")`；`vacations?.Count > 0`；`vacations.Any(x => x.VacationState == 1)`；`vacations.Any(x => x.VacationState == 3)`
- 一层业务调用：`UserVacationProvider.GetUserVacationByCardId`、`DateTimeHelper.GetWeekDay`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_classes.get_classes_user_vacation_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取请假详情 |
| 使用时机 | 在商家视角中核对“获取请假详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreClasses/GetClassesUserVacationInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetClassesUserVacationInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetClassesUserVacationInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreClassesController.cs:898` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.VacationId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.VacationId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CardId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 会员id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `VacationId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.VacationId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 放假id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo` | `VacationInfoViewModel` | 放假具体信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo.Id` | `long` | Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.VacationInfo.BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo.EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationInfo.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.VacationId < 1`；`resultMsg.State`；`vm?.Id > 0`
- 一层业务调用：`UserVacationProvider.GetUserVacationFirstOrDefaultByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_classes.get_store_classes`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取单个班级 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreClasses/GetStoreClasses` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreClassesResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreClassesResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreClassesController.cs:63` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.Id < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.Id < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Uid == 0 \\|\\| model.StoreId < 1 \\|\\| model.Id < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课目id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `long` | 班级id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ClassTitle` | `string` | 班级名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassNote` | `string` | 班级简介 | 普通业务字段 | 可按问题需要提供 |
| `Data.ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassImageId` | `long` | 课程图片id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ClassImage` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 0停课 1正常 2待开课 3已结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsCount` | `long` | 课程总节数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses` | `List<StoreClassesCourseViewModel>` | 排课信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Id` | `int` | 课程id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BeginDate` | `string` | 开始日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].EndDate` | `string` | 结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].WeekInfo` | `string` | 周几， 以逗号分隔, | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BeginTime` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].EndTime` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].IsStop` | `bool` | 是否停课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].StopBeginDate` | `string` | 开始日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].StopEndDate` | `string` | 结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].State` | `int` | 0停课 1正常 2待开课 3已结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CardCount` | `int` | 支持卡的数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Courses[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos` | `List<CourseCardBindInfoViewModelModel>` | 课卡绑定信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].PrepaidCardId` | `long` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].BindCardInfos[].CardType` | `int` | 类型：0计次，1储值 2时限 3权益 4安心充 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].ValidityPeriod` | `int` | （时限卡）扣减有效期 天 0不扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].CardValue` | `decimal` | 扣减卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services` | `List<CourseCardBindItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services[].ItemId` | `long` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Courses[].BindCardInfos[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].Services[].ItemValue` | `int` | 服务项 卡价值 0 不支持 | 普通业务字段 | 可按问题需要提供 |
| `Data.Courses[].BindCardInfos[].State` | `int` | 关联状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1 \|\| model.Id < 1`；`resultMsg.State`；`item != null`；`item.ClassImageId > 0`；`courseTimes?.Count() > 0`；`course?.Id > 0`；`newCourse.State == 1`；`courseTime.BeginDate > storeTime.Date`；`courseTime.EndDate < storeTime.Date`；`courseTime.IsStop && courseTime.StopBeginDate <= storeTime.Date && courseTime.StopEndDate >= storeTime.Date`；`newData.State == 1`
- 一层业务调用：`StoreProvider.GetStoreDateTime`、`StoreClassesProvider.GetStoreClassesFirstOrDefaultByCondition`、`StoreClassesCourseProvider.GetStoreClassseCourseElementsByCondition`、`StoreCourseProvider.GetStoreCourses`、`StoreLessonsProvider.GetClassesLessonsCount`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_classes.get_store_classes_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取班级 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreClasses/GetStoreClassesList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<StoreClassesListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<StoreClassesListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreClassesController.cs:31` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程类型：教练id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 创建开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 创建结束时间；普通业务字段；可按问题需要提供 |
| `TagId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 标签id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `State` | `int?` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 状态：0为开班 1开班 10结班；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<StoreClassesListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `long` | 班级id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ClassTitle` | `string` | 班级名字 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ClassNote` | `string` | 班级简介 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ClassImage` | `string` | 背景图 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ImageStyle` | `int` | 图片样式 0明亮 1暗黑 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ClassImageId` | `long` | 课程图片id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ClassColor` | `string` | 颜色 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 0停课 1正常 2待开课 3未排课 10结班 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PeopleCount` | `long` | 班级人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses` | `List<StoreClassesCourseViewModel>` | 排课信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].Id` | `int` | 课程id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Courses[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].CourseDescribe` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BeginDate` | `string` | 开始日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].EndDate` | `string` | 结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].WeekInfo` | `string` | 周几， 以逗号分隔, | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BeginTime` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].EndTime` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].IsStop` | `bool` | 是否停课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].StopBeginDate` | `string` | 开始日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].StopEndDate` | `string` | 结束日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].State` | `int` | 0停课 1正常 2待开课 3已结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].CardCount` | `int` | 支持卡的数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].Staffs` | `List<CourseStaffViewModel>` | 教练信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].Staffs[].Id` | `long` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Courses[].Staffs[].UserName` | `string` | 教练名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Courses[].Staffs[].UserImage` | `string` | 教练图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].CoursePlace` | `CoursePlaceViewModel` | 教室 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].CoursePlace.Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Courses[].CoursePlace.PlaceName` | `string` | 场地名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BindCardInfos` | `List<CourseCardBindInfoViewModelModel>` | 课卡绑定信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BindCardInfos[].PrepaidCardId` | `long` | 储值卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Courses[].BindCardInfos[].CardType` | `int` | 类型：0计次，1储值 2时限 3权益 4安心充 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BindCardInfos[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BindCardInfos[].ValidityPeriod` | `int` | （时限卡）扣减有效期 天 0不扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BindCardInfos[].CardValue` | `decimal` | 扣减卡余额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BindCardInfos[].Services` | `List<CourseCardBindItemViewModel>` | 服务项目列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Courses[].BindCardInfos[].State` | `int` | 关联状态；0-禁用，1-启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreClassesProvider.GetStoreClassesByPage`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_customer_mobile.business_store_customer_mobile_get_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 列表查询 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreCustomerMobile/BusinessStoreCustomerMobileGetList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessStoreCustomerMobileGetListResponseModel>>`；包装 `Task/DataResult`；Data `BusinessStoreCustomerMobileGetListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreCustomerMobileController.cs:21` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupCustomers` | `List<StoreCustomerMobielGroupViewModel>` | 顾客分组列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupCustomers[].NameGroup` | `string` | 拼音首字母分组 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupCustomers[].Customers` | `List<StoreCustomerMobileViewModel>` | 顾客列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupCustomers[].Customers[].Id` | `int` | 顾客号码ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.GroupCustomers[].Customers[].CustomerName` | `string` | 顾客姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupCustomers[].Customers[].CustomerMobile` | `string` | 顾客手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.GroupCustomers[].Customers[].NameGroup` | `string` | 拼音首字母分组 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreCustomerMobileProvider.StoreCustomerMobileGetList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_growth.get_subscription_overview`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取当前门店可查看和可办理的正式版本订购信息。 |
| 使用时机 | 在商家视角中核对“获取当前门店可查看和可办理的正式版本订购信息。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreGrowth/GetSubscriptionOverview` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<SubscriptionOverviewResponseModel>>`；包装 `Task/DataResult`；Data `SubscriptionOverviewResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreGrowthController.cs:157` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| !StoreGrowthProvider.Instance.IsOwner(model.StoreId, model.Uid)) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1 \\|\\| !StoreGrowthProvider.Instance.IsOwner(model.StoreId, model.Uid)) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.CurrentVersionId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CurrentVersionName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceEndDate` | `DateTime?` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceDateDays` | `int?` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTrial` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExpired` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans` | `List<SubscriptionPlanResponseModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].VersionId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Plans[].VersionName` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].ScenarioCode` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].UnlimitedMembers` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].UnlimitedStaff` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].IsCurrent` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].ActionCode` | `string` | buy、renew、upgrade、upgrade_buy 或 none；用于展示，不直接作为小程序订单参数。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].CanOrder` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].DisabledReasonCode` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].UpgradeCycle` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].UpgradePrice` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].UpgradeOriginalPrice` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].Prices` | `List<SubscriptionPriceResponseModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].Prices[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Plans[].Prices[].CycleTitle` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].Prices[].OriginalPrice` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].Prices[].DiscountPrice` | `decimal` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].Prices[].IsRecommend` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].Prices[].SubTitle` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Plans[].Prices[].DiscountInfo` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1 \|\| !StoreGrowthProvider.Instance.IsOwner(model.StoreId, model.Uid)`；`result.Data == null`
- 一层业务调用：`StoreGrowthProvider.IsOwner`、`StoreGrowthProvider.GetSubscriptionOverview`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 403`；`状态赋值：StatusCode = 404`；`固定提示：仅店主可查看版本与订购信息。`；`固定提示：未找到当前门店。`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_growth.get_workspace`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取当前门店的增长工作区快照。 |
| 使用时机 | 在商家视角中核对“获取当前门店的增长工作区快照。”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreGrowth/GetWorkspace` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<StoreGrowthWorkspaceResponseModel>>`；包装 `Task/DataResult`；Data `StoreGrowthWorkspaceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreGrowthController.cs:32` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.IsOwner` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsActivated` | `bool` | 沿用既有真实经营闭环口径，不因新手引导增加步骤而改变。 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsGuideComplete` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.CanSkipRemainingTracks` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.CurrentSoftVersion` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceEndDate` | `DateTime?` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceDateDays` | `int?` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsExpired` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.ShouldRemind` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonSteps` | `List<ActivationStepResponseModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonSteps[].Code` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonSteps[].IsComplete` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonSteps[].TargetId` | `long?` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tracks` | `List<ActivationTrackResponseModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].Track` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].HasProduct` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].HasDelivery` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].HasCustomerValue` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].IsComplete` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].IsSkipped` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].Steps` | `List<ActivationStepResponseModel>` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].Steps[].Code` | `string` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].Steps[].IsComplete` | `bool` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tracks[].Steps[].TargetId` | `long?` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`!result.State`
- 一层业务调用：`StoreGrowthProvider.GetWorkspace`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = result.StatusCode, Message = result.Message }`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_material.get_store_material_category_data_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 查询素材分类及分类下素材 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreMaterial/GetStoreMaterialCategoryDataList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<GetStoreMaterialCategoryDataListResponseModel>`；包装 `DataResult`；Data `GetStoreMaterialCategoryDataListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreMaterialController.cs:259` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.MaterialType < 0 \\|\\| model.MaterialType > 3) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.MaterialType < 0 \\|\\| model.MaterialType > 3) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CategoryType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 分类类型，0表示全部；普通业务字段；可按问题需要提供 |
| `MaterialType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 素材类型，0表示全部，1图片，2视频，3视频号链接；普通业务字段；可按问题需要提供 |
| `Keyword` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 素材名称关键字；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList` | `List<StoreMaterialCategoryDataViewModel>` | 素材分类集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].Id` | `int` | 素材分类ID，-1表示未分类 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.MaterialCategoryList[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.MaterialCategoryList[].CategoryType` | `int` | 分类类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].CategoryName` | `string` | 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialTotal` | `int` | 分类下素材数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList` | `List<StoreMaterialDataViewModel>` | 分类下素材集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].Id` | `int` | 素材ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.MaterialCategoryList[].MaterialsList[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.MaterialCategoryList[].MaterialsList[].CategoryId` | `int` | 素材分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.MaterialCategoryList[].MaterialsList[].CategoryName` | `string` | 素材分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].MaterialName` | `string` | 素材名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].MaterialType` | `int` | 素材类型：1图片，2视频，3视频号链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].FilePath` | `string` | 素材路径；图片/视频为文件地址，视频号为链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].FileUrl` | `string` | 素材完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].FileName` | `string` | 文件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].FileExt` | `string` | 文件扩展名 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].FileSize` | `long` | 文件大小，单位字节 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].MimeType` | `string` | 文件MIME类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].VideoCover` | `string` | 视频封面路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].VideoCoverUrl` | `string` | 视频封面完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].VideoNo` | `string` | 视频号标识 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].VideoTimeLength` | `int` | 视频时长，单位秒 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].Width` | `int` | 素材宽度 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].Height` | `int` | 素材高度 | 普通业务字段 | 可按问题需要提供 |
| `Data.MaterialCategoryList[].MaterialsList[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.MaterialType < 0 \|\| model.MaterialType > 3`
- 一层业务调用：`StoreMaterialProvider.GetMaterialCategoryDataList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_material.get_store_material_category_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 查询素材分类 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreMaterial/GetStoreMaterialCategoryList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<StoreMaterialCategoryViewModel>>`；包装 `DataResult`；Data `List<StoreMaterialCategoryViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreMaterialController.cs:119` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `CategoryType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 分类类型，0表示全部；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Id` | `int` | 素材分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].CategoryType` | `int` | 分类类型 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CategoryName` | `string` | 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].OrderBy` | `int` | 排序值，值越大越靠前 | 普通业务字段 | 可按问题需要提供 |
| `Data[].State` | `int` | 状态：1启用，0停用，-1删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`
- 一层业务调用：`StoreMaterialProvider.GetCategoryList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_material.get_store_material_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 查询素材 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreMaterial/GetStoreMaterialList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<StoreMaterialViewModel>>`；包装 `DataResult`；Data `PageData<StoreMaterialViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreMaterialController.cs:238` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `CategoryId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 素材分类ID，0表示全部；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `MaterialType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 素材类型，0表示全部，1图片，2视频，3视频号链接；普通业务字段；可按问题需要提供 |
| `Keyword` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 素材名称关键字；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<StoreMaterialViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | 素材ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CategoryId` | `int` | 素材分类ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CategoryName` | `string` | 素材分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MaterialName` | `string` | 素材名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MaterialType` | `int` | 素材类型：1图片，2视频，3视频号链接 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FilePath` | `string` | 素材路径；图片/视频为文件地址，视频号为链接地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FileUrl` | `string` | 素材完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FileName` | `string` | 文件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FileExt` | `string` | 文件扩展名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FileSize` | `long` | 文件大小，单位字节 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].MimeType` | `string` | 文件MIME类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].VideoCover` | `string` | 视频封面路径 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].VideoCoverUrl` | `string` | 视频封面完整访问地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].VideoNo` | `string` | 视频号标识 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].VideoTimeLength` | `int` | 视频时长，单位秒 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Width` | `int` | 素材宽度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Height` | `int` | 素材高度 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].OrderBy` | `int` | 排序值，值越大越靠前 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 状态：1启用，0停用，-1删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`
- 一层业务调用：`StoreMaterialProvider.GetMaterialList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_place.exists_place_reservation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 通过时间段查询是否有预约 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StorePlace/ExistsPlaceReservation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<ExistsReservationResponseModel>>`；包装 `Task/DataResult`；Data `ExistsReservationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StorePlaceController.cs:437` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StorePlaceId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StorePlaceId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StorePlaceId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StorePlaceId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 场地；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationTimeStart` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.ReservationTimeStart) \\|\\| string.IsNullOrEmpty(model.ReservationTimeEnd)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约开始时间；普通业务字段；可按问题需要提供 |
| `ReservationTimeEnd` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.ReservationTimeStart) \\|\\| string.IsNullOrEmpty(model.ReservationTimeEnd)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationNum` | `int` | 当前时间段预约的数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.StorePlaceId < 1`；`string.IsNullOrEmpty(model.ReservationTimeStart) \|\| string.IsNullOrEmpty(model.ReservationTimeEnd)`；`resultMsg.State`；`StoreReservationProvider.Instance.GetStoreReservationsByTimes(model.StoreId, model.StorePlaceId, model.ReservationTimeStart, model.ReservationTimeEnd)?.Count > 0`
- 一层业务调用：`StoreReservationProvider.GetStoreReservationsByTimes`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`固定提示：参数不正确`；`固定提示：预约时间不正确`；`固定提示：该时间段已存在预约`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_place.get_place_lessons`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取场地关联课程 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StorePlace/GetPlaceLessons` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetPlaceLessonsResponseModel>>`；包装 `Task/DataResult`；Data `GetPlaceLessonsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StorePlaceController.cs:273` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.Id < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.Id < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.Id < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 场地ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsCount` | `long` | 课程数量 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.Id < 1`；`resultMsg.State`
- 一层业务调用：`StoreLessonsProvider.GetStoreLessonsElementsByCondition`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_place.get_reservation_place`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取预约场地 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StorePlace/GetReservationPlace` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StorePlaceCloseSettingProvider.GetStorePlaceCloseSettingByStoreId |
| 返回 | `Task<DataResult<GetReservationPlaceResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationPlaceResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StorePlaceController.cs:477` |
| C/B 对照 | capi.store_place.get_reservation_place |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationDate` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.ReservationDate)) | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间（格式"2012-01-21"）；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationNum` | `int` | 每位顾客当天累计的可预约场次 | 普通业务字段 | 可按问题需要提供 |
| `Data.UserReservationNum` | `int` | 用户已约次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes` | `List<StoreReservationTimeSetingViewModel>` | 预约时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].Id` | `int` | 时间端Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationTimes[].ReservationTime` | `string` | 预约时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].ReservationTimeEnd` | `string` | 预约时间段 结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsOccupy` | `bool` | 时间被占用 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].SurplusCount` | `int` | 剩余可预约人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Places` | `List<StorePlaceReservationModel>` | 预约场地 | 普通业务字段 | 可按问题需要提供 |
| `Data.Places[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Places[].PlaceName` | `string` | 场地名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Places[].PlaceDescribe` | `string` | 场地描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Places[].ReservationNum` | `int` | 同时可预约次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Places[].ReservationTimes` | `List<PlaceTimeModel>` | 时间段(只返回关闭) | 普通业务字段 | 可按问题需要提供 |
| `Data.Places[].ReservationTimes[].Id` | `int` | 时间段Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Places[].ReservationTimes[].UserReservationNum` | `int` | 已约人次 | 普通业务字段 | 可按问题需要提供 |
| `Data.Places[].ReservationTimes[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`string.IsNullOrEmpty(model.ReservationDate)`；`!(placeModels?.Count>0)`；`resultMsg.State`；`resultMsg.Data.ReservationTimes?.Count>0`；`reservationSeting.ReservationNum > 0 && model.Uid > 0`；`placeCloseModels?.Count > 0`；`placeModel.ReservationTimes.All(x => x.Id != placeCloseTime.Id)`；`placeReservationModels?.Count > 0`；`userSum > 0 && placeItem.ReservationNum > 0`
- 一层业务调用：`StorePlaceProvider.GetStorePlaceByStoreId`、`StoreReservationSetingProvider.GetReservationSeting`、`StoreReservationTimeSetingProvider.GetReservationTimeSeting`、`StoreReservationProvider.GetStoreReservationPlaceByUid`、`StorePlaceCloseSettingProvider.GetStorePlaceCloseSettingByStoreId`、`StoreReservationProvider.GetStoreReservationPlace`
- 疑似副作用：`StorePlaceCloseSettingProvider.GetStorePlaceCloseSettingByStoreId`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`状态赋值：StatusCode = 303`；`状态赋值：StatusCode = 310`；`状态赋值：StatusCode = 305`；`固定提示：参数不正确`；`固定提示：预约时间不正确`；`固定提示：没有可预约场地`；`固定提示：当日没有可预约的时间`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_place.get_store_place_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取场地详细 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StorePlace/GetStorePlaceInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `C` — 查询路径包含疑似写入或外部副作用调用，必须人工复核：StorePlaceCloseInfoProvider.GetStorePlaceCloseInfosByPlaceId |
| 返回 | `Task<DataResult<GetStorePlaceInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetStorePlaceInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StorePlaceController.cs:370` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StorePlaceId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StorePlaceId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StorePlaceId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.StorePlaceId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 场地详细；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace` | `StorePlaceViewModel` | 场地地址 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.Id` | `int` | 场地ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StorePlace.PlaceName` | `string` | 场地名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.PlaceDescribe` | `string` | 场地描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.ReservationNum` | `int` | 同时接受预约次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.State` | `int` | 状态0暂停预约 1正常 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.CloseSettings` | `List<StorePlaceCloseSettingViewModel>` | 关闭时间 集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.CloseSettings[].Id` | `int` | 场所关闭时间设置 ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StorePlace.CloseSettings[].CloseDateStart` | `string` | 场地关闭时间开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.CloseSettings[].CloseDateEnd` | `string` | 场地关闭时间结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.CloseSettings[].CloseRemark` | `string` | 关闭的备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StorePlace.CloseSettings[].StoreOperateWeeks` | `List<int>` | 营业日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.CloseSettings[].Times` | `List<PlaceCloseTimeViewModel>` | 营业日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.CloseSettings[].Times[].CloseTimeStart` | `string` | 场地关闭时间开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlace.CloseSettings[].Times[].CloseTimeEnd` | `string` | 场地关闭时间结束 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.StorePlaceId < 1`；`resultMsg.State`；`resultMsg.Data.StorePlace?.Id>0`；`closeInfos?.Count>0`；`!string.IsNullOrEmpty(item.CloseTimes)`
- 一层业务调用：`StorePlaceProvider.GetStorePlaceById`、`StorePlaceCloseInfoProvider.GetStorePlaceCloseInfosByPlaceId`
- 疑似副作用：`StorePlaceCloseInfoProvider.GetStorePlaceCloseInfosByPlaceId`。人工复核前禁止开放。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_place.get_store_place_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取场地列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StorePlace/GetStorePlaceList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStorePlaceListResponseModel>>`；包装 `Task/DataResult`；Data `GetStorePlaceListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StorePlaceController.cs:337` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces` | `List<StorePlaceViewModel>` | 场地列表信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].Id` | `int` | 场地ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StorePlaces[].PlaceName` | `string` | 场地名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].PlaceDescribe` | `string` | 场地描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].ReservationNum` | `int` | 同时接受预约次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].State` | `int` | 状态0暂停预约 1正常 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].CloseSettings` | `List<StorePlaceCloseSettingViewModel>` | 关闭时间 集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].CloseSettings[].Id` | `int` | 场所关闭时间设置 ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StorePlaces[].CloseSettings[].CloseDateStart` | `string` | 场地关闭时间开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].CloseSettings[].CloseDateEnd` | `string` | 场地关闭时间结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].CloseSettings[].CloseRemark` | `string` | 关闭的备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.StorePlaces[].CloseSettings[].StoreOperateWeeks` | `List<int>` | 营业日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].CloseSettings[].Times` | `List<PlaceCloseTimeViewModel>` | 营业日期（星期） | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].CloseSettings[].Times[].CloseTimeStart` | `string` | 场地关闭时间开始 | 普通业务字段 | 可按问题需要提供 |
| `Data.StorePlaces[].CloseSettings[].Times[].CloseTimeEnd` | `string` | 场地关闭时间结束 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StorePlaceProvider.GetStorePlaceByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_product.business_get_store_product`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取价目表(商品)列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreProduct/BusinessGetStoreProduct` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<PageData<StoreProductViewModel>>`；包装 `DataResult`；Data `PageData<StoreProductViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreProductController.cs:26` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 页码；普通业务字段；可按问题需要提供 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页显示行数；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<StoreProductViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | 商品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ProductName` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductUint` | `string` | 商品单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductClass` | `string` | 商品分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductClassName` | `string` | 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductImg` | `string` | 商品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ProductInfo` | `string` | 商品信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | remark 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsPay` | `bool` | 是否参与支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 0下架 1上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].IsEnableDelete` | `bool` | 是否允许删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ProductProvider.GetStoreProduct`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_product.business_get_store_product_by_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取价目表(商品)详细 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreProduct/BusinessGetStoreProductById` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetStoreProductByIdResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetStoreProductByIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreProductController.cs:57` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ProductId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 商品Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo` | `StoreProductViewModel` | 商品内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.Id` | `int` | 商品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductInfo.StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ProductInfo.ProductName` | `string` | 商品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.ProductUint` | `string` | 商品单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.ProductClass` | `string` | 商品分类 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.ProductClassName` | `string` | 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.ProductImg` | `string` | 商品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.IsExchange` | `bool` | 是否兑换商品 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.ExchangeIntegral` | `int` | 兑换积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.DiscountPrice` | `decimal` | 折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.ProductInfo` | `string` | 商品信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.Remark` | `string` | remark 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ProductInfo.IsCardDiscount` | `bool` | 是否参与卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.IsPay` | `bool` | 是否参与支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.State` | `int` | 0下架 1上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.ProductInfo.IsEnableDelete` | `bool` | 是否允许删除 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`ProductProvider.GetProductById`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_set.get_store_new_operating_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺新手引导信息 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreSet/GetStoreNewOperatingInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreNewOperatingInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreNewOperatingInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreSetController.cs:600` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsStoreComplete` | `bool` | 是否全部完成 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSetStoreInfo` | `bool` | 是否设置店铺信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSetCard` | `bool` | 是否设置了会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSetGroupLessons` | `bool` | 是否设置了团课 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSetPrivateLessons` | `bool` | 是否设置了私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSetCourseCardRelation` | `bool` | 是否设置课程扣卡关联 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAddGroupLessons` | `bool` | 是否创建团课排课 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreNewOperatingProvider.GetStoreNewOperatingInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_set.get_store_vacation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取店铺放假时间 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreSet/GetStoreVacation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreVacationResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreVacationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreSetController.cs:134` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `IsMore` | `bool` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 是否更多；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMore` | `bool` | 是否有更多 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations` | `List<StoreVacationResponseInfoModel>` | 放假列表信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Vacations[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Vacations[].VacationMessage` | `string` | 放假公告信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].CardExtension` | `int` | 会员卡延期模式：0 不延期；1 仅期限类卡（CardType=2/3）； 2 所有符合有效期和状态条件的持卡记录。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].CardExtensionDays` | `int` | 会员卡延期天数；独立配置，不自动等于放假起止日期的跨度。 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].BeginDate` | `string` | 开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].EndDate` | `string` | 结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].IsShowIndex` | `bool` | 是否首页弹框展现 | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].IsVacation` | `bool` | 是否开启放假（未开启放假只做公告功能展示） | 普通业务字段 | 可按问题需要提供 |
| `Data.Vacations[].VacationState` | `int` | 1未开始 2已过期 3放假中 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`svms?.Count > 0`；`!model.IsMore`；`yxVms?.Count() > 0`；`sxVms?.Count() > 0`；`sxVms.Count > 1`；`item.IsVacation`；`item.State == 1 && item.BeginDate < DateTime.Now`；`vacations?.Count > 0`；`vacations.Any(x => x.VacationState == 1)`；`vacations.Any(x => x.VacationState == 3)`
- 一层业务调用：`StoreVacationProvider.GetStoreVacationByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.store_tag.get_store_tag_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 门店 |
| 用途 | 获取会员标签列表 |
| 使用时机 | 核对门店配置、软件版本、服务期或模块开关；需要解释顾客端差异时再调用 C 端对应接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/StoreTag/GetStoreTagList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStoreTagListResponseModel>>`；包装 `Task/DataResult`；Data `GetStoreTagListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/StoreTagController.cs:26` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagsData` | `List<StoreTagViewModel>` | 分组数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagsData[].Id` | `int` | 标签组Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TagsData[].TagType` | `int` | 0多选，1单选 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagsData[].TagGroupName` | `string` | 标签组名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagsData[].IsSystem` | `bool` | 是否是系统分组 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagsData[].Tags` | `List<StoreTagInfoViewModel>` | 标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagsData[].Tags[].Id` | `int` | 标签Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TagsData[].Tags[].TagName` | `string` | 标签组名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagsData[].Tags[].TagIcon` | `string` | 标签Icon | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`stgs?.Count>0`；`tags?.Count()>0`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`StoreTagGroupProvider.GetStoreTagGroupByTenantId`、`StoreGroupProvider.GetStoreGroupByTenantId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.business_get_is_reservation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 取消预约 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/BusinessGetIsReservation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetIsReservationResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetIsReservationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1316` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreIsReservation` | `bool` | 开启预约状态 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.business_get_reservation_by_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取预约详情 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/BusinessGetReservationById` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetReservationByIdResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetReservationByIdResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:197` |
| C/B 对照 | capi.reservation.get_reservation_by_id |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.ReservationId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.ReservationId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1 \\|\\| model.ReservationId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 预约Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReservationDate` | `string` | 预约时间（日期部分 09-23） | 普通业务字段 | 可按问题需要提供 |
| `Data.StaffId` | `int` | 技师Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationTime` | `string` | 预约时间（日期+时间） | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimeEnd` | `string` | 预约结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemIds` | `List<int>` | 项目Id(非必填) | 普通业务字段 | 可按问题需要提供 |
| `Data.PlaceId` | `int` | 场地Id(非必填) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.State` | `int` | 状态 -2未到店，-1已取消，0未确认，1以确定，2已到店 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonsId` | `int` | 预约课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CourseId` | `int` | 预约课程ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CourseType` | `int` | 0团课，1私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos` | `List<GetReservationInfosModel>` | 预约信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlValue` | `string` | 控件值 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReservationInfos[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].IsMust` | `int` | 是否必填项 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items` | `List<GetReservationItemControlsListViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationInfos[].Items[].CommonReservationControlsId` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationInfos[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationInfos[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1 \|\| model.ReservationId < 1`；`resultMsg.State`；`reservationInfo?.Id > 0`；`reservationInfos.FirstOrDefault(n => n.ControlInstructions == item.ControlInstructions) == null`；`reservationInfo.CardId > 0`；`reservationInfo.Uid > 0`
- 一层业务调用：`StoreReservationProvider.GetStoreReservationById`、`StoreReservationControlsProvider.GetReservationControlsList`、`StoreReservationDetailedProvider.GetReservationInfoList`、`CommonReservationControlsItemProvider.GetCommonReservationControlsItemList`、`StoreReservationControlsProvider.GetStoreReservationControlsByInstructtions`、`StoreLessonsProvider.GetStoreLessonsByID`、`UserCardProvider.BusinessGetUserCardBasisInfo`、`UsersInfoProvider.UsersInfoGetByUid`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数不正确`；`固定提示：预约记录不存在`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.business_get_reservation_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取已约列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/BusinessGetReservationList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetReservationListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetReservationListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:160` |
| C/B 对照 | capi.reservation.get_reservation_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间；普通业务字段；可按问题需要提供 |
| `ReservationEndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间；普通业务字段；可按问题需要提供 |
| `State` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 状态 -1已取消，0未确认，1以确定 -10全部；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetReservationListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationYear` | `string` | 预约时间（年） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationDate` | `string` | 预约时间（日期部分 09-23） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationWeek` | `string` | 预约时间（星期部分 周一） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations` | `List<ReservationDataViewModel>` | 预约数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].ReservationTime` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationTimeEnd` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].StaffName` | `string` | 技师名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ItemsName` | `List<string>` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].PlaceName` | `string` | 场地名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations[].State` | `int` | 状态 -2未到店(旷课)，-1已取消，0未确认，1以确定，2到店（签到） 5上课中 10候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].LessonsId` | `int` | 课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].CourseDesc` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].TeachCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].CourseType` | `int` | 课程类型：0私教，1团课 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].ReservationProportion` | `string` | 约课占比 格式为：已预约人数/授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].IsReservationDetailed` | `bool` | 是否有预约详情内容 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].CreateDate` | `string` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations[].IsExperience` | `bool` | 是否体验预约 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.BusinessGetStoreReservationList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.business_get_reservation_summary`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 店铺获取预约首页汇总 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/BusinessGetReservationSummary` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<BusinessGetReservationSummaryResponseModel>>`；包装 `Task/DataResult`；Data `BusinessGetReservationSummaryResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1391` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.ConfirmPeople` | `long` | 确认人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelPeople` | `long` | 取消人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.AuditPeople` | `long` | 待确认人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Reservations` | `List<ReservationToShopDataViewModel>` | 即将到店 | 普通业务字段 | 可按问题需要提供 |
| `Data.Reservations[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Reservations[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Reservations[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Reservations[].State` | `int` | 状态 -2未到店，-1已取消，0未确认，1以确定，2已到店 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetStoreReservationSummary`、`StoreReservationProvider.BusinessGetStoreReservationToShopData`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.business_get_store_lessons_reservation_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取已约课程列表(旧) |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/BusinessGetStoreLessonsReservationList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetLessonsReservationListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetLessonsReservationListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1520` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间；普通业务字段；可按问题需要提供 |
| `ReservationEndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间；普通业务字段；可按问题需要提供 |
| `State` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 状态 -1已取消，0未确认，1以确定 -10全部；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetLessonsReservationListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | 描述字段（仅做接口描述用）：[Reservations中的三个集合分别为：MorningReservationList 为上午预约课的集合；AfternoonReservationList为下午预约课集合；NightReservationList为晚上预约课集合] | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations` | `ReservationLessonsDayViewModel` | 当天预约数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList` | `List<ReservationLessonsDataViewModel>` | 上午预约课程集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.MorningReservationList[].ReservationTime` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].ReservationTimeEnd` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.MorningReservationList[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.MorningReservationList[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.MorningReservationList[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.MorningReservationList[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.MorningReservationList[].StaffName` | `string` | 技师名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.MorningReservationList[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.MorningReservationList[].State` | `int` | 状态 -2未到店，-1已取消，0未确认，1以确定，2已到店 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].LessonsId` | `int` | 课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.MorningReservationList[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].CourseDesc` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].TeachCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].CourseType` | `int` | 课程类型：0团课，1私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.MorningReservationList[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList` | `List<ReservationLessonsDataViewModel>` | 下午预约可成功集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.AfternoonReservationList[].ReservationTime` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].ReservationTimeEnd` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.AfternoonReservationList[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.AfternoonReservationList[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].StaffName` | `string` | 技师名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].State` | `int` | 状态 -2未到店，-1已取消，0未确认，1以确定，2已到店 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].LessonsId` | `int` | 课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.AfternoonReservationList[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].CourseDesc` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].TeachCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].CourseType` | `int` | 课程类型：0团课，1私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.AfternoonReservationList[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList` | `List<ReservationLessonsDataViewModel>` | 晚上预约课程集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].ReservationId` | `int` | 预约Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.NightReservationList[].ReservationTime` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].ReservationTimeEnd` | `string` | 预约时间段（时间部分 10:30） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].UserId` | `int` | 预约者Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.NightReservationList[].CardId` | `int` | 会员卡id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.NightReservationList[].UserName` | `string` | 预约者名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.NightReservationList[].UserImg` | `string` | 预约者头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.NightReservationList[].UserMobile` | `string` | 预约者电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.NightReservationList[].StaffName` | `string` | 技师名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].Remark` | `string` | 客户留言 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.NightReservationList[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Reservations.NightReservationList[].State` | `int` | 状态 -2未到店，-1已取消，0未确认，1以确定，2已到店 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].LessonsId` | `int` | 课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Reservations.NightReservationList[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].CourseDesc` | `string` | 课程描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].TeachCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].CourseType` | `int` | 课程类型：0团课，1私教 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Reservations.NightReservationList[].TeachMin` | `int` | 授课时长（分钟） | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.BusinessGetStoreLessonsReservationList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.business_get_user_cards_by_lessons_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 根据预约团课ID获取用户会员卡列表详情 |
| 使用时机 | 商家核对会员卡列表、筛选结果、余额、有效期和卡状态时使用；复现单个顾客页面还需对照 C 端接口。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/BusinessGetUserCardsByLessonsId` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<BusinessGetReservationUserCardsResponseViewModel>>>`；包装 `Task/DataResult`；Data `PageData<BusinessGetReservationUserCardsResponseViewModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1557` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可在服务端上限内选择 | 请求页数；普通业务字段；可按问题需要提供 |
| `LessonsId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.LessonsId.Equals(0) \\|\\| model.LessonsId < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 预约课ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<BusinessGetReservationUserCardsResponseViewModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ReservationId` | `int` | 预约ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].CardId` | `int` | 会员卡 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserId` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserImg` | `string` | 会员头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserName` | `string` | 会员昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserStoreName` | `string` | 会员商户昵称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RemarkName` | `string` | 会员备注名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].UserMobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardIntegral` | `int` | 积分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CardPrint` | `int` | 印章 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].State` | `int` | 预约状态：-1已取消，0未确认，1以确定， | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LastDate` | `string` | 最近一次消费时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards` | `List<ReservationUserCardChildViewModel>` | 子会员卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Id` | `int` | 子卡Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].PrepaidCardId` | `int` | 卡id(储值卡) | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].CardNumber` | `string` | 卡号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Cards[].CardImg` | `string` | 背景图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardType` | `int` | 类型：0计次，1储值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardName` | `string` | 卡名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ValidityDate` | `string` | 到期时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardDiscount` | `decimal` | 会员卡折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].CardPrice` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionPrice` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].State` | `int` | 1正常 0未开卡 -1 销卡 -2商家删除 2 已过期，-3过期续费删除 3停卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].LastDate` | `string` | 最后一次消费日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].OpenCardDate` | `string` | 自动开卡时间， 如果为“”空，需要手动解卡 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].StopCardDays` | `int` | 默认停卡天数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].MemberCardQrCode` | `string` | 会员卡的二维码 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].SingleMaxFrequency` | `int` | 单次最大次数 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].DayMaxFrequency` | `int` | 每日最大核销次数0 不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].IsLimitFrequency` | `bool` | 是否限制次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].ConsumptionTimes` | `int` | 已经销卡次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services` | `List<ReservationUserCardServiceItemViewModel>` | 服务项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Cards[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Cards[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OpeningTime` | `string` | 开课时间(时间部分) | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OpeningDate` | `string` | 开课时间（日期部分） | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessionStaffList` | `List<StoreStaffViewModel>` | 当前课对应的技师集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessionStaffList[].StaffId` | `int` | 技师Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].LessionStaffList[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessionStaffList[].StaffName` | `string` | 技师姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessionStaffList[].JobTitle` | `string` | 职称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessionStaffList[].IsVacation` | `bool` | 是否休假 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessionStaffList[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].LessionStaffList[].Tips` | `string` | 提示语 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Remark` | `string` | 预约访客备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].BusinessRemark` | `string` | 预约商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`model.LessonsId.Equals(0) \|\| model.LessonsId < 1`；`resultMsg.State`；`model.PageIndex == 1`
- 一层业务调用：`StoreReservationProvider.BusinessGetStoreLeagueLessonsReservationList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_course_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取课程列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetCourseList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetCourseListResponseModel>>`；包装 `DataResult`；Data `List<GetCourseListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:488` |
| C/B 对照 | capi.reservation.get_course_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约日期(格式 "2012-12-21”)；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 员工ID，非必填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].CourseId` | `int` | 课程ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].CourseType` | `int` | 授课类型；0-团课，1-私教 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Remark` | `string` | 备注，例如：（13/20）、（满） | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId <= 0 \|\| model.Uid <= 0`；`!success`；`result.State`
- 一层业务调用：`StoreReservationProvider.GetCanReservationCourseList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 401`；`状态赋值：StatusCode = 200`；`固定提示：参数错误`；`固定提示：日期格式错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_business_hours`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取预约门店的营业时间，供预约课表遍历左侧时间段用 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationBusinessHours` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationBusinessHoursResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationBusinessHoursResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1617` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Remark` | `string` | 接口介绍字段，不参加实际使用；注：HoursList集合数据格式为["08:00","09:00","10:00"]，时间格式为24时制 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.HoursList` | `List<string>` | 营业时间段 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetReservationBusinessHours`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_calender`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取日历不可预约时间 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationCalender` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationCalenderResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationCalenderResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:394` |
| C/B 对照 | capi.reservation.get_reservation_calender |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |
| `CourseId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程ID 非必填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.VacationDates` | `List<string>` | 店铺不可预约日期(格式 MM-dd) | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetReservationCalender`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_controls`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取自定义控件 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationControls` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationControlsResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationControlsResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1842` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreResvervationControlsList` | `List<StoreResvervationViewModel>` | 资料列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreResvervationControlsList[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreResvervationControlsList[].StoreId` | `int` | 店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreResvervationControlsList[].ControlInstructions` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreResvervationControlsList[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreResvervationControlsList[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreResvervationControlsList[].IsMust` | `int` | 是否必填项 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreResvervationControlsList[].CommonReservationControlsId` | `int` | 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreResvervationControlsList[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList` | `List<CommonResvervationViewModel>` | 控件列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].Id` | `int` | 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CommonResvervationControlsList[].StoreId` | `int` | 店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CommonResvervationControlsList[].ControlInstructions` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].IsMust` | `int` | 是否必填项 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].Items` | `List<ReservationControlsItemAddModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].Items[].ItemName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommonResvervationControlsList[].Items[].IsDefault` | `bool` | 是否是默认 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`StoreReservationControlsProvider.GetReservationControls`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_controls_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取自定义控件列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationControlsList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationControlsListResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationControlsListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1872` |
| C/B 对照 | capi.reservation.get_reservation_controls_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls` | `List<GetReservationControlsListViewModel>` | 集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlName` | `string` | 控件名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlType` | `string` | 控件类型 input,radio,select.... | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].ControlInstructions` | `string` | 控件说明 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].IsMust` | `int` | 是否必填 1是 0否 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].IsShow` | `bool` | 是否对C端显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items` | `List<GetReservationItemControlsListViewModel>` | 选项列表 控件类型 为 radio select 不为空 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items[].Id` | `int` | id 主键id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Controls[].Items[].CommonReservationControlsId` | `int` | cid 控件id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Controls[].Items[].ItemValue` | `string` | item_value 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items[].ItemName` | `string` | item_name 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Controls[].Items[].IsDefault` | `int` | is_default 是否是默认 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationControlsProvider.GetReservationControlsList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_lessons_by_staff_id`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 根据技师ID 和 起止时间查询技师预约课表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationLessonsByStaffId` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetReservationLessonsByStaffIdResponseModel>>`；包装 `DataResult`；Data `List<GetReservationLessonsByStaffIdResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1436` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 技师（教练）ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StarTime` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndTime` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationDate` | `string` | 预约时间（年-月-日 如：2021-01-07） | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationLessonsList` | `List<ReservationLessonsViewModel>` | 课程信息集合 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationLessonsList[].Id` | `int` | 课ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ReservationLessonsList[].CourseName` | `string` | 课程名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationLessonsList[].TeachCount` | `int` | 授课人数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationLessonsList[].ReservationCount` | `int` | 已预约的人数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationLessonsList[].CourseType` | `int` | 课程类型（0团课，1私教） | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationLessonsList[].ReservationUserName` | `string` | 私教预约会员名称（私教课有返回） | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].ReservationLessonsList[].BeginTime` | `string` | 课程开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ReservationLessonsList[].EndTime` | `string` | 课程结束时间 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetReservationLessonsTableByStaffId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_lessons_sum`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取新版课程日历预约课程 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationLessonsSum` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationLessonsSumResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationLessonsSumResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:1477` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 技师ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleSums` | `List<ReservationLessonsSumViewModel>` | 店铺预约日历人数信息集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleSums[].PeopleSum` | `long` | 预约人数数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleSums[].ReservationDate` | `string` | 预约日期 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetStoreLessonsReservationPeopleSum`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_people_sum`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取日历预约人数 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationPeopleSum` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationPeopleSumResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationPeopleSumResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:352` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `BeginDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 开始时间；普通业务字段；可按问题需要提供 |
| `EndDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 结束时间；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleSums` | `List<ReservationPeopleSumViewModel>` | 店铺不可预约日期 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleSums[].PeopleSum` | `long` | 预约人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleSums[].ReservationDate` | `string` | 预约日期 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationProvider.GetStoreReservationPeopleSum`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_service_item_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取项目列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationServiceItemList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetStaffServiceItemListResponseModel>>`；包装 `Task/DataResult`；Data `GetStaffServiceItemListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:537` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 店铺Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 技师Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationTime` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间 (格式 "2012-12-21 13:30")；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSelectMoreItem` | `bool` | 是否可以多选 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems` | `List<StaffServiceItemViewModel>` | 预约项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].ItemId` | `int` | 服务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ServiceItems[].ItemImg` | `string` | 项目图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].ItemUnit` | `string` | 项目单位 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].ItemDuration` | `int` | 项目时长 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].ItemInfo` | `string` | 项目描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].ReservationPeopleSum` | `int` | 可预约总人数，0不显示 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].AlreadyReservationPeopleSum` | `long` | 已经预约人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceItems[].IsReservation` | `bool` | 是否可以预约 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`model.StaffId > 0 && TenantUserProvider.Instance.GetTenantUserByStoreId(model.StoreId) .All(x => x.Id != model.StaffId)`；`resultMsg.State`；`reservationSet.IsSelectItem`；`model.StaffId > 0`；`items.Any(x => x.ReservationPeopleSum > 0)`；`retModlTemp?.ItemId > 0`；`item.ReservationPeopleSum > 0`
- 一层业务调用：`TenantUserProvider.GetTenantUserByStoreId`、`StoreReservationSetingProvider.GetReservationSeting`、`StaffServiceItemProvider.BusinessGetStaffReservationServiceItems`、`StoreServiceItemProvider.BusinessGetReservationServiceItems`、`StaffServiceItemProvider.BusinessGetStaffReservationServiceItemPeopleSum`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`状态赋值：StatusCode = 302`；`固定提示：参数不正确`；`固定提示：店员不存在`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_reservation_time_seting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取预约时间段 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetReservationTimeSeting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationTimeSetingResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationTimeSetingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:650` |
| C/B 对照 | capi.reservation.get_reservation_time_seting |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 店铺Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StaffId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 技师Id 团课非必填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间（格式"2012-01-21"）；普通业务字段；可按问题需要提供 |
| `ItemIds` | `List<int>` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按业务问题提供；仍需服务端 Schema 校验 | 项目Id(非必填)；普通业务字段；可按问题需要提供 |
| `CourseId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.CourseId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes` | `List<StoreReservationTimeSetingViewModel>` | 预约时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].Id` | `int` | 时间端Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationTimes[].ReservationTime` | `string` | 预约时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].ReservationTimeEnd` | `string` | 预约时间段 结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsOccupy` | `bool` | 时间被占用 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].SurplusCount` | `int` | 剩余可预约人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`store.StoreTypeId == "007" \|\| store.StoreTypeId == "010"`；`model.CourseId <= 0`
- 一层业务调用：`StoreProvider.GetStoreByIdAsync`、`StoreReservationTimeSetingProvider.GetReservationTimeSeting`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`；`固定提示：课程ID不能为空`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation.get_work_staff_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 工作技师列表 |
| 使用时机 | 商家核对员工、教练、权限或门店关系；不得把其他员工的个人信息无条件交给模型。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Reservation/GetWorkStaffList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetWorkStaffListResponseModel>>`；包装 `Task/DataResult`；Data `GetWorkStaffListResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationController.cs:435` |
| C/B 对照 | capi.reservation.get_work_staff_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `AppId` | `string` | 服务端上下文注入（源码未确认必填） | 绑定=ApiController推断；未发现显式必填证据 | 服务端注入：当前产品/小程序配置 | 小程序APPID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；if(model.StoreId < 1 \\|\\| model.Uid < 1) | 服务端注入：已确认门店 | 店铺Id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationTime` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约时间段；普通业务字段；可按问题需要提供 |
| `CourseId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 课程ID，非必填；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ReservationDate` | `string` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 可按客户问题选择，必须使用门店时区和合法范围 | 预约日期(格式 "2012-12-21”)；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs` | `List<StoreStaffViewModel>` | 技师列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffId` | `int` | 技师Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Staffs[].StaffImg` | `string` | 技师头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].StaffName` | `string` | 技师姓名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].JobTitle` | `string` | 职称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].IsVacation` | `bool` | 是否休假 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.Staffs[].Tips` | `string` | 提示语 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId < 1 \|\| model.Uid < 1`；`resultMsg.State`；`reservationSet.IsSelectStaff \|\| store.StoreTypeId == "007" \|\| store.StoreTypeId == "010"`；`model.CourseId > 0`
- 一层业务调用：`StoreReservationSetingProvider.GetReservationSeting`、`StoreProvider.GetStoreByIdAsync`、`StoreReservationProvider.GetLessonsStaffList`、`StoreReservationProvider.GetWorkStaffList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation_set.get_course_search_tag`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 预约课目搜索标签列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ReservationSet/GetCourseSearchTag` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetCourseSearchTagResponseModel>>`；包装 `Task/DataResult`；Data `GetCourseSearchTagResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationSetController.cs:308` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags` | `List<SearchTagTagResponseModel>` | 标签数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tags[].TagTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].TagType` | `int` | 标签类型： 3课程 5课程分类 6课程辅助工具 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`tags?.Count > 0`
- 一层业务调用：`StoreReservationTagProvider.GetCourseSearchTag`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation_set.get_reservation_seting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 获取预约设置 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ReservationSet/GetReservationSeting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationSetingResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationSetingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationSetController.cs:27` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSelectStaff` | `bool` | 是否可选技师 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSelectItem` | `bool` | 是否可选项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsVip` | `bool` | 是否仅会员预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardPayOrder` | `int` | 卡支付顺序，0预约时支付 1签到时支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignInSet` | `int` | 签到设置 1开课后自动签到 2下课后自动签到 3下课后N分钟自动签到 11老师点名签到 12扫码签到 13签字签到 | 普通业务字段 | 可按问题需要提供 |
| `Data.SignInMinutes` | `int` | sign_in_minutes 签到时间分钟 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupCardPayOrder` | `int` | 卡支付顺序，0预约时支付 1签到时支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupSignInSet` | `int` | 签到设置 1开课后自动签到 2下课后自动签到 3下课后N分钟自动签到 11老师点名签到 12扫码签到 13签字签到 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupSignInMinutes` | `int` | sign_in_minutes 签到时间分钟 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassCardPayOrder` | `int` | 卡支付顺序，0预约时支付 1签到时支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassSignInSet` | `int` | 签到设置 1开课后自动签到 2下课后自动签到 3下课后N分钟自动签到 11老师点名签到 12扫码签到 13签字签到 | 普通业务字段 | 可按问题需要提供 |
| `Data.ClassSignInMinutes` | `int` | sign_in_minutes 签到时间分钟 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSelectMoreItem` | `bool` | 是否可选多个项目 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOpenTimeConflict` | `bool` | 是否开启冲突时间过滤 | 普通业务字段 | 可按问题需要提供 |
| `Data.TimeConflictSeting` | `int` | 时间冲突设置 0固定间隔，1按项目时长 与选择开启项目联动 | 普通业务字段 | 可按问题需要提供 |
| `Data.FixedTime` | `int` | 固定时间间隔 分钟数 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsClientWechatNotice` | `bool` | 预约成功顾客微信通知 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsClientSmsNotice` | `bool` | 预约成功顾客短信通知 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAdvanceStaffWechatNotice` | `bool` | 提前技师微信通知 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAdvanceStaffSmsNotice` | `bool` | 提前技师短信通知 | 普通业务字段 | 可按问题需要提供 |
| `Data.AdvanceStaffMinute` | `int` | 技师提前通知分钟数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ItemQuantity` | `int` | 剩余短信条数 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationNum` | `int` | 每位顾客当天累计的可预约场次 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsCancel` | `bool` | 是否可以取消预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelMinute` | `int` | 开始前多少分钟可取消0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationMinute` | `int` | 顾客提前多少分钟可约0不限 | 普通业务字段 | 可按问题需要提供 |
| `Data.TimeInterval` | `int` | 预约时间间隔 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationDay` | `int` | reservation_day 提前N天可约 (-1不限制,0当天) | 普通业务字段 | 可按问题需要提供 |
| `Data.ServiceIntervals` | `int` | service_intervals 两次服务间隔（分钟 0无间隔） | 普通业务字段 | 可按问题需要提供 |
| `Data.MissAppointmentPenalty` | `int` | miss_appointment_ penalty 爽约处罚 0未开启 1每月 2每年 | 普通业务字段 | 可按问题需要提供 |
| `Data.MissAppointmentSum` | `int` | miss_appointment_sum 爽约次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.MissAppointmentPenaltyValue` | `int` | miss_appointment_ penalty_value 处罚内容（目前支持处罚时限卡有效期。计次卡、储值卡默认处罚最后一次爽约的预约项） | 普通业务字段 | 可按问题需要提供 |
| `Data.MissAppointmentBlackDays` | `int` | miss_appointment_black_days 私教爽约处罚黑名单天数（0不处罚） | 普通业务字段 | 可按问题需要提供 |
| `Data.MissAppointmentIsCard` | `bool` | miss_appointment_is_card 私教是否开启爽约用卡惩罚 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationPeopleNum` | `int` | reservation_people_num 私教课可预约人数，0不限制人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonShowMaxDay` | `int` | lesson_show_max_day 私教显示排课范围xx天 0显示所有 | 普通业务字段 | 可按问题需要提供 |
| `Data.LessonReservationTime` | `string` | 私教放课时间 空不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsGroupLessonConflict` | `bool` | is_group_lesson_conflict 私教课教练 是否与团课授课时间冲突 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOnlyStaff` | `bool` | is_only_staff 私教是否只能约指定教练 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonReservationDay` | `int` | group_lesson_reservation_day 团课提前N天可约 (-1不限制,0当天) | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonIsCancel` | `bool` | group_lesson_is_cancel 团课是否可以取消预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonCancelMinute` | `int` | group_lesson_cancel_minute 团课开始前多少分钟可取消（0 不限制 1440 1天） | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonPeopleAutoCancel` | `int` | group_lesson_people_auto_cancel 团课不满足最低人数自动取消分钟 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonMissAppointmentPenalty` | `int` | group_lesson_miss_appointment_ penalty 团课爽约处罚 0未开启 1每月 2每年 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonMissAppointmentSum` | `int` | group_lesson_miss_appointment_sum 团课爽约次数 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonMissAppointmentPenaltyValue` | `int` | group_lesson_miss_appointment_ penalty_value 团课处罚内容（目前支持处罚时限卡有效期。计次卡、储值卡默认处罚最后一次爽约的预约项） | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonMissAppointmentBlackDays` | `int` | group_lesson_miss_appointment_black_days 团课爽约处罚黑名单天数（0不处罚） | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonMissAppointmentIsCard` | `bool` | group_lesson_miss_appointment_is_card 团课是否开启爽约用卡惩罚 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonIsReserve` | `bool` | group_lesson_is_reserve 团课是否可以候补 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShowAvatar` | `bool` | is_show_avatar 是否显示头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsShowPeopleNum` | `bool` | is_show_people_num 是否显示预约人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShowPeopleList` | `bool` | is_show_people_list 是否显示预约列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShowUnfilledQuota` | `bool` | is_show_unfilled_quota 是否显示剩余可约名额 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonShowMaxDay` | `int` | group_lesson_show_max_day 显示排课范围xx天 -1显示所有 0当天 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonReservationTime` | `string` | 团课放课时间 空不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelCycle` | `int` | 0天 1周 2月 当N | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelSum` | `int` | 取消N次 0不限制 | 普通业务字段 | 可按问题需要提供 |
| `Data.CancelAppointmentDays` | `int` | N天不允许预约 | 普通业务字段 | 可按问题需要提供 |
| `Data.GroupLessonReservationMinute` | `int` | 团课顾客提前多少分钟可约（-1不限制） | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes` | `List<StoreReservationTimeSetingViewModel>` | 预约时间段数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].Id` | `int` | 时间端Id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ReservationTimes[].ReservationTime` | `string` | 预约时间段 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].ReservationTimeEnd` | `string` | 预约时间段 结束 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsReservation` | `bool` | 是否可约 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].IsOccupy` | `bool` | 时间被占用 | 普通业务字段 | 可按问题需要提供 |
| `Data.ReservationTimes[].SurplusCount` | `int` | 剩余可预约人数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`resultMsg.State`
- 一层业务调用：`StoreReservationSetingProvider.GetReservationSeting`、`StoreAddedServicesProvider.GetItemQuantity`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.reservation_set.get_reservation_tag`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 预约 |
| 用途 | 预约相关标签列表 |
| 使用时机 | 商家核对排课、预约名单、预约状态、教练/场地或预约配置时使用；顾客可见性需再对照 C 端结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/ReservationSet/GetReservationTag` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetReservationTagResponseModel>>`；包装 `Task/DataResult`；Data `GetReservationTagResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/ReservationSetController.cs:179` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid == 0 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TagType` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 标签类型：1个人标签 2擅长 3课程 4课程图片 5课程分类；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags` | `List<ReservationTagResponseModel>` | 标签数据 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].Id` | `long` | 标签id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Tags[].TagTitle` | `string` | 标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].TagSubTitle` | `string` | 子标题 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].IsCanDelete` | `bool` | 是否可以删除 | 普通业务字段 | 可按问题需要提供 |
| `Data.Tags[].State` | `int` | 状态 0禁用 1启用 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid == 0 \|\| model.StoreId < 1`；`resultMsg.State`；`tags?.Count > 0`
- 一层业务调用：`StoreReservationTagProvider.GetTagsByStoreId`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：MsgType = 0`；`状态赋值：StatusCode = 301`；`固定提示：参数不正确`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.check_user_is_rider`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 是否是骑手 |
| 使用时机 | 在商家视角中核对“是否是骑手”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/CheckUserIsRider` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<CheckUserIsRiderResponseModel>>`；包装 `Task/DataResult`；Data `CheckUserIsRiderResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:1028` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.IsRider` | `bool` | 是否是骑手 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`StoreRiderProvider.CheckUserIsRider`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.food_search`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 搜索食品 |
| 使用时机 | 在商家视角中核对“搜索食品”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/FoodSearch` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<FoodSearchResponseViewModel>>`；包装 `Task/DataResult`；Data `FoodSearchResponseViewModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:508` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.Uid < 1 \\|\\| model.StoreId < 1) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ClassId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 食品分类ID,0为全部；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `KeyWord` | `string` | 代码校验必填 | 绑定=ApiController推断；if(string.IsNullOrEmpty(model.KeyWord)) | AI 可按业务问题提供；仍需服务端 Schema 校验 | 关键词；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList` | `List<FoodListViewModel>` | 门店分类商品集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].Id` | `int` | 食品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodList[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].Img` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].RightTagName` | `List<string>` | 角标签 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].Quantity` | `string` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].SellNum` | `string` | 销量 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].Price` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].VipDisCount` | `decimal` | 会员最低折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].State` | `int` | 状态；0-下架，1-上架 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].EnjoyVipDiscount` | `bool` | 是否享受会员卡折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodList[].IsWithSku` | `bool` | 是否多规格 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.Uid < 1 \|\| model.StoreId < 1`；`string.IsNullOrEmpty(model.KeyWord)`；`resultMsg.State`
- 一层业务调用：`FoodProvider.FoodSearch`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：参数格式错误`；`固定提示：参数格式错误！`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_cart`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取点餐购物车 |
| 使用时机 | 在商家视角中核对“获取点餐购物车”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodCart` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetFoodCartResponseModel>>`；包装 `Task/DataResult`；Data `GetFoodCartResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:530` |
| C/B 对照 | capi.food.get_food_cart |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.TableId <= 0 \\|\\| model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.TableId <= 0 \\|\\| model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TableId` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.TableId <= 0 \\|\\| model.StoreId <= 0 \\|\\| model.Uid <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 桌号；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Id` | `int` | 购物车ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCount` | `int` | 商品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalPrice` | `decimal` | 总价 | 普通业务字段 | 可按问题需要提供 |
| `Data.VipDiscount` | `decimal` | 会员折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems` | `List<FoodCartItemViewModel>` | 商品列表 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCartItems[].StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCartItems[].FoodCartId` | `int` | food_cart_id 购物车ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCartItems[].FoodId` | `int` | food_name 菜品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCartItems[].FoodName` | `string` | 菜品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].FoodPrice` | `decimal` | 菜品价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].FoodImg` | `string` | 菜品图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].FoodCount` | `int` | food_num 菜品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].SkuValue` | `string` | sku_value 规格 值 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].SkuString` | `string` | sku_string 规格 文本 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].State` | `int` | state 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].CreateDate` | `DateTime` | create_date | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].UpdateDate` | `DateTime` | update_date | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].CreateBy` | `int` | create_by | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].UpdateBy` | `int` | update_by | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].EnjoyVipDiscount` | `bool` | 享受会员折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users` | `List<UserInfoViewModel>` | 点餐用户 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Uid` | `int` | 用户ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCartItems[].Users[].CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCartItems[].Users[].Mobile` | `string` | 手机号 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.FoodCartItems[].Users[].UserImg` | `string` | 用户头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.FoodCartItems[].Users[].UserName` | `string` | 用户昵称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.FoodCartItems[].Users[].IsVip` | `bool` | 是否是VIP | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services` | `List<UserCardServiceItemViewModel>` | 项目信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].ItemId` | `int` | 项目ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodCartItems[].Users[].Services[].ItemName` | `string` | 项目名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].ItemUnit` | `string` | 项目单位（服务单位） | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].CardValue` | `decimal` | 剩余金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].CardNormalValue` | `decimal` | 正金(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].CardGiveValue` | `decimal` | 赠送金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].ConsumptionValue` | `decimal` | 消费金额(次数) | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].ItemPrice` | `decimal` | 项目单价 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCartItems[].Users[].Services[].IsDefault` | `bool` | 是否默认，默认选中上次核销的项目 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.TableId <= 0 \|\| model.StoreId <= 0 \|\| model.Uid <= 0`
- 一层业务调用：`FoodCartProvider.GetFoodCart`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_category_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取食品分类 |
| 使用时机 | 在商家视角中核对“获取食品分类”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodCategoryList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetFoodCategoryListResponseModel>>`；包装 `DataResult`；Data `List<GetFoodCategoryListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:69` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`FoodCategoryProvider.GetFoodCategoryList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_inforation`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取食品详情 |
| 使用时机 | 在商家视角中核对“获取食品详情”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodInforation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetFoodInforationResponseModel>>`；包装 `Task/DataResult`；Data `GetFoodInforationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:617` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `Id` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.Id < 1) | 必须来自同一会话上游 API 结果或服务端对象引用 | 食品id；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.ImgList` | `List<string>` | 食品图片集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Name` | `string` | 食品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Describe` | `string` | 食品描述 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.VipPrice` | `decimal?` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.Quantity` | `int` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data.CategoryList` | `List<int>` | 食品分类集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.TagList` | `List<int>` | 食品标签集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.RightTagList` | `List<string>` | 食品角标集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList` | `List<EditFoodSkuListModel>` | 规格列表集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuId` | `int` | 规格id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].SkuName` | `string` | 规格名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].IsSingle` | `bool` | 是否单选；0-单选，1-多选 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuItemList` | `List<EditFoodSkuItemListModel>` | 规格属性集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuItemList[].SkuItemId` | `int` | 规格子项id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SkuList[].SkuItemList[].SkuItemName` | `string` | 规格属性名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuItemList[].SkuItemPrice` | `decimal` | 规格属性价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.SkuList[].SkuItemList[].SkuItemVipPrice` | `decimal` | 规格属性会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 上下架状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.EnjoyVipDiscount` | `bool` | 享受会员折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data.AliFoodId` | `string` | 支付宝foodid | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |

#### 代码行为与证据

- Controller 条件：`model.Id < 1`；`resultMsg.State`
- 一层业务调用：`FoodProvider.GetFoodInforation`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请填写id`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取食品列表 |
| 使用时机 | 在商家视角中核对“获取食品列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetFoodListResponseModel>>`；包装 `DataResult`；Data `List<GetFoodListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:755` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ClassId` | `int` | 分类id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].ClassName` | `string` | 分类名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Count` | `int` | 个数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].ShopCount` | `int` | 当前分类中购物车个数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList` | `List<FoodListViewModel>` | 食品列表 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].Id` | `int` | 食品id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].FoodList[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].Img` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].RightTagName` | `List<string>` | 角标签 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].Quantity` | `string` | 库存 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].SellNum` | `string` | 销量 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].Price` | `decimal` | 原价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].VipPrice` | `decimal` | 会员价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].VipDisCount` | `decimal` | 会员最低折扣 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].State` | `int` | 状态；0-下架，1-上架 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].EnjoyVipDiscount` | `bool` | 是否享受会员卡折扣价 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodList[].IsWithSku` | `bool` | 是否多规格 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`FoodProvider.GetFoodList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_order_detail`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取订单详情 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodOrderDetail` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetFoodOrderDetailResponseModel>>`；包装 `Task/DataResult`；Data `GetFoodOrderDetailResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:633` |
| C/B 对照 | capi.food.get_food_order_detail |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `ConsumptionId` | `long` | 参与组合校验 | 绑定=ApiController推断；if(model.FoodOrderId <= 0 && model.ConsumptionId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 财务ID（非必填）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `FoodOrderId` | `int` | 参与组合校验 | 绑定=ApiController推断；if(model.FoodOrderId <= 0 && model.ConsumptionId <= 0) | 必须来自同一会话上游 API 结果或服务端对象引用 | 订单ID（非必填）；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.OrderType` | `int` | 订单类型 0 堂食 1外卖 2自取 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | status 订单状态：0 未付款 ，1 已付款 | 普通业务字段 | 可按问题需要提供 |
| `Data.No` | `string` | 流水号 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodCount` | `int` | product_number 菜品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.NeedPayment` | `decimal` | need_payment 应付金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.Payment` | `decimal` | payment 实付金额。精确到2位小数;单位:元。如:200.07，表示:200元7分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Remark` | `string` | remark 买家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.BusinessRemark` | `string` | business_remark 卖家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.BuyerName` | `string` | buyer_name 买家名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.BuyerHeadImg` | `string` | 买家头像 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayMethod` | `int` | pay_method 支付方式：0 会员卡支付、1微信支付、2支付宝支付 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateDate` | `DateTime` | create_date 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.PayDate` | `DateTime` | pay_date 支付时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpdateDate` | `DateTime` | update_date 更新时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.CreateBy` | `int` | create_by 创建人 | 普通业务字段 | 可按问题需要提供 |
| `Data.UpdateBy` | `int` | update_by 更新人 | 普通业务字段 | 可按问题需要提供 |
| `Data.ConsumptionId` | `string` | consumption_id 财务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.IntegralDeduct` | `decimal` | 积分减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CommissionDeduct` | `decimal` | 佣金减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CardDeduct` | `decimal` | 权益卡、储值卡减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.CouponDeduct` | `decimal` | 优惠券减免金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems` | `List<FoodOrderItemModel>` | 订单子项 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodOrderItems[].StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodOrderItems[].FoodOrderId` | `int` | food_order_id 订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodOrderItems[].FoodId` | `int` | 菜品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.FoodOrderItems[].FoodName` | `string` | food_name 菜品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].FoodCount` | `int` | food_num 菜品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].FoodPrice` | `decimal` | food_price 菜品价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].SkuValue` | `string` | sku_value 规格值 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].SkuString` | `string` | sku_string 规格文本 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].FoodImg` | `string` | food_img 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].State` | `int` | state | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].CreateBy` | `int` | create_by | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].CreateDate` | `DateTime` | create_date | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].UpdateBy` | `int` | update_by | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].UpdateDate` | `DateTime` | update_date | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodOrderItems[].TenantId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.PreferentialType` | `int` | 优惠类型 | 普通业务字段 | 可按问题需要提供 |
| `Data.TableNo` | `int` | 桌号 | 普通业务字段 | 可按问题需要提供 |
| `Data.TableName` | `string` | 桌名 | 普通业务字段 | 可按问题需要提供 |
| `Data.PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.FinalPrice` | `decimal` | 商议金额（商家最终优惠） | 普通业务字段 | 可按问题需要提供 |
| `Data.DisCount` | `decimal` | 折扣（商家最终优惠） | 普通业务字段 | 可按问题需要提供 |
| `Data.TakeTime` | `string` | 取餐时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.TableId` | `int` | 桌台ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.CardId` | `int` | 会员卡ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RiderName` | `string` | 配送员名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.RiderMobile` | `string` | 配送员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.RiderAddress` | `string` | 配送员地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReceiverName` | `string` | 收货人姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReceiverMobile` | `string` | 收货人电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.ReceiverAddress` | `string` | 收货人地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Distance` | `int` | 配送距离 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreRiderId` | `int` | 配送员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.UserAddressId` | `int` | 收货地址ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.SendOutPrice` | `decimal` | 配送费 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreMobile` | `string` | 店铺电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.UserMobile` | `string` | 会员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.IsShowRefundButton` | `bool` | 是否显示退款按钮 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsRefunds` | `bool` | 是否已退款 | 普通业务字段 | 可按问题需要提供 |
| `Data.SourceId` | `string` | 退款原订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.RefundPrice` | `decimal` | 退款金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.StoreUserName` | `string` | 操作店员姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.FoodOrderId <= 0 && model.ConsumptionId <= 0`
- 一层业务调用：`FoodOrderProvider.GetFoodOrderDetail`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数有误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_order_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取订单列表 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodOrderList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<PageData<GetFoodOrderListResponseModel>>>`；包装 `Task/DataResult`；Data `PageData<GetFoodOrderListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:663` |
| C/B 对照 | capi.food.get_food_order_list |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `OrderState` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | AI 只能使用文档确认的枚举值 | 订单状态 0 当前 1历史,2全部,3待出餐 4 待配送；普通业务字段；可按问题需要提供 |
| `RiderId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 骑手ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `PageSize` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex == 0 \\|\\| model.PageSize == 0) | AI 可在服务端上限内选择 | 每页的数据；普通业务字段；可按问题需要提供 |
| `PageIndex` | `int` | 代码校验必填 | 绑定=ApiController推断；if(model.PageIndex == 0 \\|\\| model.PageSize == 0) | AI 可在服务端上限内选择 | 页码索引；普通业务字段；可按问题需要提供 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.PageSize` | `int` | 每页数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageIndex` | `int` | 当前页 | 普通业务字段 | 可按问题需要提供 |
| `Data.PageCount` | `int` | 总页数 | 普通业务字段 | 可按问题需要提供 |
| `Data.TotalCount` | `int` | 数据总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsNext` | `bool` | 是否有下页 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[]` | `List<GetFoodOrderListResponseModel>` | 数据集合 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Id` | `int` | 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].ConsumptionId` | `long` | 财务ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].State` | `int` | 订单状态：-2 退款 0 未付款 ，1 已付款，2已出餐，3已配送，4已完成 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].No` | `string` | 流水号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].Payment` | `decimal` | 实付金额。精确到2位小数;单位:元。如:200.07，表示:200元7分 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].CreateDate` | `DateTime` | 创建时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TableNo` | `long` | 桌号 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TableName` | `string` | 桌名 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].PeopleCount` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].TakeTime` | `string` | 取餐时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems` | `List<FoodOrderItemModel>` | 订单子项 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].Id` | `int` | id 主键 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].FoodOrderItems[].StoreId` | `int` | store_id 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].FoodOrderItems[].FoodOrderId` | `int` | food_order_id 订单ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].FoodOrderItems[].FoodId` | `int` | 菜品ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].FoodOrderItems[].FoodName` | `string` | food_name 菜品名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].FoodCount` | `int` | food_num 菜品数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].FoodPrice` | `decimal` | food_price 菜品价格 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].SkuValue` | `string` | sku_value 规格值 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].SkuString` | `string` | sku_string 规格文本 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].FoodImg` | `string` | food_img 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].State` | `int` | state | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].CreateBy` | `int` | create_by | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].CreateDate` | `DateTime` | create_date | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].UpdateBy` | `int` | update_by | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].UpdateDate` | `DateTime` | update_date | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodOrderItems[].TenantId` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].FoodTypeCount` | `int` | 食品种类数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].FoodCount` | `int` | 食品总数量 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RiderName` | `string` | 配送员名称 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].RiderMobile` | `string` | 配送员电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].RiderAddress` | `string` | 配送员地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].ReceiverName` | `string` | 收货人姓名 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].ReceiverMobile` | `string` | 收货人电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].ReceiverAddress` | `string` | 收货人地址 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].Distance` | `int` | 配送距离 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StoreRiderId` | `int` | 配送员ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].UserAddressId` | `int` | 收货地址ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.Data[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].BusinessRemark` | `string` | 商家备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.Data[].SendOutPrice` | `decimal` | 配送费 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].OrderType` | `int` | 0堂食 1外卖 2自取 | 普通业务字段 | 可按问题需要提供 |
| `Data.Data[].StoreMobile` | `string` | 商家电话 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`model.PageIndex == 0 \|\| model.PageSize == 0`；`resultMsg.State`
- 一层业务调用：`FoodOrderProvider.GetFoodOrderList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请传递参数`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_setting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取点单基本设置 |
| 使用时机 | 在商家视角中核对“获取点单基本设置”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodSetting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetFoodSettingResponseModel>>`；包装 `Task/DataResult`；Data `GetFoodSettingResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:383` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsOpen` | `bool` | 点单开关；flase-关，true-开 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsMeTake` | `bool` | 自取开关；flase-关，true-开 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsAutomaticMeal` | `bool` | 出餐设置；0-关，1-开 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsEatHere` | `bool` | 是否是堂食 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsTakeOut` | `bool` | 是否是外卖 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendOutRange` | `decimal` | 配送范围 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendOutStartTime` | `string` | 配送开始时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendOutEndTime` | `string` | 配送结束时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.SendOutPrice` | `decimal` | 配送费用 | 普通业务字段 | 可按问题需要提供 |
| `Data.StartPrice` | `decimal` | 起送金额 | 普通业务字段 | 可按问题需要提供 |
| `Data.RiderPeopleCount` | `int` | 配送人员个数 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsSubscribe` | `bool` | 是否需要订阅公众号扫码点餐 | 普通业务字段 | 可按问题需要提供 |
| `Data.CanSelectFinishTime` | `bool` | 是否可选送达时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.ExceptFinishMinute` | `int` | 期望送达时间（分钟） | 普通业务字段 | 可按问题需要提供 |
| `Data.FoodMode` | `int` | 点餐模式：0 一人一单，1多人一单 | 普通业务字段 | 可按问题需要提供 |
| `Data.TakeMakeTime` | `int` | 自提配货冗余时长 分钟 | 普通业务字段 | 可按问题需要提供 |
| `Data.ShareImg` | `string` | 分享图 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`；`m != null`；`mm != null`
- 一层业务调用：`FoodSettingProvider.GetFoodSetting`、`FoodProvider.GetRiderList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_sku_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取规格列表 |
| 使用时机 | 在商家视角中核对“获取规格列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodSkuList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetFoodSkuListResponseModel>>`；包装 `DataResult`；Data `List<GetFoodSkuListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:483` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuName` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuList` | `FoodSkuListModel` | 规格集合列表 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuList.SkuName` | `string` | 规格名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuList.IsSingle` | `bool` | 是否单选；false-单选，true-多选 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuList.SkuItemList` | `List<FoodSkuItemListModel>` | 规格属性集合 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuList.SkuItemList[].SkuItemName` | `string` | 规格属性名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuList.SkuItemList[].SkuItemPrice` | `decimal` | 规格属性价格 | 普通业务字段 | 可按问题需要提供 |
| `Data[].SkuList.SkuItemList[].SkuItemVipPrice` | `decimal?` | 规格属性会员价 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`FoodProvider.GetFoodSkuList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_table_info`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取桌台信息 |
| 使用时机 | 在商家视角中核对“获取桌台信息”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodTableInfo` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetFoodTableInfoResponseModel>>`；包装 `Task/DataResult`；Data `GetFoodTableInfoResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:153` |
| C/B 对照 | capi.food.get_food_table_info |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TableId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 桌台ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Id` | `int` | 桌台ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data.TableName` | `string` | 桌名 | 普通业务字段 | 可按问题需要提供 |
| `Data.TableNo` | `int` | 桌号 | 普通业务字段 | 可按问题需要提供 |
| `Data.PeopleNum` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data.LastUseTime` | `DateTime` | 最后使用时间 | 普通业务字段 | 可按问题需要提供 |
| `Data.Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data.TableQrcode` | `string` | 桌号二维码 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId <= 0 \|\| model.Uid <= 0`
- 一层业务调用：`FoodTableProvider.GetFoodTableInfo`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_table_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取餐台列表 |
| 使用时机 | 在商家视角中核对“获取餐台列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodTableList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<List<GetFoodTableInfoResponseModel>>>`；包装 `Task/DataResult`；Data `List<GetFoodTableInfoResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:179` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Id` | `int` | 桌台ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | 店铺ID | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].TableName` | `string` | 桌名 | 普通业务字段 | 可按问题需要提供 |
| `Data[].TableNo` | `int` | 桌号 | 普通业务字段 | 可按问题需要提供 |
| `Data[].PeopleNum` | `int` | 人数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].State` | `int` | 状态 | 普通业务字段 | 可按问题需要提供 |
| `Data[].LastUseTime` | `DateTime` | 最后使用时间 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Remark` | `string` | 备注 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].TableQrcode` | `string` | 桌号二维码 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId <= 0 \|\| model.Uid <= 0`
- 一层业务调用：`FoodTableProvider.GetFoodTableList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_table_qr_code`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取餐台二维码 |
| 使用时机 | 在商家视角中核对“获取餐台二维码”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodTableQrCode` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetFoodTableQrCodeResponseModel>>`；包装 `Task/DataResult`；Data `GetFoodTableQrCodeResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:205` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `TableId` | `int` | 源码未确认必填 | 绑定=ApiController推断；未发现显式必填证据 | 必须来自同一会话上游 API 结果或服务端对象引用 | 桌台ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.QrCodeUrl` | `string` | 点餐二维码 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：未发现显式 `if` 参数校验；这不代表所有参数都可省略。
- 一层业务调用：`FoodTableProvider.GetTableQrCode`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_table_statistics`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 餐台数据（首页） |
| 使用时机 | 在商家视角中核对“餐台数据（首页）”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodTableStatistics` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetFoodTableStatisticsResponseModel>>`；包装 `Task/DataResult`；Data `GetFoodTableStatisticsResponseModel` |
| 数据时效 | 聚合结果；时间范围和门店时区必须由请求参数确认 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:366` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required]；if(model.StoreId <= 0 \\|\\| model.Uid <= 0) | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.TableTotalCount` | `int` | 桌台总数 | 普通业务字段 | 可按问题需要提供 |
| `Data.OpenTableCount` | `int` | 用餐中桌台数 | 普通业务字段 | 可按问题需要提供 |
| `Data.PeopleCount` | `int` | 就餐人数 | 普通业务字段 | 可按问题需要提供 |
| `Data.Price` | `decimal` | 账单金额 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`model.StoreId <= 0 \|\| model.Uid <= 0`
- 一层业务调用：`FoodTableProvider.GetFoodTableStatistics`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：`状态赋值：StatusCode = 301`；`固定提示：请求参数错误`
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_food_tag_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取食品标签 |
| 使用时机 | 在商家视角中核对“获取食品标签”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetFoodTagList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetFoodTagListResponseModel>>`；包装 `DataResult`；Data `List<GetFoodTagListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:268` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsEdit` | `bool` | 是否允许修改、删除；false-不允许，true-允许 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`FoodTagProvider.GetFoodTagList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_new_order_information`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 首页获取新订单信息 |
| 使用时机 | 商家核对商城/餐饮订单列表、详情、支付和履约状态时使用。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetNewOrderInformation` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `Task<DataResult<GetNewOrderInformationResponseModel>>`；包装 `Task/DataResult`；Data `GetNewOrderInformationResponseModel` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodPartialController.cs:829` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data.Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data.IsShowNewOrder` | `bool` | 是否显示新订单 | 普通业务字段 | 可按问题需要提供 |
| `Data.NewOrderCount` | `int` | 新订单个数 | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`FoodOrderProvider.GetNewOrderInformation`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_print_device_setting`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取打印机设置 |
| 使用时机 | 在商家视角中核对“获取打印机设置”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetPrintDeviceSetting` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetPrintDeviceSettingResponseModel>>`；包装 `DataResult`；Data `List<GetPrintDeviceSettingResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:892` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | 源码属性注释缺失 | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Name` | `string` | 名称 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Img` | `string` | 图片 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodPrintSum` | `int` | 源码属性注释缺失 | 普通业务字段 | 可按问题需要提供 |
| `Data[].State` | `int` | 1-启用，0-关闭 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsXiaofeidan` | `bool` | 是否打印消费单；false-否，true-是 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsYujiedan` | `bool` | 是否打印预结单；false-否，true-是 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsJiezhangdan` | `bool` | 是否打印结账单；false-否，true-是 | 普通业务字段 | 可按问题需要提供 |
| `Data[].IsChufangdan` | `bool` | 是否打印厨房单；false-否，true-是 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodTag` | `PrintDeviceJoinFoodTagViewModel` | 厨房单标签相关的数据 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodTag.FoodPrintSum` | `int` | 打印份数 | 普通业务字段 | 可按问题需要提供 |
| `Data[].FoodTag.PrintFoodTagList` | `List<int>` | 打印机食品标签关联 数据为食品标签的id | 普通业务字段 | 可按问题需要提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`FoodProvider.GetPrintDeviceSetting`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

### `crmapi.food.get_rider_list`

| 项目 | 内容 |
| --- | --- |
| 业务域 | 餐饮订单 |
| 用途 | 获取配送人员列表 |
| 使用时机 | 在商家视角中核对“获取配送人员列表”对应的当前返回结果。 |
| 不应使用 | 写入、修改、支付、退款、核销或无法由响应字段证明的结论；历史问题不能只靠当前结果 |
| 请求 | `POST /api/Food/GetRiderList` |
| 鉴权 | 继承登录鉴权基类 |
| 工具等级 | `B` — 代码路径看起来只读，但身份字段和业务标识必须由受控上下文注入或从上游结果取得。 |
| 返回 | `DataResult<List<GetRiderListResponseModel>>`；包装 `DataResult`；Data `List<GetRiderListResponseModel>` |
| 数据时效 | 当前调用时状态；不能据此还原过去某一时刻 |
| 源码 | `Public/LingKe/Link.CRMApi/Controllers/WebApi/FoodController.cs:1052` |
| C/B 对照 | 未发现同 Controller/同语义名称的另一视角接口 |

#### 请求参数

| 参数 | 类型 | 必要性 | 代码依据/绑定 | 参数来源 | 业务含义与可见性 |
| --- | --- | --- | --- | --- | --- |
| `Uid` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：当前操作人 UID | 源码属性注释缺失；内部标识；不向客户展示；模型调用时优先使用服务端引用 |
| `StoreId` | `int` | 服务端必注入 | 绑定=ApiController推断；[Required] | 服务端注入：已确认门店 | 商家ID；内部标识；不向客户展示；模型调用时优先使用服务端引用 |

#### 响应参数

除下表 `Data` 字段外，始终先检查公共包装的 `State/StatusCode/Message`。

| 字段路径 | 类型 | 含义 | 敏感等级 | 模型可见性 |
| --- | --- | --- | --- | --- |
| `Data[].Message` | `string` | 返回信息 | 普通业务字段 | 可按问题需要提供 |
| `Data[].Id` | `int` | id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StoreId` | `int` | 店铺id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].StaffId` | `int` | 员工id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].Uid` | `int` | 用户id | 内部标识 | 不向客户展示；模型调用时优先使用服务端引用 |
| `Data[].RiderType` | `int` | 骑手状态；0-空闲，1-配送中 | 普通业务字段 | 可按问题需要提供 |
| `Data[].UserName` | `string` | 名称 | 个人信息 | 仅在当前授权场景按最小范围提供 |
| `Data[].UserImg` | `string` | 头像 | 个人信息 | 仅在当前授权场景按最小范围提供 |

#### 代码行为与证据

- Controller 条件：`resultMsg.State`
- 一层业务调用：`FoodProvider.GetRiderList`
- 疑似副作用：当前 Action 静态扫描未发现标准写入命名调用；仍需专用部署的只读账号和真实请求验收。
- 显式失败信号：Action 内未提取到固定状态码/提示，需以公共包装和 Provider 返回为准。
- Agent 结论边界：只能引用本次成功响应实际返回的字段；不得根据接口名称、空结果或错误提示补造业务原因。

## 排除项复核规则

- D 级接口已保留在“全部 Action 索引”中以证明审计范围，但不提供模型调用契约。
- C 级接口只有在确认查询过程不写业务数据、不发消息、不产生支付/核销/退款等外部状态后才能升级。
- 后续新增 Action 必须重新运行生成脚本；如果源码指纹变化而目录未更新，应阻止工具目录发布。
- XML 摘要缺失、DTO 无法解析或 Controller/Provider 结论冲突时，以“待人工复核”处理，不能由 Agent 自行解释。
